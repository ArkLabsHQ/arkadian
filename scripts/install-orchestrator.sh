#!/bin/bash

# DEPRECATED: This script is no longer needed
# The 'arkadian' command uses --append-system-prompt directly
# Kept only for Makefile compatibility

echo "✓ Orchestrator uses 'arkadian' command (--append-system-prompt)"
