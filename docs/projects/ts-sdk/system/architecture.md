# Ark TypeScript SDK — Architecture

## Repo Layout

Since 2026-05-22 the repository is a pnpm workspace monorepo. The SDK source lives under `packages/ts-sdk/`; the sibling `@arkade-os/boltz-swap` library lives under `packages/boltz-swap/` and depends on `@arkade-os/sdk` via `workspace:*`.

```
arkade-os/ts-sdk (repo root)
├── pnpm-workspace.yaml        # packages: [packages/*]; overrides + onlyBuiltDependencies
├── package.json               # root: pnpm -r build/test/lint aggregates; release script
├── tsconfig.base.json         # extended by each package's tsconfig
├── prettier config            # repo-wide format
├── tsup.base.config.ts        # shared tsup base (extended per-package)
├── bip68.d.ts                 # single ambient declaration (hoisted from per-package)
├── scripts/
│   ├── regtest.sh             # unified <pkg> <up|setup|test|down|reset|cycle> driver
│   └── release.sh / release.mjs # package-scoped release CLI
├── regtest/                   # shared regtest harness submodule
└── packages/
    ├── ts-sdk/                # @arkade-os/sdk — described below
    │   ├── src/               # (module tree in next section)
    │   ├── test/
    │   ├── scripts/smoke-dist.mjs
    │   ├── tsup.config.ts     # extends ../../tsup.base.config.ts
    │   ├── tsconfig.json      # typecheck-only (noEmit), extends ../../tsconfig.base.json
    │   └── package.json       # version 0.4.35
    └── boltz-swap/            # @arkade-os/boltz-swap — depends on workspace ts-sdk
        ├── src/
        ├── test/
        └── package.json       # version 0.3.40
```

devDeps (`tsup`, `vitest`, `typescript`, `prettier`, `husky`, `@types/node`, `fake-indexeddb`, `eventsource`) are hoisted to the root; per-package `package.json` keeps only package-unique deps.

## Module Structure

```
packages/ts-sdk/src/
├── index.ts                 # Main exports (~400 lines of re-exports). Since 0.4.33 also re-exports `isCsvSpendable` / `isCltvSatisfied` from `contracts/handlers/helpers.ts` so authors of custom `ContractHandler` implementations can reuse the canonical BIP-68 / BIP-65 timelock-maturity logic the built-in handlers already use to implement `selectPath` / `getSpendablePaths`. Since 0.4.35 (#554) also re-exports the signer-rotation surface from `wallet/signerRotation.ts` (`classifyContractSigner` / `classifyAgainstSignerSet` / `signerSetFromInfo` / `isCooperativelyMigratable` / `toXOnlySignerHex`; types `SignerStatus` / `SignerClassification` / `SignerSet`), the migration report/option types from `wallet/vtxo-manager.ts` (`MigrateDeprecatedSignerOptions`, `DeprecatedSignerMigrationReport`, `DeprecatedSignerReport`, `MigrationVtxoRef`, `MigrationLegReport`, `MigrationLegSkipReason`, `MigrationGlobalSkipReason`), `BoardingUtxoGroup` from `wallet/wallet.ts`, and `DigestMismatchError` from `providers/ark.ts`. **Unreleased on top of 0.4.43** also re-exports the unilateral-exit surface from `wallet/exit/` (`UnilateralExit`, `serializeExitPackage` / `deserializeExitPackage` / `createExitChainResolver`; types `ExitDelay` / `ExitMode` / `ExitPackage` / `ExitQuote` / `ExitStep` / `ExitTotals` / `ExitVtxoInfo` / `ExitOptions` / `ExecutorEvent` / `ExitFeeWallet` / `ExitCaptureMode` / `ExitChainResolver` / `ExitDataSource`) and the offline-first surface (`ProviderUnavailableError` + type `ProviderKind` from `providers/errors.ts`, `isRetryableProviderError` from `providers/availability.ts`, types `ServerInfoSource` from `wallet/arkInfoSnapshot.ts`, `ProviderConnectionState` from `wallet/wallet.ts`, `ContractSyncState` from `contracts/contractManager.ts`)
├── networks.ts              # Network definitions (mainnet, testnet, regtest, mutinynet); since 0.4.28 also hosts the source-of-truth defaults `DEFAULT_ARKADE_SERVER_URL = "https://arkade.computer"`, `DEFAULT_NETWORK = networks.bitcoin`, `DEFAULT_NETWORK_NAME = "bitcoin"` (moved here from `src/wallet/index.ts` to keep the import chain `script → networks → provider` cycle-free)
├── forfeit.ts               # Forfeit transaction construction
│
├── wallet/                  # Wallet implementations
│   ├── wallet.ts            # Wallet, ReadonlyWallet. Post-0.4.32: `boardingTapscript` is sourced from `BoardingContractHandler.createScript({ pubKey, serverPubKey, csvTimelock: timelockToSequence(boardingTimelock).toString() })` instead of the prior inline `new DefaultVtxo.Script({ ..., csvTimelock: boardingTimelock })` (byte-identical script for equivalent params — `getBoardingAddress()` and `pkScript` are unchanged — but the construction now routes through the registered handler). Contract-manager init persists a matching `active` boarding row via the new `ensureWalletContract` helper (create-if-missing) so `ContractWatcher` monitors the boarding Arkade address; `getBoardingAddress()` does NOT depend on this contract (derives from `boardingTapscript` directly) so the lazy contract-manager lifecycle stays intact. New internal helper `ensureWalletContract(manager, params)` registers wallet baseline (`default` / `boarding`) contracts idempotently and coalesces same-script collisions onto `default` ("default wins"): no existing row → create; same type → idempotent no-op; requested `default` + existing `boarding` (compatible) → `manager.updateContract(params.script, { type: 'default' })`; requested `boarding` + existing `default` (compatible) → accept as-is; incompatible existing type → fall through to `createContract` which throws. Companion predicate `areSameScriptBaselineTypesCompatible(existingType, requestedType)` recognizes `default ↔ boarding` as compatible (same `DefaultVtxo.Script` shape, differing only by CSV timelock; collide only against a misconfigured server with `boardingExitDelay === unilateralExitDelay`) and rejects every other cross-type pairing. The `default` baseline registration now goes through `ensureWalletContract` instead of strict `createContract`; `delegate` baseline stays strict (its script cannot collide with the others). Both helpers exported `@internal` for unit tests. `walletContractTimelocks` defaults to `[this.offchainTapscript.options.csvTimelock]` (no longer `?? DefaultVtxo.Script.DEFAULT_TIMELOCK`) since `csvTimelock` is required at script construction post-0.4.32. waitForIncomingFunds (skips notifications carrying no incoming funds — `notifyIncomingFunds` also fires for purely outgoing activity: a `vtxo_spent` event with `newVtxos: []` and onchain-only spends with empty `coins`; the prior one-shot helper resolved on the first such event and a self-send could settle on its spent half before the matching `vtxo_received` arrived, returning an empty result; helper also closes the subscription if the callback ran before the `notifyIncomingFunds` promise resolved, plugging a stop-handle leak). Since #535: send path (`buildAndSubmitOffchainTx`) and recovery path (`finalizePendingTxs`) restore the BatchSignableIdentity one-popup path lost in the InputSignerRouter refactor — when `isBatchSignable(identity)` AND every signable input across arkTx + N checkpoints (send) / across N checkpoints (recovery) resolves to the baseline key per `_signerRouter.canBatch(...)`, sign all PSBTs via a single `identity.signMultiple(requests)` call (N+1 → 1 wallet popups). Send path stashes the user-signed checkpoints, submits the unsigned ones to the server for its tapScriptSig, then merges server + user sigs via `combineTapscriptSigs` on each checkpoint (symmetric length guard rejects a server response with a wrong checkpoint count, mirroring the `signMultiple` length check). Recovery path hands `signMultiple` checkpoints that already carry the server's tapScriptSig and consumes the returned transactions directly — `signMultiple`'s contract preserves the pre-existing server sig and adds the user's share, so no `combineTapscriptSigs` merge is needed there. Mixed sends (arkTx non-batchable or a checkpoint missing a descriptor) fail fast at the unified `canBatch` call before `setPendingTxFlag` rather than mid-signing. Requests are cloned (`tx.clone()`) before handing to `signMultiple` so a misbehaving provider cannot mutate the originals before `submitTx`. HD / mixed-owner sends continue through the sequential `_signerRouter.sign` path. per-script persistence in updateDbAfterOffchainTx / updateDbAfterSettle (rows grouped by owning script, routed to each contract's address); fail-fast on undecodable wallet address in getVtxosFromRepo; `extractArkProviderUrl(provider)` structurally reads `serverUrl` off the injected ArkProvider so the indexer is built from the same host as a custom arkProvider (no longer silently paired with the public arkade.computer default). `WalletConfig.walletMode` (auto|static|hd|DescriptorProvider) drives receive-rotation wiring through WalletReceiveRotator. `offchainTapscript` exposed as a getter over a `protected` backing field; the only sanctioned write is `setOffchainTapscriptForRotation` (@internal, on the RotatableWallet surface). `updateDbAfterOffchainTx` / `_sendImpl` / `sendBitcoin` snapshot `offchainTapscript` synchronously at `_txLock` entry so a concurrent `rotate()` cannot stamp the change VTXO with mixed scripts. `signInputsByOwner` removed in favour of the InputSignerRouter; signing entry points hand the router explicit InputSigningJob[] derived from the source VTXO script. `getVtxoManager` caches the manager only AFTER `_receiveRotator.install` resolves (failing install leaves the cache untouched). `dispose` wraps rotator teardown in try/catch and rethrows after manager + super disposal so a rotator failure can't leak the contract watcher. Since 0.4.28 (#492): public `restore({ gapLimit }): Promise<void>` gap-scan recovery; coalesces concurrent callers (later callers join the in-flight promise; their `gapLimit` is silently ignored — `_restoreInFlight` checked BEFORE `gapLimit` validation so a coalescer with an invalid `gapLimit` doesn't throw); `_runRestore` detects HD via `instanceof HDDescriptorProvider` (not duck-typed method names — a non-HD provider exposing `materializeDescriptorAt`/`advanceLastIndexUsed` is correctly excluded) and computes the static-branch descriptor lazily so HD wallets skip the `xOnlyPublicKey()` derivation; `dispose()` awaits `_restoreInFlight?.catch(() => undefined)` before tearing down managers. Since 0.4.31: `ReadonlyWallet.getBoardingTxHistory` populates the boarding `ArkTransaction.key.commitmentTxid` from `utxo.virtualStatus.commitmentTxIds?.[0] ?? ""` instead of always `""` so the new `buildTransactionHistory` dedup path (see `utils/transactionHistory.ts`) can correlate a boarding receive with its leaf-VTXO row by commitment. Post-0.4.34: `Wallet.settle` resolves the receive address ONCE at the top (`offchainAddress` / `offchainPkScript` / `offchainOutputScript`) and reuses it for the no-params output, the output-fee estimation script, and the asset-routing destination matched by `findDestinationOutputIndex` — `WalletReceiveRotator.rotate` mutates `offchainTapscript` without acquiring `_txLock`, so re-calling `getAddress()` mid-settle could observe a rotated script and fail with a spurious "no output matches"; the no-params path also reads `vtxoMaxAmount` from `getInfo()` and skips (doesn't stop at) VTXOs whose net contribution would push the projected post-output-fee amount past the ceiling (boarding inputs added uncapped but counted via the running total). Since 0.4.35 (#554): hosts the server-signer-rotation write path — `rotateServerSigner(newServerPubKey, checkpointTapscript)` swaps `_arkServerPublicKey` and re-derives the offchain + boarding + checkpoint-unroll tapscripts mid-session (serialized for static / non-HD wallets); the wallet auto-subscribes to `arkProvider.onServerInfoChanged` at boot (`_serverInfoUnsub`) and folds itself onto the new signer on a `DigestMismatchError`. `getBoardingUtxosForSigners(allowedSigners: Set<string>): Promise<BoardingUtxoGroup[]>` returns the per-address signer/CSV groups the migration boarding leg needs (`getBoardingUtxos()` is a current-signer-only flatten over it); `BoardingUtxoGroup` is exported from the package root. `sendSelectedVtxosToSelf(inputs)` spends an explicit set of deprecated-signer VTXOs into one full-value active-signer output through the Ark send path (the VTXO migration primitive). `DigestMismatchError` originates in `providers/ark.ts`. Post-0.4.40 (unreleased): `ReadonlyWallet.clear()` implements the new `IReadonlyWallet.clear()` contract — `dispose()` in a `try` (stops the `ContractWatcher` + polling intervals so the poller can't hit a closing IndexedDB connection), then `Promise.all([walletRepository.clear(), contractRepository.clear()])` in the `finally` so a dispose failure still wipes; callers build a fresh instance afterward. Also post-0.4.40: `Wallet.settle` fails fast on an unsignable boarding input — after routing each boarding input through `_signerRouter.sign` it asserts the input now carries a `tapScriptSig` and, when empty, throws `unsignableBoardingInputError(input, script)` (a guarded diagnostic naming the outpoint, unresolved script hex + decoded address, and the recognized boarding addresses) rather than letting the silently-unsigned input reach arkd and be rejected as "not a wallet script". **Unreleased on top of 0.4.43 (offline-first)**: boots from a cached `StoredArkInfoSnapshot` (see `arkInfoSnapshot.ts`) when a live `getInfo` fails retryably — repositories now init BEFORE `getInfo`, and `_serverInfoSource: ServerInfoSource` (`"live" | "cache"`) + `_serverInfoLastOnlineAt` record the boot provenance. New diagnostics (sync freshness only — never the wallet balances, which always come from the repository): `getProviderConnectionState(): ProviderConnectionState` composes the boot source (Arkade) with the contract-manager's `getSyncState()` — `{ mode: "online"; source: "live"; lastOnlineAt }`, or `{ mode: "degraded"; source: "cache" | "repository"; provider: ProviderKind; reason; lastOnlineAt? }` (degraded on `arkade`/`cache` when boot fell back to the snapshot, else `indexer`/`repository` when the manager degraded) — and never forces a `ContractManager` to construct; the non-initializing `getContractSyncState()` reads the already-built manager or reports `{ mode: "online" }`
│   ├── onchain.ts           # OnchainWallet (on-chain fee payment, anchor bumping)
│   ├── ramps.ts             # Ramps (onboard/offboard); since 0.4.28 pre-checks residual change against the wallet's dust threshold and throws typed DustChangeError (closes #458) before forwarding the intent to arkd — wallet layer can catch and offer to exit the full balance. DustChangeError re-exported from src/index.ts
│   ├── batch.ts             # Batch session (round participation, tree signing)
│   ├── vtxo-manager.ts      # VtxoManager (renewal, recovery, expiry monitoring); revalidateBeforeSettle pre-flight + maybeRefreshAfterVtxoSpent reactive recovery on VTXO_ALREADY_SPENT (surgical refreshOutpoints, falls back to refreshVtxos when no outpoint metadata). Since 0.4.30 (#388): `IVtxoManager.renewVtxos(eventCallback?, options?)` accepts a `RenewVtxosOptions` payload — `thresholdSeconds` overrides the renewal threshold per-call (resolution `options.thresholdSeconds` → `settlementConfig.vtxoThreshold` → `DEFAULT_RENEWAL_CONFIG.thresholdMs`; bypasses the `settlementConfig === false` gate). The override is validated BEFORE state mutation: non-number / non-finite / non-positive values throw `TypeError` (payload arrives over the worker MessageBus so the type guard runs at runtime; a `0` / `<=100ms` threshold would otherwise silently revert to the 3-day default via `isVtxoExpiringSoon`). `RenewVtxosOptions` re-exported from `src/index.ts`. Since 0.4.34: exports `MAX_VTXOS_PER_SETTLEMENT = 50` plus sort helpers `byValueDescending<T extends {value: number}>(vtxos)` (recovery / manual settle — capped subset has the best chance of clearing dust) and `byExpiryAscending(vtxos)` (renewal / periodic settle — soonest-expiring make the cut; already-recoverable/expired sort first; VTXOs with no batch expiry or a block-height-looking expiry value sort last). Sorts run BEFORE the cap so the deferred overflow doesn't starve viable VTXOs (PR #549 review — neither go-sdk nor NArk caps; ordering the candidates before the cap diverges from nothing). Settle paths short-circuit once the filtered set reaches the cap so cycle latency stays bounded. `recoverVtxos` differentiates a genuinely empty wallet from a funded wallet whose highest-value sub-cap batch can't clear dust — the latter reports the cap, the recoverable count, and the dust threshold so operators can tell stuck-but-funded from empty. Post-0.4.34: also exports `capSettlementBatch<T extends { value: number }>(sorted, maxAmount: bigint)` — applies the count cap (hard stop) AND the server's per-output ceiling `ArkInfo.vtxoMaxAmount` (`-1n` = no-limit sentinel; server rejects over-limit outputs with `AMOUNT_TOO_HIGH`) to a pre-sorted candidate list; amount-breaching inputs are skipped (not stopping points) so smaller inputs behind an oversized one still fit, and the bound is each input's gross `value` (intentionally conservative — the helper has no fee context; do NOT "tighten" by subtracting fees). Wired into `recoverVtxos` (value-descending; below-dust error message now also names the amount limit) and `renewVtxos` (expiry-ascending; warns on individually-oversized VTXOs — unrenewable by this path, drift toward unilateral exit — and throws `No VTXOs available to renew within the per-output limit` when no batch fits). `runPeriodicSettle` caps on net (post-input-fee) contribution inline, skipping past-the-ceiling VTXOs. New private `getInfoProvider()` narrow-casts the wallet to `{ arkProvider?: ArkProvider }` for the `vtxoMaxAmount` lookup without requiring the full sweep-capable shape (`undefined` → no limit). Since 0.4.35 (#554): hosts the deprecated-signer migration surface — `IVtxoManager.migrateDeprecatedSignerVtxos(options?: MigrateDeprecatedSignerOptions): Promise<DeprecatedSignerMigrationReport>` and `getDeprecatedSignerStatus(): Promise<DeprecatedSignerReport[]>`. The migration is **fee-exempt** (mandatory rotation moves every input at full value — no intent fee, no per-input fee skip, no output-fee deduction; only the protocol dust floor on the aggregate) and **two-leg**: the VTXO leg migrates through the Ark send path (`MigrationCapableWallet.sendSelectedVtxosToSelf(inputs)` — a single full-value active-signer self output), the boarding leg keeps a separate settle-backed migration (on-chain inputs have no send path); the legs never combine into one intent and each owns its own oversized filtering + count/amount caps + dust floor and reports independently via `MigrationLegReport`. `isMigrationCapable(wallet)` guards the path (requires `arkProvider`, `arkServerPublicKey`, `onchainProvider`, `rotateServerSigner`, `sendSelectedVtxosToSelf`, `getBoardingUtxosForSigners`). Report/option types `MigrateDeprecatedSignerOptions`, `DeprecatedSignerMigrationReport`, `DeprecatedSignerReport`, `MigrationVtxoRef`, `MigrationLegReport`, `MigrationLegSkipReason` (`below-dust` | `oversized-only`), `MigrationGlobalSkipReason` (`no-deprecated-vtxos` | `unknown-wallet-signer`) are exported from the package root. Recovery/renew/periodic-settle outputs are pinned to the active signer so `EXPIRED` (post-cutoff) VTXOs recover-on-sweep (split into recoverable-now vs awaiting-sweep with a `nextSweepEta` in the report)
│   ├── delegate.ts          # DelegateManager (VTXO delegation to third-party; renamed from delegator.ts in 0.4.29 / #519 — `DelegateManagerImpl` / `IDelegateManager`, `DelegatorManagerImpl` / `IDelegatorManager` kept as `@deprecated` aliases). `delegate` filter uses an `isAnnotated` type guard narrowing `ContractVtxo` to `ContractVtxo & ExtendedVirtualCoin` (checks `tapTree`, `forfeitTapLeafScript`, `intentTapLeafScript`) instead of an unsafe `as ExtendedVirtualCoin` cast
│   ├── asset-manager.ts     # AssetManager / ReadonlyAssetManager (issue, reissue, burn); since 0.4.29 both classes + the IAssetManager / IReadonlyAssetManager types are exported from the package root
│   ├── asset.ts             # Asset types and helpers
│   ├── unroll.ts            # Unroll (unilateral exit) — prepareUnrollTransaction (build + sign) split from completeUnroll (broadcast); completeUnroll passes wallet.network to addOutputAddress for regtest bech32 support; Math.ceil(feeRate) before BigInt() to tolerate fractional sat/vB. Since 0.4.43: `Unroll.Session.create(..., virtualTxRepository?)` accepts an optional `VirtualTxRepository` — each step's raw tx (base64 PSBT) is read repo-first, fetched from the indexer on a miss, then cached best-effort (`resolveVirtualTxBase64`, never throws from the cache write). Omitted ⇒ exact previous behaviour (indexer only). The repo is experimental/inert at 0.4.43 — only this best-effort cache uses it (**unreleased on top of 0.4.43**, the new `exit/capture.ts` capture path activates the same repo when `StorageConfig.exitDataCapture` is configured; see `wallet/exit/`)
│   ├── utils.ts             # Wallet utilities; since 0.4.28 hosts shared `getDustAmount(wallet): bigint` (reads `wallet.dustAmount` when present, else falls back to `FALLBACK_WALLET_DUST_AMOUNT = 330n`) — used by ramps.ts (DustChangeError pre-check) and vtxo-manager.ts; previously duplicated inline. Since 0.4.31 (refs #521): hosts the per-contract tapscript memo path — new public types `ContractTapscripts = Pick<ExtendedVirtualCoin, "forfeitTapLeafScript" | "intentTapLeafScript" | "tapTree">` and `ContractTapscriptCache = Map<string, ContractTapscripts>`; `extendVirtualCoinForContract(vtxo, contractOrMap?, cache?)` accepts an optional cache so the taproot tree is built once per distinct `contract.script` per annotation batch instead of once per VTXO. Helper structure: `deriveContractTapscripts(contract)` runs the actual `handler.createScript(contract.params)` rebuild (now isolated, returns only the three derived fields without re-spreading `vtxo`); `extendVtxoFromContract(vtxo, contract, cache?)` short-circuits to direct derivation when no cache is supplied (`if (!cache) return { ...vtxo, ...deriveContractTapscripts(contract) }`) so unbatched callers' allocation profile is unchanged, otherwise consults the cache and returns `{ ...vtxo, ...cloneContractTapscripts(cached) }` on hits. `cloneTapLeafScript([controlBlock, script])` rebuilds a fresh `controlBlock` with `new Uint8Array(internalKey)` + `merklePath.map(hash => new Uint8Array(hash))` and a `new Uint8Array(script)`; `cloneContractTapscripts` wraps that for both leaves plus `new Uint8Array(tapTree)`. Cloning on hit is load-bearing: PSBT-building callers mutate the returned tapTree/leaves in place; without the clone, every later VTXO sharing the same contract would receive the prior caller's mutated buffers
│   ├── hdDescriptorProvider.ts # HDDescriptorProvider — allocator (getNextSigningDescriptor) + ReceiveRotatorFactory (createReceiveRotator delegates to WalletReceiveRotator.defaultBoot); getCurrentSigningDescriptor re-derives last-used index without advancing for stable boot replay. Since 0.4.28 (#492): public `materializeDescriptorAt(index)` materializes a concrete descriptor at an arbitrary HD index (used by `ContractManager.scanContracts` to probe handlers' `discoverAt(descriptor)`) and `advanceLastIndexUsed(index)` monotonically nudges the persisted `lastIndexUsed` forward (never backs off; restore uses it to fast-forward the receive cursor past discovered hits)
│   ├── walletReceiveRotator.ts # WalletReceiveRotator — owns DescriptorProvider + vtxo_received subscription + rotation chain + boot pubkey lookup (pickActiveReceive) + contract registration on rotate. ReceiveRotatorFactory / ReceiveRotatorBoot / ReceiveRotatorBootOpts interfaces; hasReceiveRotatorFactory duck-typed guard. resolveDescriptorProvider TODO(hd-maturation) keeps 'auto' === 'static' until soak time builds. Exponential backoff (1s → 60s cap) on consecutive rotate() failures. Pluggable Logger interface. NonRangeableDescriptorError typed error replaces the prior wildcard-descriptor string match. Since 0.4.28 (#492): `pickActiveReceive` deterministically tiebreaks on the highest HD index when multiple `metadata.source === 'wallet-receive'` contracts coexist (parses trailing `/N)` from each contract's `metadata.signingDescriptor` via the documented `/\/(\d+)\)\s*$/` regex; restore can create several `wallet-receive` rows ahead of the current cursor, so deterministic tiebreak is load-bearing). `WALLET_RECEIVE_SOURCE = 'wallet-receive'` is now sourced from `src/contracts/metadata.ts` (re-exported from here for backward compatibility — the source-of-truth declaration moved to the dependency-free leaf to break the `contracts → wallet` import cycle that blocked `contracts/handlers/default.ts` from reading it). `deriveDescriptorLeafPubKey` extracted to `identity/descriptor` so the rotator and the new contract-handler `discoverAt` paths share leaf-pubkey derivation
│   ├── signerRotation.ts    # 0.4.35 (#554): arkd server-signer rotation primitives. Exports the `SignerStatus` union (`CURRENT | MIGRATABLE | DUE_NOW | EXPIRED | UNKNOWN_SIGNER`), `SignerClassification` / `SignerSet` interfaces, and pure helpers `classifyContractSigner(contract, signerSet, now)` / `classifyAgainstSignerSet` / `signerSetFromInfo(info)` (builds the x-only-hex-keyed `SignerSet` from a fresh `ArkInfo`) / `isCooperativelyMigratable` / `toXOnlySignerHex(pubkeyHex)` (drops the parity prefix from a 33-byte key, lowercases a 32-byte key) — all re-exported from `src/index.ts`. Staleness is NEVER persisted: a contract's signer status is always derived at read time from its `params.serverPubKey` + the server's advertised signer set. `EXPIRED` is documented as recover-on-sweep (the VTXO keeps its batch expiry, the server sweeps it, recovery re-mints it under the active signer — NOT a forced unilateral exit)
│   ├── inputSignerRouter.ts # InputSignerRouter — per-input signer dispatch. InputSigningJob { index; lookupScript }. Routes default/delegate contracts with non-baseline owner → DescriptorProvider.signWithDescriptor (uses metadata.signingDescriptor); everything else → Identity; unmatched inputs skipped silently. Since #535: `classify(jobs)` extracted as the single source of truth for routing — returns an `InputRoutingPlan { identityIndexes: number[]; descriptorGroups: Map<descriptor, number[]> }`; `sign()` consumes it (no behaviour change for existing single-PSBT callers). Throws `MissingSigningDescriptorError` here, before any PSBT is mutated. New variadic predicate `canBatch(...jobSets: InputSigningJob[][])` flattens its arg and returns `true` iff `plan.descriptorGroups.size === 0` — i.e. every signable input across all sets resolves to the baseline `Identity` key (the descriptor provider would not be invoked). Eligibility is monotonic (the union routes entirely to baseline iff every set does), so the wallet folds N PSBTs of an arkTx + N checkpoints into one classify call (one repo round-trip + one `xOnlyPublicKey` call) instead of N+1. Used by the wallet's send/recovery paths to pre-flight the `BatchSignableIdentity.signMultiple` one-popup fast path
│   ├── signingErrors.ts     # DescriptorSigningProviderMissingError, MissingSigningDescriptorError — both re-exported from src/index.ts
│   ├── intentStateReducer.ts # 0.4.43: pure, monotonic `reduceIntentState(current, event)` — only the three batch-boundary events move state (`BatchStarted → batch_in_progress`, `BatchFinalized → batch_succeeded`, `BatchFailed → batch_failed`); terminal is sticky, everything else is a no-op. `applySettlementEventToIntent` folds in batchId / commitmentTxid / failure reason
│   ├── intentPersistenceHandler.ts # 0.4.43: `wrapHandlerWithIntentPersistence(base, deps)` wraps a `Batch.Handler` so the SDK-owned intent state is persisted from the AWAITED batch hooks (ordered before `Batch.join` returns) rather than the fire-and-forget `eventCallback`; repo errors are logged, never thrown into the settlement stream
│   ├── intentReconciliation.ts # 0.4.43: `reconcileIntents(deps)` crash-recovery — reconciles persisted non-terminal intents against authoritative indexer VTXO state on the online sync path (boot + reconnect, driven by `ContractManager`, never from `getBalance()`). `classifyIntent` is conservative (only forces terminal when provable: all-inputs-consumed → `batch_succeeded`; never-submitted/expired unspent → `cancelled`; else left locked). Per-intent freshness guard avoids overwriting a concurrent live `settle()`. `NON_TERMINAL_INTENT_STATES` derived from `ALL_INTENT_STATES`
│   ├── arkInfoSnapshot.ts   # Unreleased on top of 0.4.43: cached-server-info boot for the offline-first wallet. `StoredArkInfoSnapshot` (`version: 1`, `source: "arkade.getInfo"`; `bigint` fields as decimal strings; live `serviceStatus` NOT cached, rehydrated as `{}`) persisted to `WalletState.settings[ARK_INFO_SNAPSHOT_KEY = "arkInfoSnapshot"]` on every live `getInfo` (`serializeArkInfoSnapshot` / persist helper). On a retryable operator failure the wallet constructs from the snapshot instead of throwing — enough to derive network/scripts/address params/dust/fee policy/delays and interpret local state, NOT proof the operator is live. A structurally-invalid snapshot throws the terminal `MalformedArkInfoSnapshotError` (fails like malformed live info, never silently as if absent). Exports `ServerInfoSource = "live" | "cache"` (consumed by `wallet.ts`'s `_serverInfoSource` + `getProviderConnectionState`)
│   ├── exit/                # Unreleased on top of 0.4.43: pre-signed unilateral exit packages (keyless, Esplora-only execution). `UnilateralExit` namespace = { estimate, prepare, Executor }; `serializeExitPackage` / `deserializeExitPackage` / `createExitChainResolver` re-exported from the package root
│   │   ├── index.ts         # Assembles + re-exports the `UnilateralExit` namespace and the `./types` surface, `ExitPathError` / `resolveUnilateralPath`, `ExecutorEvent` / `ExitFeeWallet`, `ExitOptions`, `ExitCaptureMode`, `ExitChainResolver` / `ExitDataSource`, `createExitChainResolver`
│   │   ├── types.ts         # Versioned `ExitPackage` (`version: 1`, `mode?: ExitMode`, `network`, `feeRate`, `sweepAddress`, `totals`, `vtxos`, topologically-ordered `steps`); `ExitStep` union `BroadcastStep` (splitter) / `PackageStep` (1P1C parent+child) / `BumpStep` (graph-mode deferred CPFP) / `SweepStep` (pre-signed CSV sweep gated on a dependency confirmation, carries `delay: ExitDelay`); `ExitMode = "funded" | "graph"`; `ExitQuote` (adds `fundingAddress` / `currentBalanceSats` / `shortfallSats`); `ExitTotals` (`txCount` / `totalFeeSats` / `fundingRequiredSats` / `recoveredSats`); `ExitVtxoInfo` (per-VTXO `value` / `sweepFee` / `path` / `delay` / `skipped` reason); `serializeExitPackage` / `deserializeExitPackage` (validates `version`, network/sweepAddress/feeRate/createdAt, steps + totals)
│   │   ├── estimate.ts      # `estimate({ wallet, onchainWallet, sweepAddress, feeRate? }): ExitQuote` — quotes tx count + fees + funding required with nothing signed; `ExitOptions`
│   │   ├── prepare.ts       # `prepare(...): ExitPackage` — signs every unroll + sweep tx and broadcasts the fee-funding splitter (reserves the fee budget onchain)
│   │   ├── executor.ts      # `Executor` — async-iterable that drives an `ExitPackage` to completion with only an Esplora-compatible `onchainProvider`; idempotent (re-checks the chain before every action, skips already-confirmed steps); emits an `ExecutorEvent` per step; graph-mode funds/signs `BumpStep`s from its own `ExitFeeWallet`
│   │   ├── path.ts          # `resolveUnilateralPath` — resolves each VTXO's contract exit path via the handler registry (contract-aware; a path needing another party is dropped with a reason); `ResolvedExitPath` / `ExitPathError`
│   │   ├── chain.ts         # exit-chain graph assembly + `topoSortByDeps` (ancestors-first ordering of the tx DAG)
│   │   ├── sweep.ts         # pre-signable CSV sweep builder with condition-witness finalization (VHTLC preimage-in-params etc.)
│   │   ├── finalizeVirtualTx.ts # shared virtual-tx finalization (extracted, also used by the Session path)
│   │   ├── capture.ts       # `captureExitBranch(...)` — fetch + persist a received VTXO's exit branch into the `VirtualTxRepository` (idempotent, dust-gated `DEFAULT_MIN_EXIT_WORTH_SATS = 1000`, ancestors-first); `ExitCaptureMode = "lite" | "full"` (`DEFAULT_EXIT_CAPTURE_MODE = "lite"` — structure only; `"full"` also stores PSBTs). Prune-on-spend is ref-counted; capture/prune are best-effort (never block sync or exit)
│   │   ├── resolver.ts      # `createExitChainResolver` — reads exit data local-first (`ExitDataSource` provider tier → repo → indexer); `ExitChainResolver` / `ExitDataSource` (`getVtxoChain` / `getVirtualTxs`)
│   │   ├── indexerSource.ts # indexer-backed `ExitDataSource`
│   │   └── repositorySource.ts # repository-backed `ExitDataSource` (the local `virtualTxRepository`)
│   ├── expo/                # Expo / React Native wallet
│   │   ├── index.ts         # Foreground entrypoint — re-exports ExpoWallet, ExpoWalletConfig, ExpoBackgroundConfig only (no background helpers — these moved to /wallet/expo/background in #487)
│   │   ├── wallet.ts        # ExpoWallet (foreground polling, AsyncStorage-persisted config for background rehydration); setup() no longer registers the OS scheduler; dispose() no longer unregisters it (caller responsibility). Post-0.4.40 (unreleased): foreground polls are serialized on a `foregroundPollChain` promise (guarded by a `cleared` flag re-checked before re-seeding) so `clear()` can drain the in-flight poll before wiping; `clear()` sets `cleared`, stops the interval, awaits the chain, calls `wallet.clear()`, and removes queued `CONTRACT_POLL_TASK_TYPE` tasks (does NOT unregister the OS background task or persisted queue config)
│   │   ├── background.ts    # /wallet/expo/background subpath — defineExpoBackgroundTask / registerExpoBackgroundTask / unregisterExpoBackgroundTask, DefineBackgroundTaskOptions / PersistedBackgroundConfig; uses static `import * from "expo-task-manager"` / `"expo-background-task"` so Metro sees them in the static dependency graph (lazy require() in the previous shape was invisible to the collector, #486). Only module in the package that imports the two expo-* optional peer deps
│   │   └── expo-modules.d.ts # Ambient declarations for expo-task-manager + expo-background-task (the subset of APIs background.ts uses) so tsc type-checks without pulling the optional peer packages into the build
│   └── serviceWorker/       # Service worker wallet
│       ├── wallet.ts        # ServiceWorkerWallet, ServiceWorkerReadonlyWallet. Since 0.4.28: `ServiceWorkerWallet.restore({ gapLimit })` mirror of `Wallet.restore` — sends `RESTORE_WALLET` message via `sendMessageWithEvents` (streaming/long-running path so the bus deadline does not race the worker-side scan); detects `SerializedAggregateError` in the rejection payload and reconstructs via `deserializeAggregateError` so callers can inspect `.errors`. URL deprecations: `ServiceWorkerWalletOptions.arkServerUrl` / `indexerUrl` / `esploraUrl` / `delegatorUrl` all `@deprecated` — pass provider instances instead. Since 0.4.30 (#388): `ServiceWorkerWallet.renewVtxos(eventCallback?, options?)` accepts a `RenewVtxosOptions` second arg and attaches it as the `RequestRenewVtxos.payload` so the per-call `thresholdSeconds` override crosses the message bus. **0.4.40 (#571): stale-identity init guard** — `create()` and `reinitialize()` now call a new `assertWorkerIdentityMatches()` (a `GET_STATUS` round-trip asserting the worker reports the expected x-only pubkey) before returning a usable wallet, refusing to serve another identity's addresses. `sendMessageWithEvents` is folded into `sendMessageWithRetry(request, withEvents?)`; when the worker reports an in-flight init (the new `MessageBusInitializingError` / `MESSAGE_BUS_INITIALIZING`, a superset of `MESSAGE_BUS_NOT_INITIALIZED` so existing consumers still classify it as "not initialized"), the caller WAITS on a bounded backoff (`initWaitBackoffMs` ~100ms→2s cap, `MAX_INIT_WAITS = 8`) instead of forcing a redundant re-init. Worker-side (`worker/messageBus.ts`): `INITIALIZE_MESSAGE_BUS` is serialized on a FIFO chain so concurrent inits can't interleave and bind the bus to a stale identity, ordinary wallet messages are rejected while an init is pending, and `doInit()` cancels any pending tick / skips `runTick()` during re-init so a stale timer can't tick stopped or mid-rebuild handlers. Post-0.4.40 (unreleased): `ServiceWorkerReadonlyWallet.clear()` now only posts the `CLEAR` message and lets the worker do the full wipe — the old page-side parity call (`walletRepository.deleteVtxos(address)`) was removed so page and worker storage can't diverge
│       ├── worker.ts        # Worker (runs in service worker context)
│       ├── request.ts       # Request serialization
│       ├── response.ts      # Response serialization
│       ├── wallet-message-handler.ts # Service-worker proxy handlers (incl. REFRESH_OUTPOINTS message for surgical VTXO_ALREADY_SPENT recovery). Since 0.4.28: `RequestRestoreWallet` / `ResponseRestoreWallet` types + `RESTORE_WALLET` / `RESTORE_WALLET_SUCCESS` case, added to `isLongRunningRequest()` so the bus deadline never enforces inactivity timeout on the multi-minute scan. `SerializedAggregateError` wire envelope (`{ name: 'AggregateError', message, errors: { name; message }[] }`) + `serializeAggregateError` (worker → wire) + `deserializeAggregateError` + `isSerializedAggregateError` guard (page-side reconstruction); the worker's `restore` handler catches `instanceof AggregateError`, serializes it, and posts as the rejection payload. Since 0.4.30 (#388): `RequestRenewVtxos.payload?: RenewVtxosOptions` carries the per-call `thresholdSeconds` override; the worker handler forwards `message.payload` as the second arg to `wallet.vtxoManager.renewVtxos(eventCallback, payload)`. Post-0.4.40 (unreleased): the `CLEAR` handler tears down its own subscriptions (`incomingFundsSubscription` / `contractEventsSubscription`, both nulled) then delegates the entire wipe to `(this.wallet ?? this.readonlyWallet).clear()` (works for signing and readonly workers), and only resets handler state (`wallet`/`readonlyWallet` → `undefined`) after a successful clear — no more page-side clearing
│       └── utils.ts         # Service worker registration helpers
│
├── identity/                # Key management
│   ├── index.ts             # Identity, ReadonlyIdentity, BatchSignableIdentity interfaces. Since #535 the `BatchSignableIdentity` JSDoc makes two contract clauses explicit: (1) implementations MUST return exactly one signed `Transaction` per request in input order (length-checked at runtime); (2) implementations MUST preserve any partial signatures already present on the input PSBTs and only ADD their own — never drop, replace, or normalize away foreign signatures. The pending-tx recovery path (`Wallet.finalizePendingTxs`) hands `signMultiple` checkpoints that already carry the server's `tapScriptSig` and relies on the server sig surviving alongside the freshly added user sig; a provider that discards the pre-existing server sig strands the tx in the pending state. Was a silent assumption before
│   ├── singleKey.ts         # SingleKey (raw private key), ReadonlySingleKey
│   ├── seedIdentity.ts      # SeedIdentity, MnemonicIdentity, ReadonlyDescriptorIdentity
│   ├── hdCapableIdentity.ts # HDCapableIdentity / ReadonlyHDCapableIdentity (capability markers); isHDCapableIdentity(value) structural type guard checks descriptor + isOurs + signWithDescriptor + signMessageWithDescriptor. The four descriptor-aware methods on identity (isOurs, signWithDescriptor, signMessageWithDescriptor) are now @deprecated on both interface and SeedIdentity/ReadonlyDescriptorIdentity — kept only as backing for DescriptorProvider implementations; callers should go through DescriptorProvider
│   ├── descriptor.ts        # Shared descriptor helpers (isMainnetDescriptor, descriptorIsOurs, parseHDDescriptor); since 0.4.28 hosts `deriveDescriptorLeafPubKey` (extracted from `walletReceiveRotator.ts`) — shared between the receive rotator and the contract-handler `discoverAt` paths
│   ├── descriptorProvider.ts # DescriptorProvider interface (getNextSigningDescriptor, isOurs, signWithDescriptor)
│   ├── staticDescriptorProvider.ts # StaticDescriptorProvider (single-key wrapper)
│   └── serialize.ts         # Identity serialize/hydrate (envelope.descriptor stores wildcard template)
│
├── providers/               # External service communication
│   ├── ark.ts               # RestArkProvider (arkd REST + SSE); since 0.4.28 `serverUrl` parameter defaults to `DEFAULT_ARKADE_SERVER_URL` (imported from `../networks`) — `new RestArkProvider()` resolves to mainnet without explicit arg. Post-0.4.34: imports `fetch` from `../utils/fetch` so every request carries the `X-Build-Version` header. **Since 0.4.35**: `ark.ts` keeps the header-setting `fetch` (now also sets `X-SDK-VERSION`) while `indexer.ts` / `delegate.ts` / `onchain.ts` switched to `baseFetch` (no custom headers — their origins reject unknown headers in CORS preflight). Adds `ArkProvider.onServerInfoChanged(cb)` event stream and exports `DigestMismatchError` (re-exported from the package root): arkd's `X-Build-Version`/digest guard reports a build/signer change, the provider has already refreshed `ArkInfo` and fired `onServerInfoChanged`, then THROWS `DigestMismatchError` so the SDK never silently retries (mirrors NArk's `BuildVersionHandler` / dotnet-sdk #131); `getInfo` itself never throws it. `ArkInfo` gains `deprecatedSigners: DeprecatedSigner[]` (`{ pubkey: string; cutoffDate: bigint }` — arkd advertises `cutoffDate` non-nullable via grpc-gateway `EmitUnpopulated`, so `0n` is the "no cutoff" sentinel → `DUE_NOW`) and a `digest: string`
│   ├── indexer.ts           # RestIndexerProvider (indexer REST + streaming); since 0.4.28 `serverUrl` parameter defaults to `DEFAULT_ARKADE_SERVER_URL`. Post-0.4.32: `convertVtxo` ternary fixed to check `isSpent` first — `isSpent ? "spent" : isSwept ? "swept" : isPreconfirmed ? "preconfirmed" : "settled"`. The prior shape never returned `"spent"` so spent VTXOs were misclassified as `"settled"`. Correctness fix only — the `"spent"` state value was already part of `VirtualCoinStatus.state`. Since 0.4.35: imports `baseFetch` from `../utils/fetch` (NOT the header-setting `fetch`) — the indexer origin rejects the `X-Build-Version` / `X-SDK-VERSION` headers in CORS preflight
│   ├── onchain.ts           # EsploraProvider + ESPLORA_URL defaults (Ark Labs mempool deployments); since 0.4.28 `baseUrl` parameter defaults to `ESPLORA_URL[DEFAULT_NETWORK_NAME]` (bitcoin entry of the default map). **Post-0.4.33 (regtest migration)**: the `regtest` entry of `ESPLORA_URL` is now `http://localhost:3000/api` (was `http://localhost:3000`) — the in-house arkade-regtest stack exposes the mempool-spec Esplora API under `/api` (`7e34960a`). `EsploraProvider.getChainTip()` switched from `/blocks/tip` to the standard `/blocks` route (`abd86ec3`): `/blocks/tip` was electrs-only and mempool returns `[]` for it (surfaced as `No chain tip found` and cascaded into unroll / settle / sweep / delegate / vhtlc e2e); `/blocks` returns a newest-first array of recent blocks across every Esplora backend. `EsploraProvider.getFeeRate()` returns `undefined` on a 404 from `/fee-estimates` rather than throwing (`5f9a6845`): mempool (spec-correct) returns 404 for that route on regtest — it exposes fees via `/api/v1/fees/recommended` — and every caller already falls back to `MIN_FEE_RATE` on undefined, so the prior throw broke the unroll / unilateral vHTLC claim / boarding-sweep paths against a mempool-spec backend; other (5xx) failures still surface. Since 0.4.35: imports `baseFetch` (no custom headers — the Esplora origin rejects `X-Build-Version` / `X-SDK-VERSION` in CORS preflight). 0.4.40 (#587): `getTxOutspends` return type relaxed to `{ spent; txid? }[]` (some deployments, e.g. `mempool.arkade.sh`, omit the spender txid from `/outspends`) and `ExplorerTransaction` gains an optional `vin?: { txid; vout }[]` (Esplora `/address/:addr/txs` input outpoints; the electrum provider omits it, so `isExplorerTransaction` validates `vin` only when present) — the wallet uses `vin` to recover a boarding output's commitment tx when `/outspends` lacks the spender, fixing phantom-receive double-counting in `getBoardingTxHistory`
│   ├── electrum.ts          # ElectrumOnchainProvider (WebSocket Electrum) + ELECTRUM_WS_URL / ELECTRUM_TCP_HOST defaults. Since 0.4.35 (#562): `getTxStatus` classifies `RPCError`-wrapped "not found" errors so a not-yet-broadcast / unknown tx returns a clean unconfirmed status instead of throwing intermittently (de-flake). 0.4.40 (#571): new `normalizedErrorText(err)` lowercases AND strips all whitespace from the server error before substring matching — `ws-electrumx-client`'s frame parser drops the spaces inside server error messages, so the `get_merkle` classifiers (`isMissingHeightError` / `isTxNotInBlockError`) never matched the space-mangled wording and `getTxStatus` threw on the index-lag race. The `isTxNotInBlockError` needles are now whitespace-free (`notyetinablock` / `notinablock` / `notinblock` / `noconfirmedtransaction`) to match the stripped haystack while still matching servers that send spaces intact
│   ├── delegate.ts          # RestDelegateProvider + DelegateProvider interface (delegate REST; renamed from delegator.ts in 0.4.29 / #519 — `RestDelegatorProvider` / `DelegatorProvider` kept as `@deprecated` aliases). `DelegateInfo.delegatorAddress` now optional alongside the canonical `delegateAddress`; `isDelegateInfo` accepts either non-empty string; `getDelegateInfo` normalizes `delegateAddress` by explicit string type check (not truthiness). Since 0.4.35: imports `baseFetch` (no custom headers — the delegate origin rejects `X-Build-Version` / `X-SDK-VERSION` in CORS preflight)
│   ├── expoArk.ts           # ExpoArkProvider (React Native SSE); since 0.4.28 `serverUrl` parameter defaults to `DEFAULT_ARKADE_SERVER_URL`
│   ├── expoIndexer.ts       # ExpoIndexerProvider (React Native streaming); since 0.4.28 `serverUrl` parameter defaults to `DEFAULT_ARKADE_SERVER_URL`
│   ├── expoUtils.ts         # Expo streaming utilities. Post-0.4.34: `getExpoFetch` wraps the dynamically imported `expo/fetch` so it also sets the `X-Build-Version` header (from `utils/fetch.ts`'s `buildVersion`) on streaming requests; the non-Expo fallback path returns the header-setting `fetch` from `../utils/fetch`. Since 0.4.35: also imports and sets `sdkVersion` (`X-SDK-VERSION`) alongside `X-Build-Version` on the expo/fetch path
│   ├── errors.ts            # ArkError, error handling. **Post-0.4.45 unreleased (`2cf00046`)**: adds the `ArkErrorName` catalog (const object + union type — `DIGEST_MISMATCH` / `VTXO_ALREADY_SPENT` / `INVALID_TX_FILTER` / `TX_FILTERS_LIMIT_EXCEEDED`; only the structured arkd `name`s the SDK branches on, not an exhaustive server mirror) and the `isArkError(error, name?): error is ArkError` type guard (narrows to `ArkError`, optionally to a specific cataloged `name`), both re-exported from the package root; call sites (`RestArkProvider` digest-mismatch handler, `VtxoManager.extractSpentOutpoint`) replaced their `err.name === "..."` literal comparisons with `isArkError(err, ArkErrorName.X)`. **Unreleased on top of 0.4.43 (offline-first)**: adds typed `ProviderUnavailableError` (`readonly retryable = true`, original as `Error.cause`) for retryable availability failures (transport / timeout / 429 / 5xx) vs terminal ones (`ArkError` / plain `Error`), plus `ProviderKind = "arkade" | "indexer"` (deliberately NOT a structured field — own-properties don't survive `postMessage`). `throwIfHttpUnavailable(response, kind, body?)` throws on 429/5xx UNLESS the body decodes to a structured arkd error (grpc-gateway maps gRPC INTERNAL → HTTP 500 — a deliberate terminal rejection, not unavailability; mirrors NArk's BuildVersionHandler); `toProviderUnavailable(err, kind)` maps a transport `FetchError` to the typed error. All re-exported from the package root
│   ├── availability.ts      # Unreleased on top of 0.4.43: `isRetryableProviderError(err)` — `true` for `ProviderUnavailableError` / `FetchError` / `isFetchTimeoutError` (transport, timeout, already-classified 429/5xx), `false` for everything terminal (4xx, invalid JSON, schema/network-mismatch). Re-exported from the package root
│   └── utils.ts             # Provider utilities
│
├── script/                  # Bitcoin script construction
│   ├── base.ts              # VtxoScript, TapLeafScript, TapTreeCoder, getSequence; VtxoScript.exitPaths now compares isScriptValid === true (prior truthy check routed ConditionCSV leaves to CSV's decode). Since 0.4.28: `VtxoScript.address(prefix?)` defaults `prefix` to `DEFAULT_NETWORK.hrp` and `VtxoScript.onchainAddress(network?)` defaults `network` to `DEFAULT_NETWORK` (both imported from `../networks`). Since 0.4.34: builds the Taproot script tree via `assembleBtcdTaprootTree(scripts)` (from `./taprootTree.ts`) instead of `@scure/btc-signer`'s `taprootListToTree` (a Huffman builder that agreed with arkd's btcd dependency only for power-of-2 leaf counts and diverged for every other count — different merkle root, different output key, `INVALID_PSBT_INPUT` on spend); the obsolete odd-count `scripts.reverse()` workaround is removed
│   ├── address.ts           # ArkAddress encoding/decoding
│   ├── default.ts           # DefaultVtxo script. Post-0.4.32: `Options.csvTimelock` is now **required** (`csvTimelock?: RelativeTimelock` → `csvTimelock: RelativeTimelock`); the constructor's `?? Script.DEFAULT_TIMELOCK` fallback in the destructuring assignment is removed. JSDoc example updated to spell out an explicit `csvTimelock`. Breaking for direct `new DefaultVtxo.Script({...})` callers — wallet API surface unchanged. `DefaultContractHandler.deserializeParams` keeps a one-sided fallback for legacy persisted params (see `contracts/handlers/default.ts`)
│   ├── delegate.ts          # Delegation script. Post-0.4.32: same `csvTimelock`-required change as `DefaultVtxo.Script`; constructor JSDoc example updated. `DelegateContractHandler.deserializeParams` no longer keeps a `DEFAULT_TIMELOCK` fallback (delegate persistence always carries the param)
│   ├── tapscript.ts         # Tapscript types (CSV, CLTV, Condition multisig variants); per-namespace isScriptValid helpers returning true | Error, shared getVerifyIndex between condition tapscripts
│   ├── taprootTree.ts       # 0.4.34 (#8d432a13): btcd-compatible script-tree builder. Exports `assembleBtcdTaprootTree(scripts: Bytes[]): TaprootTreeNode`, plus `TaprootLeaf` / `TaprootTreeNode` (all re-exported from the package root). Reproduces btcd's `txscript.AssembleTaprootScriptTree`: Phase 1 pairs leaves left-to-right and merges an odd trailing leaf into the LAST branch built so far (NOT paired with a fresh leaf); Phase 2 is a FIFO-queue merge — take front two branches, combine, push to back, repeat until one branch remains. `VtxoScript` (in `base.ts`) consumes this in place of `@scure/btc-signer`'s `taprootListToTree` (a Huffman builder that agrees only for power-of-2 leaf counts — for every other count the two diverged → different merkle roots → arkd rejected spends with `INVALID_PSBT_INPUT`). The obsolete odd-count `scripts.reverse()` workaround in `VtxoScript` is removed. Tested against btcd-derived golden taproot keys for leaf counts 3, 5, 9, 10, 11, 13, 15, 17 in `test/fixtures/vtxoscript.json`
│   └── vhtlc.ts             # VHTLC (Virtual Hash Time-Locked Contract)
│
├── contracts/               # Contract orchestration
│   ├── types.ts             # ContractVtxo (raw-from-indexer, `Partial<TapLeaves & EncodedVtxoScript>`) and ExtendedContractVtxo (annotated, ExtendedVirtualCoin & { contractScript }) — the latter is the post-`annotateVtxos` shape used at save and forfeit-construction sites; both ContractWithVtxos and the ContractManager internal bulk-fetch helpers now return ExtendedContractVtxo. Since 0.4.28 (#492) also defines the `Discoverable` capability — `discoverAt(descriptor: string, deps: DiscoveryDeps): Promise<DiscoveredContract[]>` — plus `DiscoveryDeps` (indexer + onchain provider + wallet identity context) and `DiscoveredContract` (the handler's serializable hit shape). Structural `isDiscoverable(handler)` guard added; all three are re-exported from the package root. Since 0.4.34: `DiscoveryDeps.deprecatedSignerPubKeys?: Uint8Array[]` carries the server's deprecated signer keys (x-only, 32 bytes each) from a fresh `getInfo` snapshot at restore time — `default`/`delegate` discovery scans these alongside `serverPubKey` so signer rotation does not strand L2 VTXOs (boarding discovery still consults `serverPubKey` only — boarding's source of truth is the on-chain UTXO set)
│   ├── metadata.ts          # Dependency-free leaf module (added 0.4.28, #492) — source-of-truth declaration of `WALLET_RECEIVE_SOURCE = 'wallet-receive'`. `contracts/handlers/default.ts` imports from here; `wallet/walletReceiveRotator.ts` re-exports for backward compatibility. Existed to break the `contracts → wallet` import cycle that blocked the contract handlers from reading the tag
│   ├── constants.ts         # Dependency-free leaf module (added 0.4.34) — source-of-truth declaration of `DEFAULT_PAGE_SIZE = 500`, the indexer pagination size shared by `ContractManager`'s bulk history sync and the handler-layer batched `detectUsedScripts` probe. Lives in its own no-import module to avoid a `handlers → contractManager` import cycle (contractManager imports the handler registry, so handlers must not import back from contractManager)
│   ├── contractManager.ts   # IContractManager + impl; refreshOutpoints(outpoints) for surgical indexer-by-outpoint upserts at the contract address. Internal `getVtxosForContracts` / `fetchContractVtxosBulk` / `fetchContractVxosFromIndexer` typed `ExtendedContractVtxo[]` (dropped the unsafe `as ContractVtxo` casts). Since 0.4.28 (#492): `scanContracts({ deps, hd: boolean, gapLimit }): Promise<ScanResult>` gap-limit discovery loop iterates each registered `Discoverable` handler and probes materialized descriptors at successive HD indexes (`hd: false` short-circuits to a single static pass at index 0). HD scan caps the range at `SCAN_MAX_INDEX = 10_000` rather than `POSITIVE_INFINITY` — hitting the ceiling without closing the `gapLimit` window **throws** so a buggy/malicious handler returning unconditional hits can't hang the wallet, and so silently truncating a fund-recovery scan can't mask the failure. Each hit is persisted via a new lighter `persistAndWatchContract` that omits the per-contract `fetchContractVxosFromIndexer` pull — the trailing `Wallet.restore` follow-up `refreshVtxos({ includeInactive: true })` already covers the same scripts in one batched indexer call (saves N per-contract round trips proportional to scan hits). `createContract` factored to keep the indexer fetch for standalone callers without a trailing refreshVtxos. Returns `{ lastIndexUsed, handlerErrors: HandlerError[] }`; `ScanContractsOptions`, `ScanResult`, `HandlerError` exported from the package root. Local `let found: DiscoveredContract[];` typing avoids the inferred `| undefined` (catch path does `continue`). Since 0.4.31 (refs #521): `getVtxosForContracts` instantiates a per-call `ContractTapscriptCache = new Map()` before the `vtxos.map(vtxo => extendVirtualCoinForContract(vtxo, byScript, tapscriptCache))` so the taproot tree (built by `handler.createScript(contract.params)`) is rebuilt once per distinct contract in the batch instead of once per VTXO — the dominant cost when annotating long spent/swept histories surfaced via `getVtxos()`/`getBalance()`. `ContractTapscriptCache` typed import added from `../wallet/utils`. Since 0.4.34: `ScanContractsOptions.batchSize?` (defaults to `DEFAULT_SCAN_BATCH = 10`) windows the outer scan loop `batchSize` indices at a time on top of the per-index probe concurrency; each window is capped to `gapLimit - unused` so every probed index is one a serial scan would reach (nothing over-scanned; discovered set byte-identical; an empty wallet closes its gap window in `ceil(gapLimit/batch)` rounds). Validates `batchSize` is a positive integer alongside `gapLimit`. **Unreleased on top of 0.4.43 (offline-first)**: initialize / reads / hydration degrade to repository data on a retryable provider failure (`isRetryableProviderError`) instead of throwing — `getVtxos` / `getBalance` / history stay offline-first (the repository is the system of record). New `getSyncState(): ContractSyncState` (`{ mode: "online" | "degraded"; reason?; lastSyncedAt? }`) tracks provider-sync health; watcher-driven syncs (connection_reset recovery, vtxo delta syncs) also mark degraded on retryable failure rather than swallowing it. `ContractManagerConfig` accepts an optional `intentRepository`; the composed `ProviderConnectionState` in `wallet.ts` folds this state in. `ContractSyncState` re-exported from the package root
│   ├── contractWatcher.ts   # SSE-driven watcher; seedLastKnownVtxos baseline now script-gated (legacy wrong-script rows no longer seed phantom vtxo_spent events); extend-vtxo accumulator typed `ContractVtxo[]` (compile-time drift catch), extend-failure log now includes `txid:vout` + caught error for production grep-ability. Since 0.4.35 (#564): recovers promptly after its SSE subscription is disrupted (reconnect path) rather than silently going quiet — covered by `test/contractWatcher-reconnect.test.ts`
│   ├── handlers/default.ts  # DefaultContractHandler — since 0.4.28 (#492) implements `Discoverable.discoverAt`: probes every csvTimelock in the baseline matrix at the given descriptor's leaf pubkey and returns any contracts the indexer shows on-chain history for. Index 0 produces an untagged `DiscoveredContract`; index > 0 tags with `metadata.source = WALLET_RECEIVE_SOURCE` + `metadata.signingDescriptor` so the rotator + signing router can find it later. Type annotation widened to `ContractHandler<...> & Discoverable`. Now imports `WALLET_RECEIVE_SOURCE` from `../metadata` (cycle break). Post-0.4.32: `deserializeParams` restores the `DEFAULT_TIMELOCK` fallback for legacy/minimal persisted params (e.g. hex pubkeys with no `csvTimelock`) via an explicit `params.csvTimelock !== undefined && params.csvTimelock !== ""` test — the script-side fallback is gone after #(csvTimelock-required), so feeding `Number(undefined)` to `sequenceToTimelock` would silently decode to zero; the deserializer is intentionally permissive while the script constructor stays strict, so legacy persisted rows still hydrate. Since 0.4.34: probes the current `serverPubKey` first, then each `deprecatedSignerPubKeys` entry (deduped by scriptHex). The matched signer is threaded through the constructed script, persisted `params.serverPubKey`, and the encoded `address` so signing/forfeit on a restored VTXO resolves the right key. Per-index work is now concurrent (Promise.all over the candidate scripts) and folded into one batched `detectUsedScripts` indexer probe (from `helpers.ts`) instead of N per-variant `getVtxos` loops
│   ├── handlers/delegate.ts # DelegateContractHandler — since 0.4.28 (#492) also implements `Discoverable.discoverAt` for delegated `default + delegate` contracts (multi-timelock matrix coverage; tests assert two distinct csvTimelocks return two separate entries, each with its own `params.csvTimelock` serializing to its own `timelockToSequence` value). Post-0.4.32: `deserializeParams` no longer keeps a `params.csvTimelock` fallback to `DEFAULT_TIMELOCK` (delegate persistence always carries the param); dead `DefaultVtxo` import removed. Since 0.4.34: same deprecated-signer scan + concurrent-per-index + single-batched-indexer-probe shape as `default.ts`; the signer pubkey is threaded into both the embedded `default` params and the wrapping `delegate` params so a recovered delegated VTXO carries the right key end-to-end
│   ├── handlers/helpers.ts  # Shared handler primitives. Hosts `resolveRole(contract, context)` (descriptor/pubkey-matched sender/receiver resolution) plus the BIP-68 / BIP-65 timelock-maturity helpers `isCsvSpendable(context, sequence?)` (returns `true` for `undefined` sequence) and `isCltvSatisfied(context, locktime)` (BIP-65: `locktime < 500_000_000n` compares `context.blockHeight`, `>= 500_000_000n` compares `Math.floor(context.currentTime / 1000)`, `false` when the relevant context field is missing). The built-in handlers (`DefaultContractHandler` / `DelegateContractHandler` / `VHTLCContractHandler`) use these to implement `selectPath` / `getSpendablePaths`. Since 0.4.33 `isCsvSpendable` and `isCltvSatisfied` are re-exported from the package root so custom `ContractHandler` authors can reuse the canonical maturity logic instead of reimplementing it. Since 0.4.34: also hosts `detectUsedScripts(indexerProvider, scriptHexes)` — single batched indexer probe shared by `default`/`delegate` `discoverAt`. Pages `getVtxos({ scripts: scriptHexes, pageIndex, pageSize: DEFAULT_PAGE_SIZE })` from `../constants.ts` until every candidate is seen, deleting hits from a `remaining` set; short-page heuristic ends pagination. Replaces N per-variant `getVtxos` calls per index with one batched call
│   ├── handlers/boarding.ts # BoardingContractHandler (post-0.4.32) — registered `boarding` contract type. `BoardingContractParams = DefaultContractParams` (boarding semantics come from the type + from sourcing `csvTimelock` from the server's `ArkInfo.boardingExitDelay` rather than from renamed params; the offchain `default` contract is built from `ArkInfo.unilateralExitDelay`, so the two share the `DefaultVtxo.Script` shape but differ in CSV timelock value). All operations delegate to `DefaultContractHandler` (`createScript` / `serializeParams` / `deserializeParams` / `selectPath` / `getAllSpendingPaths` / `getSpendablePaths`). Deliberately does **not** implement `Discoverable.discoverAt` — `isDiscoverable(BoardingContractHandler) === false`; branch/index selection for HD wallets is owned by the wallet/address-provider layer, which hands the handler an already-derived pubkey. Identity/collision model: a contract's `script` is its unique key, so the boarding script gets its own row whenever its CSV timelock differs from the wallet's `default` baseline (the real-world case — sound servers keep `boardingExitDelay` strictly longer than `unilateralExitDelay`). When the two delays coincide (misconfigured/malicious server) the boarding script is byte-identical to the default script and the wallet coalesces onto `default` via `ensureWalletContract` ("default wins"; see `wallet/wallet.ts`). `BoardingContractHandler` + `BoardingContractParams` exported from the package root and registered in `contracts/handlers/index.ts` alongside `default`/`delegate`/`vhtlc`; `scripts/smoke-dist.mjs` expected handler types updated to `boarding,default,delegate,vhtlc`
│   └── vtxoOwnership.ts     # Ownership-gating helpers — applied at every contract-scoped read/write site so legacy address buckets cannot leak wrong-script rows or win txid:vout dedup; throw on user paths, warn-and-skip on background sync. Since 0.4.25 (Tier 2 of #480): `getVtxosForContract` / `saveVtxosForContract` dispatch helpers route to optional `getVtxosForScript` / `saveVtxosForScript` when the repo backend implements them, otherwise fall back to Tier 1 address-bucket read + `filterVtxosForScript` (with `validateVtxosForScript` on the save fallback). Used by `wallet.ts` (`updateDbAfterOffchainTx` / `updateDbAfterSettle`), `contractManager.ts` (`fetchContractVtxos`, `reconcilePendingFrontier`, `fetchContractVxosFromIndexer`, `getContractVtxos`), and `contractWatcher.ts` (`seedLastKnownVtxos` baseline)
│
├── musig2/                  # MuSig2 distributed signing
│   ├── index.ts             # MuSig2 module exports
│   ├── keys.ts              # Key aggregation
│   ├── nonces.ts            # Nonce generation and aggregation
│   └── sign.ts              # Partial signature creation
│
├── tree/                    # Transaction tree construction
│   ├── txTree.ts            # TxTree, TxTreeNode
│   ├── signingSession.ts    # SignerSession, TreeNonces, TreePartialSigs
│   └── validation.ts        # validateVtxoTxGraph, validateConnectorsTxGraph
│
├── intent/                  # Intent proof generation
│   └── index.ts             # Intent class. 0.4.40 (#578): `craftToSignTx(toSpend, inputs, outputs, message)` now receives the canonical proof `message` and writes it into the BIP-322 v2 global field `PSBT_GLOBAL_GENERIC_SIGNED_MESSAGE` (`0x09`) on the to_sign tx so a co-signer/verifier can recompute the `toSpend` commitment from PSBT-internal data alone. `@scure/btc-signer` has no public global-field setter, so the entry is appended directly to `tx.global.unknown` (`{ type: 0x09, key: <empty> }` → UTF-8 message bytes); `0x09` is unused in the global keymap so it round-trips as an unknown field preserved by `toPSBT()`/`fromPSBT()`
│
├── arknote/                 # ArkNote serialization
│   └── index.ts             # ArkNote class
│
├── arkfee/                  # Ark fee calculation
│   └── estimator.ts         # `Estimator` fee aggregator. **0.4.40 (#581, BREAKING)**: the aggregate method `Estimator.eval(...)` is renamed to `Estimator.evaluate(...)` — MetaMask Snaps bundle the SDK through SES, whose static "direct eval" detector rejects any bare `eval(` token, so the old name made the whole bundle unusable inside a snap (arkade-os/ts-sdk#580). The per-input helpers (`evalOffchainInput` / `evalOnchainInput` / `evalOffchainOutput` / `evalOnchainOutput`) are unaffected — SES only flags the exact `eval` identifier
│
├── asset/                   # Asset module
│   └── ...
│
├── repositories/            # Data access layer
│   ├── index.ts             # Repository interfaces
│   ├── walletRepository.ts  # WalletRepository interface — Tier 2 script-scoped methods (`getVtxosForScript` / `saveVtxosForScript` / `deleteVtxosForScript`) marked optional + `VtxoRepositoryKey = { script; address? }` since 0.4.25; backends (`inMemory/`, `indexedDB/`, `realm/`, `sqlite/`) all implement the optional methods natively (IndexedDB uses the `script` index + outpoint dedup with `shouldReplaceVtxo` tiebreaker; SQL uses `WHERE script = ?`; Realm uses `filtered("script == $0", ...)`). DB errors in `getVtxosForScript` are re-thrown rather than swallowed to `[]`. Post-0.4.40 (unreleased, `b8357c20`): `indexedDB/manager.ts` `openDatabase` now registers `db.onversionchange` on each opened connection — it `close()`s the connection and evicts it from `dbCache` / `refCounts` so an external `indexedDB.deleteDatabase()` (or a version upgrade in another tab) is not blocked by a still-open SDK connection; this unblocks the wallet-`clear()` teardown path
│   ├── contractRepository.ts # ContractRepositoryImpl (contract data, collections)
│   ├── intentRepository.ts  # 0.4.43: `IntentRepository` interface (`version: 1`) + `ArkIntent` / `ArkIntentState` / `IntentFilter` domain types, `INTENT_TERMINAL_STATES` / `isTerminalIntentState` / `ALL_INTENT_STATES`, and the backend-agnostic helpers `assertIntentIdUnique` (enforces "intentId unique when present" for in-memory/Realm — SQLite/IndexedDB do it natively), `intentMatchesFilter`, `intentPageBounds`. Keyed by `intentTxId`; `getLockedVtxoOutpoints()` returns outpoints held by NON-terminal intents (`waiting_to_submit`/`waiting_for_batch`/`batch_in_progress`) for balance exclusion — a deliberate divergence from NArk (offline-first single source of coin-locking). All types re-exported from the package root
│   ├── virtualTxRepository.ts # 0.4.43: `VirtualTxRepository` interface (`version: 1`) + `VirtualTx` (`{ txid; psbt: base64 | null; expiresAt; type }`), `VtxoBranch`, and the `ChainedTxType` enum (`Unspecified/Commitment/Ark/Tree/Checkpoint`, pinned from NArk) + `mergeChainedTxType` (never downgrades a known type to `Unspecified`). `upsertVirtualTxs` / `getBranch` / `setBranch` / `pruneForSpentVtxo`. **Experimental / inert**: only `Unroll.Session` reads/writes it as a best-effort raw-tx cache — normal sync never populates or prunes it. All re-exported from the package root
│   ├── inMemory/{intentRepository,virtualTxRepository}.ts # 0.4.43: in-memory backends (`InMemoryIntentRepository` / `InMemoryVirtualTxRepository`), exported from the package root
│   ├── indexedDB/{intentRepository,virtualTxRepository}.ts + schema.ts # 0.4.43: IndexedDB backends. The shared wallet DB stays pinned at `DB_VERSION = 3`; the `intents`/`virtualTxs`/`vtxoBranches` stores live at `INTENT_DB_VERSION = 5` (v5 makes `intents.intentId` unique) on a **dedicated DB name**, reachable only via the opt-in repos — upgrading the SDK never migrates an existing user's DB
│   ├── sqlite/{intentRepository,virtualTxRepository}.ts + transaction.ts # 0.4.43: SQLite backends (transactional batch writes); `transaction.ts` serializes writes per shared connection so batch writes can't interleave. Reachable via the `repositories/sqlite` subpath entry
│   ├── realm/{intentRepository,virtualTxRepository}.ts + schemas.ts # 0.4.43: Realm backends + Realm schema v3 (intent + virtualtx schemas). Reachable via the `repositories/realm` subpath entry
│   └── serialization.ts     # SerializedAsset / serializeAssets / deserializeAssets (bigint→decimal-string round-trip; legacy number/string accepted on read)
│
├── storage/                 # Storage adapter interface
│   └── ...
│
├── adapters/                # Platform-specific storage
│   ├── localStorage.ts      # Browser localStorage
│   ├── indexedDB.ts          # Browser/SW IndexedDB
│   ├── fileSystem.ts         # Node.js file system
│   ├── asyncStorage.ts       # React Native AsyncStorage
│   └── expo.ts              # Expo adapter re-exports
│
├── types/                   # Shared type definitions
│   └── ...
│
└── utils/                   # Utility functions
    ├── transaction.ts       # Transaction construction
    ├── arkTransaction.ts    # Off-chain tx building, tapscript signature verification. `combineTapscriptSigs(signedTx, originalTx)` merges per-input tapscript sigs in place (`originalTx.tapScriptSig ++ signedTx.tapScriptSig`) for the BatchSignableIdentity send path. Since #535 the invariant "both parties signed every input" is explicit: rejects an input-count mismatch and a missing `tapScriptSig` on either side with input-indexed errors (e.g. `combineTapscriptSigs: signedTx input 3 has no tapScriptSig`) — the prior code silently appended `undefined` when `signedTx` was unsigned, corrupting the witness. Callers that partially sign must merge only fully co-signed inputs
    ├── transactionHistory.ts # `buildTransactionHistory(vtxos, allBoardingTxs, commitmentsToIgnore, ...)` correlates VTXOs + boarding txs into `ExtendedArkTransaction[]`. Since 0.4.31: snapshots settled boarding receives into `unmatchedSettledBoardingTxs` (sorted by `createdAt`) at the top of the build and consumes them via `consumeBoardingReceive(predicate)` as matching leaves are processed, suppressing the duplicate batch-leaf row that surfaced whenever a settled boarding deposit also produced a leaf VTXO. Two consume modes: (a) when the leaf's commitment is in `commitmentsToIgnore` (or its `settledBy` is — gated by the new `(!vtxo.settledBy || !commitmentsToIgnore.has(vtxo.settledBy))` clause on the ignored-commitment guard so swept boarding deposits also take the dedup branch), consumes by `(commitmentTxid match OR settledBy match) AND createdAt ≤ vtxo.createdAt`; (b) when the leaf is **not** ignored and has no refresher, consumes by exact `amount` match (and createdAt window), and on hit suppresses the would-be batch-leaf row. New private helpers `consumeBoardingReceive(boardingTxs, predicate)` (returns bool, splices the matched entry) and `isSettledBoardingReceive(tx)` (`tx.type === TxType.TxReceived && tx.settled && tx.key.boardingTxid !== ""`). Since 0.4.35 (`40bf82f5`): skips pushing the offchain `TxSent` row when `txAmount === 0 && !assets` — a signer-rotation migration spends every VTXO to one full-amount self output (`spentAmount === changeAmount`, net 0), so the old code emitted a ghost zero-amount sent; the `if (txAmount !== 0 || assets)` guard keeps zero-amount asset sends (issuance/reissuance/burn)
    ├── unknownFields.ts     # PSBT custom fields (VtxoTaprootTree, CosignerPublicKey, etc.)
    ├── anchor.ts            # P2A (Pay-to-Anchor) and AnchorBumper
    ├── timelock.ts          # Centralized BIP68 helpers — `timelockToSequence` / `sequenceToTimelock` (RelativeTimelock ↔ sequence number); single `bip68` import site, consumed by `script/base.ts`, `script/tapscript.ts`, `utils/unknownFields.ts`, `wallet/wallet.ts`, `wallet/unroll.ts`, and re-exported from the package root since 0.4.23
    ├── txSizeEstimator.ts   # TxWeightEstimator + VSize type (fee estimation, re-exported from package root since 0.4.23)
    ├── fetch.ts             # Header wrappers over `globalThis.fetch`. Since 0.4.43 also exports `FetchError extends Error` (`{ url?, method? }`, preserves the original as `Error.cause`): `baseFetch` catches transport-level rejections (DNS/refused/TLS/CORS) and rethrows as `FetchError("Network request failed: <method> <url>")` via `describeRequest` (handles `string | URL | Request`); input type widened to `RequestInfo | URL`; re-exported from the package root. Since 0.4.35 splits into two: `baseFetch(input, init?)` is the guarded passthrough with NO Arkade headers (throws if the Fetch API is unavailable), and `fetch(input, init?)` is the **arkd-only** wrapper that sets `X-Build-Version: <buildVersion>` AND `X-SDK-VERSION: <sdkVersion>` via `new Headers(init?.headers)` + `headers.set` (composes with caller headers in any shape). Exports `buildVersion` (now `"0.9.9"`, tracks the arkd/Arkana server build) and `sdkVersion` (`` `ts-sdk/${version}` ``, `version` imported from `package.json` so it tracks every release bump). `ark.ts` + `expoUtils.ts` use the header-setting `fetch`; `indexer.ts` / `delegate.ts` / `onchain.ts` use `baseFetch` because those origins reject unknown custom headers in CORS preflight. Lets the server (Arkana) correlate requests with the client build and distinguish client versions. Lineage: `X-Server-Digest` (`02c4c5ca`) → `X-Build-Version` (`98ffc6de`) → Headers-based always-applied (`8224a35a`) → expo fetch (`159c7f7d`) → arkd-only `baseFetch` split + `buildVersion → 0.9.9` (`f8dfc3d0`) → `X-SDK-VERSION` added (`a290f2f2`)
    └── bip21.ts             # `BIP21.create()` / `BIP21.parse()` for BIP21 URIs. Post-0.4.33: amount validation aligned between create + parse. `create()` rejects non-finite amounts and amounts that fail `Number.isSafeInteger(Math.trunc(value))` (returns the URI without the `amount` param — silently omits an unsafe amount rather than emitting an out-of-range integer), and keeps the explicit negative check (callers can still pass a negative `number`). `parse()` validates the raw query-string value against the BIP21 ABNF (`*digit [ "." *digit ]`) via `/^(?:\d+\.?\d*|\.\d+)$/` — so `".5"` decodes to `0.5` and `"5."` decodes to `5` (matches `NArk`'s parser); the dropped unreachable negative check was redundant after the regex (the sign is not in the character class). Tests live in `test/bip21.test.ts` (4 new cases: digits omitted on either side of decimal, malformed values ignored, unsafe values ignored on parse, unsafe values omitted on create)
```

## Design Patterns

### Provider Pattern

All external communication is abstracted behind provider interfaces:

- `ArkProvider` — arkd server (settlement events, transaction submission, info)
- `IndexerProvider` — Indexer (address subscriptions, VTXO updates, tx history)
- `OnchainProvider` — Block explorer (UTXOs, transactions, broadcasting)
- `DelegateProvider` — Delegate service (VTXO renewal delegation); renamed from `DelegatorProvider` in 0.4.29 (#519), old name kept as a deprecated alias

Each provider has a REST implementation (`RestArkProvider`, etc.) and Expo-compatible variants for React Native.

### Identity Abstraction

The `Identity` interface decouples key management from wallet logic:

- `SingleKey` — Raw private key (simplest, for testing/prototyping)
- `SeedIdentity` — HD wallet from raw seed bytes with BIP86 derivation; `implements HDCapableIdentity`
- `MnemonicIdentity` — HD wallet from BIP39 mnemonic phrase (extends `SeedIdentity`)
- `ReadonlyDescriptorIdentity` — Watch-only from xpub-derived account descriptor template; `implements ReadonlyHDCapableIdentity`

Derivation path: `m/86'/{coinType}'/0'/0/*` (BIP86 Taproot, wildcard template).

Seed-backed and watch-only identities are now conceptually HD wallets and consume a wildcard-suffixed account descriptor template (e.g. `tr([fp/86'/0'/0']xpub/0/*)`). The public `descriptor` field carries that template; consumers materialize a concrete descriptor at index N via the descriptor library (`expand({ descriptor, network, index }).canonicalExpression`). The wire format (`SerializedSigningIdentity.descriptor` / `SerializedReadonlyIdentity.descriptor`) also stores the template; older envelopes carrying concrete `/N)` descriptors continue to deserialize via `templateOf` chop.

### Descriptor Provider Pattern

`DescriptorProvider` is a pure rotating allocator decoupled from "current state":

- `getNextSigningDescriptor()` — allocates and returns a fresh signing descriptor on each call (HD rotates the index, single-key returns the same descriptor)
- `isOurs(descriptor)` — descriptor-membership predicate
- `signWithDescriptor(requests)` / `signMessageWithDescriptor(...)` — descriptor-keyed signing

Implementations:

- `StaticDescriptorProvider` — wraps a legacy `Identity` with a single fixed descriptor.
- `HDDescriptorProvider` (`src/wallet/`) — backed by `HDCapableIdentity`; persists `{ descriptor, lastIndexUsed }` under `WalletState.settings.hd`. Read-modify-write of the index runs inside the per-repo `updateWalletState` mutex, serializing allocation across multiple provider instances on the same repo. First allocation returns index 0; the descriptor-mismatch guard refuses to reuse HD state written by a different seed. Also implements `ReceiveRotatorFactory.createReceiveRotator` (delegates to `WalletReceiveRotator.defaultBoot`), and exposes `getCurrentSigningDescriptor()` for stable boot replay (re-derives at `lastIndexUsed` without advancing).

The provider has no read-side accessor for "current rotation state" — "what addresses am I bound to right now?" is answered by querying the contract repository for active contracts tagged `metadata.source === 'wallet-receive'`, mirroring the dotnet SDK's `IArkadeAddressProvider` design.

### Receive Rotation Pattern

`WalletReceiveRotator` orchestrates the wallet-side receive lifecycle around a `DescriptorProvider`:

- **Boot** — If the provider implements `ReceiveRotatorFactory`, the wallet calls `createReceiveRotator(opts)`; otherwise falls back to `WalletReceiveRotator.defaultBoot(provider, opts)`. The default boot looks up the active default/delegate contract tagged `metadata.source = 'wallet-receive'` matching the current `serverPubKey`; if found, reuses its pubkey (no provider call); else allocates index 0 via `getNextSigningDescriptor()`.
- **Rotation** — Subscribes to `vtxo_received`. When the event fires for the currently-tagged display contract, the rotator (serialized by an internal `_hdRotationChain` mutex) allocates the next descriptor, rebuilds `offchainTapscript`, registers the new tagged contract, and marks the prior display `inactive` (the watcher's `state === 'active' || lastKnownVtxos.size > 0` filter keeps watching it until its funds clear). The first rotation does NOT deactivate the baseline.
- **Baseline anchoring** — The multi-timelock baseline matrix (default + delegate × every `walletContractTimelocks` entry) is bound to `identity.xOnlyPublicKey()` (index 0) on every boot, NEVER to the rotated pubkey. Rotated display contracts are intentionally single-timelock-single-pubkey at the current arkd delay.
- **Failure handling** — Consecutive `rotate()` failures gate future attempts behind exponential backoff (1s → 2s → … → 60s cap, resets on success). Pluggable `Logger` interface (defaults to `console`). `NonRangeableDescriptorError` is the typed signal for the silent-fallback path (replaces a prior string match on `err.message`).
- **WalletMode** (`'auto' | 'static' | 'hd' | DescriptorProvider`) drives the wiring decision. `'auto'` currently behaves like `'static'` until HD rotation matures — see `TODO(hd-maturation)` in `resolveDescriptorProvider`.

### Per-Input Signing Dispatch

`InputSignerRouter` decouples PSBT signing from the assumption of a single key:

- Callers (`Wallet._sendImpl`, settlement paths, intent proof paths) hand the router explicit `InputSigningJob[]` with `lookupScript` derived from the source VTXO script (not the witnessUtxo — checkpoint scripts in arkTx don't match the source contract).
- The router groups inputs by owning contract: rotated `default`/`delegate` contracts with a non-baseline owner route to `DescriptorProvider.signWithDescriptor` using `metadata.signingDescriptor` persisted at rotation time; baseline-owned contracts, other contract types, and the boarding script route to `Identity`.
- Throws `DescriptorSigningProviderMissingError` (no provider wired) or `MissingSigningDescriptorError` (a descriptor-capable `default`/`delegate`/`boarding` contract that cannot be routed: its owner key is not the wallet baseline and it carries no `metadata.signingDescriptor`). Since 0.4.37 the error message enumerates both causes — wallet rotated on an earlier build without persisted descriptors, *or* the contract belongs to a different identity (storage reuse) — and suggests setting the descriptor, restoring a pre-rotation snapshot, or deleting the contract. Both are exported from the package root.

### Storage Adapter Pattern

The `StorageAdapter` interface provides platform-agnostic persistence:

```typescript
interface StorageAdapter {
  getItem(key: string): Promise<string | null>
  setItem(key: string, value: string): Promise<void>
  removeItem(key: string): Promise<void>
}
```

### Service Worker Architecture

`ServiceWorkerWallet` bridges main thread and service worker via message passing:

1. Main thread creates `ServiceWorkerWallet.setup()` with identity and server URL
2. Identity is serialized and sent to service worker via postMessage
3. Worker instantiates `Wallet` inside service worker context
4. All wallet method calls are proxied through Request/Response serialization
5. Service worker persists using IndexedDB

## Crypto Dependencies

| Library | Purpose |
|---------|---------|
| `@noble/curves` | Elliptic curve operations (secp256k1) |
| `@noble/secp256k1` | Schnorr signatures |
| `@scure/bip32` | HD key derivation (BIP32) |
| `@scure/bip39` | Mnemonic generation/validation (BIP39) |
| `@scure/btc-signer` | Bitcoin transaction signing, Taproot |
| `@kukks/bitcoin-descriptors` | Output descriptor parsing |
| `@bitcoinerlab/descriptors-scure` | Ranged descriptor expansion (`expand`, `canonicalExpression`, `isRanged`, `scriptExpressions.trBIP32`) used by HD identities and descriptor helpers |
| `ws-electrumx-client` | WebSocket Electrum transport (used by `ElectrumOnchainProvider` via `WsElectrumChainSource.safeBatchRequest`) |
| `bip68` | Relative timelock encoding (CSV) |

## Build Configuration

- **Builder**: `tsup` ^8.5.0 — single `pnpm build` invocation replaces the prior 6-step `tsc` chain (`build:esm` / `build:cjs` / `build:types` / `build:expo:*`) plus `scripts/add-extensions.js` + `scripts/generate-package-files.js` + `scripts/build-browser.js` (#496). Config in `tsup.config.ts`.
- **Target**: `es2022` (was `es2020` under the prior `tsc` build)
- **Format**: Dual ESM + CJS — per-entry `.js` (ESM) + `.cjs` (CJS); per-entry `.d.ts` (ESM types) + `.d.cts` (CJS types); source maps
- **Splitting / treeshake**: `splitting: true` + `treeshake: true` (load-bearing: keeps `contractHandlers` a single runtime instance across entries — `scripts/smoke-dist.mjs` asserts singleton identity across both formats)
- **Output**: Flat `dist/` — e.g. `dist/index.js`, `dist/index.cjs`, `dist/index.d.ts`, `dist/index.d.cts`, `dist/adapters/expo.{js,cjs,d.ts,d.cts}`, `dist/wallet/expo/background.{js,cjs,d.ts,d.cts}`, etc. Was `dist/esm/`, `dist/cjs/`, `dist/types/` under the prior chain.
- **Entries** (`tsup.config.ts`): `src/index.ts`, `src/adapters/{expo,localStorage,fileSystem,indexedDB,asyncStorage}.ts`, `src/repositories/{sqlite,realm}/index.ts`, `src/worker/expo/index.ts`, `src/wallet/expo/index.ts`, `src/wallet/expo/background.ts`, plus `src/contracts/handlers/index.ts` (side-effects-only entry kept at a predictable dist path so `package.json` `sideEffects` keeps resolving)
- **External peers** (not bundled): `expo`, `expo-sqlite`, `expo-task-manager`, `expo-background-task`, `@react-native-async-storage/async-storage`
- **Typecheck**: Separate `pnpm typecheck` (`tsc --noEmit`) gates CI before `pnpm build`. `tsconfig.json` is repurposed as typecheck-only (`noEmit: true`, `moduleResolution: bundler`) — the per-format `tsconfig.cjs.json` / `tsconfig.esm.json` / `tsconfig.expo.json` are deleted, and the build is unconditional (no more `build:expo:check`)
- **Strict mode**: Enabled
- **Smoke / publish-shape gates**: `scripts/smoke-dist.mjs` runs post-build (locally: `pnpm smoke:dist`; CI: after `pnpm build`) and asserts every `package.json` `exports` target (+ `main`/`module`/`types`) exists, every relative import in `dist/**/*.d.{ts,cts}` resolves, ESM + CJS `contractHandlers` singleton identity holds across the root and `contracts/handlers` entries with registered types `{default, delegate, vhtlc}`, each Node-safe public subpath resolves via `@arkade-os/sdk`'s `exports` through a symlinked consumer, and `wallet/expo/background` stays structural-only (would eagerly require optional Expo peers at module init otherwise). CI additionally runs `npm pack --dry-run --ignore-scripts` to verify publish shape without re-running `prepack`
- **Barrel bypass in `src/index.ts`**: imports directly from defining modules in `src/contracts/` and `src/repositories/` instead of going through the local `index.ts` barrels — suppresses Rollup chunk-circularity warnings in tsup's dts emit when `splitting: true` is on. A bare side-effect `import "./contracts/handlers"` is added so handler registration survives tree-shaking (and both src + dist paths are listed in `package.json` `sideEffects`, ESM + CJS)
- **Separate adapter entry points**: Each adapter in `adapters/` has its own export path
- **Expo subpath split**: `/wallet/expo` (foreground) and `/wallet/expo/background` (OS-task helpers) are separate export entries; only the `/background` subpath imports `expo-task-manager` / `expo-background-task`, keeping them invisible to Metro's static dependency collector on the foreground subpath (#487)
- **Ambient declarations for optional Expo peers**: `src/wallet/expo/expo-modules.d.ts` covers `expo-task-manager` + `expo-background-task` + `expo-sqlite` (boltz-swap's pattern for soft-optional peers) so `tsc` type-checks without the packages installed (the previous `tsconfig` exclude of `src/repositories/indexedDB/websqlAdapter.ts` is consequently dropped, #496)
- **Node engines**: `engines.node` = `>=22.12.0 <25`; `.nvmrc` pins development to Node `24.15.0` (CI runs Node 24, #495)
