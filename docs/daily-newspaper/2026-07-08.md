# Ark Daily — 2026-07-08

> _Quiet on dusan's own repos — fulmine and go-sdk had no commits — while the action was elsewhere: arkd shipped a subscription-error change go-sdk should adopt, rust-sdk landed a big contract-manager refactor (0.10.0/0.10.1), and the wallet got a home/settings redesign._

## Front page

- **arkd added a structured `SUBSCRIPTION_NOT_FOUND` error code (50, gRPC `NotFound`)** — go-sdk/fulmine should migrate stale-subscription detection off string-matching onto the code; legacy message preserved so nothing breaks yet (`db93f3d6`) (see Protocol surface).
- **rust-sdk released 0.10.0 + 0.10.1 with a full contract-manager rewrite** — boarding outputs moved into a contract manager, VTXO annotation via contracts, sqlite contract store. A parity signal go-sdk should track (see Front page / sibling landings below).
- **fulmine and go-sdk: no commits in the last 24h** — both quiet (only local `.gitignore` tweaks) (see Wallet beat).
- **arkade-wallet shipped a home + settings redesign (#627)** plus header/tx-list fixes and a link-preview image update (see Wallet beat).
- **arkana's Slack + research feeds are stale** — newest slack-log entry is 2026-06-19, research-monitor is 2026-06-18 (~19 days old). The sync feeding them looks broken (see TL;DR).

### Sibling-SDK landings (last 7 days) — parity signal
- **rust-sdk 0.10.0/0.10.1** (`bab46b4`, Lucas Soriano): contract-manager model + client manager, sqlite contract store, VTXO annotation through contracts, `BoardingWallet` removed in favour of contract paths, fix to renew recoverable VTXOs in watcher. go-sdk has no equivalent contract-manager abstraction — worth a design look.
- **ts-sdk** (`6f98fe02`, Andrew Camilleri): bumped `@marcbachmann/cel-js` to 8.0.0 for MetaMask Snap (SES) compatibility; plus an e2e test verifying `X-Digest`/`DIGEST_MISMATCH` round-trip across a real signer rotation (`e023e1d`).

_(Parity desk proper skipped: newest arkana parity report is 2026-04-03, >14 days old.)_

## Wallet beat

### fulmine
No activity. HEAD unchanged in the last 24h; only a local uncommitted `.gitignore` edit.

### go-sdk
No activity. HEAD unchanged in the last 24h; only a local uncommitted `.gitignore` edit.

### arkade-wallet (PWA)
Active (all dated 2026-07-07, `04d9195 → 1e0a082`):
- `830b0bfd` **feat(wallet): redesign home and settings entry points** (#627, Sahil Chaturvedi) — the one substantive change.
- `1e0a082b` fix(meta): update Arkade link-preview image (#718); `10b606f5` fix header (#717); `d9f48df6` fix tx-list height (#715) — cosmetic/layout.
- `2f3f5645` update regtest submodule commit (#716).

## Protocol surface — arkd → go-sdk

- **Structured `SUBSCRIPTION_NOT_FOUND` error code + legacy message restore** — `db93f3d6` (Andrew Camilleri, PR #1140). Touches `internal/interface/grpc/handlers/indexer.go`, `pkg/errors/errors.go`. Adds code 50 (gRPC `NotFound`) with `SubscriptionMetadata{subscription_id}`; restores the legacy `"subscription <id> not found"` string that #1074 had broken; routes all indexer not-found paths through one `subscriptionErr()` mapper; `SubscribeForScripts` reconnect now returns `NotFound` instead of unclassified `Internal`.
  - **Impact on go-sdk**: migrate any stale-subscription detection off message-parsing onto the new code/name in `ErrorDetails`. No forced change — the legacy string is preserved — but the reconnect path now surfaces a proper `NotFound`, which fulmine's indexer-subscription reconnect logic can key off.

## Slack briefing

_arkana's Slack and research feeds have not refreshed since ~2026-06-19 (slack-log) / 2026-06-18 (research-monitor) — nothing in the last 7 days to report. The most recent standing items (go-sdk CI instability ahead of the June 22 signer rotation; `ARKD_SIGNER_ADDR` becoming required) are now ~3 weeks old and presumed resolved. Flagging the stale feed itself as the actionable item — see TL;DR._

## TL;DR

- **Look at arkd `db93f3d6`** and plan the go-sdk (and fulmine) migration from subscription string-matching to the `SUBSCRIPTION_NOT_FOUND` code before the legacy message ever gets dropped.
- **Scan rust-sdk's contract-manager landing** (0.10.0/0.10.1) — it's a real architectural direction; decide whether go-sdk wants a parallel contract abstraction.
- **Check whether arkana's sync is broken** — slack-log and research-monitor have been frozen since ~Jun 19; the daily briefing is running blind on Slack/research inputs.
- fulmine/go-sdk are clean and quiet — good window to pick up the arkd error-code migration.
