# Ark Daily — 2026-05-13

> _Quiet across your primary repos (fulmine, go-sdk, arkd, introspector all zero commits). The story is sibling SDKs: dotnet-sdk shipped full unilateral exit — a parity gap where go-sdk still lags — and ts-sdk's HD receive rotation was merged and reverted the same day for the third time._

## Front page

- **dotnet-sdk landed unilateral exit (PR #39, kukks)** — full pipeline (VirtualTxService, UnilateralExitService state machine, ExitWatchtowerService, P2A CPFP builder, NBXplorer+Esplora broadcasters, 12 unit tests). go-sdk has no equivalent; parity gap now widens. (see Parity desk)
- **ts-sdk HD receive rotation merged + reverted same day** — #473 (alt to #441) landed and was reverted by #488 within hours. Third attempt at HD rotation fails; root cause not yet public. (see Wallet beat → parity context)
- **Boltz now pulls claim tx from Fulmine** — boltz-backend refactor (`7c4beaa2`, michael1011) changes the swap claim path to source the claim tx from Fulmine. Direct integration touchpoint for your stack. (see Protocol surface)
- **Wallet PWA: SW lifecycle rewrite + design-system foundation** — service worker init refactored around AbortController/generation (#613, pietro909, +162/-53), and shadcn/ui + Tailwind v4 design tokens landed (#590, sahil, 50+ component files). Not cosmetic — these are foundations. (see Wallet beat)
- **fulmine/go-sdk/arkd/introspector: all silent today** — your primary repos saw zero commits in the last 24h. (see TL;DR)

## Wallet beat

### fulmine
No activity in the last 24h. (Note: daily-update reports local checkout has uncommitted changes, so pulls were skipped.)

### go-sdk
No activity in the last 24h on `claude-electrum-test-break` checkout (daily-update reports "no upstream"). Master also unchanged per arkana sdk-parity log 2026-05-12. Standing context unchanged: 3 unreviewed merges on master (#143 HD, #145 ContractManager, #169 AutoSettle), "Remove KV store" PR still open, CI meltdown day 20+.

### arkade-wallet (PWA)
Two PRs merged (`982c8bc → e96024d`):
- **#613 — Fix dead service worker** (pietro909, +162/-53) — replaces generation counter with AbortController, each init session owns an AbortSignal; lock/reset aborts with reason `'lock-reset'`, new session aborts previous with reason `'init'` ("abandon, don't clear"). Companion to yesterday's "Recover from half-initialized ArkadeSwaps handler" (boltz-swap #145). SW resilience series continues.
- **#590 — feat(ui): add shadcn/ui primitives** (sahil, 50+ files touched) — introduces `src/tokens.css` (single source of truth for color ramps 50-950, typography, shadows), `src/app.css` (Tailwind v4 `@theme` mapping), `cn()` utility (clsx + tailwind-merge). Migrates all `--darkXX` opacity tokens to solid `--neutral-XXX` colors. Dependencies added: `tailwindcss@^4.2.2`, `@tailwindcss/vite`, `clsx`, `tailwind-merge`, `class-variance-authority`. This is the Tailwind v4 design system foundation — anything you build on the wallet from here uses these tokens.

## Parity desk

_Standalone parity reports are stale (latest `sdk-parity-2026-04-03.md` is 40 days old). Using arkana's daily sdk-parity agent log (2026-05-12) for fresh sibling-SDK landings only._

**Recent sibling-SDK landings (last 24h–7 days) relevant to go-sdk:**

- **dotnet-sdk unilateral exit MERGED — `a89d47a` PR #39 (kukks)** — full pipeline: `IClientTransport.GetVtxoChainAsync/GetVirtualTxsAsync/GetVtxoTreeAsync` (gRPC + REST), `VirtualTx/VtxoBranch/ExitSession` EF Core entities (per-tx dedup of shared tree nodes), `UnilateralExitService` orchestrating `Broadcasting → AwaitingCsvDelay → Claimable → Claiming → Completed`, `ExitWatchtowerService` for partial tree broadcasts, `P2ACpfpBuilder` (v3 CPFP for 1p1c package relay), `IOnchainBroadcaster` (NBXplorer + Esplora), `PostBatchVirtualTxFetchHandler` (auto-fetch on receive), `PostSpendVirtualTxPruneHandler` (auto-prune on spend), `AddUnilateralExit()` DI extension, 12 unit tests. Tracked since 2026-03-17. **arkana sdk-parity flagged this as a confirmed gap closure: dotnet-sdk ⚠️ partial → ✅. go-sdk and rust-sdk now visibly behind on unilateral exit.**
- **ts-sdk HD receive rotation MERGED then REVERTED (#473 → #488, kukks)** — "alt to #441" approach using contract repository merged 2026-05-12, reverted same day. Third attempt at HD receive rotation in ts-sdk fails. go-sdk's HD wallet (#143) still unreviewed on master — the ts-sdk experience is a warning that the rotation/race surface is harder than it looks.
- **rust-sdk Boltz referralId on swap requests (`256b964`, bonomat)** — swap-internal, no parity impact for go-sdk.
- **dotnet-sdk SQLite ORDER BY datetime fix (#92, kukks)** — internal, no parity impact.

**Gaps where go-sdk lags (carried, not freshly recomputed):**
- Unilateral exit — now ✅ in ts-sdk, rust-sdk, **dotnet-sdk (today)**; ⚠️ partial/missing in go-sdk.
- HD receive rotation — being actively reworked in ts-sdk; go-sdk #143 unreviewed on master.

_A fresh full parity report is overdue (last standalone was 2026-04-03). Worth requesting one from arkana before cutting go-sdk's first release._

## Protocol surface — arkd → go-sdk

No commits to `arkd` master in the last 24h. **Impact on go-sdk: none today.**

Standing context: arkd v0.9.5 (released 2026-05-11) and v0.9.x changes from earlier in the week have not yet been digested into go-sdk's wrap surface. The "Remove KV store" PR + 3 unreviewed merges are the open item.

**Adjacent protocol activity worth knowing (boltz-backend, not arkd):**
- `0c0ea2a0 chore: make Ark rescan interval configurable` (michael1011) — Boltz-side operational tunable.
- `7c4beaa2 refactor: getting claim tx from Fulmine` (michael1011) — **direct integration touchpoint**: Boltz is now sourcing the swap claim tx from Fulmine rather than reconstructing it. Worth tracking what Fulmine endpoint/contract Boltz now depends on, so any fulmine-side change doesn't silently break the Boltz claim path.

## Contract surface — introspector

No commits to `introspector` master in the last 24h. No opcode changes, no grammar changes, no contract semantic changes.

## Slack briefing

### Decisions and blockers (from arkana)

- **"Remove KV store" PR open in go-sdk** — significant architectural change; arkana flagged it must not merge until #143 (HD), #145 (ContractManager), #169 (AutoSettle) on master are human-audited. Removing the store while those merges sit on master risks inconsistencies.
- **CodeRabbit AI opened a PR autonomously** — first sighting of CodeRabbit submitting code (not just review comments). arkana flagged this as a governance question: does CodeRabbit-generated code count as "human review" for the unreviewed-merge policy? Relevant to go-sdk's review hygiene right now.
- **go-sdk CI meltdown day 20+** — 5 failures yesterday (down from the 8 peak two days ago). Root cause still not identified. This is the standing P0 blocker for go-sdk's first release.
- **GetVHTLCSpendingTx RPC merged (boltz-backend #1399, day before yesterday)** — finds the tx that *spends* a VHTLC. Adjacent to fulmine's VHTLC surface; relevant if you're consuming VHTLC closure detection from Boltz.
- **fulmine v0.3.23 stable confirmed** — release-coordinator noted this yesterday; no new fulmine release activity today.

### Research findings (from arkana)

_(Latest research-monitor run was 2026-05-11.)_

- **LNHANCE gaining traction** — bundles CTV + OP_CHECKSIGFROMSTACK + OP_INTERNALKEY with Lightning-focused framing. arkana flags this as the most strategically-aligned covenant bundle for Ark. CTV signaling still 0% after ~10 weeks.
- **BOLTs #995: Simple taproot channels officially specified** (Optech #404, May 8) — P2TR + MuSig2 channels are now part of the Lightning spec, not just experimental. Eclair enables them by default. Directly relevant to Ark↔LN interop: both VTXOs and LN channels now standardized on the same primitives.
- **Bitcoin Core PSBTv2 (BIP370) implemented** — backward compatible. Potential improvements for Ark round multi-party tx construction.
- **CVE-2024-52911 (Bitcoin Core)** — use-after-free in block validation, fixed in v29.0. ~43% of nodes still on vulnerable pre-v29 software. Not protocol-critical for Ark, but ASP operators should verify their Bitcoin Core ≥ v29.0 (ideally v31.0).

### Most important — expanded

**1. dotnet-sdk unilateral exit ships a full reference pipeline that go-sdk does not have.** Kukks landed PR #39 yesterday after a 56-day tracking arc. The implementation is comprehensive: a five-state exit state machine, a watchtower service detecting partial tree broadcasts, P2A CPFP for 1p1c package relay, dual on-chain broadcaster backends (NBXplorer + Esplora), automatic prefetch on VTXO receive, and automatic prune on VTXO spend. For dusan: this raises the bar for what "production-ready unilateral exit" looks like in an SDK, and go-sdk's current surface is visibly behind. Before cutting go-sdk's first release, decide whether unilateral exit is in-scope for v1 or explicitly deferred — the sibling-SDK reference now exists, so users will compare.

**2. ts-sdk HD receive rotation merged-then-reverted within hours — third attempt fails.** Kukks merged PR #473 ("HD receive rotation via contract repository — alt to #441") on 2026-05-12 and immediately reverted via #488 the same day. The first attempt (#441) was already flagged for a P1 race condition. The fact that the contract-repository rewrite also reverted suggests the rotation/race surface is harder than it looks across the SDK family. For dusan: go-sdk #143 (HD wallet) is one of the three unreviewed merges sitting on master. The ts-sdk experience is direct evidence that HD rotation under concurrent settle/send needs more scrutiny than a single review pass.

**3. Boltz now sources the swap claim tx from Fulmine (boltz-backend `7c4beaa2`).** Michael Bumann refactored the claim path to fetch the claim tx from Fulmine rather than reconstructing it Boltz-side. This is a meaningful coupling: Boltz now depends on a specific Fulmine endpoint/contract for claims to succeed. For dusan as fulmine maintainer: identify which exact Fulmine RPC/endpoint Boltz is calling (likely related to recently-merged `GetVHTLCSpendingTx`), document it as a stability surface, and add a regression test that protects the contract before any future fulmine refactor breaks Boltz claims in production.

## TL;DR

- Ask arkana for a fresh standalone sdk-parity report — the last one is 40 days old, and dotnet-sdk just closed a major gap; you need current ground truth before go-sdk v1.
- Read dotnet-sdk PR #39 end-to-end (`a89d47a`) — decide whether go-sdk v1 includes unilateral exit or explicitly defers it. The reference implementation now exists.
- Identify and pin the Fulmine endpoint Boltz now depends on (boltz-backend `7c4beaa2`). Document it; add a contract test.
- Watch what comes out of the ts-sdk HD rotation post-mortem — same lessons apply to go-sdk #143 sitting unreviewed on master.
- fulmine/go-sdk/arkd/introspector quiet today — good window for cleanup/review before the next wave hits.
