# Ark Daily — 2026-07-18

> _Busy around the edges, quiet in the middle: fulmine's VHTLC gained a non-interactive claim path, introspector quietly shipped multi-scheme (ECDSA/P-256) checksig — and the daily pipeline failed to fetch introspector, so it's invisible in today's changelog. go-sdk itself sat still all week._

## Front page

- fulmine's `CreateVHTLC` now supports a **non-interactive claim** option; `CreateVHTLCResponse.SwapTree` gained `nonInteractiveClaimLeaf` — `service.proto` changed, so go-sdk/wallet consumers need to handle the new field (see Wallet beat → fulmine).
- introspector extended **OP_CHECKSIG + OP_CHECKSIGFROMSTACK to ECDSA/secp256k1 and ECDSA/secp256r1 (P-256)** alongside Schnorr — a contract-semantics change for any Arkade Script you design (see Contract surface).
- The daily-update pipeline **failed to fetch introspector** (tag clobber on `v0.0.1`), so none of the above shows in today's changelog — the local clone needs fixing (see TL;DR).
- **go-sdk had zero commits in the last 7 days** while ts-sdk shipped ArkCash bearer instruments and Arkade Script support (#319) — a parity gap is forming (see Wallet beat → go-sdk).
- Wallet PWA added asset-ID display and Arkade-explorer outpoint links, and bumped to ts-sdk 0.4.47 (see Wallet beat → arkade-wallet).

## Wallet beat

### fulmine
- `b27d73d` **feat: non-interactive claim option to CreateVHTLC (#452)** (louisinger, Jul 15) — large change: `service.proto` +8, `service.go` +241, `vhtlc.go`, badger `vhtlc_repo.go` +50. Adds a claim path that doesn't require the counterparty online.
- `211bbcf` **add `nonInteractiveClaimLeaf` to `CreateVHTLCResponse.SwapTree` (#459)** (louisinger, Jul 17) — follow-up wiring the new leaf into the proto response + grpc `handlers/utils.go`; e2e covenant test touched.
- Open branches on origin: `fix-get-vtxos`, `vhtlc-keyref`.

### go-sdk
- **No activity** — zero commits on `origin/master` in the last 7 days. (Local clone is well behind and was skipped by daily-update due to an uncommitted `.gitignore`; remote HEAD `c8e7196` confirmed idle.)

### arkade-wallet (PWA)
- `e14de07` show asset ID + link outpoints to Arkade explorer in Virtual Coins (#802) (Marco Argentieri, Jul 18)
- `637bbfd` show asset ID alongside ticker in Virtual Coins list (#801) (Jul 18)
- `acc6e5b` restrict currency treatment to verified asset IDs (#790) (Jul 17)
- `0c14300` upgrade ts-sdk 0.4.47 / boltz-swap 0.3.52 (#792) (Jul 17)
- Mostly asset-display + explorer-linking work plus the SDK bump; substantive, not cosmetic.

## Contract surface — introspector

_Note: introspector was **not** pulled by today's daily-update (fetch rejected — `v0.0.1` tag clobber), so these are absent from the changelog. Fetched directly for this edition._

- `1359823` **feat: multischeme checksig support (#107)** (Mihael Šinkec, Jul 16) — refactors `tapscriptSigVerifier` to a scheme layer (`parseSchemePubKey` / `schemeKey.verify`); **OP_CHECKSIG family and OP_CHECKSIGFROMSTACK now accept ECDSA/secp256k1 and ECDSA/secp256r1 (P-256)** in addition to Schnorr/secp256k1, with low-s + canonical-scalar enforcement. Adds P-256 verify benchmark + compute-limit cost docs. **Changes contract semantics** — new key schemes are now valid in checksig-based closures.
- `52f8fa1` **feat(arkade): TS SDK-style script assembler (#feat/parse-arkade-script)** (Chris Ricketts, Jul 17) — new `pkg/arkade/asm.go`: translates whitespace-delimited opcode names + hex pushes into Arkade script bytes (prefixed/unprefixed names, pushdata boundaries). On a feature branch, **not yet merged**.
- Also on master this week: `6a91381` backoff retry on arkd connect (#112, Jul 16); active `arkade-library-export` branch hardening the emulator (Bob Smith, Jul 16–17).

## TL;DR

- **Review fulmine #452/#459** — the VHTLC non-interactive claim changed `service.proto` (`CreateVHTLCResponse.SwapTree.nonInteractiveClaimLeaf`); confirm go-sdk and wallet consumers decode the new leaf before it ships.
- **Read introspector #107 (multischeme checksig)** — P-256/ECDSA keys are now valid in `OP_CHECKSIG`/`OP_CHECKSIGFROMSTACK`. If you're authoring Arkade Script contracts, the key-scheme surface just widened.
- **Fix the introspector clone in the daily pipeline** — its fetch fails on a `v0.0.1` tag clobber, so introspector changes silently drop out of the changelog. `git -C introspector fetch --no-tags` (or delete the stale local tags) restores coverage.
- **Parity watch:** go-sdk was idle 7 days while ts-sdk landed ArkCash bearer instruments + Arkade Script support (#319, louisinger). Decide whether go-sdk needs an Arkade Script path of its own. (Formal parity report is 3.5 months stale — worth regenerating.)
