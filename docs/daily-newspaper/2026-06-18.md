# Ark Daily — 2026-06-18

> _Quiet on the wire — one arkd commit (Dusan's own #1117 deprecated-signer verification) — but the clock is loud: June 22 rotation / July 4 cutoff is 4 days out and go-sdk's rotation support is still fork-only._

## Front page

- **arkd #1117 merged (Dusan): server now verifies sigs from any configured signer until its cutoff.** go-sdk/fulmine must source keys via `Config.AllSigners()`, not `SignerPubKey` alone, or reject valid sigs mid-rotation. (see Protocol surface)
- **URGENT — go-sdk signer rotation is NOT on origin/master; it lives only on the `sekulicd/signer-rotation` fork.** With rotation June 22 / cutoff July 4, go-sdk users can't migrate deprecated-signer VTXOs after rotation. (see Parity desk)
- **NEW go-sdk gap: X-SDK-VERSION header.** ts (#568), rust (#246), dotnet (#139) all shipped it; go-sdk has none on origin — risk once `BUILD_VERSION_TOO_OLD` enforcement lands (planned ~June 22). (see Parity desk)
- **June 22 rotation confirmed, three key people off that day — "tests now or never."** Operational `Migrate deprecated-signer VTXOs` script PR is open and needs review. (see Slack briefing)
- **introspector: new `OP_KECCAK256` opcode, generalized to `OP_DIGEST`** — EVM-compatible hashing for covenant/Arkade scripts. (see Contract surface)

## Wallet beat

### fulmine
No activity. Repo skipped by daily-update (uncommitted local changes); no new commits in last 24h. Note: fulmine `ci_integration` showing 3 failures in Slack monitor.

### go-sdk
No new commits on origin/master (still behind on rotation + headers — see Parity desk). Slack notes `forward and export client.SendOption alias` PR open and persistent CI failures (6+: ci_integration 4×, ci_unit 3×).

### arkade-wallet (PWA)
- `331f6fc9` — prevent double keys in local storage (#677, João Bordalo). localStorage key-collision fix; otherwise quiet.

## Parity desk

_Source: `sdk-parity.md` report dated 2026-06-17 (fresh)._

**Gaps where go-sdk lags:**
- **Arkd signer-rotation support — ❌ (origin/master).** Report line: rotation commits are on `sekulicd/signer-rotation` fork, NOT `arkade-os/go-sdk` origin. ts ✅ #554, rust ✅ #246, dotnet ✅ #132. **URGENT before June 22.**
- **X-SDK-VERSION header — ❌ NEW GAP.** ts ✅ #568, rust ✅ #246, dotnet ✅ #139. go-sdk has no equivalent on origin.
- **Selective settle (expired/recoverable) — ❌.** ts ✅, rust ✅ NEWLY #227. go-sdk settle() not yet narrowed.
- **TX_TOO_LARGE count cap & amount cap — ❌/❌.** ts ✅ (v0.4.34 / #553), dotnet count ✅ #125.
- **HD wallet restore (deprecated signers) — ❌.** ts ✅, dotnet ✅ #104.
- **Arkade extension system — ❌** (PRs #190/#191 open). **recv-for-user — ❌** (rust ✅ #238).

**Recent sibling-SDK landings (last 7 days):**
- **rust-sdk #246** (06-16): signer rotation + X-SDK-Version + digest-mismatch parity merged. **#227**: selective settle.
- **ts-sdk #568** (06-15): X-SDK-VERSION header. **#569** (06-18): export `buildVersion`/`sdkVersion`. **#554**: signer rotation.
- **dotnet-sdk #139** (06-16): X-SDK-VERSION header.

## Protocol surface — arkd → go-sdk

- **Deprecated-signer verification** — offchain tx verification now accepts sigs from *any* configured signer (active or deprecated) until each signer's `CutoffDate`. Adds `types.DeprecatedSigner`, `Config.DeprecatedSigners`, `Config.AllSigners()`; file config store gains `deprecated_signers`. (`268d19d9` #1117, Dusan Sekulic) — touches `pkg/client-lib/{asset.go,send.go,utils.go,types,store/file}`.
  - **Impact on go-sdk:** when verifying server-signed txs (`SendOffChain`, `IssueAsset`, `ReissueAsset`, `BurnAsset`), source signer keys via `Config.AllSigners()` rather than `SignerPubKey` alone — otherwise you'll reject valid sigs during the rotation window. `SignerPubKey` remains present and unchanged; old config files still decode.

## Contract surface — introspector

- **`OP_KECCAK256` added** — Keccak-256 hash opcode for Arkade Script; enables EVM-compatible hashing in covenant scripts (cross-chain/ETH bridge patterns). (`d1b7b57`, msinkec)
- **Extended to general `OP_DIGEST`** — generalizes the new hash opcode into a single digest opcode. (`4890039`, msinkec, 06-17) — on branch `feat/op-keccak256`, not yet on master.
- _On master (v0.0.3): `OP_CAT` result-size check (#100) and canonical asset-id standardization (#97) — landed within the week._

## Slack briefing

### Decisions and blockers (from arkana)
- **Signer rotation dates locked: June 22 (rotation) / July 4 (cutoff)** — @U0854KRKV3Q: postponed to the 22nd for more hands; cutoff stays the 4th. Three people off that day; "if there are any tests to conduct, it's now or never." (#dev / decisions)
- **`BUILD_VERSION_TOO_OLD` forceful nudge** — structured error already in arkd; @U0854KRKV3Q: "Seems like we will need to do a forceful nudge" — enforcement likely around June 22. BTCPay plugin v0.9.7 hardcode must be resolved first.
- **`client-lib: support deprecated signer verification` MERGED** (= arkd #1117, above).
- **`Migrate deprecated-signer VTXOs onto current signer` PR open** — the operational migration script for June 22; needs review/validation before rotation day.
- **Blockers:** go-sdk CI 6+ failures (ci_integration 4×, ci_unit 3×); fulmine ci_integration 3 failures.

### Research findings (from arkana)
- **LND 0.21.0-beta**: production taproot channels + RBF + reorg protection + onion-message forwarding — all three LN implementations now have taproot channel support (Lightning interop relevance for fulmine/Boltz path).
- **Testnet5 proposal**: replaces testnet4, removes the 20-min difficulty rule, BIP54 active from block 1 — better surface for Arkade testing.
- **Botanix Spiderchain shutdown** (June 10): BTC L2 wound down despite working tech — users preferred wrapped BTC. Cautionary adoption note for Ark.

### Most important — expanded
1. **go-sdk rotation gap is the week's real risk.** The 2026-06-17 parity report corrected an earlier ✅: go-sdk's signer-rotation commits sit on the `sekulicd/signer-rotation` fork, not `arkade-os/go-sdk` origin/master — so as shipped, go-sdk cannot migrate deprecated-signer VTXOs after rotation. With rotation 4 days out (June 22) and cutoff July 4, this is the highest-leverage thing for Dusan, who owns go-sdk. Sibling SDKs (ts/rust/dotnet) have all merged rotation.
2. **arkd #1117 (Dusan's own merge) shifts the verification contract.** The server now accepts signatures from active *and* deprecated signers until each one's cutoff, exposed via `Config.AllSigners()`. Any go-sdk/fulmine code that still reads `SignerPubKey` alone will start rejecting valid server sigs during the rotation window — so the client-side verification path needs to consume `AllSigners()` before June 22.
3. **X-SDK-VERSION is a fresh, separate go-sdk gap.** ts (#568), rust (#246), and dotnet (#139) all now send the header; arkd plans a `BUILD_VERSION_TOO_OLD` "forceful nudge" around June 22. If go-sdk never sends the header, go-sdk clients risk being treated as stale once enforcement flips on.

## TL;DR
- **Merge go-sdk signer rotation to `arkade-os/go-sdk` origin/master before June 22** — it's currently fork-only (`sekulicd/signer-rotation`); without it go-sdk users can't migrate deprecated-signer VTXOs after rotation.
- **Update go-sdk's tx verification to use `Config.AllSigners()`** per arkd #1117, or valid server sigs get rejected during the rotation window.
- **Add the X-SDK-VERSION header to go-sdk (origin)** before `BUILD_VERSION_TOO_OLD` enforcement lands (~June 22).
- **Review the `Migrate deprecated-signer VTXOs onto current signer` script** — it's the operational migration path for rotation day.
- **Triage go-sdk CI** (6+ failures) and fulmine ci_integration (3) before the rotation freeze window.
