# Ark Daily — 2026-06-07

> _Sunday-quiet across every repo dusan owns or watches: no fulmine, go-sdk, wallet, arkd, introspector, ts-sdk, rust-sdk or dotnet-sdk commits in the last 24h. Only two ecosystem commits — boltz-backend bumped LND to v0.21.0-beta, ark-telemetry fixed the Ark Go CPU metric to render as a percent. Yesterday's standing parity items (TX_TOO_LARGE guard, Arkade extension HIGH, VHTLC unreleased on go-sdk master) carry over unchanged._

## Front page

- **boltz-backend just took LND to v0.21.0-beta** (`91502ead`, #1432, michael1011) — affects fulmine's Boltz swap dependencies; worth verifying fulmine's regtest stack against the new LND before the next bump PR. (see Wallet beat)
- **All three SDK parity gaps from yesterday still standing** — TX_TOO_LARGE intent guard missing across SDKs, Arkade extension API (Layer 1 + Layer 2) shipped in ts-sdk v0.4.33 with go-sdk absent, VHTLC handler `5705340` still unreleased on go-sdk master. No motion in 24h. (see Parity desk)
- **Coinflip v0.3 escrow MERGED** Friday with arkade-script win-conditions evaluated inside the escrow contract — fully covenant-enforced settlement, but it triggered 11+ E2E failures + 2 Docker build failures. This is the production arkade-script reference the go-sdk Arkade extension gap is now measured against. (see Slack briefing)
- **Bitcoin Core #35269 MuSig2 PSBT session-id fix** (Optech #408, Friday) — prevents crash on repeated `walletprocesspsbt`; directly relevant to Ark's collaborative VTXO signing. ASPs should pull latest Core. (see Slack briefing › Research findings)

## Wallet beat

### fulmine
No activity in the last 24h. Latest upstream commit is still `3ea61e2 Bump go-sdk@2d035b64 (#401)` from 2026-04-24. Your working tree on Hetzner has an uncommitted `.gitignore` modification — that's why this morning's `daily-update` skipped the pull.

### go-sdk
No activity in the last 24h. Latest upstream is still `7f6b8d0 Contract registry: allow registering custom contract handlers (#186)` from 2026-05-25. Working tree has uncommitted `.gitignore` + a `.worktrees/` dir — daily-update skipped this pull too. **v0.10.0 remains current**, VHTLC handler still parked on master awaiting the next tag.

### arkade-wallet (PWA)
No activity in the last 24h. Latest commit remains Friday's `3d2c95d3 fix(Transaction): show roundTxid for offboarding batch transactions (#648)` by arkanaai[bot] — covered in yesterday's edition.

### Adjacent: boltz-backend
- `91502ead chore: bump LND to v0.21.0-beta (#1432)` (michael1011) — first LND bump since boltz-backend's last sync. fulmine pins its own LND container; if you bump fulmine's regtest stack to match, re-run the submarine + reverse swap E2E because LND minor bumps have historically broken HTLC accept timing.

### Adjacent: ark-telemetry
- `6f31647 fix: Display Ark Go CPU metric as a percent` (se7enz) — dashboard fix only, no agent/exporter changes.

## Parity desk

Source: `memory/agent-logs/sdk-parity.md` entry 2026-06-06T10:00Z. **No new commits across ts-sdk / go-sdk / rust-sdk yesterday; dotnet-sdk had only test/bug-fix churn.** Status of standing gaps:

**Gaps where go-sdk lags (carried over from yesterday, no movement):**

- **TX_TOO_LARGE intent guard** — server rejects intents with >91 VTXOs; go-sdk has no cap in `batch_session.go` / `sync.go` / auto-settle paths. Weight-based estimation (inputs + outputs + OP_RETURNs + witness) is the agreed-correct path per the 2026-06-05 #dev thread (@U0854KRKV3Q + @U0B791D017A).
- **Arkade extension Layer 1** — primitives (`ArkadeScript`, `EmulatorPacket`, `RestEmulatorProvider`, `sendOffChain` extensions). Shipped ts-sdk v0.4.32. go-sdk: missing.
- **Arkade extension Layer 2** — high-level API (`Arkade.connect()` / `Program` / fluent builder / `parseArtifact`). Shipped ts-sdk v0.4.33. go-sdk: missing. **Severity HIGH** — coinflip.mutinynet.arkade.sh now uses this in production, and Friday's coinflip v0.3 escrow merge means win-conditions are also evaluated through it.
- **VHTLC handler unreleased on go-sdk master** — `5705340` (vhtlc package + handler + `Derivable()` on the Handler interface) still waiting on a tag.

**Recent sibling-SDK landings (last 7 days):** no new landings since yesterday's edition — ts-sdk v0.4.33 (2026-06-04 release with Arkade contract API + handler authoring helpers `isCsvSpendable` / `isCltvSatisfied`), ts-sdk boarding HD rotation (2026-06-03, Pietro Grandi), dotnet-sdk preimage v1 spec correction (2026-06-04). All covered in prior editions.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days)

- **VTXO TX_TOO_LARGE limit = 91 (#dev, 2026-06-05)** — @U07ML5C69RC pinned the server limit via local tests; @U0854KRKV3Q and @U0B791D017A converged on weight-based estimation rather than a hardcoded VTXO count. Direct go-sdk parity work.
- **HTLC claimer moving to fulmine (#dev, 2026-05-26)** — "FINAL CALL" thread: bancod → arkade-solver, banco → arkade-intents, HTLC claimer reassigned to fulmine, @U07ML5CM486 (OpenIntentsFramework) brought in for alignment. This is the biggest pending scope change for fulmine; nothing has landed yet but the rename + reassignment was framed as final.
- **Coinflip v0.3 escrow MERGED (#github-bot, 2026-06-06)** — arkade-script now evaluates win-conditions inside the escrow contract; foundational architecture PR. Settlement is fully covenant-enforced. CI fallout: 11+ E2E failures + 2 Docker build failures the same day.
- **`feat(swaps): chain swap E2E tests + cooperative refund fixes` PR open (2026-06-06)** — fulmine-adjacent: adds E2E coverage and fixes the cooperative refund path; follows the reverse-swap-stuck-at-`transaction.mempool` thread from 2026-06-02. Worth tracking for Boltz integration parity.
- **arkd master last green 2026-05-26 — 11 days stale** (daily-briefing 2026-06-06). `feat/nostr-transport` ci_unit + ci_integration both failing. Not actively blocking dusan, but the next go-sdk release that bumps arkd-client should pull from a green commit.
- **ee2e-kv Verify Enclave: day 24+ (GOV#30/31/32 unresolved)** — long-running enclave attestation failure cluster. Not in dusan's scope but worth knowing if any wallet UX depends on attested e2ee.

### Research findings (from arkana, last 7 days)

- **PQ Lightning architecture (Osuntokun, Optech #408, 2026-06-05)** — first concrete layer-by-layer PQ proposal for Lightning. BIP324 hybrid ECDH + ML-KEM. Lattice-based for LN key management. Hybrid (EC + PQ in parallel) is the recommended migration path. Most relevant Bitcoin-side development for Ark this week.
- **Bitcoin Core #35269 MuSig2 PSBT session-id fix (Optech #408)** — fixes session ID to include each participant's public nonce; prevents false-assertion crash when `walletprocesspsbt` is called repeatedly on the same PSBT. Ark's collaborative VTXO signing path hits this exact shape.
- **CLN 26.06 (Optech #408)** — new `graceful`, `sendamount`, `xkeysend` RPCs; deprecates `pay` in favour of `xpay`; BOLT12 payment proof support. Fulmine's Lightning interop surface (when using CLN-backed setups) should track the `pay` → `xpay` migration.
- **Google Shor reconstruction (Delving Bitcoin, 2026-06-03)** — Schrottenloher independently reproduced Google's 20× qubit reduction (arXiv 2606.02235). Validates the ~500K physical-qubit estimate to break ECDLP-256. Trend, not action — PQ timeline compresses, but no immediate change to Ark's MuSig2 / Schnorr posture.
- **P2WOTS live signet demo (opus-lux, 2026-06-04)** — first PQ scheme with a working signet + wallet UI. No signature-size data disclosed; Murch's single-sig-per-key criticism still unaddressed.

### Most important — expanded

1. **HTLC claimer moving to fulmine (the only real fulmine-scope change pending).** @U0854KRKV3Q's 2026-05-26 #dev thread framed this as "FINAL CALL" alongside the bancod → arkade-solver / banco → arkade-intents rename, with @U07ML5CM486 (OpenIntentsFramework) pulled in for alignment. Nothing has landed in fulmine yet, but this is the architectural change that will shape fulmine's roadmap once it kicks off — the HTLC claimer logic that currently lives in the solver/intents stack becomes fulmine's responsibility. Worth pinging the thread for a concrete owner + start date so it doesn't slip.

2. **TX_TOO_LARGE VTXO guard — go-sdk parity that's actually shippable this week.** The 2026-06-05 #dev consensus is weight-based estimation, not a VTXO count cap; go-sdk currently ships nothing in `batch_session.go` / `sync.go` / auto-settle paths. This is the cleanest "land a meaningful go-sdk PR" target: small, well-scoped, the design has already been argued out in Slack, and it unblocks ts-sdk + dotnet-sdk parity work that's been queued behind a reference implementation.

3. **Bitcoin Core #35269 MuSig2 PSBT session-id fix.** The bug — session ID not including each participant's public nonce — triggers a false-assertion crash when `walletprocesspsbt` is called repeatedly on the same PSBT. This is exactly the shape of Ark's collaborative VTXO co-signing path (multiple participants, PSBT iterated), so any ASP running pre-fix Core is at risk of crashes during round signing. Confirm the production ASPs you care about are on a Core build that includes the fix, then mark it as a non-issue for fulmine's wallet flows.

## TL;DR

- **Look at:** the boltz-backend LND v0.21.0-beta bump (`91502ead`) — re-run fulmine's submarine + reverse swap E2E if you plan to follow.
- **Ask:** who owns the HTLC claimer migration into fulmine, and when does it start? (@U0854KRKV3Q's 2026-05-26 "FINAL CALL" thread has no follow-up.)
- **Fix:** the TX_TOO_LARGE VTXO intent guard in go-sdk — weight-based estimation is the agreed approach; small, well-scoped, and unblocks ts-sdk + dotnet-sdk parity work.
- **Verify:** the ASPs you care about are on a Bitcoin Core build that includes #35269 (MuSig2 PSBT session-id fix).
