# Ark Daily — 2026-06-09

> _Quiet across your primary repos — no new fulmine or go-sdk commits in the last 24h. The notable signal: arkd shipped an `OP_SUCCESS` guard inside `script.DecodeClosure` that every closure-decoding caller (you) will now see as a new error path. Also worth knowing: `fulmine v0.3.24-rc.0` tag and a `go-sdk v0.9.1-patch` branch surfaced on the remote — they wrap your 50-coin settlement-batch fix and altafan's settle-API bump from 2026-06-05. kukks is travelling today._

## Front page

- **arkd `script.DecodeClosure` now rejects `OP_SUCCESS` scripts** — every fulmine/go-sdk caller of `pkg/ark-lib/script.DecodeClosure` gets a new error path. Audit your callers. (see Protocol surface)
- **`fulmine v0.3.24-rc.0` tag exists upstream** (`dd08f72`, altafan, 2026-06-05) — "Bump go sdk and apply fixes to settle api". RC tag only just became visible to the local mirror. (see Wallet beat → fulmine)
- **`go-sdk v0.9.1-patch` branch exists upstream** containing your `Limit settlement batches to 50 coins (#193)` (`d53dd67`, 2026-06-05). Looks like Pietralberto is staging a patch release on top of v0.9.1. (see Wallet beat → go-sdk)
- **arkd, fulmine, go-sdk all need to align on Go 1.26.4** — arkd bumped today (#1091). If your local fulmine/go-sdk pin a toolchain directive, match it before CI drifts. (see Protocol surface)
- **LNURL-pay LUD-06 rejection bug filed yesterday** — lnurl-server adds the Boltz reverse-swap fee on top of the requested invoice amount, which all spec-compliant wallets (Phoenix, Mutiny, Zeus) reject. Affects production lnurl-server. Relevant if any fulmine/wallet flow consumes Boltz reverse-swap invoices generated by lnurl-server. (see Slack briefing)

## Wallet beat

### fulmine
No new commits in the last 24h. Local working tree has uncommitted `.gitignore` changes (why the daily-sync skipped this repo). Two new refs appeared on the remote since the previous fetch — both stale relative to today's window but worth noting:
- **Tag `v0.3.24-rc.0`** → `dd08f72` (altafan, 2026-06-05): "Bump go sdk and apply fixes to settle api". First RC against the new go-sdk settle changes.
- **Branch `hotxifx-settle`** → tip is the same `dd08f72`. Looks like the RC was cut from this branch.

### go-sdk
No new commits in the last 24h. Same uncommitted `.gitignore` state. Two new refs visible on the remote:
- **Branch `v0.9.1-patch`** → `d53dd67` (Dusan Sekulic, 2026-06-05) "Limit settlement batches to 50 coins (#193)" — your own PR (the response to the 2026-06-05 #dev TX_TOO_LARGE thread, where local max was 91 VTXOs). Now sitting on a dedicated patch branch — Pietralberto may be staging v0.9.1.1 / v0.9.2.
- **Branch `fulmine-hotfix/paginate-settlement`** → `e45ebe7` (altafan, 2026-06-05) "Revert and apply minimal fix + test" on top of your batch-limit commit. This is what fed `fulmine v0.3.24-rc.0`.

### arkade-wallet (PWA)
- `6073145a` (João Bordalo, 2026-06-08): "update regtest link (#654)". Two-line `.gitmodules` + submodule pointer bump. Cosmetic — no app surface changed.

## Protocol surface — arkd → go-sdk

Two arkd commits in the last 24h. Neither touched `pkg/client/`, `api/`, `proto/`, or `internal/interface/grpc/` — so no gRPC wrap-surface change for go-sdk. But one is impactful via `pkg/ark-lib`:

- **`afd23262` ark-lib: add unit tests to script (louisinger)** — despite the modest commit message, the diff adds a real behavioural change in `pkg/ark-lib/script/closure.go`:
  ```go
  if txscript.ScriptHasOpSuccess(script) {
      return nil, fmt.Errorf("script contains forbidden OP_SUCCESS opcode")
  }
  ```
  **Impact on go-sdk / fulmine**: every code path that calls `script.DecodeClosure` (forfeit script decoding, VTXO recovery, contract handler registration) will return this new error for any script containing an `OP_SUCCESS`. Grep your trees for `DecodeClosure(` and make sure the surrounding error handling doesn't swallow this as "unknown script" or surface a confusing message to the user.

- **`75066cc2` Bump golangk@v1.26.4 (#1091) (Pietralberto Mazza)** — toolchain bump across all arkd submodules (ark-lib, client-lib, arkd-wallet, ark-cli, api-spec, errors, kvdb, macaroons). **Impact on go-sdk / fulmine**: if either pins a `toolchain` directive in `go.mod`, align to 1.26.4 to avoid CI drift. fulmine's own Docker image was bumped to Go 1.26.3 by you 7 days ago (`df00504`) — likely needs a follow-up bump.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days, filtered to your scope)
- **2026-06-08 #dev — kukks travelling 2026-06-09 (today)**. He @-mentioned `@U0A32P2G6HK`, `@U0AK30FBR3M`, `@U07MV8W1CNQ`, `@U0ALUTJ403F` for coverage. If you need a protocol-level decision today, expect slower turnaround.
- **2026-06-08 — `Limit settlement VTXO batches to 50` PR opened + ready_for_review** (this is your work) — explicitly cited as caps matching the go-sdk default. The patch branch surfaced above is the productisation.
- **2026-06-08 — LNURL-pay LUD-06 rejection bug opened**: lnurl-server generates LNURL-pay invoices where the invoice amount = requested amount + Boltz reverse-swap fee. LUD-06 requires exact match → Phoenix / Mutiny / Zeus all reject. Production path affected. If fulmine or arkade-wallet flows ever surface lnurl-server-issued invoices, this is your problem too.
- **2026-06-08 — `Release arkd to 0.9.7` PR opened**, release build CI failing 2×. Once 0.9.7 ships, go-sdk and fulmine will want to bump.
- **2026-06-05 #dev — VTXO intent TX_TOO_LARGE limit confirmed at 91 VTXOs** (server-side); go-sdk default cap = 50. kukks suggested while-loop instead of hardcoded limit; @U0B791D017A proposed weight-based size estimation rather than count. Worth deciding which approach lands in v0.9.1-patch before the release.
- **2026-06-03 — `feat: require csvTimelock to be specified` MERGED** in arkd. Protocol-adjacent breaking change: any contract that previously omitted csvTimelock now fails. Verify go-sdk's contract construction helpers always set it.
- **fulmine ci_integration occasional failure** logged 2026-06-01 — minor, not escalating.

### Research findings (from arkana, last 7 days)
- **2026-06-06 — Bitcoin Core #35269 MuSig2 PSBT signing fix landed**. Fixed session identifier to include each participant's public nonce; prevents false assertion on repeated `walletprocesspsbt` calls. Direct relevance: any go-sdk / fulmine flow that may re-process the same PSBT (retries, recovery) would have been at risk.
- **2026-06-06 — Osuntokun's PQ Lightning architecture in Optech #408**. Hybrid ECDH + ML-KEM (lattice). Concrete first proposal. Tracking-only — no action this week.
- **2026-06-08 — P2MR/BIP-360 EC key recovery optimisation** (starius). Reduces witness to ~100 bytes. sipa + ajtowns + Ruben Somsen engaged. Long-horizon relevance to Ark's dual-path PQ VTXO design — not actionable now.

### Most important — expanded

1. **OP_SUCCESS reject in `DecodeClosure`** (arkd `afd23262`, louisinger, 2026-06-08). louisinger added a hard reject for any script containing `OP_SUCCESS` opcodes at the very top of `script.DecodeClosure`. The commit message ("add unit tests to script") undersells the change — it's a real behaviour change in a function fulmine and go-sdk both call. The motivation is defensive: `OP_SUCCESS` opcodes succeed unconditionally in script evaluation, so any closure encoding one would create a backdoor that bypasses the closure's intended semantics. For your work this week: grep both repos for `DecodeClosure(` callers and make sure the new error is mapped to a sensible user-facing message rather than a generic "invalid closure".

2. **Your own 50-coin settlement batch limit is on a patch branch** (`go-sdk v0.9.1-patch` → `d53dd67`, 2026-06-05). Pietralberto staging it on a separate branch (rather than landing it on master and tagging) suggests a patch release is imminent and possibly tied to fulmine's `v0.3.24-rc.0`. The rc tag at the top of `hotxifx-settle` was cut on the same day. Worth a quick ping to Pietralberto on the release sequence so fulmine's bump matches the eventual go-sdk tag, not an interim sha.

3. **LNURL-pay LUD-06 rejection** (opened 2026-06-08, see arkana digest). lnurl-server is adding the Boltz reverse-swap fee on top of the requested LNURL amount, but LUD-06 requires the invoice amount to match the request exactly. Phoenix / Mutiny / Zeus reject. This isn't your repo, but it's directly upstream of any Lightning-receive UX in arkade-wallet or fulmine — if a user requests "1000 sats" via LNURL and gets a 1050-sat invoice back, they can't pay it from a compliant wallet. Worth a 5-minute scan of how (or whether) the arkade-wallet receive flow currently handles this.

## TL;DR

- Grep `fulmine` and `go-sdk` for `DecodeClosure(` and confirm the new `OP_SUCCESS` error has reasonable surfacing.
- Ping Pietralberto / altafan to confirm the release sequencing for `go-sdk v0.9.1-patch` ↔ `fulmine v0.3.24-rc.0` — make sure the final fulmine release bumps to a *tagged* go-sdk version, not the patch branch sha.
- Decide (with kukks when he's back) whether go-sdk's settlement cap stays count-based (50) or moves to weight-based estimation, before v0.9.1-patch is finalised.
- If you touch fulmine today: bump its toolchain to Go 1.26.4 to track arkd's bump (#1091) and your earlier 1.26.3 bump in `df00504`.
- Don't expect protocol-level decisions from kukks today — he's travelling.
