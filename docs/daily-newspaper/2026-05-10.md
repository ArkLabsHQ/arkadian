# Ark Daily — 2026-05-10

> _Quiet weekend: zero new commits across fulmine, go-sdk, wallet, arkd, introspector and the sibling SDKs in the last 24h. Open items from late last week still need eyes — most urgently the arkd SubmitTx hotfix and two unreviewed go-sdk merges on master._

## Front page

- **arkd 42f58837 "Hotfix: prevent failure on SubmitTx to affect finalization of pending tx" (#1063, altafan, Fri 2026-05-08) is on master.** Protocol-critical: a SubmitTx error was preventing finalization of an already-broadcast tx, risking funds in a liminal state. go-sdk wraps this path — confirm the fix flows through. (see Protocol surface)
- **arkd #1058 "client-lib: Add WithReceiver option & Update explorer and wallet interfaces" (altafan, 2026-05-08) reshaped pkg/client-lib `funding.go`, `send.go`, `unroll.go`, and `wallet/wallet.go`** — this is the layer go-sdk depends on. Worth a deliberate sync. (see Protocol surface)
- **go-sdk master is carrying two unreviewed merges (GOV#19, GOV#20):** PR #145 Contract Manager (10:12Z 05-08) and PR #169 Auto-settle (17:11Z 05-08), both self-merged with zero reviews. First go-sdk release cannot be cut safely until these are audited. (see Wallet beat → go-sdk)
- **go-sdk + dotnet-sdk CI is still melting down** — go-sdk day 17+, dotnet-sdk hit an all-time-high 15 failures on 2026-05-09. dotnet-sdk#77 (HD wallet, 2 must-fix findings) remains blocked. (see Slack briefing)
- **Optech #404 (May 8) specified taproot channels (BOLTs #995) and zero-fee commitments (BOLTs #1228), and Bitcoin Core landed PSBTv2 (BIP370).** All three are tailwinds for Ark↔LN interop and round construction. (see Slack briefing → Research)

## Wallet beat

### fulmine
No commits in the last 14 days. Working tree has one unstaged edit to `.gitignore` only — not a change.

### go-sdk
No commits on `master` in the last 24h. Master tip remains `c8dbced` (RBF output mapping fix #167, 2026-05-05) plus the two GOV-flagged merges from 2026-05-08:
- **#145 Contract Manager** — `feat/contract-manager` → master 2026-05-08; ContractManager + default + VHTLC handlers + path selection + persistence. Zero reviews (GOV#19).
- **#169 Auto-settle** — self-merged 2026-05-08; new API: `WithScheduler()`, `WithoutAutoSettle()`, `WhenNextSettlement()`. Opt-out, gocron default. Partial close of the VtxoManager gap (settlement scheduling only — expiry monitoring unconfirmed). Zero reviews (GOV#20).

Active branches with recent work, unmerged: `pr-145-tracker` (typed event subscriptions, single-owner reconciler, 2026-05-08), `pr-145-fresh` (refresh-path bug repros, 2026-05-07), `feat/absorb-client-lib`, `cm-redesign-all`, `feat/drop-address-methods`, `feat/contract-handler`. Working checkout sits on `claude-electrum-test-break`.

### arkade-wallet (PWA)
No commits in the last 24h. Last activity was 2026-05-08 #610 (pietro909) — routine ts-sdk 0.4.26 / boltz-swap 0.3.30 bump. Earlier in the week the design system landed (Tailwind v4 tokens #582, sonner toast #589) plus #608/#603 layout/scanner fixes — UI-shape work, no protocol surface.

## Protocol surface — arkd → go-sdk

No arkd commits in the last 24h. Outstanding from 2026-05-08 that affects go-sdk's wrap surface:

- **42f58837 — "Hotfix: prevent failure on SubmitTx to affect finalization of pending tx" (#1063, altafan, 2026-05-08).** Touches `internal/core/application/service.go` only (30 lines moved). **Impact on go-sdk:** none directly — this is server-side flow control, not a wire change. But go-sdk's `SubmitTx` call paths now rely on the new finalization behavior; confirm integration tests exercise the partial-failure case.
- **2999d666 — "client-lib: Add WithReceiver option & Update explorer and wallet interfaces" (#1058, altafan, 2026-05-08).** Big diff across `pkg/client-lib/`: `funding.go` (+216/-…), `send.go` (+51/-…), `unroll.go`, new `receiver_opts.go` (+115), explorer batching (`GetUtxos` accepts many addresses + throttling), `wallet/wallet.go` interface change. **Impact on go-sdk:** this is the surface go-sdk wraps. Sync probably needs: a new `WithReceiver` send option, multi-address explorer batching parity, and any wallet-interface signature shift. Worth a focused diff session.

No changes to `api/` or `proto/` — wire format is stable.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days)

- **2026-05-09:** Near-zero productive activity. **dotnet-sdk CI: 15 failures** (all-time record, ↑ from 9 the prior day) — all dotnet-sdk work blocked. **go-sdk CI: 4 failures** (1× ci_unit, 3× ci_integration), day 17+. **ee2e-kv Verify Enclave** still broken on the newly-public repo.
- **2026-05-09:** "test: failing tests + docs for review of #160" CLOSED — companion PR for go-sdk#160 (ElectrumX, 2 CRITICAL concurrency bugs flagged earlier). Status of #160 itself unclear; may be abandoned or reworked.
- **2026-05-08:** **arkd "Hotfix: SubmitTx → finalization" opened** — flagged protocol-critical; needs urgent review before merge (now landed, see Protocol surface).
- **2026-05-08:** **ts-sdk "feat(wallet): HD receive rotation via contract repository (alt to #441)" marked ready_for_review** — REPLACES the P1 race in #441 (offchainTapscript mutation during concurrent settle/send). Verify the race is actually resolved before merging.
- **2026-05-08:** **ts-sdk "feat: multi-provider swap architecture (Boltz)" ready_for_review** — abstracts Boltz as one of multiple providers; large architectural review.
- **2026-05-08:** **go-sdk has 3 major unreviewed merges on master** — #143, #145, #169. First release blocked until human audit of all three.
- **2026-05-08:** **"Can't settle onchain" issue opened** — production-facing settlement bug; needs triage.

### Research findings (from arkana, 2026-05-09 research-monitor)

- **Optech #404 (May 8) — Taproot channels and zero-fee commitments now standardized.** BOLTs #995 specifies P2TR + MuSig2 simple taproot channels. BOLTs #1228 specifies zero-fee commitment channels (v3 relay + pay-to-anchor, max 114 HTLCs). Eclair #3144 enables taproot channels by default. Bitcoin Core #21283 implements PSBTv2 (BIP370). BIP451 publishes a dust UTXO disposal protocol. JIT channel fraud proofs proposed by Voegtlin. **No CTV / OP_CAT / Ark / VTXO mentions.**
- **Tether WDK locked to Spark** (Aug 2025 integration, surfaced 2026-05-09): WDK provides a single stateless API for L1 + Spark + Lightning. Developers building USDT wallets default to Spark. Includes AI-agent transaction support via QVAC. Direct ecosystem competition for Arkade Assets.
- **CTV signaling: 0%, 9 weeks running.** Continue planning around clArk, not CTV.

### Most important — expanded

1. **arkd SubmitTx hotfix #1063 (altafan, merged 2026-05-08).** What: a SubmitTx call that errored after broadcast was preventing finalization, leaving txs in a stuck/liminal state. Why it matters this week: this is upstream of every go-sdk send/settle path, and the failure mode is exactly the kind that surfaces as a hard-to-reproduce production support ticket. Action for dusan: confirm a regression test in go-sdk exercises the SubmitTx-fails-after-broadcast path, and that go-sdk's bumped client-lib pin actually picks up `42f58837`.

2. **go-sdk #145 Contract Manager + #169 Auto-settle merged unreviewed on 2026-05-08 (GOV#19, GOV#20).** What: two large feature merges (contract system; settlement scheduler with new `WithScheduler()` / `WithoutAutoSettle()` / `WhenNextSettlement()` API) self-merged with zero reviews — flagged by security-triage. Why it matters: as go-sdk maintainer, dusan owns the release, and a first-release tag right now would publish 700+ lines of unreviewed code that mutates VTXO state (auto-settle moves VTXOs between rounds). Action: schedule a deliberate audit of #145 and #169 before cutting v0.x.0.

3. **arkd client-lib reshape #1058 (altafan, 2026-05-08).** What: pkg/client-lib gained a new `WithReceiver` option, multi-address explorer batching with throttling, and the wallet interface changed. Why it matters: this is the layer go-sdk wraps directly; if go-sdk doesn't expose `WithReceiver`, callers will hit feature parity gaps the moment they upgrade. Action: diff `pkg/client-lib/` against the equivalent go-sdk wrappers (`send_opts.go`, `unroll_ops.go`, `funding`, wallet interface) and file the deltas.

## TL;DR

- Confirm go-sdk picks up arkd `42f58837` (SubmitTx hotfix) and that an integration test covers the partial-failure path.
- Audit go-sdk #145 (Contract Manager) and #169 (Auto-settle) before tagging v0.x.0 — both went in unreviewed (GOV#19/GOV#20).
- Diff arkd #1058 against go-sdk wrappers; expose `WithReceiver` and multi-address explorer batching where missing.
- Triage the dotnet-sdk CI meltdown (15 failures on 05-09) and the go-sdk day-17 CI thread before they swallow more of the week.
- Decide whether ts-sdk "HD receive rotation alt-to-#441" actually resolves the offchainTapscript race; that PR is `ready_for_review`.
