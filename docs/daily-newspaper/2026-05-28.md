# Ark Daily — 2026-05-28

> _Quiet across fulmine and go-sdk, but arkd v0.9.6 shipped yesterday carrying Dusan's own client-lib pagination fix and Bob's new tx-filter indexer RPC — both repos need a version bump. Off-repo, #dev decided the HTLC claimer is moving out of bancod (now `solver`) and into fulmine as a standalone binary._

## Front page

- **arkd v0.9.6 released yesterday** — includes Dusan's `#1081` client-lib pagination/chunking fix and Bob's `#1074` tx-filter indexer surface; both fulmine and go-sdk should bump to track. (see Protocol surface)
- **HTLC claimer moving bancod → fulmine** as `htlc-claimer`/`covenant-claimer` standalone binary; @U07MSN1PNTU proposal, no objections logged. Direct scope add for fulmine. (see Slack briefing)
- **getVtxos()/getBalance() O(lifetime VTXO history) flagged as PERFORMANCE-CRITICAL** in #dev — seconds-per-call on long-lived wallets; production adoption wall. go-sdk wraps both via `pkg/client-lib`. (see Slack briefing)
- **ts-sdk 0.4.30 cut + 12 commits in 24h** while go-sdk has had no merges in 3 days — drift widening on the renew-threshold and chain-swap timeout fixes. (see Wallet beat / Slack briefing)
- **introspector renamed to `emulator` and moved ArkLabsHQ → arkade-os**; `OP_SIGHASH` opcode + new sighash template ready_for_review — protocol-critical, needs human sign-off. Our local `$INTROSPECTOR_REPO` checkout has a tag clobber blocking fetch. (see Contract surface)

## Wallet beat

### fulmine
No activity in 24h. Last commit `3ea61e2` (2026-04-24, "Bump go-sdk@2d035b64") — repo is now **~34 days** without a merge. Local checkout has a dirty `.gitignore` (skipped from daily pull).

### go-sdk
No activity in 24h. Last merges (3+ days ago):
- `7f6b8d0` Contract registry: allow registering custom contract handlers (#186, Pietralberto)
- `5f2ba69` Make contract manager idempotent for single-key identity (#187, Bob)
- `00c9407` bump client-lib (#184, Dusan)

Local checkout has a dirty `.gitignore` + `.worktrees/` (skipped from daily pull). With arkd v0.9.6 out and tx filter + pagination changes landed in `pkg/client-lib`, a client-lib bump is the next obvious move.

### arkade-wallet (PWA)
One merge in 24h:
- `712c3189` Fix swap info page (#633, João Bordalo) — 2-line UI fix in `src/screens/Apps/Boltz/Swap.tsx` + e2e assertion. Cosmetic.

The substantive wallet work landed two days ago — `81a29b0b` Upgrade ts-sdk 0.4.28 + boltz-swap 0.3.33 (#631). Worth noting: 0.4.30 is now out, so wallet is one minor behind again.

## Protocol surface — arkd → go-sdk

Two arkd merges in the last ~36h, both already in v0.9.6 (released yesterday per slack-log):

- `c4f16324` **Add tx filters to indexer.GetSubscription (#1074, Bob Smith)** — touches `api-spec/protobuf/ark/v1/indexer.proto`, `internal/interface/grpc/handlers/indexer.go`, adds new `internal/interface/grpc/handlers/txfilter/` package (CEL-based filtering). Adds single-connection `GetSubscription` flow + new `UpdateSubscription` RPC with pluggable filters; flattens `SubscriptionFilter`. **Impact on go-sdk:** the indexer wrap surface (`pkg/client-lib/indexer/`) now needs `UpdateSubscription` + `SubscriptionFilter` exposed; `GetSubscription` signature changed. Bob explicitly reverted `pkg/client-lib` changes in this PR ("moved to bob/client-lib-stream-changes") — so the client-lib bump is decoupled and pending.
- `4473e23c` **client-lib: Fix indexer auto-pagination and chunk large vtxo filters (#1081, Dusan Sekulic)** — touches `pkg/client-lib/indexer/grpc/client.go` (70 lines changed). **Impact on go-sdk:** direct — go-sdk consumes this package, so a client-lib bump in go-sdk picks up the fix automatically.

arkd v0.9.6 also contains (from slack-log, not in our local git window): VTXO persistent storage, VHTLC unilateral exit support, auto-settle race narrowing, sub-dust offboard validation, settlement hang fix.

## Contract surface — introspector

Local checkout failed to fetch (tag `v0.0.1` clobber). Last commit captured locally is `d71547f` (2026-05-22, version bump go 1.26.3 + otel 1.41.0, Chris Ricketts) — routine.

Per slack-log (2026-05-26):
- Repo renamed: `ArkLabsHQ/introspector` → `arkade-os/emulator`. Mutinynet emulator live at https://emulator.mutinynet.arkade.sh/v1/info. Update remote on the local checkout to unblock daily fetch.
- **`OP_SIGHASH` opcode + sighash template update** marked `ready_for_review` (per slack-monitor 2026-05-26). Adds new opcode and changes signing template — protocol-critical, requires human sign-off, not yet merged. If/when this lands, downstream Arkade Script consumers (anything compiling against the introspector grammar) will need to handle the new opcode.
- "Enforce canonical encoding for Arkade payloads" PR opened — deterministic serialization across implementations.

## Slack briefing

### Decisions and blockers (from arkana)

- **arkd v0.9.6 shipped (2026-05-27)** — contains Dusan's #1081 plus VTXO persistent storage, VHTLC unilateral exit, auto-settle race fix, settlement hang fix, sub-dust validation. Governance retroactive review still pending per release-coordinator log.
- **bancod → `arkade-os/solver` rename executed (2026-05-27)** by @U07MV8W1CNQ in #dev. `banco` (intents umbrella) → likely `arkade-intents`, TBD. Public launch gated on @U07ML5CM486 taxonomy + copy review.
- **HTLC claimer moving bancod → fulmine** (#dev, 2026-05-26) — @U07MSN1PNTU proposed extracting HTLC claim code into fulmine as standalone `htlc-claimer`/`covenant-claimer` binary (only Boltz uses it). @U07MV8W1CNQ suggested `bancod → executor`. @U07ML5CM486 ambivalent on naming as long as `intents` stays as the category umbrella. No objections.
- **fulmine ci_integration: 4 failures on 2026-05-26** — flagged as "very elevated, worst day for fulmine since earlier in the month." Now off the most recent log but worth a recheck.
- **VHTLC questions answered in #dev (2026-05-27)** by @U0AEWD7LHTJ + @U0854KRKV3Q: (1) Boltz VHTLC script reconstruction → use Boltz restore endpoint; (2) LN→Arkade invoice preimage hash → encoded in invoice; (3) inconsistent `lockupDetails.serverPublicKey` on mainnet chain swaps acknowledged by @U0854KRKV3Q. Useful protocol docs material.
- **Boltz deferred ARK chain refund persistence (money-at-risk, opened 2026-05-25)** — partial fix `lockupDetails.timeouts for restored chain swaps` merged yesterday; full persistence-across-restart issue still tracked open.

### Research findings (from arkana)

- **Chimera Card + Wirex (2026-05-27)** — self-custodial Bitcoin debit card via Arkade VTXOs, 80M+ merchants, launches June 2026. First physical merchant integration on Arkade. Direct competitor to Lightspark's Grid (175M, custodial). VTXO settle-latency at point-of-sale will become a real benchmark.
- **Onion message jamming (Delving Bitcoin, May 23-25)** — 4 mitigation proposals on the table. Affects BOLT12 reliability and therefore Ark-Lightning interop.
- **Credible Exit paper (2026-05-17)** — formal proof Ark supports ~2M credible exits in a 14-day window vs Lightning's ~1.1M (1.8x). Useful framing for fulmine/go-sdk positioning.
- **CTV signaling**: still 0% at ~14 weeks. Dead for this window.
- **PQ discourse**: 5 PQ threads in 8 days on Delving Bitcoin (P2WOTS, Winternitz/Lamport, sipa's BoP-2, Lopp game-theory, nkaretnikov lattice). No consensus on scheme.

### Most important — expanded

1. **HTLC claimer relocating into fulmine.** On 2026-05-26 in #dev, @U07MSN1PNTU proposed moving the HTLC claim code out of `bancod` (now `solver`) into fulmine as a standalone `htlc-claimer`/`covenant-claimer` binary, on the rationale that only Boltz consumes it. @U07ML5CM486 didn't push back. This concretely expands fulmine's scope — Dusan owns fulmine, so the design and packaging of that binary is on his plate as soon as the rename settles. Worth pinging @U07MSN1PNTU this week to lock in the binary boundary (separate cmd vs fulmine subcommand) before code starts moving.

2. **arkd v0.9.6 is out, both of Dusan's repos lag it.** Dusan's own pagination/chunking fix (`#1081`) landed in arkd v0.9.6 yesterday, alongside Bob's tx-filter indexer surface (`#1074`) that changes the `GetSubscription` signature and adds an `UpdateSubscription` RPC. fulmine last bumped go-sdk on 2026-04-24; go-sdk last bumped client-lib on 2026-05-22 (before #1074 / #1081 merged). Both repos need a coordinated bump: go-sdk first (pick up #1081 + decide whether to expose tx-filter UpdateSubscription), then fulmine. The proto surface change is the riskier piece — best to look at Bob's `bob/client-lib-stream-changes` branch before writing the wrap.

3. **getVtxos/getBalance is O(lifetime VTXO history).** Flagged in #dev on 2026-05-26 as PERFORMANCE-CRITICAL — both core wallet APIs scan full lifetime VTXO history on every call, so long-lived wallets degrade to seconds-per-call. go-sdk wraps both, so the production wall will hit go-sdk consumers (including fulmine) too. ts-sdk has a memoize-tapscripts PR open as a partial fix (#521 → #529 perf fix, opened 2026-05-27). go-sdk needs its own rewrite to O(spendable set) — not a 24h fix, but worth opening a tracking issue this week so it doesn't get re-discovered the hard way during a Chimera demo.

## TL;DR

- Open a go-sdk PR to bump arkd → v0.9.6 (picks up Dusan's `#1081` pagination fix) and decide whether to wrap Bob's new `UpdateSubscription` + tx-filter surface from `#1074`.
- After that lands, bump fulmine onto the new go-sdk — fulmine has been off-mainline for ~34 days.
- Ping @U07MSN1PNTU to confirm the htlc-claimer binary boundary (standalone vs fulmine subcommand) before code starts moving.
- File a go-sdk tracking issue for getVtxos()/getBalance() O(lifetime) — production wall, ts-sdk already has a perf PR in flight.
- Fix the local `$INTROSPECTOR_REPO` remote: rename to `arkade-os/emulator` and clear the clobbering tag so daily-update can fetch.
