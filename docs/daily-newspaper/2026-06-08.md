# Ark Daily — 2026-06-08

> _Monday-quiet: zero commits in 24h across fulmine, go-sdk, arkd, ts-sdk, rust-sdk, dotnet-sdk, introspector and wallet. Only arkana-knowledge ticked, with bookkeeping commits. The arkana sdk-parity scan (2026-06-07T10Z) added one new go-sdk gap — ts-sdk's HD-restore gap-limit-capped batch probing — and dotnet-sdk is mid Boltz-swap refactor that fulmine should watch._

## Front page

- **New go-sdk parity gap added overnight: ts-sdk HD restore gap-limit-capped batch probing** (`DEFAULT_SCAN_BATCH=10`, `ScanContractsOptions.batchSize`) — deepens `Wallet.restore()` divergence. Unreleased on ts-sdk, missing in go-sdk. (see Parity desk)
- **dotnet-sdk Boltz refactor in flight (14 commits 2026-06-05→07)** — ARK→BTC chain swap fix, submarine swap refund destination, `BoltzOperationClassifier` / `BoltzRouteHelper` extraction, terminal `Failed` state, full `BoltzSwapStatus` enum. Fulmine's Boltz integration should pull the design notes before the next Boltz refactor PR on fulmine merges. (see Parity desk + Wallet beat › fulmine)
- **Yesterday's four standing items all still standing**: TX_TOO_LARGE VTXO intent guard (go-sdk missing), Arkade extension Layer 1+2 (go-sdk missing, severity HIGH), VHTLC handler `5705340` unreleased on go-sdk master, HTLC claimer migration into fulmine still unowned. Nothing landed, nothing moved. (see Parity desk + Slack briefing)
- **arkd master last green 2026-05-26 — now 13 days stale**; `feat/nostr-transport` ci_unit + ci_integration still both red. Any go-sdk release that bumps `pkg/client/` should pull from a green sha, which means waiting it out or pinning to `bob/dag-1`. (see Slack briefing)

## Wallet beat

### fulmine
No activity in the last 24h. Latest upstream commit is still `3ea61e2 Bump go-sdk@2d035b64 (#401)` from 2026-04-24. Hetzner working tree still has the uncommitted `.gitignore` modification — that's why `daily-update` skipped the pull again today. The open `refractor(swaps): refactor Boltz swap logic for clarity and correctness` PR (filed 2026-06-07) is the only fulmine-adjacent activity worth tracking; it replaces the closed chain-swap E2E PR and lines up with the dotnet-sdk Boltz refactor.

### go-sdk
No activity in the last 24h. Latest upstream is still `7f6b8d0 Contract registry: allow registering custom contract handlers (#186)` from 2026-05-25 — that's **14 days stale on master**. Hetzner working tree still has uncommitted `.gitignore` + `.worktrees/` — daily-update skipped this pull too. v0.10.0 remains current; VHTLC handler still parked on master without a tag.

### arkade-wallet (PWA)
No activity in the last 24h. Latest commit remains Friday's `3d2c95d3 fix(Transaction): show roundTxid for offboarding batch transactions (#648)` by arkanaai[bot] — covered in the 2026-06-07 edition.

## Parity desk

Source: `memory/agent-logs/sdk-parity.md` entry 2026-06-07T10:00Z (latest). The dated `sdk-parity-YYYY-MM-DD.md` files stopped at 2026-04-03 and have been superseded by the agent log.

**Gaps where go-sdk lags:**

- **NEW — HD-restore gap-limit-capped batch probing** (ts-sdk, unreleased: `DEFAULT_SCAN_BATCH=10`, `ScanContractsOptions.batchSize`). Deepens `Wallet.restore()` gap against go-sdk. Worth grepping ts-sdk for the commit and mirroring the API shape before it lands in a ts-sdk release and becomes the reference.
- **TX_TOO_LARGE VTXO intent guard** — server rejects intents with >91 VTXOs; go-sdk has no cap in `batch_session.go` / `sync.go` / auto-settle paths. Agreed approach (2026-06-05 #dev, @U0854KRKV3Q + @U0B791D017A): weight-based estimation, not a hardcoded count.
- **Arkade extension Layer 1** (primitives: `ArkadeScript`, `EmulatorPacket`, `RestEmulatorProvider`, `sendOffChain` extensions). Shipped ts-sdk v0.4.32. go-sdk: missing.
- **Arkade extension Layer 2** (high-level API: `Arkade.connect()` / `Program` / fluent builder / `parseArtifact`). Shipped ts-sdk v0.4.33. go-sdk: missing. **Severity HIGH** — coinflip.mutinynet.arkade.sh runs this in production and Friday's coinflip v0.3 escrow merge moved win-conditions through it.
- **VHTLC handler unreleased on go-sdk master** — `5705340` (vhtlc package + handler + `Derivable()` on the Handler interface) still awaiting a tag.

**Recent sibling-SDK landings (last 7 days) worth a look:**

- **dotnet-sdk Boltz refactor**, 14 commits 2026-06-05→07: ARK→BTC chain swap fix, refund destination for submarine swaps (feat), `BoltzOperationClassifier` + `BoltzRouteHelper` extraction, `Failed` state made terminal, full `BoltzSwapStatus` enum, partials + tests. fulmine's Boltz integration uses a much older shape — worth checking whether the classifier + route helper split is something to mirror before the fulmine `refractor(swaps)` PR merges.
- **ts-sdk v0.4.33** (2026-06-04): Arkade contract API + handler authoring helpers (`isCsvSpendable` / `isCltvSatisfied`). Already covered.
- **ts-sdk boarding HD rotation** (Pietro Grandi, 2026-06-03). Already covered.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days, dusan scope only)

- **HTLC claimer moving to fulmine (#dev, 2026-05-26)** — "FINAL CALL" thread: bancod → arkade-solver, banco → arkade-intents, HTLC claimer reassigned to fulmine, @U07ML5CM486 (OpenIntentsFramework) brought in for alignment. Still no follow-up since the thread was framed final. Biggest pending scope change for fulmine.
- **TX_TOO_LARGE limit = 91 VTXOs (#dev, 2026-06-05)** — @U07ML5C69RC pinned the server cap via local tests; @U0854KRKV3Q + @U0B791D017A converged on weight-based estimation as the correct approach. Direct go-sdk parity work, well-scoped, design already argued out.
- **`refractor(swaps)` Boltz refactor PR open (2026-06-07)** — fulmine-adjacent; supersedes the closed chain-swap E2E PR. Watch for review feedback; title typo ("refractor") still uncorrected.
- **arkd master last green 2026-05-26 — 13 days stale** (daily-briefing 2026-06-07). `feat/nostr-transport` ci_unit + ci_integration both failing. Not actively blocking dusan, but the next go-sdk release that bumps arkd-client should pull from a green sha.
- **`feat: require csvTimelock to be specified` MERGED (2026-06-03)** — CSV timelock is now mandatory in arkade-script contracts. Breaking change for any contract that omitted it; verify nothing in fulmine's vHTLC contract emit path drops the field.

### Research findings (from arkana, last 7 days)

- **PQ Lightning architecture** (Osuntokun, Optech #408, 2026-06-05) — first concrete layer-by-layer PQ proposal for Lightning. BIP324 hybrid ECDH + ML-KEM, lattice-based for LN key management. Hybrid (EC + PQ in parallel) is the recommended migration path. Trend, not action.
- **Bitcoin Core #35269 MuSig2 PSBT session-id fix** (Optech #408) — fixes session ID to include each participant's public nonce; prevents false-assertion crash when `walletprocesspsbt` is called repeatedly on the same PSBT. Ark's collaborative VTXO signing path has this exact shape — ASPs running pre-fix Core can crash during round signing.
- **CLN 26.06 (Optech #408)** — new `graceful`, `sendamount`, `xkeysend` RPCs; deprecates `pay` in favour of `xpay`; BOLT12 payment proof support. fulmine's Lightning interop surface (when using CLN-backed setups) should track the `pay` → `xpay` migration before the deprecation bites.
- **Google Shor reconstruction** (Schrottenloher, arXiv 2606.02235, Delving Bitcoin 2026-06-03) — independently reproduced Google's 20× qubit reduction. Validates ~500K physical-qubit estimate to break ECDLP-256. Trend only, no immediate action.

### Most important — expanded

1. **dotnet-sdk's Boltz refactor (14 commits, 2026-06-05→07) is the most relevant signal for fulmine this week.** The split — `BoltzOperationClassifier` (decides which Boltz swap shape a request belongs to) + `BoltzRouteHelper` (picks the route) + full `BoltzSwapStatus` enum + terminal `Failed` state — is a clean factoring that the open fulmine `refractor(swaps)` PR could either mirror or compete with. Before that fulmine PR moves out of draft, it's worth reading the dotnet-sdk diff to decide whether to align the classifier/route split or deliberately diverge. Aligning means cheaper cross-SDK debugging when a swap stalls; diverging is fine if you have a reason. Don't just merge in isolation.

2. **TX_TOO_LARGE VTXO guard — still the cleanest go-sdk PR to ship this week.** Zero motion in 24h, the design (weight-based estimation, not a count cap) was already argued out in #dev on 2026-06-05 with both @U0854KRKV3Q and @U0B791D017A converging. ts-sdk and dotnet-sdk parity work has been queued behind a reference implementation. Small, well-scoped, and unblocks two sibling SDKs.

3. **Bitcoin Core #35269 MuSig2 PSBT session-id fix verification — still open from yesterday.** Same risk: ASPs running pre-fix Core can crash during round signing because session ID didn't include each participant's public nonce. The simple action is to confirm the production ASPs you care about are on a Core build that includes the fix and then mark this as a non-issue for fulmine's wallet flows. Don't let it carry over a third day.

## TL;DR

- **Look at:** the dotnet-sdk Boltz refactor diff (commits 2026-06-05→07, BoltzOperationClassifier/RouteHelper split) before the fulmine `refractor(swaps)` PR moves out of draft.
- **Ship:** the TX_TOO_LARGE VTXO intent guard in go-sdk — weight-based estimation, design already aligned in #dev. Small, well-scoped, unblocks ts-sdk + dotnet-sdk.
- **Verify:** the ASPs you care about are on a Bitcoin Core build that includes #35269 (MuSig2 PSBT session-id fix). Carrying over from yesterday.
- **Ask:** who owns the HTLC claimer migration into fulmine, and when does it start? @U0854KRKV3Q's 2026-05-26 "FINAL CALL" thread is still without follow-up.
- **Housekeep:** decide whether to commit or stash the `.gitignore` modification on fulmine + the `.gitignore`/`.worktrees/` changes on go-sdk so `daily-update` can stop skipping those pulls.
