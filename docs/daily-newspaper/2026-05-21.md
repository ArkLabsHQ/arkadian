# Ark Daily — 2026-05-21

> _Quiet on arkd and introspector. The day's signal is concentrated in your stack: arkade-wallet shipped mnemonic identity (#624), fulmine CI is escalating to 3×, and a protocol-critical non-interactive VHTLC reverse-swap PR is open against fulmine. dotnet-sdk shipped network-config defaults that go-sdk doesn't have._

## Front page

- **arkade-wallet merged mnemonic identity for new wallets** (#624 by pietro909) — BIP86 Taproot derivation, encrypted PBKDF2+AES-GCM storage, plus a service-worker rewrite to carry the mnemonic instead of a derived SingleKey. Touches your wallet stack directly. (see Wallet beat)
- **fulmine `ci_integration` at 3 failures** in 24h — escalating from 1× the prior day. Your repo. (see Slack briefing → Decisions and blockers)
- **fulmine has a PROTOCOL-CRITICAL open PR: "feat: non interactive claim in reverse swap"** — extends the 2026-05-18 non-interactive VHTLC claim work to reverse swaps. Two open non-interactive VHTLC PRs are now in flight; both need human sign-off per governance. (see Slack briefing → Most important)
- **dotnet-sdk shipped `ArkNetworkConfig` defaults for Esplora + Electrum (WS / TCP)** (#96 by Andrew Camilleri / Kukks) — go-sdk has no equivalent. Parity signal. (see Slack briefing → Decisions and blockers)
- **go-sdk CI: 2× `ci_integration` + 1× `ci_unit`** — day 26+ meltdown ongoing. Your repo. (see Slack briefing → Decisions and blockers)

## Wallet beat

### fulmine
- No commits landed in the last 24h. Daily-update skipped pulling fulmine due to uncommitted local changes (`.gitignore` modified locally).
- Slack: `ci_integration` failures escalated to 3× in 24h (was 1× the day before).
- Slack: PR "feat: non interactive claim in reverse swap" opened — see Slack briefing for impact.

### go-sdk
- No commits landed in the last 24h. Daily-update skipped pulling go-sdk due to uncommitted local changes (`.gitignore` modified, untracked `.worktrees/`).
- Slack: CI still in meltdown — 2× `ci_integration` + 1× `ci_unit` (day 26+).

### arkade-wallet (PWA)
Two commits in last 24h on `master` at `$WALLET_REPO`:
- `9848c02c` **fix prettyAssetNumber (#626)** (João Bordalo) — small fix in `src/lib/assets.ts` with a new unit test in `src/test/lib/asset.test.ts`. Cosmetic correctness.
- `dfb1e9d3` **feat(wallet): mnemonic identity for new wallets (#624)** (pietro909) — substantive. New `lib/mnemonic.ts` with PBKDF2 + AES-GCM encrypted storage and BIP86 Taproot derivation. New wallets now use `MnemonicIdentity`; legacy `SingleKey` wallets keep working. Restore screen auto-detects mnemonic (12 words) vs nsec/hex. Service worker rewritten to receive the raw mnemonic (and `isMainnet`) instead of a derived SingleKey, so the worker's `buildServices` can construct the `MnemonicIdentity` internally for signing. Multiple `fix:` iterations in-PR addressing CodeRabbit feedback, DOMException handling, null guards, and the service-worker identity loss.

## Slack briefing

### Decisions and blockers (from arkana)

- **fulmine `ci_integration`: 3 failures** — escalating (was 1× the prior day). _Source: `slack-log.md` 2026-05-20 Blockers._ Your repo.
- **go-sdk `ci_integration`: 2 failures + `ci_unit`: 1 failure** — "back in meltdown (day 26+)." _Source: `slack-log.md` 2026-05-20 Blockers._ Your repo.
- **fulmine PR opened: "feat: non interactive claim in reverse swap"** — PROTOCOL-CRITICAL. Extends the 2026-05-18 non-interactive VHTLC claim PRs ("fix for non interactive VHTLC claim" + "feat: NonInteractiveClaim option to CreateVHTLC") to reverse swaps. Two open non-interactive VHTLC PRs now in flight; VHTLC exit paths require human sign-off per governance rules. _Source: `slack-log.md` 2026-05-20 Discoveries._
- **wallet#624 "mnemonic identity for new wallets" MERGED** — touched by `ts-sdk@0.4.27`, which release-coordinator flagged as a GOV VIOLATION (#489 P1 race; zero human reviews). The mnemonic-identity change now sits on top of a release with an open race condition. _Sources: `slack-log.md` 2026-05-20 + 2026-05-19._
- **wallet#627 (home & settings redesign) approved by code-review agent** — UI-only; SDK bumps `0.4.27` (ts-sdk) and `0.3.32` (boltz-swap bundle) are backward-compatible. 5 non-blocking issues flagged. _Source: `slack-log.md` 2026-05-20 Discoveries._
- **dotnet-sdk: `feat(hosting): ArkNetworkConfig defaults for Esplora + Electrum (WS / TCP)`** merged today (#96, Kukks). Hosting defaults are a documented parity gap relative to go-sdk's wrap surface. _Source: today's daily-changelog + `slack-log.md` 2026-05-20 Discoveries._
- **Issue opened: "Add tx filters to indexer.GetSubscription"** — indexer subscription filter feature request. Follows yesterday's VTXO filter discussion; would surface through `pkg/client/` if it lands in arkd. _Source: `slack-log.md` 2026-05-20 Discoveries._
- **"Convert ts-sdk to pnpm monorepo with boltz-swap" marked ready_for_review** — large structural change; would simplify ts-sdk version propagation long-term but is high-risk while ts-sdk CI is at 6 failures/day. Parity question: if the JS side moves to a monorepo, what's the go-sdk story for boltz-swap coupling? _Source: `slack-log.md` 2026-05-20 Discoveries._
- **"feat(telemetry): integrate OpenTelemetry for tracing, metrics, and logs" PR opened** — observability infrastructure change. Watch for what it instruments in pkg/client/ if it's against arkd. _Source: `slack-log.md` 2026-05-20 Discoveries._

### Research findings (from arkana)

- **"Credible Exit and The Law of Conservation of Blockspace"** (BitcoinErrorLog, Delving Bitcoin, May 13; flagged by research-monitor 2026-05-17) — formal blockspace bound proof. **Ark supports ~2M credible exits in a 14-day window vs Lightning's ~1.1M — a 1.8x advantage.** Validates Ark's design philosophy and the 4-week VTXO expiry as a fundamental scalability parameter, not just UX.
- **Optech #405** (research-monitor 2026-05-15) — BIN-2026-0002 assigned for **OP_CHECKCONTRACTVERIFY** (covenant opcode with direct Ark relevance). OP_TEMPLATEHASH on Inquisition signet. **BOLTs #995 (simple taproot channels) + #1228 (zero-fee commitments) standardized.** PSBTv2 (BIP370) implemented in Bitcoin Core.
- **Bitcoin Core 31.0** (research-monitor 2026-05-17) — Cluster mempool PR #34616 merged to main; V3 policy + sibling eviction RBF standard. rc4 on testnet, full release expected late 2026. Direct benefit to Ark's forfeit/connector tx broadcasting.
- **CTV** — still 0% signaling at 12+ weeks (multiple research runs). Dead window. LNHANCE remains the most strategically aligned covenant bundle for Ark.
- **sipa "Bird of Prey 2"** (Delving Bitcoin, May 18) — hybrid Schnorr + PQ signature research. Educational, NOT a Bitcoin proposal. Worth tracking as a signal that PQ thinking continues in Core.

### Most important — expanded

1. **wallet#624 mnemonic identity is now your problem to track for parity.** WHAT: pietro909 merged BIP86 Taproot-derived mnemonic identities for new wallets, with a service-worker rewrite that propagates the raw mnemonic into `buildServices`. WHO: pietro909, merged 2026-05-20. WHY: this is the wallet stack you're building toward, and the SingleKey-vs-MnemonicIdentity split now exists. Question for this week: does go-sdk need its own `MnemonicIdentity` analogue, or do consumers stay on `SingleKey` plus an external BIP32 derivation? The PWA's service-worker fix suggests `MnemonicIdentity` lacks a `toHex()` — worth knowing before any go-sdk parity work begins.

2. **Fulmine has a second PROTOCOL-CRITICAL non-interactive VHTLC PR open, and your CI is escalating.** WHAT: "feat: non interactive claim in reverse swap" extends the 2026-05-18 non-interactive VHTLC claim work (which itself was two PRs — `NonInteractiveClaim` option on `CreateVHTLC` + fix) to the reverse-swap path. WHO: opened in the past 24h (slack-monitor 2026-05-20). WHY: VHTLC exit paths are governance-gated and require human sign-off; this is the third surface in a week to touch the non-interactive claim flow. Concurrently, fulmine `ci_integration` jumped from 1× to 3× failures in 24h. If the non-interactive PR is breaking integration tests, that's a coupled signal worth checking before reviewing.

3. **dotnet-sdk shipped network-config defaults that go-sdk does not have.** WHAT: Kukks merged `ArkNetworkConfig` defaults for Esplora + Electrum (WS / TCP) via dotnet-sdk #96. WHO: Andrew Camilleri (Kukks), merged 2026-05-20. WHY: sibling-SDK parity matters and this is exactly the kind of QoL hosting-config feature go-sdk consumers ask for. Cheap to port; concrete parity gap surfaced today.

## TL;DR

- **Diagnose fulmine `ci_integration` escalation (1× → 3×)** — likely correlated with the non-interactive VHTLC reverse-swap PR; check before reviewing.
- **Decide go-sdk's stance on `MnemonicIdentity`** — wallet#624 lands BIP86 mnemonic identity in the PWA; parity question is live for go-sdk and the sibling SDKs.
- **Sanity-check wallet#624 sitting on ts-sdk 0.4.27** — that release has a flagged P1 race (#489, GOV VIOLATION); confirm whether the race affects the mnemonic path.
- **Consider porting `ArkNetworkConfig` Esplora/Electrum defaults to go-sdk** — dotnet-sdk #96 just shipped it; cheap parity win.
- **Don't get drawn into go-sdk day-26+ CI meltdown without a fresh failure mode** — Slack shows it's the same persistent integration/unit pair; root-cause work probably belongs in a focused block, not a morning pass.
