# Ark Daily — 2026-07-05

> _Quiet day across the fleet — dusan's own repos idle, arkd/introspector silent. One fresh landing anywhere: dotnet-sdk's LUD-06 reverse-swap invoice fix, which fulmine's swap path should mirror._

## Front page

- Only fresh commit fleet-wide in the last 24h: **dotnet-sdk `303eaf6`** — "reverse-swap invoice must equal requested amount (LUD-06)". fulmine drives Boltz reverse swaps; worth confirming it doesn't bundle the swap fee into the invoice amount. (see Most important — expanded)
- **fulmine + go-sdk: no commits in 24h**, and both local clones are still frozen — daily-update skipped both again for uncommitted local changes (fulmine HEAD Apr 24, go-sdk HEAD May 25). (see Wallet beat)
- **arkd + introspector: no changes in last 24h.** Yesterday's re-vendor task still stands — arkd `fc7f23ec` (BIP-322 intent-proof 0x09 global) needs picking up in go-sdk's vendored `ark-lib`. (see TL;DR)
- **Slack + research intel remains stale** — arkana's slack-log stops at June 19, research-monitor at June 18; no new signal in 7+ days. Treat below as background. (see Slack briefing)

## Wallet beat

### fulmine
No activity in last 24h. HEAD still `3ea61e2` (2026-04-24, "Bump go-sdk@2d035b64"). Skipped again by daily-update for uncommitted local changes.

### go-sdk
No activity in last 24h. HEAD still `7f6b8d0` (2026-05-25, "Contract registry: allow registering custom contract handlers"). Also skipped by daily-update for uncommitted local changes.

### arkade-wallet (PWA)
No new commits in last 24h. Most recent landings remain July 1:
- `341bf560` Allow deprecated keys delegate (#708) — rotation-related (João Bordalo)
- `4ead848e` Upgrade ts-sdk 0.4.41 + boltz-swap 0.3.46 (#709) (pietro909)

## Slack briefing

> ⚠️ Source freshness: arkana's slack-log ends **2026-06-19** and research-monitor ends **2026-06-18** — ~2.5 weeks stale. No new decisions/blockers/research to surface today. Nothing below is fresh.

### Most important — expanded

- **dotnet-sdk landed the LUD-06 reverse-swap fix — check fulmine has the equivalent.** `303eaf6` (Tobi Adeyemi, #138) makes the dotnet reverse-swap invoice equal exactly the requested amount, i.e. it stops bundling the Boltz reverse-swap fee into the invoice. This is the dotnet counterpart of the same LUD-06 bug ts-sdk/boltz-swap fixed on June 15 — where a fee-inflated invoice caused all LUD-06-compliant wallets to reject the payment. Why it matters for dusan: fulmine (not go-sdk — go-sdk has no reverse-swap/invoice code) is what drives Boltz reverse swaps for the Arkade wallet stack; if fulmine still constructs the invoice with the swap fee folded in, Lightning receives originated through fulmine will be rejected by strict LUD-06 wallets. Quick grep of fulmine's swap/invoice construction against the dotnet diff would confirm parity.

## TL;DR

- **Confirm fulmine's reverse-swap invoice equals the requested amount (LUD-06).** dotnet just fixed it (`303eaf6`); ts-sdk fixed it June 15. go-sdk has no swap code, so this is a fulmine-side check.
- **Still open from yesterday:** re-vendor `ark-lib` in go-sdk to pick up arkd `fc7f23ec` (BIP-322 intent-proof `0x09` global) and refresh proof fixtures, or intent proofs diverge from arkd's commitment.
- **Unstick the local clones:** commit or stash the uncommitted changes in fulmine and go-sdk so daily-update stops skipping them (frozen since April/May — tomorrow's sync is blind to real upstream activity).
- Nothing else urgent — quiet day.
