# Ark Daily — 2026-05-14

> _Quiet day. Primary repos (fulmine, go-sdk, arkd, introspector, all sibling SDKs) all silent in the last 24h. The wallet shipped two UX fixes, the 20+ day fleet-wide CI meltdown finally got its root-cause fix yesterday via arkade-regtest, and Boltz is in the middle of a backend-cleanup wave that touches the Fulmine integration surface._

## Front page

- **Fleet-wide CI root cause fixed yesterday — `arkade-regtest` default fulmine image bumped `v0.3.21 → v0.3.23`** (PR #23, joão; merged by kukks). The `boltz/boltz:latest` image had moved to RPCs only present in fulmine v0.3.23 — every CI that spins up the regtest stack was failing for 20+ days. Watch today whether btcpay-arkade / boltz-swap CI failure counts drop. **go-sdk's CI meltdown is a separate root cause, still open.** (see Slack briefing → Decisions)
- **Wallet #556 — non-blocking boarding settlement UX merged** (sahil, +reviews from bordalix). Replaces the full-screen `WaitingForRound` overlay with an inline "Processing your boarding transaction…" banner and **deletes the `WaitingForRound` component entirely** (extends to Vtxos coin-control and Send/Details too). Foundation for any boarding/settle UX you carry into Arkade Wallet. (see Wallet beat → arkade-wallet)
- **Boltz backend cleanup wave** (michael1011): BIP-69 ordering, derive `l1BlockNumber` from latest L2 block (no L1 subscription), poll block height instead of subscribing, websocket efficiency PR #1401. Follows yesterday's `7c4beaa2` "getting claim tx from Fulmine" — Boltz is hardening the integration boundary fulmine sits on. (see Wallet beat → fulmine)
- **boltz-swap v0.3.31 released** with `expo-task-manager / expo-background-task` isolated to a `/expo/background` subpath (Pietro Grandi). If anything in your stack uses boltz-swap and was hitting the Metro lazy-require bug, the fix is in this version. (see Slack briefing → Decisions)
- **All your primary repos silent today** — fulmine, go-sdk, arkd, introspector, ts-sdk, rust-sdk, dotnet-sdk: zero commits in the last 24h. Standing P0 on go-sdk first release is unchanged. (see TL;DR)

## Wallet beat

### fulmine
No commits in the last 24h. Local checkout still has uncommitted changes (`M .gitignore`); daily-update skipped the pull. Last fulmine commit on master is still `4da0368 Add GetVHTLCTransaction RPC (#408)` from 3 days ago.

**Adjacent — Boltz is reshaping the fulmine integration surface this week:**
- Yesterday `7c4beaa2 refactor: getting claim tx from Fulmine` (michael1011) — Boltz now sources the swap claim tx from Fulmine directly.
- Today `35a1ef7d refactor: derive l1BlockNumber from latest L2 block` and `6bb5430e refactor: poll block height instead of subscription` — block-height plumbing changes; if fulmine pushes block-height events to Boltz over a subscription, that producer side may go unused.
- `a25adaaa fix: unclean shutdowns` and PR #1401 (`ws-efficiency`) — ws lifecycle hardening.

Worth a 10-minute read of the boltz-backend diff to confirm fulmine's exposed contract (which RPC, which subscription) is still load-bearing or has been quietly demoted.

### go-sdk
No commits in the last 24h. Local checkout is on `claude-electrum-test-break` (no upstream — daily-update skipped). Master last touched 6 days ago.

Standing context unchanged from yesterday:
- 3 unreviewed merges on master: #143 HD Wallet, #145 Contract Manager, #169 Auto Settle.
- "Remove KV store" PR landed on master 2026-05-13 (per arkana slack-monitor) — significant architectural change merged on top of the unreviewed three. Audit before first release.
- CI meltdown day 21+ (separate root cause from the fleet-wide one fixed yesterday). No diagnosis yet.
- "Default HD-wallet descriptor to Bip86" issue opened yesterday — descriptor-standard alignment request, open for triage.

### arkade-wallet (PWA)
Two PRs merged (`e96024d → f2cfa79`):
- **#556 — fix(transaction): non-blocking boarding settlement UX** (Sahil Chaturvedi). Replaces blocking `WaitingForRound` overlay with an inline `Info` banner + `LoadingIcon` (32px → 20px size match), guards against conflicting "settled + pending" banner states, removes a stale `setTxInfo` callback that could corrupt navigation. **Follow-up commits delete `WaitingForRound` entirely** — extends the non-blocking pattern to `Vtxos.tsx` (Coin Control rollover) and `Send/Details.tsx` (mainnet payments use `LoadingLogo` like Lightning/Ark). Also bumps the `regtest` submodule and updates `.env.regtest`. This is the canonical pattern the wallet now uses for any "round in flight" UX — worth knowing if you're building Arkade Wallet flows that touch settlement.
- **#616 — fix support** (João Bordalo, +16/-5 in `Settings/Support.tsx`) — UI fix for the support screen. Companion to yesterday's "failed to load support chat" issue.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days)

- **Fleet CI root cause found and fixed (day 21)** — `boltz/boltz:latest` requires `arklabshq/fulmine:v0.3.23`; arkade-regtest defaulted to `v0.3.21`. PR #23 on arkade-regtest (`update default fulmine image`) merged yesterday. Watch for btcpay-arkade and boltz-swap CI to recover today. **Note: go-sdk's own CI meltdown is independent of this fix and still open.**
- **"Remove KV store" merged in go-sdk (yesterday)** — landed despite arkana's risk flag, on top of the 3 unreviewed master merges (#143, #145, #169). go-sdk integration correctness must be re-verified before P0 first release.
- **Two Enclave KMS autonomy PRs opened**: (1) atomic `KMSKeyID` flip + drop `/Migration/*` staging, (2) enclave self-bootstraps its own KMS key end-to-end. Both protocol-critical, both await human review. Not in your scope but explains why kukks's Slack signal is heavy on enclave work this week.
- **CodeRabbit AI opened a PR autonomously (2026-05-12)** — first sighting of CodeRabbit submitting code, not just review comments. Open governance question: does CodeRabbit-generated code count as "human review" for the unreviewed-merge policy? Relevant to go-sdk hygiene right now.
- **Standing protocol surface news that affects go-sdk's wrap layer**:
  - arkd `Client-lib: renaming of packages and components` (#1064, Pietro, 3 days ago) — renamed `pkg/client/` → `pkg/client-lib/`. **go-sdk imports of `pkg/client/...` will need to be updated**; check if your local checkout already reflects this.
  - arkd `GetVHTLCSpendingTx` rpc merged 2026-05-12 — finds the tx that *spends* a VHTLC; relevant if go-sdk needs to surface VHTLC closure detection.
  - arkd `GetVHTLCTransaction` rpc merged on fulmine side 3 days ago.

### Research findings (from arkana, last 7 days)

_Latest research-monitor run: 2026-05-13. Optech #405 expected today (2026-05-15)._

- **Chimera Wallet integrates Arkade as primary payment layer** (announced 2026-05-13) — first major third-party wallet using Arkade as its primary payment layer. Non-custodial, PWA-based (v0.2 live), $15M Nimbus Capital backing, May 27 TGE. Validates the ASP model for third-party builders. Open question: SDK integration quality — are they on `@arkade-os/ts-sdk`, a custom integration, or something else?
- **LNHANCE gaining traction** — bundles CTV + OP_CHECKSIGFROMSTACK + OP_INTERNALKEY with Lightning-focused framing. arkana flags this as the most strategically aligned covenant bundle for Ark; CTV signaling still 0% after 11 weeks.
- **BOLTs #995: simple taproot channels officially specified** (Optech #404, May 8) — P2TR + MuSig2 channels are now part of the LN spec, Eclair enables by default. Both VTXOs and LN channels now standardized on the same primitives — tailwind for any Ark↔LN interop you build into Arkade Wallet.
- **CVE-2024-52911 (Bitcoin Core, disclosed 2026-05-05)** — use-after-free in block validation, fixed in v29.0; ~43% of nodes still on vulnerable software. Operational only — verify any arkd you operate is on Core ≥ v29.0.

### Most important — expanded

**1. Fleet-wide CI root cause was an image-version mismatch, fixed via arkade-regtest PR #23 yesterday.** For 20+ days every CI that spun up the regtest stack (btcpay-arkade, boltz-swap, and others) had been failing because `boltz/boltz:latest` upgraded to use new RPC methods only present in `arklabshq/fulmine:v0.3.23`, while arkade-regtest defaulted to `v0.3.21`. PR #23 (joão) merged yesterday bumps the default to v0.3.23. **For dusan**: monitor btcpay-arkade and boltz-swap CI today to confirm recovery — and note that **go-sdk's CI meltdown (day 21) is a different root cause** that this fix does *not* address. The arkana slack-monitor entries were specific that go-sdk's failures don't depend on the boltz/fulmine image. If the wider fleet recovers and go-sdk doesn't, that isolates go-sdk's problem, which is good news for diagnosis.

**2. "Remove KV store" merged in go-sdk on top of three unreviewed master merges.** Per arkana's slack-monitor for 2026-05-13: "Remove KV store" landed despite the explicit risk flag from 2026-05-12. This is on top of #143 (HD Wallet, +large), #145 (Contract Manager), and #169 (Auto Settle, +704 lines, self-merged with zero reviews — GOV#20). The KV-store-removal interacts with all three (they likely touched the store layer). For dusan, this means **the go-sdk first release P0 cannot be cut safely until those four merges are audited together.** Worth raising with kukks/altafan before any tagging happens — the parity-desk gap (e.g. dotnet-sdk shipping unilateral exit Tuesday) is now compounded by an integration-correctness debt.

**3. Boltz is reshaping the integration boundary fulmine sits on, this week.** In the last 48 hours: `7c4beaa2 refactor: getting claim tx from Fulmine` (yesterday), and today's batch `35a1ef7d derive l1BlockNumber from latest L2 block`, `6bb5430e poll block height instead of subscription`, `a25adaaa fix: unclean shutdowns`, `105d6d61 use BIP-69 in boltz-core`, plus `ws-efficiency` PR #1401. Boltz is centralising on Fulmine for the claim-tx side and decoupling from L1 subscriptions on the block-height side. **For dusan**: a focused review of the boltz-backend diff would tell you exactly which fulmine endpoints Boltz now depends on (and which it has stopped using) — useful before you cut a fulmine release that might silently break a contract Boltz is now leaning on.

## TL;DR

- **Verify CI recovery** today — watch btcpay-arkade / boltz-swap to confirm the fleet-wide fix landed cleanly. If go-sdk CI still fails, you've isolated its independent root cause.
- **Do an audit pass on go-sdk master** — KV store removal + 3 unreviewed merges (#143/#145/#169) need joint review before P0 first release. Don't tag until this is done.
- **Read the boltz-backend diff for `7c4beaa2` + today's 4 commits** — confirm which fulmine RPCs/subscriptions Boltz now depends on; protect that contract before any fulmine release.
- **Check if your go-sdk checkout has the arkd `pkg/client/ → pkg/client-lib/` rename digested** — the `Client-lib: renaming of packages and components` arkd PR (#1064, 3 days ago) will require import updates downstream.
- **Quiet day on primary repos** — good window to do the audits above without firefighting new commits.
