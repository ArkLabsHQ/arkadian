# Ark Daily — 2026-07-21

> _Quiet day: no new commits on fulmine or go-sdk, arkd shipped a sweep-tx event that rides go-sdk's existing tx-event stream, and dotnet-sdk fixed a CLTV bug worth checking in go-sdk._

## Front page

- arkd merged **#1155 sweep-tx event** — checkpoint sweeps now emit a `SweepTxType` `TransactionEvent` on the *existing* tx-event subscription; go-sdk must tolerate/handle the new event so it isn't dropped or mis-decoded. (see Protocol surface)
- dotnet-sdk fixed **CLTV locktime propagation to the ark tx, not just the checkpoint** (#161) — likely a parity item; verify go-sdk propagates CLTV the same way. (see Parity desk)
- Your repos are quiet: **fulmine** and **go-sdk** have no new commits in 24h; only `.gitignore` is dirty locally (sync noise). (see Wallet beat)
- **Ops:** arkana's Slack extract and research-monitor feeds are stale (last updated Jun 19) — no fresh Slack/research signal in this edition. (see Slack briefing)

## Wallet beat

### fulmine
No activity. HEAD unchanged at `3ea61e2` (Bump go-sdk, 2026-04-24). Only `.gitignore` modified locally — sync noise, not WIP.

### go-sdk
No activity. HEAD unchanged at `7f6b8d0` (contract registry custom handlers, 2026-05-25). Only `.gitignore` modified locally.

### arkade-wallet (PWA)
Cosmetic only: `0a025e83` fix vertical alignment of nsec input (#804, João Bordalo). Prior day's asset-ID display work (#801/#802) already landed. Nothing functional to track.

## Parity desk

_Formal parity report is stale (newest is 2026-04-03, >14 days) — gaps not recomputed. One fresh sibling-SDK landing worth flagging:_

- **dotnet-sdk `#161` — propagate CLTV locktime to the ark tx, not just the checkpoint** (`7c68e66`, d4r). If go-sdk only sets CLTV on the checkpoint tx and not the ark tx, it has the same bug dotnet just fixed. Worth a quick check on go-sdk's ark-tx construction path.

## Protocol surface — arkd → go-sdk

- **Propagate sweep tx event on checkpoint sweep** — `f444de6c` (louisinger, #1155). Touches only `internal/core/application/{service.go,sweeper.go}` + e2e test; no `pkg/client/`, `api/`, `proto/`, or `internal/interface/grpc/` change.
  - **Impact on go-sdk:** No signature/proto break, but a new `SweepTxType` `TransactionEvent` (carrying `SweptVtxos`) now flows through the *existing* transaction-event subscription. Verify go-sdk's tx-event consumer tolerates/handles the new type rather than dropping or mis-decoding it.

## Slack briefing

_arkana's `slack-log.md` and `research-monitor.md` were last updated 2026-06-19 — no content in the last 7 days. The extract pipeline appears to have stopped refreshing; flagging so you know the feed is stale, not silent. Nothing actionable to surface from month-old entries._

## TL;DR

- **Check go-sdk's tx-event consumer** handles arkd's new `SweepTxType` event (#1155) — otherwise sweep events may be silently dropped.
- **Compare go-sdk vs dotnet-sdk #161** — confirm go-sdk propagates CLTV to the ark tx, not just the checkpoint. If not, port the fix.
- Nothing else urgent — fulmine/go-sdk quiet, wallet cosmetic.
- **Housekeeping:** arkana's Slack/research feeds have been stale since Jun 19; worth a nudge if you still rely on them before handoff.
