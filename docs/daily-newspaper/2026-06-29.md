# Ark Daily — 2026-06-29

> _Quiet again, stale desk: zero commits across all priority repos in the last 24h, and the Slack/research/parity feeds the desk reads are all 9+ days old — the briefing is still running half-blind._

## Front page

- **No new commits in the last 24h** anywhere in dusan's scope — fulmine, go-sdk, wallet, arkd, the sibling SDKs and introspector are all unchanged since Jun 26. (see Wallet beat)
- **Input pipeline is still stale.** Slack log stops at Jun 19, research-monitor at Jun 18, parity report at Apr 3 (>14 days → Parity desk skipped). Today's daily-update pulled 0 commits and again failed to fetch `introspector`/`bancod` (tag-clobber). Treat Slack items below as historical. (see Slack briefing)
- **fulmine & go-sdk skipped by daily-update for a 10th-ish day** — both still carry only a `M .gitignore` working-tree edit. Harmless, but commit or stash it so auto-sync stops skipping your two primary repos. (see Wallet beat)
- **Still-open from the last live signal (Jun 19):** the per-leaf signer-key fix (`fix(arkd-wallet): select signer key per leaf across all multisig closures`) was PROTOCOL-CRITICAL for the Jun 22 rotation, and go-sdk CI was unstable going in. A week on, worth a one-line confirmation both resolved. (see Slack briefing)

## Wallet beat

### fulmine
No commits in the last 24h. HEAD remains `3ea61e2 Bump go-sdk@2d035b64 (#401)` (Apr 24). Only working-tree change is `M .gitignore` — this is why daily-update keeps skipping the repo. No fresh CI/PR signal (Slack feed stale).

### go-sdk
No commits in the last 24h. HEAD remains `7f6b8d0 Contract registry: allow registring custom contract handlers (#186)` (May 25). Only working-tree change is `M .gitignore` — same skip cause as fulmine.

### arkade-wallet (PWA)
No activity in the last 24h. Latest commit is still `074c5244 fix(receive): show 'Edit amount' after setting an asset amount (#697)` (Jun 26, Dunsin) — already covered in prior editions. Recent wallet churn was cosmetic/UX only; no protocol surface touched.

## Slack briefing

> _Source caveat: newest slack-log entry is 2026-06-19 (10 days old) and newest research-monitor entry is 2026-06-18. Nothing below is from the last 24h; it is the last live signal, surfaced only because the items remained open and in dusan's scope._

### Decisions and blockers (from arkana, as of Jun 19)
- **go-sdk CI instability** — 6+ ci_integration + 3 ci_unit failures flagged as the top CI risk going into the Jun 22 signer rotation. Confirm it stabilised post-rotation.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** — PR open, tagged PROTOCOL-CRITICAL for rotation correctness. Check whether it merged.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question** — issue open: trigger settlement when *one* VTXO is near-expiry vs the current all-VTXOs gate. Affects settlement aggressiveness / user-facing latency; a go-sdk-relevant protocol decision still awaiting a call.

### Research findings (from arkana, as of Jun 18)
- **LND zero-timestamp gossip DoS** — crafted gossip crashes LND < v0.20.1 (Delving Bitcoin). Already patched upstream; relevant only if any Lightning node in the swap path runs an old LND.
- **CTV** — still 0% signalling, ~18 weeks in (nearly halfway through the timeout). No movement affecting covenant plans.

### Most important — expanded
- **The desk is still reading stale inputs — that remains the real story.** Today's daily-update pulled 0 commits, again skipped fulmine and go-sdk on uncommitted `.gitignore` changes, and again failed to fetch `introspector` and `bancod` (both `would clobber existing tag` errors on `git fetch`). Meanwhile arkana's Slack, research and parity feeds have not advanced in 10+ days. WHO: this is the automated pipeline (daily-update + arkana monitors), not a person. WHY it matters: for over a week the morning briefing has had no fresh Slack/PR/parity signal, so "quiet day" likely reflects a stalled pipe as much as a quiet ecosystem. A 5-minute check that the slack-monitor / research-monitor / sdk-parity crons and the repo mirrors are alive would restore trust in the next editions.

## TL;DR

- **Unblock your own repos:** the `.gitignore` edits in fulmine and go-sdk are making daily-update skip both every day — commit or stash them so auto-sync resumes.
- **Fix the fetch failures:** `introspector` and `bancod` fail `git fetch` with tag-clobber errors; `git fetch --tags --force` (or pruning local tags) on those mirrors clears it.
- **Check the arkana feeds:** Slack/research/parity logs are 10+ days stale — verify the slack-monitor / research-monitor / sdk-parity crons still run, or the desk stays blind.
- **Post-rotation confirmation (low effort, high value):** verify the Jun-22 rotation landed cleanly — did the per-leaf signer-key fix merge, and did go-sdk CI recover?
- Otherwise: nothing shipped today — genuinely quiet on the code front.
