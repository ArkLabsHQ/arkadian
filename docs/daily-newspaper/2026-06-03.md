# Ark Daily — 2026-06-03

> _Big day for dusan's repos: **go-sdk v0.10.0 RELEASED** (60-day drought broken), and **first fulmine commit in 41 days lands** — both yours. Sibling work: altafan reshapes arkd's `pkg/client-lib` on `refactor-client-lib` (direct go-sdk wrap impact), emulator/introspector ships DLEQ proof + arkade sighash options, dotnet-sdk merges unified wallet recovery._

## Front page

- **go-sdk v0.10.0 RELEASED** (`b117416`, altafan, 2026-06-02 18:17). Ends 60-day drought; 17 PRs since v0.9.1 (HD wallet, contract manager+registry, auto settle, KV removed, cursor pagination, your spend-sync fix). Release-coordinator hasn't caught up yet — its log still says "no releases ever" (see Wallet beat → go-sdk).
- **Your `Synchronize spend operations (#183)` merged 16h ago** — closes the 17-day P1 auto-settle race that arkana had flagged PROTOCOL-CRITICAL. Co-authored with altafan. Made it into v0.10.0 (see Wallet beat → go-sdk).
- **Your fulmine PR #413 landed — `Bump Go Docker images 1.26.2 → 1.26.3`** (`df00504`, 20h ago). First fulmine commit in 41 days, motivated by an arkd master toolchain bump (see Wallet beat → fulmine).
- **arkd `refactor-client-lib` branch pushed 9h ago: 138 files, +14k/-11k** — altafan splits `pkg/client-lib` into `client-wallet` + reusable handlers (batch-session, offchain-tx, unroll, redemption). Your `Settle()`/`Send()` go-sdk surface is downstream when this merges (see Protocol surface).
- **emulator (formerly introspector) shipped 2 DLEQ proof tests + arkade sighash options + BIP342 OP_CODESEPARATOR lock-in** — kukks's DLEQ-in-Arkade-Script test is the kind of capability dot you want to know exists. (see Contract surface).

## Wallet beat

### fulmine

- **`df00504` Bump Go Docker images from 1.26.2 to 1.26.3 (#413)** — yours, dusan, 20h ago, **merged to master**. First fulmine master commit since `3ea61e2 Bump go-sdk@2d035b64 (#401)` (2026-04-24) — **41-day drought broken.** Reason in the commit body: "arkd master now requires go >= 1.26.3, which breaks the CI Docker build when GOTOOLCHAIN=local is set." Touches `Dockerfile`, `arkd.Dockerfile`, `arkdwallet.Dockerfile`, `mockboltz.Dockerfile`. Co-authored with Claude Opus 4.6.
- A `bump-go-1.26.3` branch appeared on the remote (likely your branch before merge).
- Working tree still has only the uncommitted `.gitignore` edit.

### go-sdk

**Released v0.10.0** (`b117416`, altafan, 18h ago tag/commit). Master tip now `b117416 Update readme`. 17 PRs in this release; the headline items:

- **Your `336bacb Synchronize spend operations to prevent collisions (#183)`** — 16h ago, **closes the P1 auto-settle race that's been open 17 days** (mutex-guarded spend serialization, refactored tx handler, stop-on-lock; +1k LOC in `asset.go` / `batch_session.go` / `send.go` / `tx_handler.go` + `tx_handler_internal_test.go` 660 new lines). Co-authored with altafan.
- **Your `7a83350 Send/Receive/Restore load test (#185)`** — already covered yesterday.
- **`5f2ba69 Make contract manager idempotent for single-key identity (#187)`** (Bob Smith) — also in v0.10.0.

Per arkana's parity report, v0.10.0 ships: HD wallet (#143), contract manager (#145) + custom handlers registry (#186), auto settle (#169), KV store removed (#173, breaking simplification), Balance method update (#140), cursor-based pagination (#178), and your contract idempotency + spend-sync. **P1 auto-settle race: CLOSED.** Post-v0.10.0 on master (unreleased): the `vhtlc/` + `contract/handlers/vhtlc/` packages from `5705340` are still being polished.

**P0 #176 (ListKeys empty after contract creation) — NOT in v0.10.0.** Still unresolved.

**Caveat:** release-coordinator log dated `2026-06-03T04:00:00Z` still reports go-sdk as "HARD BLOCKED — no releases ever" and lists `#191 VHTLC/swap: 7 criticals (MuSig2 nonce reuse, fund loss)`. The "no releases ever" claim is stale (v0.10.0 is on origin and tagged), but the **#191 cryptography critique is real** and gates the next release — not v0.10.0 itself.

Working tree: uncommitted `.gitignore` + `.worktrees/` dir (unchanged).

### arkade-wallet (PWA)

Quiet. One merge in the last 24h:

- `00983717` don't check lnUrl conditions if ark address is present (#643) — João Bordalo, 34h ago. UX fix already covered yesterday.

No other master activity. No new dep bumps. No structural changes. Branch work continues (`643efd93` denigiri migration from Andrew Camilleri lingers on a non-master branch).

## Parity desk

Parity report dated 2026-06-02 (fresh, `project-context/sdk-parity.md`). Most prior gaps are now stale or resolved post-v0.10.0; arkana itself notes **"New formal parity gaps: NONE"** on the latest line.

**Resolved / narrowed (today's net effect):**

- ✅ **go-sdk release drought: RESOLVED.** New baseline v0.10.0 (was 60 days since v0.9.1).
- ✅ **go-sdk P1 auto-settle race: CLOSED** via your #183.
- 🟡 **go-sdk VHTLC** — post-v0.10.0 on master in the `vhtlc/` package, **not yet released**.
- 🟡 **dotnet-sdk `Wallet.restore()` gap: narrowed.** dotnet-sdk PR #104 (`569bfcf feat(recovery): unified IWalletRecoveryService`, Andrew Camilleri, 20h ago, **merged to master**) ships HD + SingleKey recovery facade, restores boltz swaps inline, returns a `WalletRecoveryReport`. `ServiceWorkerWallet.restore()` variant still has no dotnet-sdk equivalent.

**Recent sibling-SDK landings (last 24h) go-sdk should consider:**

- **dotnet-sdk #118 fix: close batch-topic subscription race + serialize concurrent submarine swap creation** (`265b74b`, kukks, 25h ago). **Cross-SDK quality alert from arkana:** "any SDK using a stream-based batch subscription model should audit for the same `_streamId=null` race on startup. Intent crossing into `WaitingForBatch` during the connect window was never subscribed → never received `BatchStartedEvent` → permanent settle stall." Audit go-sdk's batch subscription / stream-connect path for the same.
- **ts-sdk branch `fix/vtxo-spent-state-missing` — `122c33ea fix(indexer): include isSpent check in virtualStatus.state ternary`** (arkana[bot] AI-authored, 8h ago, NOT merged). `convertVtxo` was never setting `virtualStatus.state = "spent"`; spent VTXOs reported `settled`. Pre-emptive cross-SDK audit prompt for go-sdk's equivalent VTXO virtual-state derivation.

**Open gaps unchanged from yesterday:**

- **Arkade extension system** (ts-sdk v0.4.32) — go-sdk ❌ still missing `ArkadeVtxoScript`, `EmulatorPacket`, `RestEmulatorProvider`, etc.
- **Delegator → Delegate rename** (ts-sdk v0.4.29) — terminology drift in go-sdk.
- **`Arkade-Boltz-Preimage-v1`** deterministic preimage (dotnet-sdk only) — implementing in go-sdk would put it ahead of ts/rust.

**Pre-emptive audit checklist from the last 3 days of cross-SDK fixes (still actionable in go-sdk):**

1. Batch checkpoint signing — ts-sdk #535's three silent-corruption classes.
2. Batch subscription `streamId=null` race — dotnet-sdk #118.
3. `vtxo_spent` events with empty `newVtxos` short-circuit — ts-sdk #536.
4. VTXO `virtualStatus.state` `spent` classification — ts-sdk branch fix.

## Protocol surface — arkd → go-sdk

No merges to arkd `master` in the last 24h — master tip still `c4f1632 Add tx filters to indexer.GetSubscription (#1074)` (Bob Smith, 8 days). **Branch activity is significant**, all on `origin/refactor-client-lib`:

- **`9fe31fe1 Fixes`** (altafan, 9h ago) — touches `pkg/client-lib/batch-session/collaborative_exit.go`, `offchain-tx/args.go` (+131 lines), `offchain-tx/asset.go`, `offchain-tx/build.go`, `offchain-tx/send.go`, `client-wallet/asset.go`, `client-wallet/batch_session.go`, `client-wallet/send.go`. Moves a `redemption/redeem.go` → `unroll/branch.go`.
- **`90789d4e Reusable unroll methods`** (altafan, 9h ago) — adds `pkg/client-lib/unroll/{branch.go,complete.go,types.go,unroll.go,utils.go}` (+625 LOC), gutting `pkg/client-wallet/unroll.go` from 410 → 0 lines (deleted as inline implementation, replaced by a thin caller). Also adds `pkg/client-lib/go.mod` (+18 lines) and `go.sum` (+237) — **client-lib is now a separate Go module**, which is the source of the import-path breakage.

**Branch scope (vs master): 138 files, +14,022 / −10,922.** This is the change arkana's release-coordinator log refers to as PR #1078 ("Split client-lib and client-wallet"). Key restructure:

- `pkg/client-lib/asset.go` (-543), `batch_session.go` (-774), `batch_session_handler.go` (-922) deleted as monoliths.
- Reborn under `pkg/client-lib/batch-session/` as `batch_session.go`, `handler/{handler.go,types.go,utils.go}`, `intent.go`, `redeem_notes.go`, `settle.go`, `collaborative_exit.go`, `utils.go` — plus paired `_test.go` files.
- `pkg/client-lib/client/grpc/` flattens up to `pkg/client-lib/client/` (reconnect, types, the grpc internals).
- `pkg/client-lib/explorer/mempool/` flattens up to `pkg/client-lib/explorer/` (1040-line `mempool/explorer.go` deleted; lives in `service.go` now).
- New `pkg/client-wallet/` package with `wallet.go`, `unroll.go`, `wallet_opts.go`, `types/`, `store/{file,inmemory}/`, `identity/`, `utils.go`, `send.go`, `init.go`, etc. — the wallet layer is now physically separate from the protocol client.

**Impact on go-sdk:** when this merges, **every go-sdk import of `pkg/client-lib/*` will need to be rewritten**. The split of batch-session into a `handler` sub-package and the relocation of unroll/redemption helpers will affect go-sdk's `Settle()`, `CollaborativeExit()`, `Unroll()`, and `Send()` wrap paths. The new `client-lib/go.mod` means go-sdk pulls client-lib as a sub-module — your `go.mod` will need a corresponding bump (compare PR #184 from last week which was just a version bump). PR was marked CHANGES_REQUESTED in release-coordinator's snapshot; merge is not imminent. **Worth reading the diff now** so you're not surprised by the import churn.

## Contract surface — emulator (formerly introspector)

7 branch commits in the last 24h; nothing merged to master. Two distinct work streams:

**msinkec — OP_CODESEPARATOR + arkade sighash (on `feat/op-codeseperator` and `feat/compute-limits`):**

- **`ec74db0 add arkade sighash options parameter`** (msinkec, 8h ago) — `pkg/arkade/sigvalidate.go` +42 lines, `script_test.go` -6/+6, `test/signed_pay_to_output_test.go` minor. Extends the signature-validation path to take an explicit sighash options bag (likely for the CODESEPARATOR sighash interaction).
- **`01a5e3f lock in BIP342 OP_CODESEPARATOR position behaviour`** (msinkec, 10h ago) — 181 lines added to `pkg/arkade/script_test.go`; pure test lock-in. **The BIP342 semantics shift from yesterday's branch is now backstopped by tests.**
- **`dd4e214 unexport blank code sep value`** (msinkec, 22h ago) — cosmetic encapsulation cleanup before merge.
- **`f368921 address PR comments`** (msinkec, 23h ago) — README + compute-limits test re-org.
- **`67dbd8a add zero limit test`** (msinkec, 12h ago, on `feat/compute-limits`) — opcode count compute limit edge case.

Per release-coordinator: emulator #91 (compute limits) was **merged today at 06:53Z**; emulator #92 (OP_CODESEPARATOR) is APPROVED but still gated on human cryptographic sign-off. So compute-limits has landed; CODESEPARATOR is one approval away.

**kukks — DLEQ proofs in Arkade Script (on `test/dleq-proof`):**

- **`db1faec test: DLEQ proof for blinded-signature key equality`** (Andrew Camilleri, 17h ago) — 153 new lines in `pkg/arkade/dleq_test.go`. Self-contained Cashu/BDHKE-style Discrete Log Equality proof: Bob proves the same secret `a` links `A = a*G` and `C' = a*B'` without revealing `a`. Covers valid round-trip + 4 soundness cases (wrong A, wrong C', tampered s, tampered e). No production code touched — proof/verify helpers live in the test file.
- **`0a13dca test: verify DLEQ proof inside an Arkade Script`** (Andrew Camilleri, 15h ago) — 299 new lines in `pkg/arkade/dleq_script_test.go`. **End-to-end DLEQ verification entirely in Arkade Script.** Public instance (G, A, B', C') is baked into the locking script; proof (e, s) is supplied as witness. Script recomputes `R1 = s*G - e*A` and `R2 = s*B' - e*C'` with `OP_ECMUL`/`OP_ECADD` (using `(n-e)*P` for subtraction), reconstructs SEC1 compressed points from affine coordinates, derives the Fiat-Shamir challenge with `OP_SHA256`/`OP_BIN2NUM`/`OP_MOD`, and asserts it matches `e`. Tampered-s, tampered-e, and self-consistent-proof-for-different-secret all rejected.

  Per arkana, this is the kind of "PR opened, PROTOCOL-CRITICAL, human cryptographic review required" item — emulator #95 — that materially expands what's *possible* to express on top of Arkade Script: blind-signature schemes, Cashu-style ecash mint primitives, anything needing DLEQ for key-equality assertions. **Worth knowing this works** even if you're not building it.

Reminder: this repo moved to `arkade-os/emulator` upstream — the local clone still uses the old `ArkLabsHQ/introspector` remote and daily-update is still failing the fetch with the `would clobber existing tag v0.0.1` error from yesterday. The branch commits are visible only because they were explicitly fetched.

## Slack briefing

### Decisions and blockers (from arkana, last 7 days, dusan-scope only)

- **`Synchronize spend operations to prevent VTXO_ALREADY_SPENT races` MERGED** (2026-06-02) — yours; closes the 17-day P1 auto-settle race. Slack-monitor's #1 "Decisions" line today.
- **`Bump Go Docker images` MERGED** (2026-06-02) — yours (fulmine #413), driven by arkd master Go 1.26.3 toolchain bump.
- **`chore(regtest): bump denigiri pin`** (2026-06-02) — denigiri (in-house nigiri replacement) now actively used in regtest; the nigiri-removal cascade you're tracking is now operational.
- **`fix(e2e): close batch-topic subscription race + serialize concurrent submarine swap creation` MERGED** in dotnet-sdk #118 (kukks, 2026-06-02) — **cross-SDK quality alert** for go-sdk's batch subscription path (see Parity desk).
- **`go-sdk ci_unit: 3 failures` (2026-06-02)** — continuing instability. With v0.10.0 cut, may need a follow-up ci_unit pin.
- **`fulmine ci_integration: 1 failure` (2026-06-01)** — occasional, still flaky.
- **DLEQ-proof PR opened in emulator (#95)** (2026-06-02) — flagged PROTOCOL-CRITICAL by slack-monitor; gates emulator next-release window.
- **`feat: Nostr transport for arkd [WIP]` opened** (2026-06-02) — censorship-resistant round transport. WIP, but watch.
- **Sweepable rounds query** (@U0A32P2G6HK, #dev, 2026-05-28) — production sweep-monitoring concern, unchanged from yesterday.
- **Boltz failure-invoice testing gap** (@U0AEWD7LHTJ, #dev, 2026-05-28) — still unanswered; affects swap test infrastructure (your go-sdk swap_test additions).

### Research findings (from arkana, last 7 days)

- **`BIP-110 ReducedData` PRs to Bitcoin Core** (2026-06-02 research run) — temporary soft fork restricting on-chain data storage; 55% activation threshold; **256-byte stack element limit could theoretically constrain Ark scripts.** Research-monitor's view: "likely non-event" but flagged as worth watching for Arkade Script implications.
- **Regular signet reorgs (ajtowns proposal)** (2026-06-02) — would benefit Arkade reorg testing if adopted.
- **CTV signaling: 0%, ~15 weeks** — dead window. `OP_TEMPLATEHASH` (MCCV) gaining mindshare as the drop-in replacement.
- **MCCV v0.1.0 CTV vault released** (2026-05-28) — velocity-control pattern (timelock scales linearly with withdrawal amount). Applicable to large-VTXO redemption UX.
- **Optech #407 — Eclair v0.14.0 in production**: splicing + simple taproot channels + zero-fee commitments all live. First major LN impl with all three. Sets the Ark↔LN interop bar.
- **Optech #407 — BIP449 OP_TWEAKADD (draft)**: tapscript opcode `P + tG`. Track for VTXO key derivation simplification.

### Most important — expanded

**1. go-sdk v0.10.0 is out — the 60-day window is closed, the next one starts now.**

Tag `v0.10.0` (`b117416`) is on the remote as of 2026-06-02 18:17. The release bundles 17 PRs of work from 2026-04-04 onwards, including HD wallet (#143), contract manager (#145) + custom-handler registry (#186), auto settle (#169), KV store removal (#173), cursor-based pagination (#178), your spend-collision fix (#183), and your load-test (#185). **Why it matters:** the drought was being noted in every parity report and was approaching "narrative" status — that's now done. Two follow-ups: (a) arkana's release-coordinator log is **stale** — it still describes go-sdk as "HARD BLOCKED — no releases ever," which will mislead anyone reading the digest until it refreshes; (b) the post-v0.10.0 `vhtlc/` package on master is unreleased but is the **biggest single feature addition** for the next cut, and PR #191 (VHTLC/swap path) is the gate, with 7 criticals open per release-coordinator (MuSig2 nonce reuse + fund-loss class). That's the next-release work, and the clock has already restarted.

**2. arkd's `refactor-client-lib` is a 138-file restructure that will reach go-sdk on merge.**

altafan pushed `9fe31fe1 Fixes` and `90789d4e Reusable unroll methods` 9h ago. Combined branch state vs master: 138 files, +14,022 / −10,922 LOC. The shape of the change: `pkg/client-lib`'s monolithic `asset.go` / `batch_session.go` / `batch_session_handler.go` are deleted and re-emerge as a `batch-session/` sub-package with `handler/`, `intent.go`, `redeem_notes.go`, `settle.go`, `collaborative_exit.go`. The `client/grpc/` and `explorer/mempool/` layers flatten one directory up. A new `pkg/client-wallet/` package is born and absorbs the wallet-shaped code (wallet.go, store/, identity/, types/). Most consequentially, `pkg/client-lib/go.mod` appears — **client-lib becomes its own Go sub-module**. **Why it matters for you:** go-sdk imports `pkg/client-lib` extensively (`Settle()`, `CollaborativeExit()`, `Unroll()`, `Send()` all wrap it). When this lands, you'll need (a) a coordinated `bump client-lib` PR like #184 last week, but bigger; (b) import-path rewrites across go-sdk's surface; (c) likely a separate `go.mod` accommodation for the sub-module split. It's CHANGES_REQUESTED in release-coordinator's snapshot, so not imminent — but **the right time to read the branch diff is before it merges**, not after.

**3. DLEQ-in-Arkade-Script is a capability test — Cashu-class primitives are now expressible.**

kukks's `0a13dca` is a 299-line E2E test that verifies a Cashu/BDHKE-style Discrete Log Equality proof entirely inside an Arkade Script: public points baked into the locking script, proof `(e, s)` supplied as witness, script recomputes the Fiat-Shamir challenge with `OP_ECMUL`/`OP_ECADD`/`OP_SHA256`/`OP_BIN2NUM`/`OP_MOD` and asserts equality. Soundness cases — tampered `s`, tampered `e`, and a self-consistent proof for a different secret — are all rejected. **Why it matters:** DLEQ is the building block for blind-signature key consistency (Cashu mint signatures, blind Schnorr verification, anonymous credential schemes). Until this test landed it was unclear whether the Arkade Script opcode set was rich enough to verify a DLEQ proof on-stack; it now demonstrably is. Even if you're not building blind-sig primitives in fulmine/go-sdk this week, **knowing that this surface exists** changes what's plausible for future VHTLC/contract designs (e.g., privacy-preserving swap protocols, blinded Lightning AMP-style payments inside Ark). Worth a 10-minute read of `pkg/arkade/dleq_script_test.go` when it merges.

## TL;DR

- **Note the v0.10.0 release.** Verify your `#183` actually shipped in the release commit set (it did per the parity log; `336bacb` is included). Then start the post-v0.10.0 conversation: VHTLC release (`vhtlc/` package + PR #191's MuSig2 nonce-reuse fixes) is the next milestone.
- **Read the `refactor-client-lib` branch diff in arkd before it merges** — 138 files, new client-lib `go.mod`, batch-session re-org — your go-sdk wrap surface will need import rewrites and a coordinated bump.
- **Audit go-sdk's batch subscription / stream-connect path for the `streamId=null` race** that dotnet-sdk #118 just fixed (cross-SDK quality alert from arkana).
- **Optional: audit go-sdk's VTXO `virtualStatus` derivation** against the `isSpent`-classification fix on ts-sdk's `fix/vtxo-spent-state-missing` branch.
- **Skim the DLEQ-in-Arkade-Script test** (`pkg/arkade/dleq_script_test.go`, 299 LOC) once emulator #95 merges — capability map update for what's expressible on top of Arkade Script.
