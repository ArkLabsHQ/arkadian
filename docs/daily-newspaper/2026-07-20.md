# Ark Daily — 2026-07-20

> _Quiet day: zero commits landed in the trailing 24h across fulmine, go-sdk, arkade-wallet, arkd and introspector. The only recent motion is wallet's asset-ID display work (last push 07-18). Arkana's Slack/research feeds are stale (no refresh since ~06-19), so no fresh decisions to report._

## Front page

- **No new activity in dusan's repos.** fulmine and go-sdk have had no commits for weeks (fulmine HEAD `3ea61e2`, 04-24; go-sdk HEAD `7f6b8d0`, 05-25). Daily-update skipped both (uncommitted local changes) so the mirrors may lag GitHub — worth a manual `git fetch` if you expect PRs. (see Wallet beat)
- **arkade-wallet is the only moving repo**, and only up to 07-18: asset-ID surfacing in Virtual Coins + ts-sdk 0.4.47 bump. All display/UI, nothing protocol-facing. (see Wallet beat)
- **Intelligence feeds are stale.** Slack-log and research-monitor last updated ~06-19; the SDK parity report is from 04-03 (>14 days). Treat any Slack/parity content today as month-old — the pipeline that feeds them hasn't run. (see Slack briefing)

## Wallet beat

### fulmine
No activity. HEAD is `3ea61e2` "Bump go-sdk@2d035b64 (#401)" (04-24). Mirror was skipped by daily-update (uncommitted local changes) — if you're expecting recent PRs, fetch fresh before trusting this.

### go-sdk
No activity. HEAD is `7f6b8d0` "Contract registry: allow registring custom contract handlers (#186)" (05-25). Also skipped by daily-update (uncommitted local changes).

### arkade-wallet (PWA)
No commits in the last 24h. Latest landings (07-17→07-18), all cosmetic/display, not protocol:
- `e14de073` Show asset ID and link outpoints to Arkade explorer in Virtual Coins (#802) — Marco
- `637bbfdf` Show asset ID alongside ticker in Virtual Coins list (#801) — Marco
- `acc6e5ba` Restrict currency treatment to verified asset IDs (#790) — Marco
- `0c143004` Upgrade ts-sdk 0.4.47 / boltz-swap 0.3.52 (#792) — pietro909

## Slack briefing

Arkana's Slack and research logs have not refreshed since **2026-06-19** — roughly a month stale. There is no fresh decision, blocker, or research signal to surface for today. The pipeline that populates `slack-log.md` / `research-monitor.md` appears to have stopped; nothing new has entered dusan's scope through those channels.

Last-known open threads from the 06-19 snapshot (for reference only, likely resolved by now): go-sdk CI instability ahead of the June 22 signer rotation, and the `ARKD_SIGNER_ADDR` required-env-var rollout. If any of these are still live, they'd surface in GitHub directly rather than here.

## TL;DR

- Nothing actionable landed in your repos in the last 24h — genuinely quiet.
- If you expect recent fulmine/go-sdk PRs, `git fetch` the mirrors: daily-update skipped both for uncommitted local changes, so this edition reflects possibly-stale local state.
- Heads-up on tooling: the arkana Slack/research/parity feeds have gone stale (~06-19). If you rely on them, they need a poke before they're trustworthy again.
