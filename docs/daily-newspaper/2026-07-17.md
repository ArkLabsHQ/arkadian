# Ark Daily — 2026-07-17

> _Quiet in dusan's own repos (fulmine, go-sdk, arkd all silent), but introspector shipped multischeme CHECKSIG — ECDSA/P-256 now usable in Arkade Script — and cut v0.0.5. ts-sdk landed submarine-refund deferral worth mirroring._

## Front page

- **introspector: multischeme CHECKSIG landed on master** — `OP_CHECKSIG` family and `OP_CHECKSIGFROMSTACK` now accept ECDSA/secp256k1 and ECDSA/secp256r1 (P-256) keys alongside Schnorr. New contract semantics for Arkade Script authors. `1359823` (Mihael Šinkec) (see Contract surface).
- **introspector cut tag v0.0.5** — new release tag appeared this cycle; worth checking what fulmine/go-sdk pin against if they consume the introspector engine (see Contract surface).
- **ts-sdk deferred boltz submarine refunds instead of dropping them** — CLTV-immature refunds are now persisted and retried (`bd1241bb`, `af56f6a8`, `e17d438e`). fulmine's submarine-swap refund path should consider the same behaviour (see Slack briefing → parity note).
- **fulmine, go-sdk, arkd: no commits in 24h** — dusan's primary surface is quiet today (see Wallet beat).
- **Heads-up: Slack + research feeds are stale** — newest Slack extract is 2026-06-19, newest research log 2026-06-18; nothing fresh in the last 7 days (see Slack briefing).

## Wallet beat

### fulmine
No activity in last 24h. (Repo skipped by daily-update — uncommitted local changes — but `git log --since=1.day` confirms no new commits.)

### go-sdk
No activity in last 24h. (Same: skipped for uncommitted local changes; no new commits.)

### arkade-wallet (PWA)
Two infra-only commits — nothing product-facing:
- `82403d36` fix: copy `pnpm-workspace.yaml` in Docker build (#787) (pietro909)
- `910c7cca` fix: patch dependabot alerts in vitest, vite, js-yaml, picomatch, babel/core (#783) (pietro909)

Purely build/dependency hygiene — no wallet feature or UI change.

## Contract surface — introspector

- **`1359823` feat: multischeme checksig support (#107)** (Mihael Šinkec) — wires a scheme layer into the `OP_CHECKSIG` family and extends `OP_CHECKSIGFROMSTACK` to ECDSA/secp256k1 and ECDSA/secp256r1 (P-256), in addition to existing Schnorr/secp256k1. Adds scheme-prefixed pubkeys (e.g. `0x11||compressed-P256`), low-s enforcement and canonical-scalar checks on both curves, plus a scheme-agnostic sig/hashtype splitter. Touches `pkg/arkade/opcode.go`, `sigscheme.go`, `sigvalidate.go`. **Semantics change**: contracts can now verify ECDSA and P-256 signatures natively — relevant to any Arkade Script closure design.
- **Tag `v0.0.5` cut** — new introspector release tag observed this cycle.
- `6a91381` Backoff retry on arkd connect (#112) (louisinger) — resilience for the arkd client connection; routine.
- Emulator cleanup on side branch (`f4ab032`, `4a184c3`, `f62dcc9`, Bob Smith) — Close/leak fixes, arkd-client close on error paths, restored parseIntent test coverage. Housekeeping, no semantic change.

_Note: introspector fetch failed in daily-update (tag clobber on `v0.0.1`); the above is read directly from `origin/master` / branch refs._

## Slack briefing

_The Slack extract (`slack-log.md`) has not updated since **2026-06-19** and the research-monitor log since **2026-06-18** — both ~4 weeks stale. Nothing falls in the last 7 days, so no fresh decisions/blockers/findings to report. Feed staleness itself is worth flagging to whoever runs the monitors._

### Parity note (from today's changelog, dusan-scope)
- **ts-sdk 0.4.46** landed CLTV submarine-refund deferral: `bd1241bb feat: defer boltz refunds rejected as CLTV-immature`, `af56f6a8 fix: retry deferred submarine refunds instead of dropping them`, `e17d438e Persist deferred refund retries`. If fulmine's Boltz submarine-swap refund logic currently drops CLTV-immature refunds, this is the pattern to mirror for parity/robustness.

## TL;DR

- **Read the introspector multischeme CHECKSIG PR (#107, `1359823`)** — decide whether ECDSA/P-256 signature verification changes any Arkade Script closures you're designing in fulmine/go-sdk.
- **Check what introspector version fulmine/go-sdk pin** now that **v0.0.5** is tagged — pull it in if you want the new CHECKSIG schemes.
- **Consider mirroring ts-sdk's deferred submarine-refund behaviour** in fulmine — persist + retry CLTV-immature Boltz refunds rather than dropping them.
- **Ping whoever runs the Slack/research monitors** — the feeds are ~4 weeks stale (last 2026-06-19 / 06-18); the newspaper's Slack section is flying blind until they refresh.
