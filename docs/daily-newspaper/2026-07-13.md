# Ark Daily — 2026-07-13

> _Quiet day, stale feeds: no commits landed in any priority repo in the last 24h, and the Slack/research pipelines haven't refreshed since ~June 20 — this edition runs on last-known state, clearly dated._

## Front page

- **Data pipelines are stale.** Zero pulled commits across all 27 repos today (daily-changelog); fulmine/go-sdk were skipped on pull due to uncommitted local changes. Slack log and research-monitor both frozen at ~June 18–20. Treat everything below as last-known, not fresh. (see Wallet beat)
- **go-sdk CI was flagged persistently unstable** (6+ integration + 3 unit failures) in the run-up to the June 22 signer rotation — worth a quick confirm that it has since stabilized. (see Slack briefing)
- **`settle()` semantics changed under go-sdk:** `feat(ark-client): narrow settle() to expired/recoverable VTXOs` merged June 17 — only expired/recoverable VTXOs now settle, preventing premature settlement of valid ones. If your fulmine flows call Settle(), verify the narrower behavior is what you expect. (see Slack briefing)
- **Signer rotation (June 22) and cutoff (July 4) are both now in the past.** Confirm fulmine/go-sdk are pinned to the current signer and that deprecated-signer VTXOs were migrated — the operational script (`Migrate deprecated-signer VTXOs onto current signer`) was still a PR as of mid-June. (see Slack briefing)

## Wallet beat

### fulmine
No activity in last 24h. Repo clone frozen at `3ea61e2` (2026-04-24, "Bump go-sdk@2d035b64" #401) and was skipped on today's pull (uncommitted local changes) — local clone may be behind origin.

### go-sdk
No activity in last 24h. Repo clone frozen at `7f6b8d0` (2026-05-25, "Contract registry: allow registring custom contract handlers" #186) and skipped on pull (uncommitted local changes) — clone likely behind origin.

### arkade-wallet (PWA)
No activity in last 24h. Newest commit is `590cff5d` (2026-07-10, "fix unlock splash" #752) — 3 days old, cosmetic/UX. Nothing landed today.

## Slack briefing

> ⚠️ _Source stale: newest slack-log entry is 2026-06-19, ~3.5 weeks old. Items below are last-known state for dusan's scope, dated at source — not today's news._

### Decisions and blockers (from arkana)
- **go-sdk CI instability (blocker, June 17–19):** 6+ `ci_integration` + 3 `ci_unit` failures, called out as the most critical CI risk ahead of the June 22 rotation. Status since then unknown (feed stale).
- **`feat(ark-client): narrow settle() to expired/recoverable VTXOs` MERGED (June 17):** settle() now only touches expired/recoverable VTXOs.
- **`client-lib: support deprecated signer verification` MERGED (June 17):** client-side verification of deprecated-signer signatures — relevant to the rotation transition window.
- **`forward and export client.SendOption alias` PR opened (June 17):** minor go-sdk client surface addition.
- **`Sync default explorer URLs with ts-sdk and dotnet-sdk` PR opened (June 17):** SDK parity item for default explorer URLs.
- **Signer rotation timeline (confirmed June 17):** rotation June 22, cutoff July 4; `ARKD_SIGNER_ADDR` env var became required via the standalone signer extraction. Both dates now past.

### Research findings (from arkana)
- **LND zero-timestamp gossip DoS (Delving Bitcoin, June 18):** crafted `channel_update`/`node_announcement` gossip crashes LND nodes <v0.20.1. ASPs / LN-connected infra should confirm they're on v0.20.1+. Relevant to fulmine's Lightning swap path.
- **Botanix Spiderchain shutdown (June 10):** Bitcoin L2 wound down after a year — tech worked, adoption didn't (users preferred wrapped BTC). Cautionary datapoint for Ark's go-to-market.
- **CTV:** still 0% signaling, ~18 weeks in — no movement on covenant activation.

### Most important — expanded
- **go-sdk `settle()` narrowing (June 17 merge).** `feat(ark-client): narrow settle() to expired/recoverable VTXOs` changed Settle() to only act on expired and recoverable VTXOs rather than all VTXOs, explicitly to stop premature settlement of otherwise-valid VTXOs. This is a behavioral change in the exact wrap surface fulmine depends on — if any fulmine settlement flow assumed the old "settle everything" semantics, it now behaves differently. Worth confirming against your current go-sdk pin (the local clone is at May 25, so likely already includes it).
- **go-sdk CI instability ahead of rotation (June 17–19, arkana Slack monitor).** go-sdk carried 6+ integration and 3 unit-test failures right as the June 22 signer rotation approached, repeatedly flagged as the top CI risk. Since the feed went stale on June 20 there's no confirmation it was resolved — as maintainer, dusan should check whether main is green now, especially given the rotation and July 4 cutoff have both since passed.
- **LND gossip DoS <v0.20.1 (Delving Bitcoin, June 18, via arkana research-monitor).** A zero-timestamp gossip message can crash LND nodes older than v0.20.1. Fulmine's submarine/reverse swap paths ride Lightning, so any connected LND (yours or Boltz-side) should be verified at v0.20.1+. Low effort to check, high blast radius if ignored.

## TL;DR

- **Confirm the data feeds are actually stale, not broken:** fulmine & go-sdk clones have uncommitted local changes blocking pulls, and Slack/research logs stopped updating ~June 20. If you expect fresh activity, the pipeline needs attention before tomorrow's edition.
- **Verify go-sdk main is green** — CI was unstable (6+ int / 3 unit) as of the last signal; the rotation it was blocking is now behind us.
- **Confirm rotation cleanup is done:** current-signer pin in fulmine/go-sdk + deprecated-signer VTXO migration (June 22 rotation / July 4 cutoff both past).
- **Check any LND you or Boltz touch is ≥ v0.20.1** (gossip DoS fix) if fulmine's Lightning swap paths are live.
- Otherwise nothing landed today — genuinely quiet.
