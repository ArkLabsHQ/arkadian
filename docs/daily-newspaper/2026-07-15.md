# Ark Daily — 2026-07-15

> _Quiet day: dusan's repos (fulmine, go-sdk) were silent, the wallet PWA saw only cosmetic polish, and arkd shipped nothing new. The one carry-over is still the go-sdk proto regen for arkd's `depth`/marker-DAG change. Heads-up: the Slack & research monitor feeds haven't updated since June 19._

## Front page

- **fulmine and go-sdk both had zero commits** in the last 24h — nothing but a stray `.gitignore` tweak in each working tree. (see Wallet beat)
- **Carry-over from yesterday**: arkd's "Scale the DAG" (#908) added a `depth` field + marker-DAG to `indexer.proto`/`types.proto`; go-sdk still needs the additive proto regen to surface `depth`. No new arkd changes today. (see TL;DR)
- **arkade-wallet PWA activity was purely cosmetic** — receipt polish, a chart hover-label fix, a flaky-test fix, and a regtest submodule bump. Nothing functional. (see Wallet beat)
- **arkd v0.9.14 was released via ark-infra** (#114), alongside an ECS + NBXplorer-on-EC2 stand-up (#111) — worth noting which arkd go-sdk now targets in deployed envs. (see TL;DR)
- **Stale-feed warning**: arkana's Slack log (last entry Jun 19) and research-monitor (Jun 18) are ~26 days old — the ingestion pipeline may have stopped. Worth a poke. (see TL;DR)

## Wallet beat

### fulmine
No activity. (Only an uncommitted `.gitignore` edit in the working tree.)

### go-sdk
No activity. (Only an uncommitted `.gitignore` edit in the working tree.)

### arkade-wallet (PWA)
Cosmetic/routine only — no functional changes:
- `e414ce52` Polish transaction detail receipt (#726, Sahil Chaturvedi)
- `7630bc4a` Fix bitcoin chart hover label (#766, Sahil Chaturvedi)
- `7df5ab4a` fix flaky send-onchain test (#780, João Bordalo)
- `ee30182e` update regtest commit / submodule bump (#779, João Bordalo)

## TL;DR

- **go-sdk proto regen (carry-over)**: pull arkd's additive `depth`/marker-DAG proto change (#908, `eb75b6ba`) into go-sdk bindings when convenient — skipping it means go-sdk silently drops the new per-vtxo `depth` data. Nothing breaks on the wire (additive), so not urgent.
- **Note the deployed arkd version**: ark-infra cut `arkd v0.9.14` (#114) — confirm go-sdk integration tests target it.
- **Check the arkana feeds**: Slack log and research-monitor haven't updated since ~Jun 19. If you rely on the daily briefing, the upstream cron may be stuck — worth asking whether it's intentional.
- Otherwise: **nothing actionable — a genuinely quiet day** for fulmine, go-sdk, and the wallet stack.
