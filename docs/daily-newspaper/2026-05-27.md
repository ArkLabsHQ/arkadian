# Ark Daily — 2026-05-27

> _Your primary repos sat still, but the surfaces under them moved: arkd shipped a **breaking** indexer-subscription reshape (#1074) alongside your own pagination fix (#1081), introspector became **emulator** and merged a wave of new Arkade Script opcodes (OP_ECADD/ECMUL/ECPAIRING, OP_MODEXP, OP_REVERSEBYTES, OP_SIGHASH), and a #dev restructure proposes moving the HTLC claimer **into fulmine**._

## Front page

- **arkd `#1074` reshapes the indexer subscription proto — breaking** (`c4f16324`, Bob Smith). `ScriptsFilter`/`ModifyScripts`/`OverwriteScripts`/`ScriptsFilterResult` are removed; replaced by CEL `expressions` + `ScriptFilter{add,remove}`; `UpdateSubscriptionResponse` is now empty. go-sdk code on the old types won't compile once client-lib catches up (see **Protocol surface**).
- **Your own arkd `#1081` landed** (`4473e23c`, Dusan Sekulic) — client-lib indexer auto-pagination fix (1-based page index, terminate on `Current >= Total`) + chunked large `GetVtxos` filters. Directly fixes go-sdk indexer paging (see **Protocol surface**).
- **introspector → emulator, plus 4 new opcodes merged in the last ~20h** — OP_ECADD/ECMUL/ECPAIRING (#79), OP_MODEXP (#80), OP_REVERSEBYTES (#84), and the protocol-critical OP_SIGHASH (#88). Repo renamed to `github.com/arkade-os/emulator` (see **Contract surface**).
- **HTLC claimer is proposed to move INTO fulmine** as a standalone `htlc-claimer`/`covenant-claimer` binary (bancod → arkade-solver restructure, FINAL CALL in #dev). New responsibility headed for your repo (see **Slack → Most important**).
- **go-sdk Contract Registry is now on `origin/master`** — `#186` custom contract handlers + `#187` idempotent contract manager. Yesterday these were branch-only; the limbo is partly resolved (see **Wallet beat → go-sdk**).

## Wallet beat

### fulmine
Master idle — still `3ea61e2` (Bump go-sdk, 2026-04-24, ~one month). Active branch work on `offline-ln-receive` (louisinger, 2026-05-26):
- `744e1d0` fix refund
- `f398081` apply rabbit suggestions
- `7fad730` lint

Offline Lightning-receive + refund handling in progress. Incoming: the #dev proposal to fold the HTLC claimer into fulmine (see Slack). Local working copy still uncommitted → daily-update skipped the pull again.

### go-sdk
Local master lags (`625a2e2`, 2026-05-13), but `origin/master` advanced from `#184` to `7f6b8d0`:
- `7f6b8d0` **Contract registry: allow registering custom contract handlers (#186)** (Pietralberto Mazza, 2026-05-25)
- `5f2ba69` **Make contract manager idempotent for single-key identity (#187)** (Bob Smith, 2026-05-25)

These were on `origin/contract-registry` in yesterday's edition — now on master. Standing items unchanged: **P0 #176** (ListKeys empty after contract creation), **P1 auto-settle race**, **#172** day 25 stalled, **54 days since v0.9.1** (2026-04-03) with no release candidate.

### arkade-wallet (PWA)
No activity in last 24h (`$WALLET_REPO` fetch clean). Still at `#631` (ts-sdk 0.4.28 + boltz-swap 0.3.33 upgrade) from yesterday's edition.

## Parity desk

Source: `memory/project-context/sdk-parity.md` dated **2026-05-25** (within the 14-day gate). **No new gaps in 24h.** Reference versions: ts-sdk **v0.4.28**, rust-sdk **v0.9.0**, go-sdk **v0.9.1** (54 days no release).

**Gaps where go-sdk lags** (carried over, unchanged from yesterday):
- **`Wallet.restore()`** (HD gap-scan recovery, user-facing static factory) — ts-sdk v0.4.28 has it; dotnet-sdk has it (PR #77 defects); go-sdk: ❌. _Parity 2026-05-22._
- **`ElectrumOnchainProvider`** — ts-sdk full provider; dotnet-sdk config-level defaults (PR #96); go-sdk: ❌. _Parity 2026-05-20._
- **Per-call `vtxoThreshold` on `renewVtxos`** — ts-sdk `options.thresholdSeconds` (`8c194aec`, unreleased); go-sdk: no per-call threshold. _Parity 2026-05-22._
- **Narrowed `settle()`** — rust-sdk v0.9.0 narrowed `settle()` to expired/recoverable + renamed full-renewal to `settle_all()`. go-sdk unchanged. **Divergence, not a missing feature.** _Parity 2026-05-22._

**Recent sibling-SDK landings:** the headline this window is go-sdk's own **Contract Registry merge to master** (`#186`/`#187`) — go-sdk's custom-handler + collision-detection model remains the most complete contract extensibility surface across the four SDKs (rust-sdk still has no equivalent).

## Protocol surface — arkd → go-sdk

- **`c4f16324` — Add tx filters to `indexer.GetSubscription` (#1074)** (Bob Smith). Reshapes the subscription filter surface: removes `ScriptsFilter`/`ModifyScripts`/`OverwriteScripts`/`ScriptsFilterResult`; `SubscriptionFilter` is now `repeated string expressions` + `ScriptFilter{add,remove}`; `UpdateSubscriptionResponse` is now empty; new `txfilter` CEL package; `UpdateSubscription` is now atomic.
  - **Impact on go-sdk**: code referencing the old indexer filter types will not compile against the regenerated `indexer.proto`; migrate to `expressions` + `ScriptFilter`. **Not forced yet** — the PR reverted client-lib stream wiring out to branch `bob/client-lib-stream-changes`, so go-sdk isn't required to migrate until client-lib lands those changes. Plan for it.
- **`4473e23c` — client-lib: fix indexer auto-pagination + chunk large vtxo filters (#1081)** (Dusan Sekulic). Page index now 1-based; loop terminates on `page.Current >= page.Total` (was `Next >= Total`); large `GetVtxos` script/outpoint filters chunked by `maxPageSize`.
  - **Impact on go-sdk**: directly corrects go-sdk's indexer paging behavior once go-sdk bumps `pkg/client-lib`. Your fix.

## Contract surface — introspector (now emulator)

All merged to master in the last ~20h on the renamed `arkade-os/emulator` repo (was `ArkLabsHQ/introspector`):

- **`bb5dd3c` — OP_ECADD / OP_ECMUL / OP_ECPAIRING (#79)** (Mihael Šinkec). Elliptic-curve point addition, scalar multiplication, and pairing opcodes — enables pairing-based crypto in Arkade Script contracts.
- **`9423f79` — OP_MODEXP (#80)** (Mihael Šinkec). Modular exponentiation opcode (operand size limited). Opens RSA-style / number-theoretic constructions.
- **`1a14f58` — OP_REVERSEBYTES (#84)** (Mihael Šinkec). Byte-order reversal opcode.
- **`32fdb1b` — Update sighash template + add OP_SIGHASH (#88)** (Mihael Šinkec). **Protocol-critical**: new OP_SIGHASH opcode plus a sighash-template change for introspector signatures (canonical sighash mask). Flagged in #dev as requiring human sign-off.
- **`aa86d59` / `2fc8cad` — Rename introspector → emulator (#90)** (Chris Ricketts / louisinger). Module path is now `github.com/arkade-os/emulator`. Update any imports and tooling references.

## Slack briefing

### Decisions and blockers (from arkana)

- **bancod → arkade-solver / banco → arkade-intents restructure** (FINAL CALL, #dev). U07MSN1PNTU proposed moving the **HTLC claim code from bancod into fulmine** as a standalone `htlc-claimer`/`covenant-claimer` binary (only Boltz uses it); U07ML5CM486 (OpenIntentsFramework) added for alignment; huddle likely. _(Expanded below.)_
- **getVtxos() / getBalance() are O(total lifetime VTXO history)** — PERFORMANCE CRITICAL (#dev). Both core wallet APIs scan full VTXO history on every call; seconds per call on long-lived wallets; a production-adoption wall. Needs an O(spendable set) rewrite. Hits the go-sdk + wallet stack directly.
- **introspector → emulator live**; mutinynet emulator at `emulator.mutinynet.arkade.sh`. OP_SIGHASH + opcode wave merged (see Contract surface).
- **"Enforce canonical encoding for Arkade payloads" PR opened** — deterministic serialization across implementations; relevant to contract/SDK encoding.
- **boltz-swap deferred ARK chain refunds lost across restart** — CRITICAL, still OPEN (money-at-risk, opened 2026-05-25). fulmine wraps boltz-swap, so fulmine restarts can shed pending refunds. _(Carried from yesterday.)_
- **go-sdk `ci_unit` failure** recurring.

### Research findings (from arkana)

_(research-monitor last ran 2026-05-25; Mon/Wed/Fri cadence.)_
- **Chimera TGE is today (2026-05-27)** — 3-layer architecture (mainchain + Lightning/Boltz + **Arkade VTXOs**), PWA-based. Live competitive reference for the arkade-wallet stack; still no public confirmation of which SDK + version they ship.
- **Onion message jamming** (Delving Bitcoin, active May 23–25) — practical LN concern, 4 mitigation proposals; affects BOLT12 reliability and thus fulmine's Boltz/LN interop.
- **Ibis Wallet** (Optech #406) — second third-party wallet listing optional Ark support after Chimera; validates the third-party-platform thesis.
- **Bitcoin Core 31.0 live since 2026-04-19** — cluster mempool, V3/TRUC policy, sibling-eviction RBF all on mainnet; affects fulmine/arkd forfeit + connector-tx broadcasting. _(Carried from yesterday.)_
- **PQ discourse accelerating** (5 threads in 8 days) — tracking, not actionable.

### Most important — expanded

**1. A wave of new Arkade Script opcodes merged on the renamed emulator repo.**
In the last ~20 hours, Mihael Šinkec's OP_ECADD/OP_ECMUL/OP_ECPAIRING (#79), OP_MODEXP (#80), OP_REVERSEBYTES (#84), and OP_SIGHASH (#88) all landed on master, and the repo was renamed introspector → `arkade-os/emulator`. The EC/pairing and modexp opcodes meaningfully widen what Arkade Script contracts can express (pairing-based and number-theoretic constructions); OP_SIGHASH is protocol-critical because it changes how signatures commit to the transaction, and was explicitly flagged in #dev as needing human sign-off. For dusan: any contract work built on Arkade Script should be re-baselined against the new opcode set, and import paths/tooling pointing at `ArkLabsHQ/introspector` must move to `github.com/arkade-os/emulator`.

**2. The HTLC claimer is proposed to move into fulmine.**
In the bancod → arkade-solver / banco → arkade-intents restructuring (a FINAL CALL discussion in #dev), U07MSN1PNTU proposed pulling the HTLC claim code out of bancod and into fulmine as a standalone `htlc-claimer`/`covenant-claimer` binary, reasoning that only Boltz consumes it. U07ML5CM486 (OpenIntentsFramework) was brought in for alignment and a huddle is likely. This would land a new component directly in dusan's repo — fulmine would own the HTLC/covenant claim path that currently lives in bancod. Worth joining that huddle to scope the interface and ownership boundary before the decision is made without you.

**3. arkd #1074 is a breaking indexer-subscription change for go-sdk's wrap surface.**
Bob Smith's #1074 (`c4f16324`) removes the `ScriptsFilter`/`ModifyScripts`/`OverwriteScripts`/`ScriptsFilterResult` types and replaces them with CEL `expressions` + `ScriptFilter{add,remove}`, makes `UpdateSubscriptionResponse` empty, and adds a new `txfilter` CEL package. go-sdk wraps arkd's client-lib, so any go-sdk code on the old indexer types will fail to compile against the regenerated `indexer.proto`. The reprieve: the client-lib stream wiring was reverted to branch `bob/client-lib-stream-changes`, so go-sdk isn't forced to migrate yet — and dusan's own pagination fix (#1081) already landed alongside it.

## TL;DR

- **Plan the go-sdk indexer migration for arkd #1074** — old `ScriptsFilter` types are gone; not urgent (client-lib wiring sits on a branch) but it's coming.
- **Get into the bancod→fulmine HTLC-claimer huddle** and scope the `htlc-claimer` interface/ownership before fulmine inherits it by default.
- **Re-baseline Arkade Script work against the new emulator opcodes** (OP_SIGHASH especially — protocol-critical, needs sign-off) and repoint imports to `github.com/arkade-os/emulator`.
- **Decide go-sdk release cadence** — Contract Registry (#186/#187) is now on master; 54 days since v0.9.1. Cut a release to stop the parity gap widening.
- **Flag the getVtxos/getBalance O(n) performance issue** — it'll bite the wallet stack on long-lived wallets; needs an O(spendable set) rewrite before broader adoption.
