# Ark Daily — 2026-06-27

> _Quiet across your stack: fulmine/go-sdk had no pulled commits (local trees dirty, fetch skipped), wallet shipped one cosmetic fix, and ts-sdk landed two parity-worth changes. Slack/research feeds are ~8 days stale and predate the June 22 signer rotation — worth confirming that landed cleanly._

## Front page

- **fulmine & go-sdk: no new commits in 24h** — daily-update SKIPPED both because the local repos have uncommitted changes, so it never fetched. Your working tree may be ahead of/behind origin; nothing was pulled. (see Wallet beat)
- **ts-sdk landed two changes go-sdk should weigh**: BIP-322 `PSBT_GLOBAL_GENERIC_SIGNED_MESSAGE` (0x09) on intents (#578) and a phantom-receive inflation fix for boarding sweeps (#587). Parity candidates. (see TL;DR)
- **arkd cut channelz introspection** behind `ARKD_ENABLE_CHANNELZ` (#1127, `5fae2026`) — internal gRPC server wiring only, zero go-sdk/proto impact, but could help inspect per-stream state during INC-1 bidi-streaming work. (see Protocol surface)
- **Intel feeds are stale**: newest Slack extract is 2026-06-19, newest research run 2026-06-18 — both predate the **June 22 signer rotation**. Confirm `ARKD_SIGNER_ADDR` rollout + VTXO migration completed. (see Slack briefing)

## Wallet beat

### fulmine
No new commits pulled in last 24h. daily-update reported **SKIPPED: uncommitted local changes** — the fetch never ran, so this reflects your local tree, not origin. If you expect remote activity, commit/stash and re-pull.

### go-sdk
Same as fulmine — **SKIPPED: uncommitted local changes**, no fetch. No local commits in the window.

### arkade-wallet (PWA)
One commit, purely cosmetic UX: `074c5244` **fix(receive): show 'Edit amount' after setting an asset amount** (#697, Dunsin Abisuwa). No protocol or SDK surface touched.

## Protocol surface — arkd → go-sdk

One arkd commit, **no impact on your wrap surface**:
- Expose gRPC channelz introspection on the admin port via `ARKD_ENABLE_CHANNELZ` (default `false`) — `5fae2026` (Andrew Camilleri, #1127).
- **Impact on go-sdk**: none. Confined to `internal/interface/grpc` + `internal/config`; no `pkg/client/`, `api/`, or `proto/` changes. Opt-in, admin-port only.

## Slack briefing

> _Source caveat: latest Slack extract is 2026-06-19 (8 days old) and predates the June 22 rotation. Treat as last-known-state, not current._

### Decisions and blockers (from arkana)
- **June 22 signer rotation** was the dominant thread (cutoff July 4). Required `ARKD_SIGNER_ADDR` env var + standalone arkd-signer service before deploy; `Migrate deprecated-signer VTXOs onto current signer` was the operational script. As of June 19 this was imminent — **verify it executed cleanly** (#dev, @U0854KRKV3Q coordinating).
- **go-sdk CI instability** flagged repeatedly through June 17–19: 6+ ci_integration + 3 ci_unit failures, called "the most critical CI risk" heading into rotation. Check whether it stabilised.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** — PROTOCOL-CRITICAL rotation-correctness fix, open as of June 19; flagged for urgent review.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question** — should settlement trigger when *one* VTXO is near-expiry vs the current all-VTXOs gate? Affects settlement aggressiveness/latency; relevant to go-sdk's `Settle()` semantics. Still open.

### Research findings (from arkana)
- **LND zero-timestamp gossip DoS** (Delving Bitcoin, June 18): crafted gossip crashes LND nodes < v0.20.1. Patched upstream; relevant if any Ark/Boltz path touches an LND node — confirm versions. (Boltz/Lightning interop)
- **Transaction privacy survey** (nkaretnikov, June 18): new coinjoin/payjoin/Silent Payments survey that may bear on Ark privacy posture. Trend-level.
- **CTV** still 0% signaling, ~18 weeks in — no movement affecting covenant-dependent designs.

### Most important — expanded
- **June 22 signer rotation — did it land?** The entire June 17–19 Slack window was rotation prep: standalone signer extraction (`ARKD_SIGNER_ADDR` now required), per-leaf signer-key selection fix, and the deprecated→current VTXO migration script. @U0854KRKV3Q postponed to June 22 for staffing ("now or never" on testing). Because your feed cuts off June 19, you have **no record of the outcome** — this is the single most important thing to confirm today, since go-sdk wraps the client surface that signs against these keys.
- **go-sdk CI was the top rotation risk.** Repeated reports (June 17/18/19) of 6+ ci_integration + 3 ci_unit failures, explicitly called out as the most critical instability before rotation. If it's still red, that directly blocks confident go-sdk releases — worth a look at the Actions tab first thing.

## TL;DR

- **Confirm the June 22 signer rotation succeeded** — check `ARKD_SIGNER_ADDR` deploy + the deprecated→current VTXO migration; your intel feed predates it.
- **Re-sync fulmine and go-sdk locally** — both were skipped for uncommitted changes, so you're flying blind on origin. Commit/stash, then `git pull`.
- **Triage go-sdk CI** — was unstable (6+ integration failures) heading into rotation; verify it recovered.
- **Evaluate ts-sdk #578 (BIP-322 intent signed-message) and #587 (boarding-sweep phantom-receive fix) for go-sdk parity** — both are SDK-surface changes siblings now have.
- **Eyeball the open `SETTLEMENT_MIN_EXPIRY_GAP` decision** — it changes when `Settle()` should fire; worth a position before it's decided.
