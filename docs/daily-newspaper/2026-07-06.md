# Ark Daily — 2026-07-06

> _Quiet day: no commits in fulmine, go-sdk, arkade-wallet or introspector; arkd landed one isolated fee-estimation fallback with zero go-sdk impact. Note: both of dusan's repos are blocked from syncing by a local `.gitignore` edit — the real headline._

## Front page

- **fulmine and go-sdk are not syncing** — daily-update SKIPPED both because of an uncommitted `M .gitignore` in each working tree; new remote commits won't appear here until that's cleared (see TL;DR).
- **arkd #1089** landed a fee-estimation fallback (`minRelayTxFee`) — isolated to `pkg/arkd-wallet/` internals, **no impact on go-sdk's wrap surface** (see Protocol surface).
- **arkade-wallet quiet in last 24h** — last landings were 5–6 days ago (HD address rotation #682, deprecated-key delegate #708, ts-sdk 0.4.41 bump #709) (see Wallet beat).
- **Knowledge base is stale** — newest Slack/research/parity extracts are from 2026-06-19/20 (~17 days old), so Parity desk and Slack briefing are skipped today (see TL;DR).

## Wallet beat

### fulmine
No commits in last 24h. ⚠️ Repo was SKIPPED by daily-update — uncommitted `M .gitignore` blocks `git pull`, so remote activity (if any) is not reflected.

### go-sdk
No commits in last 24h. ⚠️ Same as fulmine — SKIPPED by daily-update over an uncommitted `M .gitignore`.

### arkade-wallet (PWA)
No commits in last 24h. Most recent landings (5–6 days ago, for context):
- `37eff0a5` feat: HD address rotation, dev-mode selectors, restore recovery (#682, pietro909)
- `341bf560` Allow deprecated keys delegate (#708, João Bordalo)
- `4ead848e` Upgrade ts-sdk 0.4.41 / boltz-swap 0.3.46 (#709, pietro909)

## Protocol surface — arkd → go-sdk

- **Fee-estimation fallback** — arkd-wallet now passes `fallbackFeeRate=minRelayTxFee` to nbxplorer's fee endpoint so estimation doesn't error when nbxplorer can't return a rate. `0718d54b` (Se7enZ, #1089).
  - **Impact on go-sdk:** None. Single-file change in `pkg/arkd-wallet/core/infrastructure/nbxplorer/service.go`; no `pkg/client/`, `api/`, `proto/`, or gRPC interface touched.

## TL;DR

- **Fix the sync blocker:** clear the uncommitted `M .gitignore` in both `$FULMINE_REPO` and `$GO_SDK_REPO` (commit, stash, or checkout) so daily-update can pull again — until then this desk is blind to remote fulmine/go-sdk activity.
- **arkd #1089 needs nothing from you** — fee fallback is arkd-wallet-internal; no go-sdk update required.
- **Knowledge base needs a refresh** — Slack, research-monitor, and sdk-parity extracts are ~17 days stale; parity gaps can't be assessed until arkana re-runs.
- Otherwise nothing actionable — genuinely quiet day.
