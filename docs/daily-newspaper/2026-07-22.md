# Ark Daily — 2026-07-22

> _Quiet across dusan's repos (fulmine/go-sdk untouched); the day's real signal is upstream — ts-sdk cut 0.4.48 with a canonical-VTXO-facts refactor + ArkadeCash rename, and prod arkd moved to v0.9.15._

## Front page

- **ts-sdk 0.4.48 landed a "canonical VTXO facts" normalization boundary** — a design pattern go-sdk currently lacks; worth reviewing for parity. (see Wallet beat → arkade-wallet)
- **Prod arkd upgraded to v0.9.15** via ark-infra (`8205ab4`) — go-sdk pins arkd `client-lib` at a May snapshot; confirm wrap surface still matches v0.9.15. (see Protocol surface)
- **"ArkCash" renamed to "ArkadeCash"** across ts-sdk (`c116bba2`) — cosmetic upstream, but a naming signal if go-sdk/fulmine reference the old term. (see Wallet beat)
- **arkana Slack + research monitors are stale** — newest entries are 2026-06-18/19, a ~33-day gap; the Slack/research feeds below are not current. (see Slack briefing)
- **fulmine and go-sdk had zero commits in 24h** — both repos idle (heads April/May). (see Wallet beat)

## Wallet beat

### fulmine
No activity. HEAD unchanged at `3ea61e2` (Bump go-sdk, 2026-04-24).

### go-sdk
No activity. HEAD unchanged at `7f6b8d0` (Contract registry custom handlers, 2026-05-25). Pins: `arkd/pkg/client-lib` @ 2026-05-22 snapshot, `arkd/pkg/ark-lib v0.8.1`.

### arkade-wallet (PWA)
2 commits (`$WALLET_REPO`, `0a025e8 → 0942056`):
- `0942056a` — Upgrade ts-sdk 0.4.48 / boltz-swap 0.3.53 (#813, pietro909). Pulls in the canonical-VTXO-facts refactor and rate-limiting fixes below.
- `1cc9fcd0` — Buy/sell upsell copy: explicit about SEPA bank transfers (#814, Marco Argentieri). Cosmetic copy only.

**Upstream driver — ts-sdk 0.4.48** (the 0.4.48 release, Pietro Grandi). Notable for go-sdk parity:
- `d380bd5e` / `7c3ad311` — add **canonical VTXO facts + normalization boundary**, and drive wallet behavior from them. New architectural seam; go-sdk has no equivalent.
- `d796c245` — origin-scoped rate gate with Retry-After backoff; `7939e6d6` + `f9fa12a5` close a release/report race and negative Retry-After handling.
- `bb64229d` — restore arkade VTXO/boarding discrimination in batch handler; `c037da31` — partition migration inputs the send path would reject.
- `c116bba2` — rename ArkCash → ArkadeCash.

## Protocol surface — arkd → go-sdk

No new commits in `$ARKD_REPO` (pkg/client/, api/, proto/, internal/interface/) in the last 24h.

- **Prod deployment moved to arkd v0.9.15** — `8205ab4` in ark-infra (Se7enZ). **Impact on go-sdk:** go-sdk's `client-lib` pin is a 2026-05-22 snapshot; no code change required today, but worth a sanity check that the wrapped client/API surface is still current against v0.9.15 before the next go-sdk release.

## Slack briefing

**⚠ Feed staleness:** arkana's `slack-log.md`, `slack-monitor.md`, and `research-monitor.md` have no entries newer than **2026-06-18/19** — a ~33-day gap to today. The monitors appear to have stopped running. Nothing below reflects the last 7 days; treat as context only, and consider checking whether the arkana monitor cron is alive.

### Decisions and blockers (from arkana, last real data 2026-06-19)
- Deprecated-signer rotation was scheduled June 22 with a **July 4 cutoff** — both dates are now past; if the feed is trustworthy this is done, but there's no post-June-19 confirmation on record.
- Last-recorded go-sdk state was CI-unstable (6 ci_integration + 3 ci_unit failures) ahead of the June rotation — stale; current CI status unknown.

## TL;DR

- **Look at ts-sdk's canonical-VTXO-facts refactor** (`d380bd5e`, `7c3ad311`) and decide whether go-sdk should adopt the same normalization boundary — this is the day's one real parity signal.
- **Verify go-sdk's arkd `client-lib` pin against prod v0.9.15** before the next cut; the pin is a May snapshot.
- **Check the arkana monitor cron** — Slack/research feeds have been dark since June 19; the daily Slack briefing is flying blind until it's restarted.
- fulmine and go-sdk need no attention today — both idle.
