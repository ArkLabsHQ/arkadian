# Ark Daily — 2026-05-19

> _Quiet on dusan's own repos (no fulmine/go-sdk commits) but heavy persistence-layer surgery in ts-sdk by kukks — exactly the area the 2026-05-17 repositories-atomicity issue flagged for go-sdk parity. arkd bumped to Go 1.26.3; introspector landed a Groth16 verifier test._

## Front page

- **ts-sdk shipped three persistence-layer commits in the last 20h** — repo-first virtual-tx resolution for unilateral exit (`1bed00b`), automated prune-on-spend for `VirtualTxRepository` (`6ccc5bb`), and prune-on-`settledBy` + refreshOutpoints recovery (`fad8093`) — all by kukks. This is the ts-sdk side of the repositories-atomicity work; go-sdk parity gap widens (see Slack briefing → Most important).
- **fulmine VHTLC non-interactive claim** — two PRs opened 2026-05-18 (`fix for non interactive VHTLC claim` + `feat: NonInteractiveClaim option to CreateVHTLC`) — protocol-critical claim-path change; you own fulmine, so this needs your sign-off before merge (see Slack briefing).
- **arkd bumped to Go 1.26.3** (`965cb53d`, louisinger, ~15h ago) — touches every `go.mod` in arkd including `pkg/client-lib`, `pkg/ark-lib`, `pkg/arkd-wallet`. go-sdk's go.mod should bump in lockstep (see Protocol surface).
- **introspector landed a Groth16 BN254 verifier integration test** (`03be0db`, msinkec, ~10h ago) and a CSFS-via-EC-opcodes emulation test (`2778227`) — the EC pairing opcodes from 2026-05-14 are now exercised end-to-end (see Contract surface).
- **rust-sdk picked up BOLT12 submarine-swap support** 4 days ago (`bde53f1`, pi-chat) and validated Boltz VHTLC addresses today (`e7d0f1d`) — fulmine's swap surface now has a sibling implementing BOLT12.

## Wallet beat

### fulmine

- No commits on `master` in the last 24h. Last merge remains **#408 GetVHTLCTransaction RPC** (`4da0368`, fulmine-dev, 2026-05-11).
- Local checkout state is unchanged from yesterday — repo still has uncommitted changes (daily-update skipped fulmine again for that reason).
- **New PR worth tracking** (per slack-monitor 2026-05-18): `fix for non interactive VHTLC claim` and `feat: NonInteractiveClaim option to CreateVHTLC` — both opened against fulmine, both protocol-critical.

### go-sdk

- No commits on `origin/master` in the last 24h. Last activity was the 2026-05-15 cursor-based pagination (`9228c9d`) and the vtxo `created_at` tie-breaker fix (`d81cd4a`), both altafan.
- Local checkout is still on `claude-electrum-test-break` (no upstream); daily-update skipped it.
- ListKeys P0 fix PRs from 2026-05-17 (`ClaimKey` interface + claim-on-`GetKey`) are still in-review — not on master.

### arkade-wallet (PWA)

- Three commits since yesterday morning (all merged 2026-05-18):
  - `98f2ef09` feat(ui): migrate core components to shadcn primitives (3/3) — #593, Sahil Chaturvedi. Closes out the Tailwind v4 + shadcn primitives track. Token system, Modal/Checkbox/Select/Toggle migrated, Framer Motion animations, 50+ component files updated.
  - `edfaf66a` Optional HTTP Basic Authorization configuration — #619, Se7enZ.
  - `b9068d57` make LNURL trigger paste button in input address — #620, João Bordalo.
- Net: cosmetic + small UX. shadcn migration is the only meaningful one and it's a long-running track now complete.

## Protocol surface — arkd → go-sdk

Two arkd master commits in the last ~15h, both louisinger:

- `965cb53d` **go 1.26.3** — toolchain bump across all 8 sub-modules (`go.mod`, `api-spec/go.mod`, `pkg/ark-cli`, `pkg/ark-lib`, `pkg/arkd-wallet`, `pkg/client-lib`, `pkg/errors`, `pkg/kvdb`, `pkg/macaroons`) plus every CI workflow.
  - **Impact on go-sdk:** go-sdk should bump its `go.mod` to 1.26.3 to stay aligned with `pkg/client-lib` / `pkg/ark-lib` it wraps; otherwise build-tag drift becomes a CI risk.
- `3d422299` **arkd-wallet update** — bumps two Dockerfiles (`arkdwallet.Dockerfile`, `arkdwallet.btcwallet.Dockerfile`) only.
  - **Impact on go-sdk:** none — image-only bump for the arkd-wallet sidecar.

## Contract surface — introspector

Four msinkec commits in the last ~18h:

- `03be0db` **implement gorth16 verifier integration test** (~10h ago) — adds `test/groth16_bn254_test.go` (351 lines). Exercises the gnark Groth16 BN254 verifier against the introspector. Means the EC pairing opcodes (`OP_ECPAIRING`, added 2026-05-14) are now provably runnable against real SNARK proofs.
- `2778227` **add integration test for csfs emulation using EC opcodes** (~18h ago) — adds `test/csfs_ecdsa_secp256r1_test.go` (281 lines). Demonstrates the new EC opcodes can emulate `OP_CHECKSIGFROMSTACK` over secp256r1 — useful proof point for any covenant-style contract that would otherwise need CSFS.
- `2a97cf1` fix lint + `b845216` format — routine, no semantic change.

No new opcodes today; no grammar/parser changes — this is verification depth on opcodes already added.

## Slack briefing

### Decisions and blockers (from arkana)

- **PROTOCOL-CRITICAL — Non-interactive VHTLC claim PRs opened in fulmine** (#github-bot, 2026-05-18): `fix for non interactive VHTLC claim` + `feat: NonInteractiveClaim option to CreateVHTLC`. New claim mode that doesn't require interactive coordination. Per arkana's flagging, requires human sign-off before merge.
- **NArk persistence parity PR opened in dotnet-sdk** (2026-05-18): `feat(repositories): NArk persistence parity — IntentRepository + VirtualTxRepository (storage layer)` — direct response to the 2026-05-17 repositories-atomicity issue. dotnet-sdk is moving fast on this; go-sdk is the slow lane.
- **`feat: add WithOutputTaprootTree SendOption` PR opened** (2026-05-18; matches arkd `5a50e4cd` from 2026-05-13) — taproot-tree output option for send operations.
- **`Upgrade ts-sdk 0.4.27` PR opened** (2026-05-18) — propagates a release that release-coordinator already flagged as a governance violation (P1 race in ts-sdk#489, zero human reviews). Should not be merged until the v0.4.27 race is resolved.
- **arkd CI meltdown front opens**: 4× `ci_integration` + 4× Trivy failures in 24h on arkd (previously stable). Separate root cause from the resolved boltz/fulmine issue. bancod also showing first CI failures.
- **`Allow registering custom contract handlers` marked ready_for_review** (2026-05-18) — extensible contract-handler registry, in-review.
- **External C++ Arkade SDK spotted** (#dev, kukks 2026-05-18): `@we_satoshis` on X building a C++ SDK independently. First community-driven SDK sighting.

### Research findings (from arkana)

- **Optech #405 (2026-05-15)**: `BIN-2026-0002` assigned for `OP_CHECKCONTRACTVERIFY` (covenant opcode with direct Ark relevance). `OP_TEMPLATEHASH` now on Inquisition signet.
- **"Credible Exit and the Law of Conservation of Blockspace"** (Delving Bitcoin, BitcoinErrorLog, 2026-05-13): Ark supports ~2M credible exits in a 14-day window vs Lightning's ~1.1M — 1.8× advantage. Validates Ark's design.
- **Bitcoin Core 31.0**: Cluster mempool PR #34616 merged to main. V3 policy + sibling-eviction RBF standard. rc4 on testnet. Direct benefit for Ark's forfeit/connector tx broadcasting.
- **BOLT #995 (simple taproot channels) + #1228 (zero-fee commitments)** standardized in Optech #404. Both Ark VTXOs and LN channels now standardized on P2TR + MuSig2 — directly benefits Ark↔LN interop.
- **CTV signaling**: still 0% at 12+ weeks. Effectively dead for this window.

### Most important — expanded

1. **ts-sdk persistence-layer surgery is happening in front of go-sdk.** In the last 20 hours kukks merged three commits to ts-sdk: repo-first virtual-tx resolution for unilateral exit (`1bed00b`, lite materialise-on-demand + cache), automated prune-on-spend for `VirtualTxRepository` during sync (`6ccc5bb`), and prune-on-`settledBy` with `refreshOutpoints` poll/recovery path covered (`fad8093`). This is the same persistence-layer that yesterday's "Repositories: atomicity, silent failures, cross-backend divergence" ts-sdk issue surfaced. dotnet-sdk has already opened a parity PR (`IntentRepository` + `VirtualTxRepository`). **Why it matters for dusan:** as go-sdk maintainer you now have ts-sdk and dotnet-sdk landing the persistence-correctness work that maps directly onto the four unreviewed SQL-touching merges on go-sdk master (#143 HD Wallet, #145 Contract Manager, #169 Auto-Settle, #173 Remove KV store) — and onto the still-unresolved go-sdk #176 ListKeys P0. The repositories-layer audit you were going to open this week now has two reference implementations.

2. **Non-interactive VHTLC claim in fulmine is protocol-critical and you own fulmine.** Two PRs opened against fulmine on 2026-05-18: `fix for non interactive VHTLC claim` and `feat: NonInteractiveClaim option to CreateVHTLC`. Together they implement a claim mode that doesn't need interactive coordination — meaningful change to the VHTLC state machine. arkana's slack-monitor classes it PROTOCOL-CRITICAL and explicitly says human review is required before merge. **Why it matters this week:** this is your tree. The combination of changing both the create-side (`NonInteractiveClaim` option) and the claim-path fix in the same window means there's a coupled invariant — review them together, not separately.

3. **arkd's Go 1.26.3 bump is a low-friction action item for go-sdk.** `965cb53d` (louisinger, ~15h) bumps every arkd sub-module to Go 1.26.3, including `pkg/client-lib` and `pkg/ark-lib` — the wrap surface go-sdk depends on. go-sdk's own `go.mod` is the natural follow-up. **Why it matters:** small, but if it drifts past a release boundary on either side you get CI surprises. A 1-line PR clears it.

## TL;DR

- Review the **two non-interactive VHTLC PRs in fulmine** — protocol-critical, you're the maintainer; they should land coupled, not separately.
- Bump **go-sdk to Go 1.26.3** to track arkd `965cb53d`; one-line `go.mod` change.
- Read kukks's three ts-sdk persistence commits (`1bed00b`, `6ccc5bb`, `fad8093`) — the reference implementation for the go-sdk repositories-layer audit you were going to scope.
- Watch for the `Upgrade ts-sdk 0.4.27` PR landing in wallet/fulmine — **don't merge** until ts-sdk #489 P1 race is resolved.
- Skim introspector `03be0db` (Groth16 BN254 test) and `2778227` (CSFS-via-EC-opcodes test) — confirms the EC pairing surface is real and usable for covenant-style contracts.
