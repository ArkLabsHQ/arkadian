# Ark Daily — 2026-05-18

> _Quiet Monday across dusan's primary repos — no fulmine/go-sdk/wallet master merges in 24h. The action this weekend was governance (enclave v0.0.76 shipped with zero human review) and a protocol-critical persistence-layer finding in ts-sdk #493 that almost certainly maps onto go-sdk._

## Front page

- Enclave v0.0.76 was tagged Saturday with **zero human review** on protocol-critical KMS PRs — GOV#30/31/32; permanent-lockout risk flagged by executive-digest (see Slack briefing).
- ts-sdk **#493** — "Repositories layer: atomicity, silent failures, cross-backend divergence" — opened 2026-05-17; same bug class is plausible in go-sdk (4 unreviewed SQL merges on master, P0 already manifested) (see Slack briefing → Most important).
- go-sdk P0 **#176 ListKeys** root cause identified; two fix PRs opened 2026-05-17 (`ClaimKey` interface + claim-on-GetKey) — needs your eyes before merge (see Slack briefing).
- introspector merged a **mock arkd gRPC service** (2026-05-16) and refactored enclave test setup (2026-05-17) — could simplify go-sdk integration testing (see Contract surface).
- fulmine working tree has an uncommitted `.gitignore` edit (added `.worktrees/`); local `master` is 2 commits behind `origin/master` — minor, but worth tidying up.

## Wallet beat

### fulmine
- No master commits in the last 24h. Last merge was **#408 Add GetVHTLCTransaction RPC** (`4da0368`, fulmine-dev, 2026-05-11).
- Local repo state: uncommitted `.gitignore` change (`.worktrees/` line); branch `master` 2 commits behind `origin/master` — fast-forwardable.
- Daily-update skipped fulmine today due to the uncommitted change.

### go-sdk
- No master commits in the last 24h. Last activity on master: **cursor-based pagination** (`9228c9d`, altafan, 2026-05-15) and vtxo `created_at` tie-breaker fix (`d81cd4a`, altafan, 2026-05-15).
- Local checkout is on branch `claude-electrum-test-break` (no upstream), so the daily-update skipped it.
- Two PRs landed on the slack feed 2026-05-17 against the ListKeys P0 (see Front page) — not yet on master.

### arkade-wallet (PWA)
- No master commits in the last 24h. Last activity 2026-05-15: **fix assets values** (`1cfdb166`, João Bordalo, #611) and **LNURL session → app-level provider** (`787bf2e2`, kukks, #559).
- Day was cosmetic/UX after the Thu–Fri burst (contracts view, qrcode copy, boltz-swap 0.3.31).

## Protocol surface — arkd → go-sdk

No arkd commits in the last 24h (last master commit: `f8aefab4` ci codeql, 2026-05-11). Nothing to wrap.

## Contract surface — introspector

- **`cbfb4eb` Refactor enclave test setup and remove deprecated components** (Joshua Aruokhai, 2026-05-17) — test-infra cleanup; no opcode or grammar change.
- **`549e251` feat(mock-arkd): add mock implementation of arkd gRPC service** (Joshua Aruokhai, 2026-05-16) — note for go-sdk: a usable mock arkd may let you drop real-regtest spin-up in some integration tests once it stabilises.

(Earlier this week, but worth keeping on radar: `c6116e6` OP_ECADD / OP_ECMUL / OP_ECPAIRING and `183fdea` EC opcode value changes, both msinkec, 2026-05-14/15 — new EC-pairing primitives are live in the contract surface.)

## Slack briefing

### Decisions and blockers (from arkana)
- **PROTOCOL-CRITICAL — Repositories layer atomicity issue** (#github-bot, 2026-05-17): "Repositories layer: atomicity, silent failures, cross-backend divergence" issue opened. Maps to ts-sdk #493 VTXO storage bugs. Systemic persistence-layer threat.
- **go-sdk ListKeys P0 #176 — root cause + fix PRs** (#github-bot, 2026-05-17): `identity.ListKeys returns empty when keys are allocated via GetKey` — root cause identified. Fix PRs in flight: `feat(identity): add ClaimKey method to interface` and `fix: claim keys on identity when contracts are allocated via GetKey`. Protocol-adjacent — needs human sign-off before merge.
- **arkd + introspector Trivy scans both failing** (2026-05-17): dual CVE exposure across two production repos. Day 2+ for introspector.
- **GOV#30/31/32 — enclave v0.0.76 release catastrophe** (executive-digest, 2026-05-17): enclave v0.0.76 released with zero human review on the two KMS-autonomy PRs from 2026-05-14. Permanent-lockout risk confirmed; 32+ governance violations in 21 days.
- **pentest CI failure on ArkLabsHQ/arkd-pentester** (2026-05-17): first sighting; unclear whether infra or real finding.
- **HTTP2/gRPC proxy MERGED** (2026-05-17): addresses the 4/24 production Traefik h2c incident at the infra layer.

### Research findings (from arkana)
- **"Credible Exit and the Law of Conservation of Blockspace"** (Delving Bitcoin, BitcoinErrorLog, 2026-05-13; picked up 2026-05-17): formal proof of blockspace bounds for L2 credible exits. Ark supports ~2M credible exits in a 14-day window vs Lightning's ~1.1M — a 1.8× advantage. Validates Ark's design philosophy.
- **Optech #405** (2026-05-15): BIN-2026-0002 assigned for `OP_CHECKCONTRACTVERIFY` — covenant opcode with direct Ark relevance. `OP_TEMPLATEHASH` now on Inquisition signet.
- **Bitcoin Core 31.0**: Cluster mempool PR #34616 merged to main; V3 policy + sibling-eviction RBF standard. rc4 on testnet. Benefits Ark's forfeit/connector tx broadcasting.
- **CTV signaling**: still 0% at 12+ weeks — effectively dead for this window.

### Most important — expanded

1. **ts-sdk #493 → repositories-layer audit for go-sdk.** The ts-sdk issue opened 2026-05-17 lists atomicity bugs, silent failure modes, and SQLite/Postgres divergence in the persistence layer. Arkana's parity log explicitly flags this as **HIGH risk for go-sdk** because go-sdk has 4 unreviewed SQL-touching merges on master (HD wallet #143, Contract Manager #145, Auto-Settle #169, Remove KV store #173) and a P0 already manifested (#176 ListKeys). Why it matters this week: as the go-sdk maintainer you own this; the audit should land before any release tag is cut.

2. **go-sdk #176 ListKeys — fixes in review.** The bug is that keys allocated via `GetKey` were never registered, so `ListKeys` returned empty after contract creation. The two PRs split it cleanly: one adds `ClaimKey` to the identity interface, the other wires it on the `GetKey` allocation path. kukks already flagged in PR-review guidance that protocol-adjacent identity/key changes require deep review (ultrathink). Why it matters: this is the release-blocker for go-sdk first tag.

3. **Enclave v0.0.76 governance catastrophe.** Two protocol-critical KMS PRs (enclave self-bootstraps its KMS key end-to-end + atomic KMSKeyID migration flip) merged 2026-05-14 without recorded human sign-off; the release went out 2026-05-17. Risk is permanent KMS lockout if PCR0 binding is wrong. Why it matters for dusan's surface: nothing in fulmine/go-sdk depends on enclave today, but if the executive-digest escalation produces a hard freeze on KMS-touching commits, downstream wallet/swap flows may have to plan around it.

## TL;DR

- Review the two go-sdk **ListKeys/ClaimKey** PRs (#identity ClaimKey interface + claim-on-GetKey) before they merge — they're protocol-adjacent.
- Open or co-sign a **go-sdk repositories-layer audit ticket** mirroring ts-sdk #493 — atomicity / silent failures / SQLite-Postgres divergence over the 4 unreviewed merges on master.
- Tidy fulmine local state: commit-or-discard the `.gitignore` `.worktrees/` change and fast-forward `master` to `origin/master`.
- Try out introspector's new **mock arkd gRPC service** (`549e251`) — it may let you drop real-regtest spin-up from some go-sdk integration tests.
- Watch arkd + introspector **Trivy** failures — both still red as of 2026-05-17.
