# Ark Daily — 2026-05-06

> _Busy day on dusan's stack: RBF bumpfee fix landed across go-sdk #167 + fulmine #406, arkd #1060 cleared a single-key wallet KeyID bug, and louisinger's #1055 widened the client-lib `Utxo` surface to carry boarding-asset balances forward through batch settle._

## Front page

- **RBF bumpfee fix shipped end-to-end** — go-sdk c8dbced (#167) maps replacements by output index instead of script, plus exposes `Store()` on `ArkClient` and adds `DeleteUtxo` on `utxoStore`; fulmine dfac29f (#406) bumps both deps and adds a `sanitize()` pass that drops stale boarding UTXOs at unlock. (see Wallet beat)
- **arkd client-lib gains `Utxo.Assets`** — louisinger 216951ee (#1055) carries boarding-UTXO asset balances forward in `getFundsToSettle` and indexes them in the proof PSBT (`assetInputs[len(vtxos)+boardingIndex+1]`). Anything wrapping `pkg/client-lib/types.Utxo` or `toIntentInputs` needs to mirror the layout. (see Protocol surface)
- **arkd #1060 unblocks single-key wallets** — dusan's `2444c613` + `1815bb99` populate `KeyRef.Id = "m"` and return `NextIndex == 1` once initialized, fixing the empty-KeyID bug that was breaking single-key callers. (see Protocol surface)
- **API breaking-change policy now documented** — Dunsin 701a100d (#1007) lands `api-spec/BREAKING_CHANGES.md`: arkd is alpha, OpenAPI is auto-generated from protobuf, and breakage in proto = breakage in OpenAPI. Reference next time a tag-number debate flares. (see Protocol surface)
- **fulmine and go-sdk worktrees are dirty** — both repos have uncommitted `.gitignore` edits, so the daily-update skipped them and the cron's pull is behind origin. (see TL;DR)

## Wallet beat

### fulmine
- **dfac29f #406 — Bump go-sdk and client-lib for RBF bumpfee fix and single-key wallet** (Dusan, 2026-05-05). Wires single-key wallet via `WithWallet(singleKeyWallet)`, switches Service init to use `filestore.NewWalletStore(datadir)`, and adds a new `sanitize(ctx)` pass invoked after `recoverChainSwaps` in `UnlockNode`. `sanitize` walks `utxoStore.GetAllUtxos`, calls `Explorer().GetUtxos(boardingAddr)`, and `DeleteUtxo`s outpoints that aren't on-chain anymore. `internal/core/application/service.go` +60 lines.
- **d242a22 #404 — e2e test for cooperative refund of underfunded submarine swap** (michael1011, 2026-05-04). Adds `swap_test.go` coverage for the new Boltz cooperative-refund path; touches `ci.integration.yaml`, `vhtlc_test.go`, `utils_test.go`.
- Local working tree has uncommitted `.gitignore` change — daily-update skipped this repo.

### go-sdk
- **c8dbced #167 — Fix RBF replacement mapping outputs by index instead of script** (Dusan, 2026-05-05). The substantive part of yesterday's stack. Also: `Store()` now exposed on the `ArkClient` interface (so fulmine can reach the utxo store), `DeleteUtxo` added to `utxoStore`, plus an e2e test that creates a Bitcoin Core wallet, sends 5 boarding txs with `replaceable=true`, runs `bumpfee` (which can reorder outputs), mines, and verifies settle still succeeds.
- Local working tree dirty (`.gitignore`) on branch `claude-electrum-test-break` — daily-update skipped.

### arkade-wallet (PWA)
- **7192bdf9 #601 — fix layout issues** (João Bordalo).
- **e2f22ab9 #602 — fix(format): show fiat amounts with two decimals** (Sahil Chaturvedi).
- Cosmetic only; no protocol-touching changes.

## Protocol surface — arkd → go-sdk

- **216951ee #1055 — `Utxo.Assets []Asset` added; boarding assets indexed in proof PSBT** (louisinger).
  - `pkg/client-lib/types/types.go`: new field on `Utxo`.
  - `pkg/client-lib/batch_session.go`: `getFundsToSettle` now also walks `boardingUtxos` for asset balances when there are no explicit outputs.
  - `pkg/client-lib/utils.go`: `toIntentInputs` writes `assetInputs[len(vtxos)+boardingIndex+1] = coin.Assets` — the +1 is the fake intent input at index 0.
  - **Impact on go-sdk**: any code that mirrors `client-lib.Utxo` or constructs proof PSBTs has to add the `Assets` field and respect the `vtxos → boarding → +1 fake` ordering, or boarding-asset settles will produce malformed proofs.
- **2444c613 #1060 + 1815bb99 — single-key wallet `KeyRef.Id` populated, `NextIndex == 1` after init** (Dusan).
  - `pkg/client-lib/wallet/singlekey/bitcoin_wallet.go` + `wallet.go`. **Impact on go-sdk**: callers that previously got an empty KeyID from single-key wallets and worked around it can drop the workaround. fulmine #406 already adopts this.
- **701a100d #1007 — `api-spec/BREAKING_CHANGES.md`** (Dunsin). Policy doc only — no code surface impact, but it's the canonical reference for proto/OpenAPI evolution rules.
- **fd062bb9 #1057 — Postgres 17.8 + `ARKD_SESSION_DURATION` regtest env** (Se7enZ). Infra; no client-lib impact.
- **0eb3ac4d #1059 — repo-relative path after build-cli** (Dunsin). DX only.

## Slack briefing

### Decisions and blockers (from arkana)
- **go-sdk first release (P0)** — still standing, multi-week. Master now also carries the unreviewed HD wallet code from #143 (3 critical findings) and the ElectrumX backend from #160 with 2 flagged concurrency bugs (goroutine leak + concurrent TCP writes). Audit before tagging. [from #github-bot via slack-monitor, 2026-05-01]
- **ts-sdk #441 (HD rotation Phase C2) P1 race** — `offchainTapscript` mutation races concurrent settle/send; must block merge. Sibling SDK problem but worth knowing because go-sdk #143's HD work mirrors this surface. [#github-bot, 2026-05-01]
- **`fix!: represent asset amounts as bigint (alt to #469)` opened on ts-sdk** — breaking-change marker (`!`); competing approach to #469. Anything in go-sdk that does asset-amount arithmetic in `uint64` will need to track the resolution. [#github-bot, 2026-05-01]
- **bancod release pipeline broken** — "Release Binaries and Docker Build" failing. Tangential to dusan's stack but worth noting if anything depends on bancod artifacts. [slack-monitor, 2026-05-01]
- **rust-sdk E2E Test Matrix Runner** — three+ weeks of consecutive failures, no fix in sight. [slack-monitor, 2026-05-02 → 05-05]

### Research findings (from arkana)
- **CTV+CSFS developer consensus narrowing** (Delving Bitcoin, surfaced 2026-05-05). steven_roose: "more people are coming to agreement" on CTV+CSFS as the minimal first covenant step; OP_TEMPLATEHASH+OP_INTERNALKEY variant proposed. Miners still 0% signaling — 7 weeks. The "Ark case for CTV" post (Roose) remains the canonical reference: send-to-others without rounds, automatic VTXO reissuance, Lightning receive without round participation.
- **Optech #403 (May 1) — PQ wallet wave** (research-monitor 2026-05-02). Four parallel proposals; Buchner's tapscript-PQ-keys-today (no soft fork required) is the most directly relevant — could enable PQ-resistant VTXO leaves without waiting on consensus. Osuntokun's zk-STARK BIP32 recovery (<3s proofs) is a long-tail safety net.
- **BIP54 Consensus Cleanup demo on signet** (Optech #403). Antoine Poinsot showed slow-to-validate blocks — likely the next soft fork to activate, ahead of CTV. Establishes a governance template if it lands.
- **Spark USDB now pays 6% daily BTC rewards** (research-monitor 2026-05-05). Yield-bearing Bitcoin-native stablecoin. Competitive ground for Arkade Assets to evaluate.

### Most important — expanded
1. **RBF bumpfee fix is now end-to-end across dusan's stack.** Yesterday Dusan landed go-sdk #167 (output-index mapping, plus `Store()`/`DeleteUtxo` on ArkClient/utxoStore) and immediately bumped fulmine to it in #406. The new fulmine `sanitize()` runs after `recoverChainSwaps` on unlock and prunes boarding UTXOs whose outpoints are no longer on-chain — the missing half of the RBF story. The e2e test sends 5 replaceable boarding txs, `bumpfee`s each, mines, and asserts settle still works. With this in, RBF-replaced boarding UTXOs no longer poison the local DB.
2. **arkd client-lib's `Utxo.Assets` carry-forward changes the proof PSBT layout for go-sdk.** louisinger's #1055 is more than a test PR — it widens the boarding/settle path so asset balances on boarding UTXOs are aggregated alongside VTXO assets, and the proof PSBT now writes asset metadata at `assetInputs[len(vtxos) + boardingIndex + 1]`. If go-sdk has any code that constructs proof PSBTs independently or mirrors `client-lib.Utxo`, it has to add the `Assets` field and respect the index layout, or boarding-asset settles will fail validation downstream.
3. **CTV+CSFS narrowing matters for Ark's planning horizon.** Roose's read that the community is converging on CTV+CSFS as the minimal first step — paired with the Ark case for CTV (send-to-others without rounds, automatic VTXO reissuance, Lightning receive without round participation) — is the strongest signal in months that the consensus story has a viable next chapter. The 0% miner signaling gap is unchanged, but BIP54 landing first would establish a fork-activation template that CTV could follow.

## TL;DR

- `git pull` fulmine and go-sdk locally — both have uncommitted `.gitignore` so the cron skipped yesterday's tip (fulmine `dfac29f`, go-sdk `c8dbced`). The dirty file is fluff; tidy it so the daily picks up these repos again.
- Audit go-sdk for any code that wraps or mirrors `client-lib.Utxo` — add the `Assets` field and verify the proof-PSBT index layout matches arkd #1055 (`vtxos → boarding → +1 fake`).
- Decide whether the new `Store()` exposure on `ArkClient` (go-sdk #167) is the surface you want long-term — fulmine #406 already calls `s.Store().UtxoStore()` from `sanitize`, which couples fulmine to that interface shape.
- Check go-sdk #160 (ElectrumX) reviewer notes — the goroutine leak + concurrent TCP writes flagged by code-review still need addressing before any first-release tag.
- Skim `api-spec/BREAKING_CHANGES.md` (arkd #1007) — short, sets the rules of engagement for the next time someone wants to retag a proto field.
