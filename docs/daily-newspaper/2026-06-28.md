# Ark Daily — 2026-06-28

> _Quiet day, stale desk: zero commits landed across all priority repos in the last 24h, and the Slack/research/parity feeds the desk reads from are all 9+ days old — today's edition is running half-blind._

## Front page

- **No new commits in the last 24h** anywhere in dusan's scope — fulmine, go-sdk, wallet, arkd, the sibling SDKs and introspector are all unchanged since Jun 26. (see Wallet beat)
- **Input pipeline is stale.** Slack log stops at Jun 19, research-monitor at Jun 18, parity report at Apr 3. Today's daily-update pulled 0 commits and could not fetch `introspector`/`bancod` (tag clobber errors). Treat the Slack/Parity sections as historical, not fresh. (see Slack briefing)
- **fulmine & go-sdk were skipped by daily-update** due to uncommitted local changes — both are just `.gitignore` edits, harmless, but worth committing/stashing so the auto-sync stops skipping your two primary repos. (see Wallet beat → fulmine/go-sdk)
- **Still-open from the last live signal (Jun 19):** `fix(arkd-wallet): select signer key per leaf across all multisig closures` was flagged PROTOCOL-CRITICAL for the Jun 22 rotation, and go-sdk CI was unstable going into it. Worth confirming both resolved post-rotation. (see Slack briefing)

## Wallet beat

### fulmine
No commits in the last 24h. HEAD is still `3ea61e2 Bump go-sdk@2d035b64 (#401)` (Apr 24). Only local change is a `M .gitignore` working-tree edit — this is why daily-update skipped the repo. No CI/PR signal available (Slack feed stale).

### go-sdk
No commits in the last 24h. HEAD is still `7f6b8d0 Contract registry: allow registring custom contract handlers (#186)` (May 25). Only local change is a `M .gitignore` — same skip cause as fulmine.

### arkade-wallet (PWA)
No activity in the last 24h. Latest commit remains `074c5244 fix(receive): show 'Edit amount' after setting an asset amount (#697)` (Jun 26, Dunsin) — already covered in prior editions. Recent wallet churn was cosmetic/UX (receive-amount editing, regtest/boarded-funds links), no protocol surface touched.

## Slack briefing

> _Source caveat: newest slack-log entry is 2026-06-19 (9 days old) and newest research-monitor entry is 2026-06-18. Nothing below is from the last 24h; it is the last live signal, surfaced because the items remained open and in dusan's scope. No new decisions/blockers available for today._

### Decisions and blockers (from arkana, as of Jun 19)
- **go-sdk CI instability** — 6+ ci_integration + 3 ci_unit failures flagged as the most critical CI risk going into the Jun 22 signer rotation. Confirm it stabilised post-rotation.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** — PR open, tagged PROTOCOL-CRITICAL for rotation correctness. Check whether it merged.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question** — issue open: trigger settlement when *one* VTXO is near-expiry vs the current all-VTXOs gate. Affects settlement aggressiveness / user-facing latency; a go-sdk-relevant protocol decision still awaiting a call.

### Research findings (from arkana, as of Jun 18)
- **LND zero-timestamp gossip DoS** — crafted gossip crashes LND < v0.20.1 (Delving Bitcoin). Already patched upstream; relevant only if any Lightning node in the swap path runs an old LND.
- **CTV** — still 0% signalling, ~18 weeks in (nearly halfway through the timeout). No movement that affects covenant plans yet.

### Most important — expanded
- **The desk is reading stale inputs — this is today's real story.** Today's daily-update pulled 0 commits, skipped fulmine and go-sdk on uncommitted `.gitignore` changes, and failed to fetch `introspector` and `bancod` (both `would clobber existing tag` errors on `git fetch`). Meanwhile the Slack, research and parity feeds in arkana-knowledge haven't advanced in 9+ days. WHY it matters: for at least a week the morning briefing has had no fresh Slack/PR/parity signal, so "quiet day" may partly reflect a broken pipe rather than a quiet ecosystem. Worth a 5-minute check that arkana's monitors and the repo-sync are actually running before trusting the next few editions.

## TL;DR

- **Unblock your own repos:** the `.gitignore` edits in fulmine and go-sdk are making daily-update skip both — commit or stash them so auto-sync resumes.
- **Fix the fetch failures:** `introspector` and `bancod` fail `git fetch` with tag-clobber errors; a `git fetch --tags --force` (or pruning the local tags) on those mirrors will clear it.
- **Check the arkana feeds:** Slack/research/parity logs are 9+ days stale — verify the slack-monitor / research-monitor / sdk-parity crons still run, or the desk stays blind.
- **Post-rotation confirmation (low effort, high value):** verify the Jun-22 rotation landed cleanly — did the per-leaf signer-key fix merge, and did go-sdk CI recover?
- Otherwise: nothing shipped today — genuinely quiet on the code front.
