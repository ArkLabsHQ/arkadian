# Ark Daily — 2026-07-02

> _Quiet on dusan's own repos — fulmine and go-sdk had no new commits — while the emulator (introspector) cut v0.0.4 broadening SubmitIntent and the arkade-wallet PWA upgraded onto ts-sdk 0.4.41. Note: both fulmine and go-sdk local checkouts are behind origin with uncommitted changes, so daily sync skipped them._

## Front page

- Emulator (introspector) tagged **v0.0.4**: `#101` now accepts **all intent message types in SubmitIntent** — check go-sdk/fulmine emulator round-trips still hold. (see Contract surface)
- arkade-wallet PWA upgraded to **ts-sdk 0.4.41 / boltz-swap 0.3.46** (`#709`) — pulls in the new activity-history API and "don't-fail-on-delegate-errors"; worth a smoke test. (see Wallet beat)
- **fulmine & go-sdk local repos are stale**: uncommitted local changes blocked daily pulls; origin/master is 5–7 days ahead. Reconcile to unblock sync. (see Wallet beat)
- arkd change today is a `channelz` admin-port whitelist (`b33f7edf`) — **no impact** on go-sdk's client/proto surface. (see Protocol surface)
- arkana's Slack + research logs are stale (last updated Jun 18–19); no fresh company signal for today. (see Slack briefing)

## Wallet beat

### fulmine
No new commits in last 24h. Local checkout is ~10 weeks behind origin with uncommitted local changes (daily sync SKIPPED it). After a manual fetch, newest `origin/master` commit is `13f5020` (5 days ago, chain-swap/LNURL UI work by kukks). **Action:** stash/commit local changes and pull.

### go-sdk
No new commits in last 24h. Local checkout is ~5 weeks behind origin with uncommitted local changes (daily sync SKIPPED it). Newest `origin/master` is `0dfc5f0` (7 days ago). Recent-but-not-today landings worth noting once pulled: `e04b36f` signer key rotation at restart/runtime (Dusan Sekulic), `55d640c` `WithScript` option on `ListVtxos` (#194).

### arkade-wallet (PWA)
- `341bf560` Allow deprecated keys delegate (#708) — João Bordalo, 18h ago
- `4ead848e` Upgrade ts-sdk 0.4.41 / boltz-swap 0.3.46 (#709) — pietro909, 18h ago. Brings the ts-sdk activity-history refactor and delegate-error tolerance into the wallet.

## Protocol surface — arkd → go-sdk

- **channelz endpoints whitelisted in permissions** (`b33f7edf`, Se7enZ, #1133). Adds a `channelz` read entity (`GetTopChannels`/`GetServers`/`GetChannel`/…) restricted to the **admin port**.
  - **Impact on go-sdk:** none. Touches `internal/interface/grpc/permissions/` only; no change to `pkg/client`, proto, or the Ark/Indexer service surface go-sdk wraps.

## Contract surface — introspector

Emulator (arkade-os `emulator`, formerly `introspector`) cut **v0.0.4**. Four commits in ~24h:

- `b5d3b3a` **feat: accept all intent message types in SubmitIntent** (#101, Dunsin Abisuwa) — broadens the intent parser; touches `emulator/v1/service.proto`, `internal/application/intent.go`, and new `grpc/handlers/parser.go`. Changes what SubmitIntent will accept. **No new opcodes or grammar changes.**
- `d0d3437` remove tls cfg (#105, louisinger)
- `4e1a84f` inject tag version (#106, louisinger) — v0.0.4 tag
- `7812547` bump golang.org builtin deps (#103, louisinger)

## Slack briefing

> _Note: arkana's slack-log.md is last updated **2026-06-19** and research-monitor **2026-06-18** — ~2 weeks stale. Items below are dated; the June 22 signer rotation has since passed, so treat action items as likely resolved and verify._

### Decisions and blockers (from arkana, through Jun 19)
- **go-sdk CI instability** [#dev, Jun 19]: 6+ ci_integration + 3 ci_unit failures flagged ahead of the June 22 signer rotation. Likely resolved now — worth confirming CI is green.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** [Jun 19] — PR opened; PROTOCOL-CRITICAL for rotation correctness. Relevant to go-sdk's rotation handling.
- **`Add Arkade Script support with emulator co-signing`** [Jun 19] — PR opened; arkade-script co-signing via the emulator. Directly on the introspector/emulator track dusan touches for contracts.

### Research findings (from arkana, through Jun 18)
- **LND zero-timestamp gossip DoS** [Delving Bitcoin, Jun 18]: crafted gossip crashes LND < v0.20.1; patched in v0.20.1+. ASPs / LN nodes should verify versions — relevant to fulmine's Lightning interop path.
- **CTV** still at ~0% signaling, ~18 weeks in.

### Most important — expanded
1. **Emulator SubmitIntent broadening (#101, fresh today).** Dunsin Abisuwa's change makes the emulator accept all intent message types in SubmitIntent and adds a dedicated parser layer. This matters because go-sdk and fulmine exercise the emulator when validating Arkade Script contracts; a wider-accepting SubmitIntent can change which intents round-trip, so any integration test that asserted rejection of specific message types may now behave differently. Pairs with the Jun 19 "Arkade Script support with emulator co-signing" PR — the emulator contract-testing surface is actively moving.
2. **Signer-key-per-leaf rotation fix (arkd-wallet, Jun 19).** The `select signer key per leaf across all multisig closures` PR was flagged PROTOCOL-CRITICAL for the June 22 rotation. dusan's go-sdk already carries `e04b36f` (signer key rotation at restart/runtime); once go-sdk is pulled current, confirm its rotation handling agrees with how arkd-wallet now selects keys per leaf, so wraps don't sign with the wrong leaf key.

## TL;DR

- **Reconcile fulmine & go-sdk local checkouts** — both have uncommitted changes blocking daily pulls; origin is 5–7 days ahead. This is why the sync keeps skipping your two repos.
- **Smoke-test arkade-wallet** after the ts-sdk 0.4.41 bump (#709): the activity-history refactor and delegate-error tolerance changed underneath the UI.
- **Verify emulator integration** in go-sdk/fulmine against emulator v0.0.4 (#101) — SubmitIntent now accepts all intent message types.
- Once go-sdk is current, cross-check its signer-key rotation logic against the arkd-wallet per-leaf key-selection fix flagged Jun 19.
- No action on arkd — today's channelz change is admin-only.
