# Ark Daily — 2026-05-20

> _Quiet across dusan's primary repos — no fulmine or go-sdk commits — but ts-sdk 0.4.27 (with unresolved P1 race) propagated into wallet, arkd cut a Go 1.26.3 toolchain bump that downstream consumers must align with, and the PCR0 enclave signing endpoint shipped with zero human review._

## Front page

- ts-sdk **0.4.27** landed in **arkade-wallet** via #623 (pietro909) — same release that the parity desk has flagged as a GOV violation (PR #489 P1 race, zero human reviews) (see Wallet beat + Parity desk)
- **arkd** bumped to Go **1.26.3** (`2c9612a0`, louisinger, #1070) — fulmine, go-sdk, introspector should align their toolchains (see Protocol surface)
- **PCR0 signing endpoint merged on enclave** (#111) with no governance sign-off; protocol-critical attestation attack surface (see Slack briefing → Most important)
- **boltz-backend** merged "allow committing underpaid chain swaps" + boltz-core v4 bump — chain-swap semantics change that ripples into the fulmine swap path (see Wallet beat)
- **go-sdk** now 46 days without a release; P0 #176 (ListKeys empty after GetKey) and P1 auto-settle race still unresolved (see Parity desk)

## Wallet beat

### fulmine
No master commits in last 24h. _daily-update marked the repo SKIPPED due to uncommitted local changes — dusan has work-in-progress locally._

### go-sdk
No master commits in last 24h. _Also SKIPPED for uncommitted local changes._ Per arkana's parity log (2026-05-19): **46 days since last release**; P0 #176 (`identity.ListKeys` returns empty after `GetKey` allocates contracts) still open, P1 auto-settle race vs user-initiated submissions producing `VTXO_ALREADY_SPENT` also unresolved. The two ListKeys fix PRs (`ClaimKey` interface + claim-on-`GetKey`) opened 2026-05-17 were both closed without confirmed merge — resolution path ambiguous, worth a status check.

### arkade-wallet (PWA)
Three commits — only one is consequential:
- `5e86af42` Upgrade ts-sdk 0.4.27 + boltz-swap 0.3.32 (#623, pietro909) — pulls in the GOV-flagged ts-sdk release (see Parity desk)
- `fb57f127` Accept `lightning:` prefix in paste-address button (#625, bordalix) — small UX
- `447f0186` Minor fixes (#621, bordalix) — react-on-restore error + lint + typography; cosmetic

## Parity desk

_Using arkana's parity run log from 2026-05-19 (latest formal snapshot 2026-04-03 is stale; not used)._

**Gaps where go-sdk lags** (carried from 2026-04-03 + arkana running notes):
- **Arkade Script / introspector client support** — ts-sdk PR #319 active; go-sdk has tracking issue only, no implementation
- **CovVHTLC** (7th tapscript leaf, covenant-claim) — ts-sdk PR #396; go-sdk has tracking issue only
- **Delegation** — no implementation, no PR; rust-sdk has #190 (idle)
- **SettlementConfig / auto-renewal** — ts-sdk v0.4.14+, no go-sdk equivalent
- **Configurable per-message timeouts** — ts-sdk v0.4.14+
- **BatchSignableIdentity** — ts-sdk PR #395
- **IntentRepository / VirtualTxRepository / virtualTxMode / IntentStateReducer** — ts-sdk branch (not yet on master) and dotnet-sdk NArk PR; once any of those merge, formal gaps open against go-sdk

**Recent sibling-SDK landings (last 7 days)**:
- **ts-sdk c0442fbf** — Expo background-task isolation (#487), 2026-05-19
- **ts-sdk 97d64efc** — Node 24 LTS upgrade (#495), 2026-05-19
- **ts-sdk v0.4.27** released earlier in week — HD receive rotation Phase C2 + ContractWatcher fix; **carries unreviewed P1 race** (same class as #441/#473)
- **dotnet-sdk #94** — deterministic asset group ordering + ts-sdk cross-SDK fixture parity, 2026-05-19; internal but cross-SDK consistency relevant
- **rust-sdk** — no master commits; v0.8.0 (Feb 19) now ~91 days old

## Protocol surface — arkd → go-sdk

One arkd commit in 24h:

- **`2c9612a0` Bump golang@1.26.3 (#1070, louisinger)** — touches `go.mod` across all modules including `pkg/client-lib/go.mod`, `pkg/ark-lib/go.mod`, `api-spec/go.mod`, `pkg/arkd-wallet/go.mod`. **Impact on go-sdk:** no API/proto/client surface change — but go-sdk's go.mod must be bumped to Go 1.26.3 before consuming the next arkd tag, or `go mod tidy` will reject mismatched toolchain directives. Same applies to fulmine and introspector.

## Slack briefing

### Decisions and blockers (from arkana)
- **ts-sdk 0.4.27 GOV violation propagated** — merged into both wallet (#623) and the boltz-swap bundle (`416406a`) despite release-coordinator's block flag; users of either path now exposed to the P1 race. arkana's action item: "ts-sdk 0.4.28+ fix must be shipped and propagated; urgent."
- **PCR0 signing endpoint merged** — enclave PR #111 ("Implement PCR0 signing functionality and expose endpoint") merged via `c173a68` with no recorded human review; PCR0 is the enclave attestation register, exposing a signing endpoint creates a new attack surface. arkana classified PROTOCOL-CRITICAL.
- **boltz-backend `ef82d25f`** — "allow committing underpaid chain swaps" (michael1011, via #1411); chain-swap commitment semantics relaxed for underpayment. Touches the swap state machine fulmine wraps.
- **boltz-backend `1b93bc79`** — boltz-core bumped to v4. fulmine pins boltz dependencies — version bump may surface API differences.
- **introspector OP_MODEXP PR opened** (modular exponentiation opcode) + companion issue "Unify compute-budget system for heavy opcodes" — joins OP_ECADD/ECMUL/ECPAIRING (2026-05-14). Active EVM-style crypto opcode track; will eventually require go-sdk introspector client to know these opcodes.
- **boltz-swap CI: 6 failures yesterday** — highest single-day on that repo; fulmine fix from 2026-05-13 not fully containing fleet CI meltdown.

### Research findings (from arkana)
_Last research-monitor run: 2026-05-19._
- **"Credible Exit and The Law of Conservation of Blockspace"** (BitcoinErrorLog, Delving Bitcoin, May 13) — formal proof that Ark supports ~2M credible exits in a 14-day window vs Lightning's ~1.1M (**1.8x advantage**). Validates Ark's blockspace efficiency framing.
- **BIN-2026-0002 assigned for OP_CHECKCONTRACTVERIFY** (Optech #405) — covenant opcode with direct Ark relevance.
- **Bitcoin Core 31.0 cluster mempool PR #34616** merged to main; V3 policy + sibling-eviction RBF now standard. Improves predictability of Ark connector-tree broadcasting.
- **CTV signaling still 0%** at 12+ weeks since BIP9 start. Effectively dead for this window.
- **Chimera TGE in 7 days** (May 27) — first major third-party wallet using Arkade as primary payment layer.

### Most important — expanded

**1. ts-sdk 0.4.27 P1 race is now in arkade-wallet.** Pietro merged the version bump (#623) into wallet master yesterday, even though arkana's release-coordinator had flagged 0.4.27 as a governance violation (PR #489 shipped HD rotation Phase C2 with a P1 race condition — same class as #441 / #473 — with zero human reviewers). Why it matters for dusan: when go-sdk consumes ts-sdk fixtures or shared protocol behavior, you're now testing against a release that the parity desk says shouldn't be released. Hold ts-sdk 0.4.27 out of fulmine + go-sdk pipelines until v0.4.28 lands a fix.

**2. go-sdk is 46 days into a release drought with two P0/P1 protocol bugs unresolved.** ListKeys returning empty after `GetKey` (#176) and the auto-settle vs user-submission race producing `VTXO_ALREADY_SPENT` are both protocol-critical for the wallet stack. The two ListKeys fix PRs (`ClaimKey` interface + claim-on-`GetKey`) opened on 2026-05-17 were both **closed without confirmed merge** as of yesterday — arkana flagged the resolution path as ambiguous. If you're the maintainer, that pair needs a clear status: rolled into another PR? abandoned? superseded? Either way, no go-sdk release can ship while #176 is open.

**3. PCR0 signing endpoint merged on enclave with no human review.** Enclave #111 (`c173a68`) exposes a PCR0-backed signing endpoint. PCR0 is the platform-configuration register that anchors enclave attestation — exposing a signing path against it materially changes the attestation trust model. The merge happened with no governance sign-off; arkana queued it as PROTOCOL-CRITICAL. Not your direct surface, but if fulmine or go-sdk ever calls into enclave-attested signing, this is the assumption set that just changed underneath you.

## TL;DR

- **Bump Go toolchain to 1.26.3** in fulmine and go-sdk go.mod to match arkd `2c9612a0` — required before next arkd consumption.
- **Do not bump ts-sdk to 0.4.27** in fulmine/go-sdk-adjacent tooling until v0.4.28 ships the #489 P1 race fix.
- **Resolve go-sdk #176 (ListKeys P0)** — both fix PRs were closed without confirmed merge; clarify status before any go-sdk release.
- **Review boltz-backend `ef82d25f`** ("allow committing underpaid chain swaps") and `1b93bc79` (boltz-core v4) for fulmine swap-state-machine impact; pin or upgrade deliberately.
- **Track introspector OP_MODEXP + compute-budget unification** — once merged, go-sdk's introspector client will need opcode awareness.
