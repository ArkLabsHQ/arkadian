# Ark Daily — 2026-05-29

> _Quiet across dusan's primary repos — no fulmine/go-sdk/arkd commits in 24h — but ts-sdk shipped 0.4.31 with chain-swap completion-txid fixes that should land in go-sdk, and dotnet-sdk cut v0.9.1 with NArk payment improvements. go-sdk release drought now 55 days._

## Front page

- **go-sdk release drought at 55 days** — parity report flags P0 #176 + P1 auto-settle race still unresolved on master, no new commits (see Wallet beat → go-sdk; Parity desk).
- **ts-sdk v0.4.31 released yesterday with 5 chain-swap completion-txid fixes** (PRs #523/#530/#531) — go-sdk's chain-swap surface likely has the same bug class. Worth a look (see Parity desk).
- **dotnet-sdk v0.9.1 shipped** with `LnurlHelper` lifted into NArk.Core.Payments and BIP21 build flexibility — these are clean parity targets for go-sdk's payment helpers (see Parity desk).
- **introspector → emulator rename completed earlier in week**; local repo still on old origin and fetch is failing on tag conflict. Heads-up if you script against it (see Contract surface).
- **btcpay-arkade E2E hit 9 failures yesterday — new all-time worst day** for that pipeline; regtest version bump suspected. Not your repo but worth knowing (see Slack briefing).

## Wallet beat

### fulmine
No commits in last 24h. Last commit on master is still `3ea61e2` (2026-04-24, "Bump go-sdk@2d035b64"). Local repo has uncommitted `.gitignore` change that is blocking the daily sync — clear when convenient. No CI noise from fulmine today.

### go-sdk
No commits in last 24h. Last commit `7f6b8d0` (2026-05-25, "Contract registry: allow registring custom contract handlers" #186, Pietralberto). Local repo also has uncommitted `.gitignore` change blocking daily sync. Parity desk has the action items.

### arkade-wallet (PWA) — $WALLET_REPO
No commits in last 24h. Last commit `712c3189` (2026-05-27, "Fix swap info page" #633, João Bordalo). Quiet.

## Parity desk

_Source: most recent parity report `agent-logs/sdk-parity.md` (2026-05-28). 14-day window — section valid._

### Gaps where go-sdk lags

- **Release drought: 55 days** — last release v0.9.1, 8 days behind dotnet-sdk and 9 days behind ts-sdk's recent cadence.
- **P0 issue #176** — still unresolved on master.
- **P1 auto-settle race** — still unresolved on master (same class flagged since mid-May).
- **VtxoManager per-call threshold** — ts-sdk released this in v0.4.30 (#512). go-sdk has no equivalent.
- **`delegator → delegate` rename** — ts-sdk shipped this in v0.4.31 (#519/#520). go-sdk has no rename or alias (rust-sdk + dotnet-sdk also diverge here). Naming gap will compound if not addressed soon.
- **InMemorySwapRepository / AssetManager public exports** — new ts-sdk public API in v0.4.31 (#517). No go-sdk counterpart.
- **Wallet string-config deprecated** — ts-sdk v0.4.31 (#511). go-sdk parity unknown.
- **ServiceWorkerWallet.restore()** — ts-sdk #501. Not relevant for go-sdk (no SW), but the underlying restore semantics are.
- **VHTLC multi-vtxo + refundArk return-type change** — boltz-swap PR #505 (breaking). go-sdk's VHTLC surface will need to match.

### Recent sibling-SDK landings (last 7 days)

- **ts-sdk v0.4.29, v0.4.30, v0.4.31** — three releases in the window. Notable: delegator→delegate rename (#519/#520), mainnet defaults (#516), InMemorySwapRepository + AssetManager exports (#517), VtxoManager per-call threshold (#512), ServiceWorkerWallet.restore() (#501), VHTLC multi-vtxo + refundArk return type (boltz-swap #505), tapscript memoization (#521 — O(n) → O(1) per contract on getVtxos/getBalance), duplicate boarding refill history fix (#531/#530), 5 PRs fixing chain-swap completion-txid (#523/#529/etc).
- **dotnet-sdk v0.9.1** — `LnurlHelper` lifted to `NArk.Core.Payments` (#106), `ArkBip21.Build()` flexibility (#109), `PollScriptsForVtxos` log demoted to Debug (#98), regtest version compatibility fix (#108).
- **rust-sdk** — v0.9.1 release prep merged (#231) and introspector → emulator env var fix (62eee99). No new features.

## Protocol surface — arkd → go-sdk

No arkd commits in last 24h. The two arkd PRs from May 26 that affect go-sdk's wrap surface (logged here in case they haven't been propagated):

- **`c4f16324` — Add tx filters to indexer.GetSubscription (#1074, Bob Smith)** — adds optional tx filters to the `GetSubscription` stream. **Impact on go-sdk:** if go-sdk wraps `IndexerService.GetSubscription`, the call signature gains an optional filter parameter — non-breaking but worth surfacing in go-sdk's indexer client.
- **`4473e23c` — client-lib: Fix indexer auto-pagination and chunk large vtxo filters (#1081, Dusan Sekulic)** — you authored this; lives in `pkg/client/`. Confirm go-sdk has bumped past this client-lib commit; older go-sdk wraps will still hit the auto-pagination bug.

## Contract surface — introspector

No introspector commits landed in last 24h. Last master commit: `d71547f` (2026-05-22, "Chore: version bump - go 1.26.3 & otel 1.41.0", Chris Ricketts).

Two heads-up items that are not commits but matter:

- **Local fetch is failing** — `v0.0.1` tag would be clobbered by remote tag move, and a new branch `feat/compute-limits` exists. The repo was renamed from `ArkLabsHQ/introspector` to `arkade-os/emulator` on 2026-05-26 (per slack-monitor). Your local clone is still on the old origin. Re-point or re-clone when convenient.
- **In-flight branches worth watching** (not on master, not in commit log): `feat/op-sighash`, `feat/ec-opcodes`, `feat/op-verify-zkp`, `feat/modexp`, `feat/compute-limits`, `feat/reversebytes`. The OP_SIGHASH branch is the one flagged "PROTOCOL-CRITICAL, ready_for_review, requires human sign-off" by slack-monitor on 2026-05-26 — when it lands it will change signing template semantics for contracts.

## Slack briefing

### Decisions and blockers (from arkana)

- **btcpay-arkade E2E: 9 failures in 24h — all-time worst day for that pipeline.** Regtest version change is the suspected trigger. `fix(tests): fix failing integration tests for a new regtest version` (dotnet-sdk #108) merged yesterday but failures may predate. Channel: #github-bot via slack-monitor 2026-05-28.
- **dotnet-sdk CI: 6 failures yesterday** — elevated; likely tied to v0.9.1 integration or the regtest bump.
- **"Backup and verify arkd secrets" issue opened** — production secret backup unverified. Affects e2ee.vtxos.com and arkd v0.9.6 production. Money-at-rest operational gap.
- **#dev: production sweepable rounds query** — @U0A32P2G6HK (a dev) shared SQL counting unswept rounds in production and asked how many we typically carry. Indicates monitoring interest in sweep timeliness.
- **#dev: Boltz failure-invoice testing gap** — @U0AEWD7LHTJ asked how to generate a Boltz-guaranteed-fail invoice on Mutinynet for testing failure paths; no response. **Direct fulmine relevance** — VHTLC failure paths are fulmine's domain.
- **getVtxos()/getBalance() O(n) lifetime VTXO history** — flagged 2026-05-26 as production performance time bomb. ts-sdk shipped the fix in v0.4.31 (#521, memoize tapscripts per contract). Worth checking whether go-sdk has the same shape.
- **arkd v0.9.6 released 2026-05-27** — protocol-critical contents (auto-settle narrowing, VTXO persistent storage, VHTLC unilateral exit). go-sdk consumers should bump.

### Research findings (from arkana)

- **Chimera Card + Wirex (2026-05-27)** — self-custodial Bitcoin debit card via Arkade VTXOs, 80M+ merchants, card launches June 2026. First physical-merchant payment integration on Arkade.
- **Ibis Wallet (Optech #406, 2026-05-22)** — second third-party wallet supporting Ark (after Chimera). Android, power-user focus. Validates protocol adoption trajectory.
- **Bitcoin Core 31.0 was released April 19** (earlier entries had it wrong) — cluster mempool, V3 sibling eviction RBF, package relay for non-TRUC zero-fee parents. All benefits ASPs running 31.0 today.
- **Onion message jamming in Lightning (Delving Bitcoin, May 23-25)** — practical attack on per-peer rate limits. Affects BOLT12 reliability, which affects Ark↔Lightning interop.
- **Credible Exit paper (Delving Bitcoin, May 13)** — Ark supports ~1.8x more credible exits in a 14-day window than Lightning. Positioning ammunition.

### Most important — expanded

1. **ts-sdk's chain-swap completion-txid fix wave is the actionable signal for go-sdk this week.** Pietro merged 5 PRs across v0.4.31 (#523, #529, #530, #531, plus tapscript memoization #521) all narrowing the same class of bug: completion txid being returned as the swap ID rather than the on-chain txid, and duplicate boarding history. If go-sdk wraps the same chain-swap completion path, the bug shape is almost certainly there too. Worth a half-day audit before the next release.

2. **dotnet-sdk v0.9.1 lifted `LnurlHelper` out of samples into `NArk.Core.Payments` (#106).** kukks is treating LNURL as a first-class payment primitive in NArk. go-sdk has no equivalent in `pkg/client/` or wherever payment helpers live. If you want SDK-surface parity, this is the next obvious gap — and a relatively bounded one to close.

3. **go-sdk release drought is now 55 days** and the parity report has flagged it three runs in a row. P0 #176 and the P1 auto-settle race are both still on master without a release. Even a v0.9.2 patch release that just ships what's already on master would clear the parity-desk red flag and let downstream consumers (fulmine, banco) stop pinning to commit SHAs.

## TL;DR

- **Half-day audit of go-sdk's chain-swap completion-txid handling** vs ts-sdk PRs #523/#529/#530/#531 — same bug class is likely.
- **Cut go-sdk v0.9.2** — 55-day drought, P0 #176 and auto-settle race waiting on a release tag.
- **Clear the uncommitted `.gitignore` changes** in fulmine and go-sdk local clones — they're blocking arkana's daily sync.
- **Re-point or re-clone the local introspector → emulator** — current origin is dead (repo moved to `arkade-os/emulator` on 2026-05-26).
- **Answer @U0AEWD7LHTJ in #dev** about generating Boltz-fail invoices on Mutinynet — fulmine territory, no one else has replied.
