# Ark Daily — 2026-06-06

> _Quiet day on dusan's primary repos — no fulmine or go-sdk commits, one cosmetic wallet fix. But three live parity items linger: TX_TOO_LARGE VTXO guard (all SDKs), Arkade extension API (HIGH after coinflip went live), and unreleased VHTLC handler on go-sdk master._

## Front page

- **VTXO intent TX_TOO_LARGE — go-sdk has no guard** despite the #dev discussion crediting it with a "default cap = 50"; code audit confirms no cap in `batch_session.go`, `sync.go`, or auto-settle paths. Server rejects >91 VTXOs. (see Parity desk)
- **Arkade extension parity gap escalated to HIGH** — coinflip is in production on mutinynet using ts-sdk v0.4.33's `Arkade.connect()` / `Program` API; go-sdk has neither the primitives nor the high-level layer. (see Parity desk)
- **go-sdk VHTLC handler still unreleased on master** (`5705340`, 2026-05-29) — next tag should ship it; ts-sdk already has multi-VTXO VHTLC via boltz-swap. (see Parity desk)
- **boltz-backend instrumented all async locks** (`4fdd15c8`, #1428, michael1011) — useful prod signal for diagnosing fulmine submarine/reverse swap hangs. (see Slack briefing)
- **arkd cut nothing today** — no new pkg/client/, api/, or proto/ changes; go-sdk's wrap surface is stable. (see Protocol surface)

## Wallet beat

### fulmine
No activity in the last 24h. Local master is 6 commits behind `origin/master`; latest upstream commit is your own `df00504 Bump Go Docker images from 1.26.2 to 1.26.3 (#413)` from 4 days ago. Still uncommitted local changes on your worktree (`.gitignore`) — daily-update skipped the pull.

### go-sdk
No activity in the last 24h. Master is 3 commits behind `origin/master`; latest upstream is `b117416 Update readme (altafan)` 4 days ago. Local has uncommitted `.gitignore` + `.worktrees/` — daily-update skipped this pull too. **v0.10.0 still current**; VHTLC handler (`5705340`) waiting on master for the next release.

### arkade-wallet (PWA)
One commit: `3d2c95d3 fix(Transaction): show roundTxid for offboarding batch transactions (#648)` by arkanaai[bot] — adds `tx.roundTxid` as a third fallback in the transaction detail view (`src/screens/Wallet/Transaction.tsx`, 1 line changed) so offboarding batch txs show a TXID. Purely cosmetic, no protocol/wallet logic touched.

## Parity desk

Source: `memory/project-context/sdk-parity.md` dated 2026-06-05 (fresh).

**Gaps where go-sdk lags:**

- **TX_TOO_LARGE intent guard** (NEW, all SDKs) — server rejects intents with >91 VTXOs. Audit found no cap in `batch_session.go` / `sync.go` / auto-settle paths. `defaultListVtxosLimit=1000` is a list query cap, not an intent guard. Proposed solutions: hardcoded cap (20 conservative / 50 moderate) or weight-based tx size estimation. _Weight-based is the correct approach_ per @U0854KRKV3Q + @U0B791D017A — tx size depends on inputs + outputs + OP_RETURNs + witness, not VTXO count.
- **Arkade extension system, Layer 1** — primitives (`ArkadeScript`, `EmulatorPacket`, `RestEmulatorProvider`, `sendOffChain` extensions). Shipped in ts-sdk v0.4.32. **go-sdk: missing.** rust-sdk has low-level opcodes only.
- **Arkade extension system, Layer 2** — high-level contract API (`Arkade.connect()` / `Program` / fluent builder / `parseArtifact`). Shipped in ts-sdk v0.4.33 (846 LOC + 741 LOC tests, Claude-authored). **go-sdk: missing.** Severity HIGH — coinflip.mutinynet.arkade.sh runs on this API.
- **Cross-SDK Boltz preimage spec** — dotnet-sdk corrected to `Arkade-Boltz-Preimage-v1` = `SHA-256(signer.Sign(key, SHA-256("Arkade-Boltz-Preimage-v1") || xonly(32) || u32le(index)))`. Future go-sdk implementation MUST use `xonly(32)`, not `descriptor.ToString()`.
- **go-sdk VHTLC handler unreleased** — `5705340` on master (standalone `vhtlc/` package + `contract/handlers/vhtlc/`; adds `Derivable()` to Handler interface). Closes the VHTLC gap vs ts-sdk once tagged.

**Recent sibling-SDK landings (last 7 days):**

- ts-sdk v0.4.33 (2d ago) — Arkade contract API + handler authoring helpers (`isCsvSpendable`, `isCltvSatisfied`) exported (PR #541, bonomat).
- ts-sdk boarding HD rotation + default/boarding collision fix (3d ago) — multi-address sweep + restore, server-pubkey filter, malformed-row handling. Protocol-adjacent, merged after careful review (Pietro Grandi, 9+ commits).
- dotnet-sdk preimage fix `a5f17d5` (2d ago) — corrected v1 spec; pin tests verify descriptor-form independence.

## Protocol surface — arkd → go-sdk

No arkd commits in the last 24h. The latest upstream activity that touches go-sdk's wrap surface is still arkd v0.9.6 (released 2026-05-27, in your registry). No new `pkg/client/`, `api/`, `proto/`, or `internal/interface/grpc/` deltas to react to.

## Slack briefing

### Decisions and blockers (from arkana)

- **#dev 2026-06-05 — VTXO intent TX_TOO_LARGE = 91**: @U07ML5C69RC reported the local test result, @U0854KRKV3Q (kukks) suggested a while-loop rather than hardcoded limit and possibly cap at 20 for safety, @U0B791D017A proposed weight-based size estimation. Action item flagged across go-sdk + sibling SDKs.
- **2026-06-05 — `Boarding HD rotation + default/boarding collision fix` MERGED** in ts-sdk. Protocol-adjacent. dusan's go-sdk does not yet have HD boarding rotation logic — likely the next ts-sdk → go-sdk port.
- **arkd ci_integration: 2 failures yesterday**; ts-sdk CI: 3 failures; wallet Playwright: 1 failure. fulmine CI not flagged in the last 3 days of slack logs.
- **2026-06-04 — arkd#1090 (`Add filters to GetVirtualTxs`) flagged must-fix** by bot review: (1) down migration ordering — view recreated before DROP COLUMN, PostgreSQL dependency error; (2) silent scan truncation when packet-type filters used without time-range bound. Affects indexer surface go-sdk queries.

### Research findings (from arkana)

- **2026-06-04 — Google's Shor improvement independently reproduced** (Schrottenloher, arXiv 2606.02235): 20× reduction in qubit requirements (~500K physical qubits to break Bitcoin ECC). PQ timeline compressing — trend-level, not decision-requiring.
- **2026-06-04 — P2WOTS live signet demo** (opus-lux): first PQ signature scheme with working signet + wallet UI. No signature-size data disclosed yet.
- **2026-06-02 — BIP-110 ReducedData soft fork** (55% activation threshold) — temporary 256-byte stack element limit. Likely non-event for Ark scripts but worth tracking if it activates.
- **CTV: still 0%, ~15 weeks. Dead for now.**
- **2026-05-31 — MCCV v0.1.0 CTV vault released** — velocity-control / recovery-keys design patterns applicable to Ark wallet UX.

### Most important — expanded

1. **TX_TOO_LARGE VTXO guard — go-sdk needs it before any wallet with >91 spendable VTXOs tries to settle.** The #dev discussion on 2026-06-05 surfaced that go-sdk has no guard despite what folklore suggested — `defaultListVtxosLimit=1000` is a list query cap, not an intent size check. For dusan's go-sdk maintainership: pick a path (cap at 20–50, or weight-based size estimation as kukks/@U0B791D017A prefer) and ship it before the next wallet that accumulates VTXOs hits a silent settle failure in prod.

2. **Coinflip going live on the ts-sdk Arkade extension API turned a "future work" gap into a HIGH-severity production gap for go-sdk.** Layer 1 primitives shipped 2026-06-03 (v0.4.32), Layer 2 contract API shipped 2026-06-04 (v0.4.33, ~1.6k LOC). The fluent `Arkade.connect()` / `Program` builder is what coinflip.mutinynet.arkade.sh runs on. If anyone wants to build a contract-driven app against go-sdk, they currently can't — they'd have to fall back to raw arkd RPCs. Worth deciding now whether go-sdk will ship the same API surface or whether the contract layer is intentionally ts-only.

3. **boltz-backend instrumented all async locks (`4fdd15c8`, michael1011, PR #1428).** Tactically small but relevant to fulmine prod debugging — once telemetry picks this up, the next time a submarine swap stalls you should see exactly which lock is held. Worth checking the new metric names against fulmine's boltz integration to confirm the labels line up.

## TL;DR

- **Pick a TX_TOO_LARGE strategy for go-sdk** (hardcoded cap vs weight-based) and open a PR — also note ts-sdk and dotnet-sdk need the same guard, so a single design doc could land all three.
- **Decide go-sdk's stance on the Arkade extension API.** If yes, the ts-sdk v0.4.32+v0.4.33 surface is the spec; if no, document that go-sdk users must drop to arkd RPCs for covenant apps.
- **Tag a go-sdk release** to ship the VHTLC handler from master (`5705340`) — it's been sitting on master since 2026-05-29.
- **Local hygiene**: fulmine and go-sdk worktrees both have uncommitted `.gitignore` (and go-sdk has a stray `.worktrees/`) that's blocking daily-update from pulling. Stash or commit to keep the sync clean.
- **Check fulmine boltz lock metric labels** against PR #1428 from boltz-backend.
