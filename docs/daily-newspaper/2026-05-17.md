# Ark Daily — 2026-05-17

> _Quiet Sunday — zero commits on fulmine, go-sdk, arkd, wallet, or introspector in the last 24h. The unresolved fires from earlier in the week (go-sdk P0 #176, auto-settle race, compiler CI) all carry over. Nothing new landed, nothing got fixed._

## Front page

- **go-sdk #172 / P0 #176 are now day 13** — `ListKeys()` returns empty after contract creation (HD wallet + Contract Manager broken in combination); 43+ days since last go-sdk release (v0.9.1). No fix on master. (see Wallet beat → go-sdk)
- **Auto-settle race condition (GOV#20, PR #169) still unfixed** — protocol-critical: auto-settle and user submissions race on the same VTXO → `VTXO_ALREADY_SPENT`. Confirmed 2026-05-15, no PR on master yet. (see Slack briefing → Most important)
- **ts-sdk HD Phase C2 receive rotation merged (PR #489, 2 days ago)** — Andrew Camilleri's fourth attempt at HD receive rotation, this time routed via contract repository (dotnet-sdk's design). go-sdk has no equivalent. (see Parity desk)
- **dotnet-sdk landed unilateral exit support (PR #39, 5 days ago)** — Andrew Camilleri. This is SDK-level exit; go-sdk does not have an equivalent surface yet. (see Parity desk)
- **compiler Build CI broken since 2026-05-15** — "compiler validation layer" merge broke the build; blocks StabilityVault, LayerZero/USDT0, pooled-model contract work. Not in your scope, but downstream contract types that ts-sdk would have to surface depend on it. (see Slack briefing)

## Wallet beat

### fulmine
No commits in last 24h. Master is clean except for local uncommitted `.gitignore` edit on your dev box (the daily sync reported "SKIPPED: uncommitted local changes"). Most recent merged work is still PR #408 GetVHTLCTransaction RPC (6 days ago).

### go-sdk
No commits in last 24h on master or your active branch (`claude-electrum-test-break`, no upstream).

Status of the carry-over fires:
- **P0 issue #176** — `ListKeys()` empty after contract creation. Day 13. Root cause: Contract Manager (PR #145) interacts incorrectly with HD wallet key store (PR #143). Both were merged unreviewed. No fix on master.
- **P1 auto-settle race** — GOV#20 / PR #169. Auto-settle scheduler and user submission path both spend the same VTXO concurrently with no mutex. Confirmed 2026-05-15.
- 43 days since last release (v0.9.1, 2026-04-03). The CI failure streak that hit "8 failures/day" on 2026-05-11 has since gone quiet — first quiet CI day in 23 days on 2026-05-16 — but the underlying P0 + race + 4 unreviewed merges all remain.

Your own most recent work (last 7d) — for reference: `9228c9d` Cursor-based pagination (2d), `cceecee` Add paginated VTXO listing with filter support (3d), `f184aad` Remove KV store #173 (4d), `625a2e2` Update Balance method #140 (4d).

### arkade-wallet (PWA)
No commits in last 24h. Recent landings (last 5 days) are stability-and-polish: `1cfdb166` Fix assets values (#611), `787bf2e2` LNURL session extracted to app-level provider (#559), `9a3a0b08` Add contracts view (#618), `e84af680` Fix qrcode copy button (#617), `0d98677c` Upgrade boltz-swap 0.3.31 (#615). Not cosmetic-only — LNURL session lifecycle and contracts view are real product surface — but nothing protocol-touching.

## Parity desk

Pulling from arkana's `project-context/sdk-parity.md` (most recent entry dated 2026-05-16 — fresh).

**Gaps where go-sdk lags:**
- **HD wallet receive rotation on incoming payments** — only ts-sdk has this. PR #489 (HD Phase C2, merged 2026-05-15) rebuilds `offchainTapscript` on `vtxo_received` and registers a new default contract via ContractManager. go-sdk, rust-sdk, and dotnet-sdk have BIP-32 derivation but no automatic receive descriptor rotation triggered by incoming payments. Parity report: "deepens the existing HDDescriptorProvider gap."
- **HD wallet base feature has a P0 regression** — go-sdk HD wallet (PR #143) was ✅, now downgraded to ⚠️ in the parity report after issue #176. Feature present but `ListKeys()` is broken after any contract creation.
- **VtxoManager auto-renewal** — go-sdk has the feature (PR #169) but the race condition makes it "actively harmful to users who initiate manual payments while the scheduler is running." Per the parity report: "worse than ⚠️ — broken-by-design in concurrent usage."

**Recent sibling-SDK landings (last 7 days), worth your awareness:**
- **dotnet-sdk PR #39** (`a89d47a`, 5d, Andrew Camilleri) — **unilateral exit support**. SDK-level exit surface. go-sdk has nothing equivalent.
- **dotnet-sdk PR #79** (`8561378`, 6d, Andrew Camilleri) — **multi-provider swap architecture (Boltz)**. Generalises beyond a single Boltz provider.
- **dotnet-sdk PR #90** (`f15d274`, 6d, Andrew Camilleri) — **reconcile pending Arkade txs stranded between Submit and Finalize**. Recovery for stuck transactions. fulmine got the same fix on 2026-05-11; go-sdk has no recovery path documented.
- **dotnet-sdk PR #75** (`9f60e18`, 6d) — payment tracking improvements (merged after 31 days; race condition concern from review may be unresolved).
- **dotnet-sdk PR #92** (`091fc7e`, 5d) — efcore SQLite ORDER BY fix via DateTimeOffset → long ticks mapping. dotnet-specific, not a parity item.
- **rust-sdk `256b964`** (5d, Bonomat) — `feat(boltz): send referralId on swap requests`. Lone commit. go-sdk's Boltz integration likely needs the same.
- **ts-sdk PR #489** — HD Phase C2 (above). Also PR #490 regtest bump (CI only).

## Slack briefing

### Decisions and blockers (last 7d, filtered to dusan's scope)

- **2026-05-16** — Quiet day. **compiler Build CI: 3 failures**, escalating from 1× on 2026-05-15; the "compiler validation layer" merge broke the build. Blocks StabilityVault, LayerZero/USDT0, pooled-model. go-sdk CI: 0 failures — first quiet day in 23+ days.
- **2026-05-15** — Several merges relevant to you:
  - ts-sdk PR #489 HD receive rotation **merged** (governance concern: StabilityVault PR went `ready_for_review` → merged the same day, no review window).
  - **Auto-settle race** issue opened (PROTOCOL-CRITICAL, real-money impact).
  - **#dev clarifications** (@U0854KRKV3Q): `/vtxos` is p2tr only — no asset-specific filters planned; instead a future "packet filtering mechanism." `/tx/finalize` can be called anytime after `/tx/submit` with the same txid. **Boltz VHTLC claim protocol**: no Boltz API call needed — detect HTLC VTXO sent to your key + preimage → spend from claim path; Boltz detects the witness spend, extracts preimage from the `condition` PSBT field, claims LN inbound.
- **2026-05-14** — go-sdk P0 #176 (`ListKeys` empty after contract creation) opened. Both Enclave KMS autonomy PRs merged. `OP_ECADD/OP_ECMUL/OP_ECPAIRING` PR opened (EC opcodes for introspector). "Add paginated VTXO listing with filter support" opened (your PR).
- **2026-05-13** — Fleet-wide CI root cause found (`boltz/boltz:latest` vs `arklabshq/fulmine:v0.3.21` mismatch); fixed via arkade-regtest PR #23. **Remove KV store merged in go-sdk** (your #173) despite risk flag from 3 unreviewed merges on master.
- **2026-05-12** — "Remove KV store" PR opened. CodeRabbit AI opened its first autonomous PR (governance concern). GetVHTLCSpendingTx RPC merged in fulmine.
- **2026-05-11** — **arkd v0.9.5 released** (GOV#12 exit delay hardcode + GOV#17 now in production). `feat(recovery): reconcile pending Arkade txs stranded between Submit and Finalize` merged in fulmine. uint64 underflow bug in commitment tx handler opened — critical. GetVHTLCTransactionRPC PR opened.

### Research findings (last 7d, filtered to your scope)

- **2026-05-15 (Optech #405)** — **BIN-2026-0002 assigned for OP_CHECKCONTRACTVERIFY (OP_CCV)**. Salvatore Ingala's covenant opcode designed for MATT; directly relevant to Ark introspection patterns. **OP_TEMPLATEHASH** on Inquisition signet (#100). **PSBTv2 (BIP370) now in Bitcoin Core** — directly relevant to Ark's collaborative tx building.
- **2026-05-15** — **BOLTs #995 standardized**: simple taproot channels (P2TR + MuSig2) — Ark↔LN interop now standardized on the same primitives. **BOLTs #1228**: zero-fee commitment channels standardized.
- **2026-05-13** — **Chimera Wallet integrates Arkade as primary payment layer** ($15M from Nimbus Capital, May 27 TGE, PWA-based v0.2). First major third-party wallet using Arkade. Worth watching for SDK integration quality — open question: are they on arkade-os SDK or custom?
- **2026-05-15 — CTV signaling still 0%** (~12 weeks). Effectively dead for this activation window.
- **2026-05-11** — **CVE-2024-52911**: use-after-free in Bitcoin Core script validation v0.14.0–v28.x; fixed in v29.0; ~43% of nodes still vulnerable. arkd operators should verify they're on v29.0+.

### Most important — expanded

1. **go-sdk P0 #176 is now 13 days unfixed and it's the gating reason there's no release.** The parity report says go-sdk's HD wallet + Contract Manager combination is "effectively broken" — `ListKeys()` returns `[]` after any contract creation because PR #145 (Contract Manager) and PR #143 (HD wallet key store) interact incorrectly. Both were merged unreviewed. As maintainer this is your call: someone has to either fix #176 or revert one of the two PRs. The longer it sits, the harder a revert gets because new code lands on top — your "Remove KV store" #173 is one of those layers.
2. **Auto-settle race is protocol-critical and silent failure.** The PROTOCOL-CRITICAL flag from slack-monitor 2026-05-15 isn't a CI thing — it's a real-money correctness bug. Auto-settle scheduler (PR #169) and user-initiated submissions both attempt to spend the same VTXO with no mutex. User-facing symptom: `VTXO_ALREADY_SPENT` errors during manual payments when the scheduler happens to fire. Until this is fixed, **auto-settle is actively harmful to any user who also makes manual payments** — which is to say, all users. The parity report calls it "broken-by-design in concurrent usage."
3. **Andrew Camilleri's dotnet-sdk has now landed unilateral exit, multi-provider swap, and pending-tx recovery in 6 days.** Three sibling-SDK landings in your week that go-sdk does not have. The recovery one (#90) is the same shape as fulmine's recovery PR from 2026-05-11. Unilateral exit (#39) is a new SDK surface entirely. Worth deciding: do these become go-sdk roadmap items, or is go-sdk deliberately leaving them out of scope?

## TL;DR

- **Triage #176 today** — either commit a fix or decide on a revert plan for PR #143 or #145; this is now blocking everything go-sdk.
- **Get a mutex onto the auto-settle / submit path** before the next release — the race is real-money and the parity report has formally downgraded the feature.
- **Read PR #489 (ts-sdk HD Phase C2)** even briefly — it uses dotnet-sdk's contract-repository pattern for receive rotation, and is the obvious template if you ever wire HD receive rotation into go-sdk.
- **Decide on parity for dotnet-sdk #39 (unilateral exit) and #90 (pending-tx recovery)** — roadmap or explicitly out-of-scope; either is fine, but the gap is now visible.
- **Optional**: scan Optech #405 — OP_CCV BIN assignment + PSBTv2 in Bitcoin Core are the two protocol-side items that touch Ark's design space this quarter.
