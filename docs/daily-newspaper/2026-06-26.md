# Ark Daily — 2026-06-26

> _Quiet day on dusan's own turf: fulmine and go-sdk both sat on uncommitted local WIP (no upstream commits), one arkd interceptor-ordering fix landed that shifts startup error precedence, and ts-sdk shipped a breaking arkfee rename. Slack/research feeds are stale (newest Jun 19) — the June 22 signer rotation has come and gone with no fresh confirmation in the logs._

## Front page

- arkd `#1125` reorders gRPC interceptors so the **readiness** handler runs first — a not-ready server now returns the readiness error before `ERR_VERSION`/digest/macaroon errors. If go-sdk or fulmine reconnect logic branches on startup error type, expect a different error first. (see Protocol surface)
- **ts-sdk shipped a breaking arkfee rename**: `Estimator.eval()` → `evaluate()` (`#581`, BREAKING). Parity signal — check go-sdk's fee-estimator surface for the same naming. (see Sibling SDKs)
- **rust-sdk landed server-info TTL refresh** (`#251`) incl. signer-rotation TTL refresh + offline client config. Worth mirroring in go-sdk for rotation correctness. (see Sibling SDKs)
- Both **fulmine and go-sdk were skipped on the sync box for uncommitted local changes** — local WIP is sitting un-pushed; nothing landed upstream in 24h. (see Wallet beat)
- **Slack & research logs are 7 days stale** (newest entry Jun 19). The Jun 22 rotation, the go-sdk CI instability, and `ARKD_SIGNER_ADDR` rollout are all unverified in current data — confirm outcomes manually. (see Slack briefing)

## Wallet beat

### fulmine
No upstream activity in 24h. Sync skipped the repo for **uncommitted local changes** — local WIP present, nothing pushed. CI status unknown from available data.

### go-sdk
No upstream activity in 24h. Sync skipped the repo for **uncommitted local changes** — local WIP present, nothing pushed. (Last known Slack signal, Jun 19: go-sdk CI was unstable — 6+ ci_integration + 3 ci_unit failures — heading into rotation; status since is unverified.)

### arkade-wallet (PWA)
Cosmetic only. Two commits by João Bordalo: `e512413` make boarded-funds link point to arkade.space (`#699`), `2f208c27` update regtest link (`#698`). No functional change.

## Sibling SDKs

_(Formal Parity desk skipped — newest parity report is `sdk-parity-2026-04-03.md`, >14 days stale. Notable sibling landings in the last 24h, surfaced manually:)_

- **ts-sdk** — `0b00d834` `fix(arkfee)!`: rename `Estimator.eval()` → `evaluate()` for SES/Snap compatibility (`#581`, Andrew Camilleri) — **BREAKING**. Also `e96dbe63` boltz-swap `descriptionHash` on reverse swaps (`#576`), `c9a7e75` service-worker stale-identity guard (`#571`).
- **rust-sdk** — merged `feat/server-info-ttl` (`#251`, Lucas Soriano): refresh server info by TTL, keep batch server-info snapshot consistent, signer-rotation TTL refresh, offline client config.
- **dotnet-sdk** — `220d758` `feat(tests)`: add rust-sdk test vectors (`#149`, d4r).

go-sdk consideration: the rust-sdk **server-info TTL refresh** and the ts-sdk **arkfee `evaluate()` rename** are the two items most likely to imply a go-sdk follow-up.

## Protocol surface — arkd → go-sdk

- **gRPC interceptor reordering + digest panic guard** — `67332ef` (louisinger, `#1125`). Touches `internal/interface/grpc/interceptors/`. Readiness handler now runs *before* version-compat, digest, and macaroon-auth handlers (was last); `Settings.Digest()` returns `"settings not initialized"` instead of nil-dereferencing when `SignerPubkey`/`ForfeitPubkey` are unset.
  - **Impact on go-sdk**: No proto/signature change — nothing to regenerate. But the error a client sees against a still-booting server changes: the readiness error now precedes `ERR_VERSION`/digest/macaroon errors. If go-sdk's client init or reconnect logic branches on these error types during startup, the readiness error will surface first.

## Slack briefing

> ⚠️ Source data is stale — newest `slack-log.md` entry is **2026-06-19** (7 days old). The items below were the live concerns heading into the Jun 22 rotation; treat as "verify current status," not "today's news."

### Decisions and blockers (from arkana)
- **go-sdk CI instability** (Jun 19) — 6+ ci_integration + 3 ci_unit failures, flagged as the most critical CI risk before the Jun 22 rotation. Status since unknown. (#dev / CI monitor)
- **`ARKD_SIGNER_ADDR` now required** (Jun 19) — standalone arkd-signer extraction means deployments must point this env var at the signer service; ops gate for the rotation architecture.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** (Jun 19, open) — PROTOCOL-CRITICAL for rotation correctness; flagged for urgent review before Jun 22.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question** (Jun 19) — should settlement trigger when *one* VTXO is near-expiry vs the current all-VTXOs gate? Affects settlement aggressiveness and user-facing latency.
- **Subscription ID logging / SDK adoption pending** (Jun 18) — a one-shot subscription variant (create ID + subscribe in one call) shipped in prod "a version or two ago"; SDKs (incl. go-sdk) haven't adopted the new subscription model yet.

### Research findings (from arkana)
- **LND Zero-Timestamp Gossip DoS** (Delving Bitcoin, Jun 18) — crafted zero-timestamp gossip crashes LND <v0.20.1; ASPs/Lightning interop should confirm node versions are patched.
- **CTV signaling** still 0%, ~18 weeks into the timeout — no movement relevant to covenant-dependent design.
- **Botanix Spiderchain shutdown** (Jun 10) — a Bitcoin L2 wound down after 1yr mainnet despite working tech; users preferred wrapped BTC. Adoption cautionary tale.

### Most important — expanded
- **arkd interceptor reordering (`#1125`).** louisinger reordered the gRPC interceptor chain so readiness is evaluated before version/digest/macaroon, and hardened `Settings.Digest()` against a nil-deref panic on an uninitialised signer/forfeit pubkey. It matters to dusan because go-sdk wraps `pkg/client/` and fulmine sits on top: any client-side logic that distinguishes "server not ready" from "version mismatch" during startup/reconnect now sees the readiness error first. No regeneration needed, but a behavioral assertion in client tests could flip.
- **go-sdk CI instability into the rotation (Slack, Jun 19).** As of the last captured Slack snapshot, go-sdk had 6+ integration and 3 unit failures and was called out as the top CI risk three days before the Jun 22 signer rotation. Since that data is now a week old and the rotation date has passed, the most valuable thing dusan can do is confirm whether go-sdk CI was stabilised and the rotation completed cleanly — the logs don't yet show the answer.
- **ts-sdk arkfee `evaluate()` rename (`#581`, BREAKING).** Andrew Camilleri renamed `Estimator.eval()` to `evaluate()` for SES/Snap compatibility. For dusan this is a parity tripwire: if go-sdk exposes an analogous fee-estimator method, the SDKs are now diverging on a public name, and downstream Arkade-wallet code targeting multiple SDKs will need to branch.

## TL;DR

- **Verify the Jun 22 rotation outcome** — Slack data predates it. Confirm go-sdk CI stabilised, `ARKD_SIGNER_ADDR` rolled out, and the per-leaf signer-key fix merged.
- **Commit or stash your local WIP** — both fulmine and go-sdk were skipped on the sync box for uncommitted changes; nothing is pushed.
- **Check go-sdk's arkfee/estimator surface** against ts-sdk's `eval()`→`evaluate()` rename (`#581`) — keep public naming aligned.
- **Eyeball arkd `#1125`** if any go-sdk/fulmine reconnect path branches on startup error type — readiness now precedes version/digest/macaroon errors.
- **Consider mirroring rust-sdk's server-info TTL refresh** (`#251`) in go-sdk for signer-rotation correctness.
