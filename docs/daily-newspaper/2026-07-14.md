# Ark Daily — 2026-07-14

> _arkd carried the day: "Scale the DAG" reshapes the vtxo model and adds a `depth` field, plus a breaking DFS taptree-encode change — go-sdk needs a proto regen and fulmine/introspector need a taptree sanity check. Dusan's own repos (fulmine, go-sdk, arkade-wallet) were quiet._

## Front page

- **arkd shipped a breaking taptree encode change** — `TapTree.Encode()` now writes DFS-ordered caterpillar depths, not hardcoded depth=1 (`37faa97e`). If introspector re-encodes/byte-compares taptrees or fulmine relies on the old layout, verify now. (see Protocol surface)
- **go-sdk needs a proto regen**: arkd added `depth` (field 15) to `IndexerVtxo`/`types.Vtxo` and a `renewable_only` filter (field 10) to `GetVtxos` — both additive, no wire break, but bindings are stale. (see Protocol surface)
- **arkd "Scale the DAG" (#908)** replaces the per-vtxo `swept` bool with a marker-DAG model, drops the `swept` column, and ships migrations + a one-way-in-spirit backfill — DB operators must coordinate deploy. (see Protocol surface)
- **Your repos were quiet**: no commits in fulmine, go-sdk, or arkade-wallet in the last 24h. (see Wallet beat)
- **Slack/research feeds are stale** — arkana's last extract is 2026-06-19 (~3.5 weeks old); nothing fresh to brief. (see Slack briefing)

## Wallet beat

### fulmine
No activity in the last 24h (no new commits on origin/master; local checkout has uncommitted changes, skipped by daily-update).

### go-sdk
No activity in the last 24h. Note: it does have pending downstream work — regenerate proto bindings for arkd's new `depth` field and `renewable_only` filter (see Protocol surface).

### arkade-wallet (PWA)
No activity in the last 24h (`$WALLET_REPO` up to date at master).

## Protocol surface — arkd → go-sdk

arkd master advanced `557f7c5 → eb75b6b` (5 commits). Wrap-surface-relevant changes:

- **Marker-DAG vtxo model — "Scale the DAG" (#908)** `eb75b6ba` (Bob Smith). Replaces per-vtxo `swept` bool with `marker`/`markers` (JSONB); new migrations + `markerbackfill/`; touches `api-spec/protobuf/ark/v1/{indexer,types}.proto`, `internal/interface/grpc/handlers/{indexer,parser}.go`.
  - **Impact on go-sdk**: `IndexerVtxo` and `types.Vtxo` both gain `depth` (field 15, uint32) — regenerate proto bindings; any exhaustive vtxo-field mapping should pick up `depth`. Additive, no wire break.
- **`renewable_only` filter on GetVtxos (#1149)** `695cb53a` (Pietralberto Mazza). New `GetVtxosRequest` field 10 = union of spendable+recoverable; filters now documented as mutually exclusive and script-filter-only.
  - **Impact on go-sdk**: additive request field — regenerate bindings if go-sdk wants to expose the renewable filter; nothing breaks if ignored.
- **DFS-depth taptree encode (#1150) — BREAKING** `37faa97e` (louisinger). `TapTree.Encode()` in `pkg/ark-lib/txutils` now writes DFS-ordered left-caterpillar depths instead of hardcoded depth=1.
  - **Impact on go-sdk / downstream**: anything decoding, comparing, or re-encoding arkd-produced taptrees must match the new ordering. This is the one true semantic break in the batch.
- **GetVtxoChain perf** `eb75b6ba`: cursor-based pagination + lazy marker-window loading (`GetOffchainTxsByTxids`, `idx_checkpoint_tx_offchain_txid`). Fewer round-trips; no API break.
- Routine: stream-log interceptor fix `f8344a56`; Go bumped to 1.26.5 `c17eab0f`.

**DB operators**: the DAG migration drops the vtxo `swept` column and adds JSONB markers + backfill; `.down.sql` exists but the backfill is one-way in spirit — coordinate the deploy.

## Contract surface — introspector

Only routine activity in the last 24h (origin/master, 2026-07-13):
- `657475f` go1.26.5 (#110) (louisinger) — toolchain bump, matches arkd.
- `5a4d854` update arkd and arkd-wallet version in test env (Mihael Šinkec) — CI env only.

No new opcodes or grammar/parser changes. **Watch item**: arkd's DFS taptree-encode change (`37faa97e`) — if introspector byte-compares or re-encodes taptrees, confirm it matches the new caterpillar scheme. (Last opcode work, `OP_DIGEST` #98, was 2026-06-30 — out of window.)

## Slack briefing

_Source note: arkana's `slack-log.md` and `research-monitor.md` newest entries are dated **2026-06-19 / 2026-06-18** (~3.5 weeks stale). No current briefing available; the standing items below are the latest on record and several have likely resolved (the June 22 signer rotation and July 4 cutoff have both passed). Treat as background only._

### Standing items relevant to your scope (as of 2026-06-19, unverified today)
- **go-sdk CI instability** — flagged as the most critical CI risk ahead of the June 22 rotation (6+ ci_integration + 3 ci_unit failures). Worth confirming go-sdk CI is green now.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question** — open issue on whether settlement should trigger when at least one VTXO is near-expiry vs the current all-VTXOs gate. Affects settlement aggressiveness; relevant to fulmine/go-sdk settle paths.
- **Subscription ID logging / streaming model** — a streaming subscription variant that creates the ID in one call shipped in prod "a version or two ago"; SDKs (incl. go-sdk) hadn't adopted it. Possible go-sdk follow-up.

### Most important — expanded
- **DFS taptree-encode break (arkd #1150, `37faa97e`, louisinger).** arkd changed `TapTree.Encode()` to emit DFS-ordered caterpillar depths instead of always depth=1. This is today's only genuine semantic break: any code that decodes, re-encodes, or byte-compares arkd-produced taptrees — plausibly in introspector's contract path and in fulmine's VHTLC/taptree handling — will diverge if it assumed the old layout. Verify against the new scheme before it surfaces as a signature or comparison mismatch in production.
- **`depth` field + marker-DAG (arkd #908, `eb75b6ba`, Bob Smith).** The vtxo model moved from a `swept` boolean to a marker-DAG, and both `IndexerVtxo` and `types.Vtxo` now carry a `depth` field. For go-sdk this is a straightforward-but-required proto regen; skipping it means go-sdk silently drops the new `depth` data and stays blind to DAG depth per vtxo. Additive, so nothing breaks on the wire — but the bindings should be refreshed alongside the `renewable_only` filter.

## TL;DR

- **Regenerate go-sdk proto bindings** against arkd's updated `indexer.proto`/`types.proto` — pick up `depth` (field 15) and `renewable_only` (GetVtxos field 10). Both additive.
- **Verify the DFS taptree-encode change (`37faa97e`)** doesn't break fulmine's taptree/VHTLC handling or introspector's contract encoding before it bites in prod.
- **If you operate an arkd DB**, plan the "Scale the DAG" migration deploy — it drops the vtxo `swept` column and runs a backfill that's one-way in spirit.
- **Sanity-check go-sdk CI is green** — the last Slack record (stale, 2026-06-19) had it as the most unstable SDK; confirm that's since resolved.
