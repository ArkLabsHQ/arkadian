# Ark Daily — 2026-05-12

> _Loud arkd day: `pkg/ark-sdk` → `pkg/client-lib` rename lands (breaking) and v0.9.5 ships to infra; go-sdk and fulmine are quiet on master but the CI meltdown hit a new all-time record yesterday — go-sdk 8 failures in one day._

## Front page

- **arkd merged `pkg/ark-sdk` → `pkg/client-lib` rename (#1064, `89d93031`)** — breaking import-path change touching ~58 files and renaming `wallet` package → `identity`; every go-sdk and fulmine consumer will need import rewrites before bumping arkd. (see Protocol surface)
- **arkd v0.9.5 is now in production via ark-infra (`29b6cb8`)** — carries GOV#12 (exit-delay hardcode) + GOV#17 to mainnet; monitor for regressions tied to stuck-tx recovery that merged alongside it. (see Slack briefing → Decisions)
- **go-sdk CI hit 8 failures yesterday — new all-time single-repo record, day 19+ of meltdown** — fleet-wide CI is now the #1 unblock for cutting go-sdk's first release. (see Slack briefing → Decisions)
- **uint64 underflow in commitment-tx handler corrupts TxSent records with MAX_UINT64 amounts** — fix PR open; corrupt records may already exist in production stores. Affects anything reading commitment-tx amounts. (see Slack briefing → Most important)
- **fulmine has uncommitted local changes blocking daily sync** — `git status` shows `M .gitignore`; resolve before tomorrow's sync attempts re-skip. (see Wallet beat → fulmine)

## Wallet beat

### fulmine
- No master commits in last 24h.
- Daily sync **SKIPPED**: uncommitted local change (`M .gitignore`) in working tree at `$FULMINE_REPO`. Either commit or stash before next sync.
- CI escalating per slack-monitor: `ci_integration` (2×) + `ci_unit` (1×) on 2026-05-11.

### go-sdk
- No commits to master in last 24h. Checkout is on local branch `claude-electrum-test-break`; no upstream → daily sync skipped.
- Background: PRs #145 (Contract Manager) and #169 (Auto settle) merged 2026-05-08 with **zero reviews** (GOV#19, GOV#20) — still standing items for human audit before any release tag.
- Last master commit was `c8dbced` (2026-05-05, RBF output mapping fix #167). PR #113 (Boltz+VHTLC, dusan) was last flagged at 14 days idle in the most recent parity snapshot — still no movement visible.

### arkade-wallet (PWA)
- **wallet `982c8bc3`** — `fix(satora): rename lendaswap to satora` (#612, Bonomat). Cosmetic rename of swap-provider branding from "lendaswap" to "satora" — no API surface change. Worth noting only because "satora" is now the second swap provider being plumbed in alongside Boltz (see dotnet-sdk #79).

## Protocol surface — arkd → go-sdk

Two commits in 24h, but one of them is large and breaking.

- **`89d93031` (Pietralberto Mazza) — "Client-lib: renaming of packages and components" (#1064)**
  - Renames `pkg/ark-sdk` → `pkg/client-lib`, `wallet` package → `identity`, `ark_sdk.go` → `wallet.go`, `wallet/singlekey/wallet.go` → `identity/singlekey/identity.go`. Diff: +723 / −817 across 58 files; `service.go` has ~87 lines of signature churn.
  - **Impact on go-sdk**: heavy. Every import of `pkg/ark-sdk/...` must be rewritten to `pkg/client-lib/...`, and `wallet.Wallet` references → `identity.Identity`. Do not bump arkd in go-sdk until the import-rewrite PR is staged; re-verify call sites in `service.go` against the new exported signatures.
- **`f8aefab4` (Se7enZ) — "ci: Remove empty codeql workflow file" (#1054)**
  - **Impact on go-sdk**: none. Removes a 1-line empty workflow.

## Contract surface — introspector

No commits in last 7 days. No new opcodes, no grammar changes, no contract-semantics movement.

## Slack briefing

### Decisions and blockers (from arkana)
_Source: arkana-knowledge `memory/slack-log.md` (last 5 days)._

- **arkd v0.9.5 released and deployed via ark-infra** — GOV#12 + GOV#17 now in production. (slack-log 2026-05-11)
- **"feat(recovery): reconcile pending Arkade txs stranded between Submit and Finalize" MERGED** — protocol-critical stuck-tx recovery (dotnet-sdk PR #90, flagged 2026-05-10). Now in production alongside v0.9.5. Monitor for incorrect recovery = potential fund loss or double-credit. (slack-log 2026-05-11)
- **go-sdk first release remains P0** — release-automation merged 2026-05-07, but no human-cut tag; CI now at all-time worst, blocks the cut. (slack-log 2026-05-11)
- **"Feat/collaborative redeem with vtxos" CLOSED** — cooperative exit / collaborative VTXO redemption experiment abandoned. (slack-log 2026-05-11) — worth knowing if you were tracking that path for arkade-wallet UX.
- **"GetVHTLCTransactionRPC" PR opened** — new RPC endpoint for VHTLC tx fetch by ID; protocol-adjacent, will touch go-sdk's RPC client surface once it lands. (slack-log 2026-05-11)
- **fulmine CI escalating** — `ci_integration` (2×) + `ci_unit` (1×) on 2026-05-11. (slack-log 2026-05-11)
- **"feat: Fee estimator" marked ready_for_review** (opened 2026-05-08) — relevant if it lands in ts-sdk first; parity tracking will follow. (slack-log 2026-05-11)
- **"feat: multi-provider swap architecture (Boltz)" — dotnet-sdk #79 MERGED** — `ISwapProvider` interface, "satora" planned as second provider; the same satora rename you saw in wallet today. Most advanced swap abstraction of any SDK; go-sdk has no equivalent. (sdk-parity log 2026-05-11)
- **"Add swapProviderUrl to arkade config for Lightning support" MERGED** — Lightning swap provider URL now configurable in arkade. (slack-log 2026-05-11)

### Research findings (from arkana)
_Source: arkana-knowledge `memory/agent-logs/research-monitor.md` (last 7 days)._

- **CVE-2024-52911 (Bitcoin Core)** — use-after-free in block validation, v0.14.0–v28.x. Miner could crash nodes. Fixed in v29.0. ~43% of nodes still vulnerable. arkd operators should verify Bitcoin Core ≥ v29.0. (research-monitor 2026-05-11)
- **Taproot channels specified in BOLTs #995** (Optech #404, May 8) — P2TR + MuSig2 for Lightning channels. Direct relevance for Ark↔LN interop: both VTXOs and LN channels now standardized on the same primitives. (research-monitor 2026-05-09)
- **PSBTv2 in Bitcoin Core** (Optech #404) — multi-party PSBT construction support; possible win for Ark round participation flows. (research-monitor 2026-05-09)
- **Tether WDK locked to Spark** — USDT wallet developers default to Spark; competitive lock-in risk for Arkade Assets. (research-monitor 2026-05-09)
- **BitVM2 live on Citrea mainnet (Jan 27, 2026)** — first practical covenant-like system in production, no consensus changes needed. (research-monitor 2026-05-11)
- **LNHANCE bundle (CTV + CSFS + INTERNALKEY)** gaining traction as the most strategically aligned proposal for Ark; CTV alone still at 0% signaling (10 weeks). (research-monitor 2026-05-11)

### Most important — expanded

1. **arkd `pkg/ark-sdk` → `pkg/client-lib` rename is the single most actionable thing today.**
   Pietralberto Mazza merged #1064 yesterday (`89d93031`), renaming the entire arkd client-side package tree and collapsing `wallet`→`identity`. Every consumer in go-sdk and fulmine that imports `pkg/ark-sdk/...` will fail to compile on the next arkd bump. There's also ~87 lines of churn in `service.go`, so import-path search-and-replace is necessary but not sufficient — re-verify call signatures. Treat as a one-day rewrite PR in go-sdk first, then fulmine.

2. **uint64 underflow corrupting TxSent records is the worst correctness bug in the queue.**
   Slack-monitor flagged this on 2026-05-11: the commitment-tx sent-amount calculation can underflow, wrapping to `MAX_UINT64 - actual_amount`. Fix PR is open; corrupt records may already exist in production stores. If anything in go-sdk or fulmine reads commitment-tx amounts for display, accounting, or settlement logic, it should reject or scrub MAX_UINT64-shaped values until backfill clarity exists.

3. **go-sdk CI at 8 failures in a single day (19+ day meltdown) is now the binding constraint on your first tag.**
   Release automation has been merged for 5 days. The blocker is no longer tooling — it's that ci_integration + ci_unit are red on master with no root cause identified. Until that's debugged, you cannot safely cut v0.1.0 even after auditing the GOV#19/#20 zero-review merges.

## TL;DR

- Open a go-sdk import-rewrite PR for `pkg/ark-sdk` → `pkg/client-lib` and `wallet` → `identity` before any arkd bump.
- Resolve the uncommitted `M .gitignore` in fulmine working tree so daily sync doesn't keep skipping.
- Decide on go-sdk first-release strategy: either invest in CI root-cause (19+ days red is unsustainable) or scope-limit a tag that explicitly excludes the failing integration paths.
- Read the uint64-underflow fix PR carefully; if it changed the type of any field on the sent-amount path, that's another go-sdk surface to update.
- Park reading: "The Ark case for CTV" (Roose, Delving Bitcoin) — referenced in research-monitor 2026-05-05 as the canonical doc on why CTV helps Ark.
