# Ark Daily — 2026-07-16

> _Quiet on dusan's own repos — fulmine & go-sdk sync was skipped (dirty tree), no remote pull today. The day's real signal is in the sibling SDKs: ts-sdk aligned to arkd 0.9.14 and dotnet added GetVtxoChain pagination — both parity cues for go-sdk._

## Front page

- **fulmine & go-sdk had no synced activity today** — daily-update skipped both (uncommitted `.gitignore`), so nothing was pulled from remote; verify their real state manually. (see Wallet beat)
- **ts-sdk aligned its indexer/provider layer to arkd 0.9.14** (#616) — go-sdk should confirm whether it needs the same 0.9.14 alignment. (see SDK siblings)
- **dotnet-sdk shipped intent-authenticated GetVtxoChain pagination** (#158) — a parity item for go-sdk's `pkg/client` wrap. (see SDK siblings)
- **Wallet PWA removed the tabs concept (#782) and now shows 8-decimal BTC (#777)** — UX shifts in the wallet dusan is building toward. (see Wallet beat)
- **arkana's Slack & research feeds are stale** — last update 2026-06-19 (~4 weeks); no fresh decisions/blockers/research to surface. (see TL;DR)

## Wallet beat

### fulmine
No synced activity. daily-update **SKIPPED** fulmine (uncommitted local changes — `.gitignore`), so no remote commits were pulled today; the local clone is behind (HEAD still `3ea61e2`, Apr 24). No 24h visibility — check GitHub directly if you expect landings.

### go-sdk
No synced activity. Same story — **SKIPPED** for a dirty `.gitignore`; local HEAD is `7f6b8d0` (May 25). No 24h visibility from the mirror.

### arkade-wallet (PWA)
Two UI PRs merged (`ee30182 → 3d4d736`), both by João Bordalo — largely cosmetic/UX:
- `3d4d736` Show eight decimals btc (#777)
- `cc2b7479` removes concept of tabs (#782) — a navigation/IA change worth a glance since it touches wallet structure.

## SDK siblings

_(Parity desk proper is skipped — arkana's parity report is 2026-04-03, >14 days stale. These are today's raw sibling-SDK landings relevant to go-sdk parity.)_

- **ts-sdk** (`8f45350 → 6c6e533`):
  - `fb609250` feat: align indexer/provider layer with **arkd 0.9.14** (#616) — go-sdk parity check: does the client wrap need the same alignment?
  - `126bea23` fix: patch 12 dependabot alerts (vitest/vite/js-yaml/uuid/…) (#617)
  - `7aa575a2`/`c59751a9` unify `isSubdust` + use it for `sendBitcoin` dust check (#614) — dust-check consolidation; worth mirroring if go-sdk has duplicate dust logic.
  - `2cf00046` type `isArkError` name as `ArkErrorName`.
- **dotnet-sdk** (`be6ae62 → 2294ddd`):
  - `f752a8b` feat: **intent-authenticated GetVtxoChain pagination** (#158) — parity item for go-sdk.
  - `2294ddd` fix(exit): unilateral exit + regtest update (#154).
- **rust-sdk**: no activity today.

## Slack briefing

_No fresh Slack or research data — arkana's `slack-log.md` and `research-monitor.md` both stop at 2026-06-19 (~4 weeks stale). Nothing new in dusan's scope to report; flagged in TL;DR._

## TL;DR

- **Check go-sdk vs arkd 0.9.14**: ts-sdk just aligned its indexer/provider layer to 0.9.14 (#616). Confirm go-sdk's `pkg/client` wrap is on the same arkd version.
- **Watch GetVtxoChain pagination**: dotnet landed intent-authenticated pagination (#158); decide if go-sdk should follow for parity.
- **Unstick fulmine & go-sdk sync**: both were skipped today for an uncommitted `.gitignore` — commit/stash it so the daily mirror pulls again and this desk regains 24h visibility.
- **Feeds are stale**: arkana's Slack/research monitors last ran 2026-06-19. If you rely on this desk for team decisions, the upstream cron looks stopped — worth poking.
