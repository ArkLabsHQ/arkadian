# Ark Daily — 2026-07-11

> _Quiet on fulmine; go-sdk's swap persistence store (#205) landed, ts-sdk shipped a big offline-first + unilateral-exit release from Kukks that go-sdk still lacks, and arkd made an internal offchain-retry fix relevant to fulmine._

## Front page

- go-sdk **#205 "Add swap persistence store"** landed — SQLite-backed chain-swap/swap store + new migration; the persistence foundation for SwapManager. (see Wallet beat → go-sdk)
- ts-sdk shipped a large **offline-first wallet + full unilateral exit-package** release (Kukks, PRs #606/#608/#609). go-sdk has neither — the parity gap is widening. (see Parity desk)
- arkd **#1147**: a failed offchain tx at the *request* stage is now resubmittable to finalization (previously permanently rejected). Internal only — no go-sdk wrap-surface break, but behaviorally relevant to fulmine's retry flow. (see Protocol surface)
- arkade-wallet added **BRL fiat support (#743)** + an unlock-splash fix (#752); rest of the day was cosmetic. (see Wallet beat → arkade-wallet)
- Slack + research feeds are **stale (no updates since Jun 19–20)** — no fresh decisions, blockers, or research to report this cycle. (see Slack briefing)

## Wallet beat

### fulmine
No activity in the last 24h. Latest on origin/master remains `25c6720` "recover SendOffChain from stranded already-spent vtxos" (#450, Kukks, 2026-07-06).

### go-sdk
- `c8e7196` **Add swap persistence store (#205)** (Dusan Sekulic, 2026-07-10) — adds `swap/store/` with SQL-backed repos for chain swaps and swaps, domain models/errors, migration `20260708135000_add_swap_tables`, and sqlc-generated queries. Removes the old `swap/btc_htlc_contracts.go` (−131 lines); reworks `chainswap.go` / `chainswap_resume.go` to persist state. This is the persistence layer under SwapManager — your own workstream, flagged here because it's now on master.

### arkade-wallet (PWA)
- `c57d1a91` **Add Brazilian Real (BRL) fiat currency support (#743)** (Marco Argentieri, 2026-07-10) — functional: new fiat option.
- `590cff5d` **fix unlock splash (#752)** (João Bordalo, 2026-07-10) — cosmetic/UX.
- Everything else in the window (home-card borders, chart range, send-form autofill) was cosmetic polish.

## Parity desk

_Formal gap report is stale (latest `sdk-parity-*.md` is 2026-04-03, >14 days old) — gaps not recomputed. Fresh sibling-SDK landings below are from git._

**Recent sibling-SDK landings (last 7 days) go-sdk should consider:**
- **ts-sdk — offline-first wallet** (Pietro Grandi, merged #609): typed provider errors, best-effort sync, cached server-info fallback for offline boot, sync/connection state surfaced across the service-worker boundary, and offline diagnostics. go-sdk has no equivalent offline/degraded-state model.
- **ts-sdk — unilateral exit packages** (Kukks, merged #606/#608): pre-signed exit-DAG assembly, keyless idempotent exit-package executor, indexer/repository-backed exit-data sources, configurable exit-data capture (Lite default / Full opt-in), and VHTLC condition-witness finalization. This is a substantial unilateral-exit surface go-sdk does not expose — worth scoping for parity given fulmine relies on go-sdk.

## Protocol surface — arkd → go-sdk

- `557f7c5c` **Fix retry accepting an already requested but failed offchain tx (#1147)** (Pietralberto Mazza, 2026-07-10). Change is in `internal/core/domain/offchain_tx.go` — the `OffchainTx` state machine now allows retrying a *requested-but-failed* tx (clears `Stage.Failed`/`FailReason`, re-enters `OffchainTxRequestedStage`).
  - **Impact on go-sdk**: none to the wrap surface — no `pkg/client/`, `api/`, or `proto/` touched. Behavioral note for fulmine: a failed offchain tx at the request stage is now resubmittable to reach finalization instead of being permanently rejected.

## TL;DR

- Nothing urgent on fulmine — quiet day. Your go-sdk #205 swap store is on master; next step is wiring SwapManager on top of it.
- Skim the ts-sdk **unilateral exit-package** work (Kukks, #606/#608) — it's the largest cross-SDK feature you don't have; decide if/when go-sdk needs parity before the wallet leans on exits.
- Note the arkd #1147 behavior change: request-stage offchain-tx failures are now retryable — check whether fulmine's retry logic assumes they're terminal.
- No fresh Slack/research to action; the knowledge feeds haven't updated since Jun 19–20 — worth pinging whoever runs arkana's monitors if you expected newer extracts.
