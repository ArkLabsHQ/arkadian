# Ark Daily — 2026-05-11

> _Quiet Monday morning across master branches — but wallet feature work
> hummed on two branches yesterday, arkade-wdk shipped a Lightning swap
> config, and the arkd-side "SubmitTx stranded tx recovery" PR from
> Sunday is the one thing for dusan to weigh in on this week._

## Front page

- **arkd: SubmitTx-stranded-tx recovery PR is open** — broader follow-up to the SubmitTx→finalize hotfix that landed in arkd `42f58837` on 2026-05-08. Reconciles transactions already stranded between Submit and Finalize. Protocol-critical; go-sdk's client-lib wraps the affected path. (see Protocol surface)
- **go-sdk master has 3 zero-review merges sitting unreleased** — PR #143 (HD wallet), #145 (Contract Manager, GOV#19), #169 (Auto-settle, GOV#20) all on master, all self-merged by altafan. v0.9.1 (2026-04-03) does not include these. First-release P0 is increasingly load-bearing. (see Wallet beat → go-sdk)
- **arkade-wdk (Pietro's dev kit) added `swapProviderUrl` for Lightning support** — `cbd56b5` lands today via PRs #20 (release-script) and #21 (fix-ln). Relevant if Arkade wallet stack consumes WDK. (see Wallet beat → arkade-wdk)
- **João's "Fix assets values" branch landed 7 fixes yesterday** — BIP21 asset amount validation, encodeBip21Asset decimals, unitsToCents empty-string crash, fiat stale-closure. Branch `fix_assets_values` on wallet, not yet merged. Part of the broader bigint/asset-amount wave from ts-sdk v0.4.23. (see Wallet beat → arkade-wallet)
- **CI meltdown still ongoing** — dotnet-sdk 14 failures yesterday (15 day before, all-time record), go-sdk ci_unit day 18+. Blocks the dotnet-sdk #77 HD wallet code-review-defect fix and silently delays go-sdk's first release. (see Slack briefing)

## Wallet beat

### fulmine

No master activity in 24h. Last commit `dfac29f` on 2026-05-05 ("Bump go-sdk and client-lib for RBF bumpfee fix and single-key wallet", #406). Local working tree has `.gitignore` modified — daily-update skipped the pull. CI was last green for that release bump.

### go-sdk

- **No master commits in 24h**, no new tags. master remains at `c8dbced` (RBF output mapping fix #167, 2026-05-05).
- Local checkout is on branch `claude-electrum-test-break` — pre-existing working branch.
- One commit on `pr-145-tracker` branch on 2026-05-08 (`a3ea49e`, "tracker: introduce single-owner reconciler + typed event subscriptions") — outside the 24h window but relevant to the contract-manager surface.
- **Standing P0**: first release. GitHub Releases automation landed last week; only a human tag is missing. Three unreviewed master merges (#143, #145, #169) make this a "cut it but audit first" situation.
- **ElectrumX PR #160 status unclear**: companion test PR `test: failing tests + docs for review of #160` was CLOSED on 2026-05-09. #160 has 2 CRITICAL concurrency bugs (goroutine leak, concurrent TCP writes) flagged in code-review. Worth pinging altafan today to confirm whether #160 is abandoned or being reworked.

### arkade-wallet (PWA)

`origin/master` last commit `582320bd` on 2026-05-08 (ts-sdk 0.4.26 + boltz-swap 0.3.30 bump). All recent activity is on feature branches:

- **`origin/wt-home-v2-20260501` (Sahil, 4 commits 2026-05-10):**
  - `86040732` feat(wallet): redesign asset swap flow
  - `238212b4` style(wallet): refine activity and swap flows
  - `c1a8459f` style(wallet): polish wallet flows and settings
  - `8af80871` style(settings): neutralize settings row icons
- **`origin/fix_assets_values` (João, 7 commits 2026-05-10):**
  - `d81aaeba` adds BIP21 asset amount validation
  - `c7223018` adds missing asset decimals in encodeBip21Asset call
  - `eb148819` fix unitsToCents crashes on empty string
  - `f65ca6c1` add radix to parseInt
  - `84841ef6` guard against fractional millisatoshis
  - `d2c75b93` fix stale closure in fiat
  - `2c65f469` remove not used type

João's branch is the wallet-side coordination for the ts-sdk bigint/asset-amount wave (ts-sdk v0.4.23 made asset amounts BREAKING bigint). This branch is the wallet's catch-up to that change. Neither branch is open as a PR against master yet per the daily changelog.

### arkade-wdk (Pietro's WDK)

Today's pull moved master `b2c9f16 → cbd56b5` (6 commits):

- `cbd56b5` Merge PR #20 (release-script)
- `6e4fdf0` Merge PR #21 (fix-ln)
- `7eb1607` Add `swapProviderUrl` to arkade config for Lightning support
- `775c92b` Upgrade ts-sdk 0.4.26 — boltz-swap 0.3.30
- `25ac39a` Add release script
- `2d7263d` Rollback upgrade — lint issues

WDK CI was flagged as failing on 2026-05-08 (first sighting). After today's PRs, worth checking if CI clears.

## Parity desk

Most recent dated parity file is 2026-04-03 (38+ days old), but arkana's rolling `agent-logs/sdk-parity.md` is current. Pulling from that:

**Gaps where go-sdk lags (still open):**
- **bigint asset amounts** — ts-sdk v0.4.23 BREAKING. go-sdk still on int64. Cross-SDK coordination needed. (rolling parity log 2026-05-05)
- **HDDescriptorProvider Phase C1** — ts-sdk v0.4.23 only; rust-sdk has `Bip32KeyProvider`; go-sdk's HD wallet (PR #143 merged 2026-04-29) covers BIP-32 derivation but not the descriptor-provider abstraction.
- **Forfeit / unilateral exit key separation** — rust-sdk-only commit `2caa25d` allows distinct forfeit vs unilateral-exit keys. go-sdk and others unverified.
- **Multi-timelock VTXO discovery (`walletContractTimelocks`)** — ts-sdk v0.4.21 only.
- **Arkade Script + introspector client crates** — rust-sdk PR #212 only (`ark-script`, `ark-introspector-client`). go-sdk has no equivalent yet.

**Recent sibling-SDK landings (last 7 days):**
- **ts-sdk v0.4.26** (2026-05-08): ESM-compatible declaration imports (#485). No new API.
- **ts-sdk v0.4.25** (2026-05-07): Tier 2 VTXO ownership gating (#480/#482), pre-flight outpoints (#478), VTXO_ALREADY_SPENT reconcile by outpoint (#477).
- **ts-sdk v0.4.24** (2026-05-06): Unilateral-exit bundle (#479 — internal refactor + ConditionCSVMultisig fix), Tier 1 ownership gating (#481).
- **rust-sdk** (2026-05-05): PR #212 confirmed merged (Arkade script + introspector client); PR #213 (block-based unilateral exit delay fix); PR #218 (ReverseSwapData invoice metadata, BREAKING).
- **dotnet-sdk** (2026-05-08): PR #77 still blocked by 2 code-review defects + CI meltdown.

The ts-sdk VTXO-ownership-gating two-tier work (#480 series) is the most consequential parity item to track for go-sdk — wrong-script VTXO leaks are a correctness issue the go-sdk should not be exposed to.

## Protocol surface — arkd → go-sdk

No arkd master commits in the last 24h. Most recent relevant landings (sitting on master, last 3 days):

- **`42f58837`** (Pietralberto Mazza, 2026-05-08) — Hotfix: prevent SubmitTx failure from affecting finalization of pending tx (#1063). **Impact on go-sdk**: client-lib's submit/finalize state-machine wrapper inherits this behaviour. The broader recovery PR (open on arkd, see Slack briefing) builds on this.
- **`2999d666`** (Pietralberto Mazza, 2026-05-08) — client-lib: Add `WithReceiver` option + Update explorer/wallet interfaces (#1058). **Impact on go-sdk**: this is the surface go-sdk wraps. `WithReceiver` is a new client option; explorer's `GetUtxo` now accepts many addresses. Worth a one-line wrap in go-sdk's high-level API.
- **`2444c613`** (Dusan Sekulic, 2026-05-05) — client-lib: Fix single-key wallet empty KeyID (#1060). **Impact on go-sdk**: bug fix already on master; verify go-sdk's single-key wallet flow still calls the right KeyID path post-fix.
- **`216951ee`** (louisinger, 2026-05-05) — Add tests for unrolled VTXO rejoining batch (#1055). **Impact on go-sdk**: test-only — confirms the protocol PR that enables unrolled VTXOs with assets to rejoin a batch. go-sdk should consider whether its VTXO state-machine handles "rejoined" state.

## Contract surface — introspector

No introspector activity in the last 7 days. Master is unchanged. No new opcodes or grammar changes.

## Slack briefing

### Decisions and blockers (from arkana)

- **arkd "feat(recovery): reconcile pending Arkade txs stranded between Submit and Finalize" PR opened (2026-05-10)** — Sunday — protocol-critical follow-up to the SubmitTx hotfix that already merged on 2026-05-08. Reconciles transactions ALREADY stranded in the Submit→Finalize gap, not just future prevention. Touches in-flight tx state machine. Needs urgent human review before merge.
- **dotnet-sdk CI meltdown — 14 failures yesterday (15 the day before)** — 3rd consecutive day at 9+/day, all-time single-day record. dotnet-sdk completely non-functional. Blocks dotnet-sdk #77 (HD wallet recovery, 2 must-fix defects).
- **go-sdk ci_unit (1×) — day 18+** — meltdown ongoing, no root cause found.
- **arkd-pentester CI failing again** — was 7+ consecutive days of failure in late April, now recurring. Verify whether pentest harness is broken vs. real regressions caught.
- **3 unreviewed go-sdk merges on master** — #143, #145, #169 — first release P0 cannot be cut safely without human audit of all three.
- **ee2e-kv Verify Enclave failing** — continuing.

### Research findings (from arkana)

- **Optech #404 (2026-05-08): Taproot channels specified (BOLTs #995)** — P2TR + MuSig2 standardised in Lightning. Eclair defaults them on (feature bits 80/81). Direct benefit to Ark↔LN interop — both Ark VTXOs and LN channels now standardise on P2TR + MuSig2.
- **Zero-fee commitment channels specified (BOLTs #1228)** — v3 relay + pay-to-anchor. Validates Ark's existing fee-bumping approach for VTXO exit transactions.
- **PSBTv2 (BIP370) implemented in Bitcoin Core** — multi-party tx construction natively. Potentially useful for Ark round participation workflows.
- **Lightspark Grid Global Accounts (Bitcoin 2026, David Marcus)** — Bitcoin-powered Visa principal membership. 175M merchants, 33 countries (→100 by EOY). Built on Spark + Lightning. The most significant competitive milestone for the Bitcoin L2 space since monitoring began.
- **CTV signaling still 0%** — 9 weeks. Continue clArk approach.

### Most important — expanded

1. **arkd "stranded pending tx recovery" PR is the one that matters this week.** The SubmitTx→finalize hotfix (`42f58837`, merged 2026-05-08) was the *prevention* patch; this Sunday-opened PR is the *recovery* patch that reconciles transactions already stuck in the Submit→Finalize gap. It touches the in-flight transaction state machine, which is exactly the surface go-sdk's submit/finalize wrapper depends on. Wrong logic here could either finalize transactions that should not be finalized, or fail to recover legitimate stuck ones. Worth dusan reading the diff carefully before it lands — both for arkd risk and for whether go-sdk needs a corresponding client-side change.

2. **go-sdk's first release is now blocked by three unreviewed master merges, not by tooling.** PR #143 (HD wallet), #145 (Contract Manager), and #169 (Auto-settle) all merged on master with zero reviews and self-merged by altafan (GOV#19 and GOV#20 in security-triage's log). The GitHub Releases automation that arkana flagged last week made the cutting mechanical; the real blocker is that you cannot in good conscience tag v0.10 over those three diffs without an audit. If you only have time for one of the three, audit #145 (Contract Manager, +large surface, the protocol-touchy one).

3. **Taproot channels are now formally in BOLTs.** BOLTs #995 + Eclair defaulting them on (feature bits 80/81) means Ark↔LN swaps via fulmine can plan for taproot channel support without waiting for further spec churn. P2TR + MuSig2 on both ends — Ark VTXOs and LN channels — opens cleaner atomic-swap constructions. Not urgent, but worth slotting on the fulmine roadmap because the LN side is now standardised and ts-sdk's boltz-swap is already on 0.3.30.

## TL;DR

- **Read the arkd "stranded pending tx recovery" PR** (Sunday, on arkd) — protocol-critical, touches the path go-sdk wraps. Comment before it merges.
- **Decide on the go-sdk v0.10 cut.** Three unreviewed merges on master; pick whether to audit-then-tag this week or carve them onto a release branch. The automation is no longer the bottleneck — your sign-off is.
- **Ping altafan re: go-sdk #160 (ElectrumX).** Companion test PR was closed Friday; status of #160 itself is unclear and it carries 2 CRITICAL concurrency bugs.
- **Glance at João's `fix_assets_values` wallet branch** before he opens a PR — 7 fixes around BIP21 / fractional-millisat / fiat closures, all tied to the ts-sdk bigint wave.
- **No urgency in introspector or in the sibling SDKs today** — quiet across the board.
