# Ark Daily — 2026-07-12

> _Quiet Sunday: ts-sdk cut releases 0.4.44→0.4.45 capping a 2-day offline-first wallet push, while dusan's own repos went dark — fulmine & go-sdk pulls were skipped over a stray local `.gitignore` edit, so there's no verified visibility into either today._

## Front page

- **ts-sdk shipped an offline-first wallet feature set** (typed provider errors, best-effort sync, diagnostics, service-worker connection state, cached server-info boot) over the last 2 days, released as `@arkade-os/sdk@0.4.44/0.4.45`. Directly relevant to the arkade-wallet PWA and a parity target for go-sdk. (see Wallet beat → Sibling SDKs)
- **fulmine & go-sdk did NOT sync today.** daily-update SKIPPED both pulls because each clone has an uncommitted `.gitignore` change (adding `.worktrees/`). You have zero fresh visibility into your own repos until that's cleared. (see Wallet beat)
- **No arkd protocol-surface changes in 24h.** Last client-affecting commits (#1147, #1144, #1141) are 2–4 days old — nothing new for go-sdk's wrap surface today.
- **Arkana Slack + research feeds are stale** — newest entries are Jun 19 / Jun 18, no data for the Jul 5–12 window. Your intel monitors appear to have stopped ~3 weeks ago. (see Slack briefing)

## Wallet beat

### fulmine
No verified activity in 24h. daily-update **SKIPPED the pull** — local clone has an uncommitted `.gitignore` edit (`+.worktrees/`). Local HEAD is stale (`3ea61e2` "Bump go-sdk@2d035b64" #401, ~3 months old), so remote state is unknown today. Clear the working tree to restore sync.

### go-sdk
Same story — pull **SKIPPED** over an uncommitted `.gitignore` edit, and that edit is malformed (`.worktrees.worktrees/` on one line). Local HEAD is 7 weeks old (`7f6b8d0` contract-registry custom handlers #186, `5f2ba69` idempotent contract manager #187). No verified activity.

### arkade-wallet (PWA)
No activity in the last 24h (up to date). Last commits are 2–3 days old and mostly cosmetic/fiat:
- `590cff5d` fix unlock splash (#752, João Bordalo)
- `c57d1a91` add Brazilian Real (BRL) fiat support (#743, Marco Argentieri)
- `90cf4832` preserve home card borders on first load (#747, Sahil Chaturvedi)

### Sibling SDKs — ts-sdk (parity-relevant, last 2 days)
The parity report is stale (2026-04-03, >14d) so the Parity desk is skipped, but this ts-sdk landing is fresh and worth tracking for go-sdk + PWA reuse. Released as `sdk@0.4.44/0.4.45`, `boltz-swap@0.3.49/0.3.50` (Pietro Grandi):
- `53ec42a2` feat: offline-first wallet — typed provider errors, best-effort sync, diagnostics
- `a447dfcb` feat: cached server-info fallback for offline wallet boot
- `53d91b27` feat: surface sync/connection state across the service-worker boundary
- `36899749` feat: expose provider-connection state from ExpoWallet foreground
- `7a7913b9` fix: don't misclassify structured 5xx as provider-unavailable
- `98d64b15` fix: close degraded-state gaps in offline diagnostics
- **Consider for go-sdk:** typed provider-error taxonomy + cached server-info boot are protocol-agnostic and would give go-sdk clients the same offline resilience.

## Slack briefing

_No fresh data: arkana's `slack-log.md` was last updated 2026-06-19 and `research-monitor.md` 2026-06-18 — nothing covers the Jul 5–12 window. The monitors appear to have stopped ~3 weeks ago; treat as an outage to investigate rather than a quiet week. (Historical June context: the June 22 signer rotation / July 4 cutoff have both passed; go-sdk CI instability was the top recurring blocker heading into rotation.)_

## TL;DR

- **Unblock your own repos:** the daily sync is skipping fulmine & go-sdk because of an uncommitted `.gitignore` edit (`.worktrees/`) — commit, revert, or configure sync to stash it. Bonus: go-sdk's line is malformed (`.worktrees.worktrees/`) — fix it.
- **Review ts-sdk's offline-first work** (`53ec42a2`→`010d6fb2`) for go-sdk parity and arkade-wallet PWA reuse: typed provider errors, cached server-info boot, service-worker connection state.
- **Investigate the arkana feed outage** — Slack + research monitors haven't updated since Jun 19; you're currently blind to team decisions and research signal.
- **Nothing urgent in arkd or introspector** — no client- or contract-surface changes in 24h.
