# Ark Daily — 2026-06-19

> _Quiet in dusan's repos (fulmine & go-sdk both untouched), but arkd changed the client error contract — go-sdk's digest-mismatch path now depends on gRPC status details. Signer rotation is 3 days out and go-sdk CI is red._

## Front page

- **arkd changed the client error contract**: guard/digest errors now arrive as typed gRPC `status` with `arkv1.ErrorDetails`; go-sdk's `isDigestMismatch` substring path is gone — confirm your client reads the new detail (see Protocol surface).
- **Signer rotation is June 22 (3 days)**, cutoff July 4 — and go-sdk `ci_integration` is showing 4 failures going into it (see Slack briefing).
- **Unilateral swap refunds + full refund test matrix** is `ready_for_review` — directly relevant to fulmine's swap safety net (see Slack briefing).
- **New single-call streaming subscription variant** shipped in prod "a version or two ago" but SDKs haven't adopted it — go-sdk update pending (see Slack briefing).
- **fulmine & go-sdk: no commits in last 24h** (both skipped by daily-update due to uncommitted local changes) (see Wallet beat).

## Wallet beat

### fulmine
No activity. (Repo skipped by daily-update — uncommitted local changes; no new commits in last 24h.)

### go-sdk
No activity. (Repo skipped by daily-update — uncommitted local changes; no new commits in last 24h.)

### arkade-wallet (PWA)
Two commits, both maintenance:
- `7a028c2f` add build and sdk versions to chatwoot custom attributes (#686) — support-tooling, version-aware triage (João Bordalo)
- `248d3b51` Upgrade ts-sdk 0.4.37 / boltz-swap 0.3.42 (#684) — dependency bump (pietro909)

## Protocol surface — arkd → go-sdk

- **Guard errors converted to gRPC `status` before reaching the client** — `0bae0249` (Dunsin Abisuwa, #1108). Guard failures (unary + stream) now return typed gRPC codes with structured `arkv1.ErrorDetails` instead of opaque errors.
  - **Impact on go-sdk**: `isDigestMismatch` now matches on `ErrorDetails.Name` via `status.FromError`, not error-message substring — DIGEST_MISMATCH detection/refresh depends on the server populating status details. Confirm go-sdk's client surface (and any error-string matching) picks up the new error-detail contract; the old substring path is removed.
- **arkd-wallet retries nbxplorer connection at startup** (30×/5s) — `ccda5c50` (Se7enZ, #1083). Internal resilience; affects nothing dusan calls.

## Slack briefing

### Decisions and blockers (from arkana)
- **Guard-error → gRPC-status PR MERGED** (#dev) — closes the 2026-06-13 "bare Unknown error" issue; DIGEST_MISMATCH and version-guard errors now return proper gRPC codes. Direct go-sdk client impact (see Protocol surface).
- **go-sdk `ci_integration`: 4 failures** — part of multi-repo CI instability (ts-sdk, dotnet-sdk, wallet also failing) persisting into the June 22 rotation window.
- **Subscription ID logging fix exists, SDK adoption pending** — @U0854KRKV3Q to @U0AMN06K5B7 (#dev): a streaming subscription variant that creates the ID in one call shipped in prod "a version or two or three ago"; SDKs haven't adopted it; needs SDK-side update (go-sdk relevant).
- **`feat(swaps): unilateral refunds + full refund test matrix`** — `ready_for_review`; non-cooperative swap refund path + full test matrix. Relevant to fulmine swap completeness; flagged for review before June 22.
- **Build version enforcement**: `BUILD_VERSION_TOO_OLD` "forceful nudge" planned around June 22; ts-sdk 0.4.37 / boltz-swap 0.3.42 released and pulled into wallet.

### Research findings (from arkana)
- **LND zero-timestamp gossip DoS** (Delving Bitcoin, June 18) — crafted gossip crashes LND <v0.20.1; mutinynet believed to run v0.19. Lightning-interop relevance for fulmine; fix is upgrade to v0.20.1+.
- **CTV**: still 0% signaling, ~18 weeks in (nearly halfway through timeout).
- **Transaction privacy survey** (nkaretnikov, June 18) — new coinjoin/payjoin/SP survey; may touch Ark privacy.

### Most important — expanded
- **arkd's client error contract changed under go-sdk.** PR #1108 (`0bae0249`, Dunsin Abisuwa) converts guard errors to typed gRPC `status` with `arkv1.ErrorDetails`, and `isDigestMismatch` now keys off `ErrorDetails.Name` rather than substring-matching the message. This matters this week because any go-sdk (or fulmine) code path relying on digest-refresh now silently breaks if the server isn't populating status details — and arkd v0.9.9 is already in prod with headers soft-launched. Verify go-sdk consumes the new error-detail shape before the rotation.
- **June 22 signer rotation is 3 days out, and go-sdk CI is red.** @U0854KRKV3Q confirmed rotation June 22 / cutoff July 4, with three key people unavailable on the day and "tests must be done now or never." go-sdk `ci_integration` is sitting at 4 failures across the rotation window. If go-sdk is in the validation path for the migration, a red CI is a real risk to confidence on rotation day.
- **Unilateral swap refunds are ready for review.** The `feat(swaps): unilateral refunds + full refund test matrix` PR adds the non-cooperative refund path plus a full scenario matrix — a critical safety net for swaps in production. Given fulmine's swap surface, this is worth a review pass before the rotation locks attention.

## TL;DR
- **Verify go-sdk reads `arkv1.ErrorDetails`** from gRPC status for digest-mismatch/guard errors — arkd #1108 removed the substring path that go-sdk likely relied on.
- **Stabilize go-sdk `ci_integration`** (4 failures) before June 22 — rotation day needs green confidence.
- **Review `feat(swaps): unilateral refunds + full refund test matrix`** while it's `ready_for_review` — fulmine swap safety.
- **Plan go-sdk adoption of the single-call streaming subscription variant** (already in prod; cuts log noise).
- **If fulmine touches mutinynet LN**, confirm LND ≥ v0.20.1 (zero-timestamp gossip DoS).
