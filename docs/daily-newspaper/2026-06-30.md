# Ark Daily — 2026-06-30

> _Quiet across dusan's stack: fulmine and go-sdk untouched, arkd cut two server-side fixes that don't touch the client surface, and the only wallet change was a cosmetic Paste button. Slack/research feeds are 10 days stale._

## Front page

- **Primary repos idle.** No commits in fulmine (last: Apr 24) or go-sdk (last: May 25) in the last 24h. (see Wallet beat)
- **arkd #1130 + #1131 are safe for go-sdk/fulmine** — scanner NBXplorer backoff + logging-interceptor metadata cap; both server-side, nothing under `pkg/client/`, `api/`, or `proto/`. (see Protocol surface)
- **dotnet-sdk fixed SSE subscriptions** by sending `Accept: text/event-stream` (#152) — worth confirming go-sdk's SSE/stream transport sets the same header. (see TL;DR)
- **boltz-backend moved 5 commits**, incl. ARK timeout-delta hardening (#1450) and a `send` gRPC hook (#1446) — relevant to fulmine's swap path. (see Wallet beat / TL;DR)
- **Slack + research logs are stale** (last write Jun 20). The briefing below is from the Jun 17–19 window — the June 22 signer rotation it counts down to has already passed; treat as historical and verify current state. (see Slack briefing)

## Wallet beat

### fulmine
No activity. Last commit `3ea61e2` (Apr 24, "Bump go-sdk@2d035b64"). Only an uncommitted `.gitignore` tweak locally; daily-update skipped the pull for that reason.

### go-sdk
No activity. Last commit `7f6b8d0` (May 25, "Contract registry: allow registering custom contract handlers"). Local `.gitignore` change only; daily-update skipped the pull.

### arkade-wallet (PWA)
Purely cosmetic: `a1ccfbf1` "always show Paste button (#700)" (João Bordalo). No logic/state change.

## Protocol surface — arkd → go-sdk

Two commits landed (`5fae202 → e621788`); **neither touches go-sdk's wrap surface** — included as confirmation, not action.

- **Scanner reconnects to NBXplorer with exponential backoff** instead of dropping out — `e6217887` (Chris Ricketts, #1130). Touches `core/application/scanner/` + `infrastructure/nbxplorer/`. **Impact on go-sdk:** none — arkd-wallet internal reliability fix, no client-facing change.
- **Logging interceptor caps "metadata of interest" value sizes** before logging — `ab51920d` (Chris Ricketts, #1131). Touches `internal/interface/grpc/interceptors/logger.go` only. **Impact on go-sdk:** none — server-side log hardening, no endpoint/signature/schema change.

## Slack briefing

> _Source log last updated Jun 20; entries below are from Jun 17–19. The June 22 signer rotation referenced has already occurred — verify post-rotation state rather than acting on the countdown._

### Decisions and blockers (from arkana)
- **go-sdk CI flagged heavily unstable** ahead of rotation — "6+ ci_integration + 3 ci_unit failures" (Jun 19), called "most critical CI risk." Worth confirming go-sdk CI is green now. (#dev)
- **`ARKD_SIGNER_ADDR` env var became required** with the standalone arkd-signer extraction (PROTOCOL-CRITICAL, Jun 19). Affects any deployment wiring fulmine talks to.
- **`feat(ark-client): narrow settle() to expired/recoverable VTXOs` MERGED** (Jun 17) — go-sdk's `settle()` now only settles expired + recoverable VTXOs, preventing premature settlement of valid ones. Behavior change dusan should be aware of.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question open** — should settlement trigger when *one* VTXO is near-expiry vs the current all-VTXOs gate? Open protocol-design call affecting settlement latency.
- **Subscription-ID streaming variant not yet adopted by SDKs** (Jun 18, kukks → U0AMN06K5B7) — a single-call open+subscribe model shipped in prod "a version or two ago"; SDKs (incl. go-sdk) still on the split model.

### Research findings (from arkana)
- **LND zero-timestamp gossip DoS** (Delving Bitcoin, Jun 18) — crafted `channel_update`/`node_announcement` crashes LND < v0.20.1; patched in v0.20.1+. Relevant to fulmine's Lightning/Boltz path; ASPs should verify LN node versions.
- **Botanix Spiderchain shutdown** (Jun 10) — Bitcoin L2 wound down after a year: tech worked, adoption didn't (users preferred wrapped BTC). Flagged as a cautionary case study for Ark.

### Most important — expanded
- **go-sdk `settle()` semantics narrowed to expired/recoverable VTXOs (merged Jun 17).** The `ark-client` change means a `Settle()` call no longer sweeps valid, non-expired VTXOs into a round — it targets only expired and recoverable ones. For dusan this is the single most consequential client-behavior shift in the window: any fulmine or go-sdk code that assumed `settle()` settles *everything* needs re-checking, and tests that asserted the old behavior may need updating.
- **Signer rotation required `ARKD_SIGNER_ADDR` and go-sdk CI was unstable going into it (Jun 19).** The standalone arkd-signer extraction made `ARKD_SIGNER_ADDR` a hard deployment prerequisite, and arkana flagged go-sdk's CI (6+ integration failures) as the top risk three days before the June 22 rotation. Since that date has passed, the action is verification rather than prep: confirm go-sdk CI is green and that nothing in fulmine's deployment assumes the pre-rotation signer wiring.

## TL;DR

- **Nothing urgent in your repos today** — fulmine and go-sdk had no commits; arkd's two fixes don't touch your wrap surface.
- **Check go-sdk's SSE/stream transport** sends `Accept: text/event-stream` — dotnet-sdk just fixed exactly this (#152); go-sdk may have the same latent gap.
- **Verify go-sdk CI is green post-rotation** — it was flagged as the top instability risk before June 22; the Slack log predates the rotation, so confirm current state directly.
- **Sanity-check `settle()` call sites** in fulmine/go-sdk against the merged "expired/recoverable only" narrowing — old assumptions about settling all VTXOs no longer hold.
- **Boltz-backend swap path moved** (timeout-delta hardening #1450, `send` gRPC hook #1446) — glance if fulmine's swap integration tracks boltz-backend closely.
