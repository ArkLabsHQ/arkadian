# Ark Daily — 2026-05-25

> _Sunday quiet across your stack: zero commits in fulmine, go-sdk, arkd, wallet, ts-sdk, dotnet-sdk and introspector in the last 24h. Only fresh items in scope are two community-contributor entries on go-sdk that opened late yesterday — issue #188 (regtest startup race) and PR #189 (poll arkd until ready, the fix). Enclave continued its self-merge streak overnight with #116 (GOV#65)._

## Front page

- **go-sdk #188 + #189 from a new contributor** — Dunsin-cyber opened a regtest startup-race bug report at 20:34Z yesterday and posted a polling fix PR 24 minutes later (#189). Small (`test/docker/setup.go`), benign, but your repo and worth a triage decision (see Wallet beat → go-sdk).
- **go-sdk #172 hits day 23 stalled** — release-coordinator continues to list it as the lead hard-block alongside the 5 P0 bugs and #182/#183. CI is green; the excuse is gone (see Wallet beat → go-sdk).
- **Enclave #116 (AWS profile support) opened and self-merged in ~40 min overnight** — GOV#65, 8th consecutive aruokhai self-merge. Not your code but is the same path that already serves e2ee.vtxos.com traffic (see Slack briefing).
- **Latest parity report (2026-05-24) confirms no new gaps** — arkana's snapshot details the go-sdk Contract Registry surface (Registry interface, `WithContractHandler` wallet option) which is still on `origin/contract-registry`, not master (see Parity desk).
- **Chimera TGE in 2 days (2026-05-27)** — still no confirmed answer on which SDK + version they ship. Last reading unchanged: 3-layer arch (mainchain + Lightning/Boltz + Arkade VTXOs), PWA.

## Wallet beat

### fulmine
No commits in last 24h. Master still at `3ea61e2` "Bump go-sdk@2d035b64" from 2026-04-24 (now ~one month idle). Release-coordinator still lists fulmine as **GATED** on #411 (two `CHANGES_REQUESTED` from arkanaai) plus the go-sdk#182 dependency. No new PRs or issues. Local working copy still has the same one-line `.gitignore` change (adds `.worktrees/`) — daily-update keeps skipping for that reason.

### go-sdk
No master commits in last 24h. Master last touched 2026-05-22 (`00c9407` "bump client-lib #184"). New since yesterday's newspaper:

- **Issue #188 — "make regtest sometimes fails because it talks to arkd before it's ready"** (Dunsin-cyber, 2026-05-24 20:34Z). Issue-triage's diagnosis: `test/docker/setup.go:39` does `time.Sleep(3s)` then immediately `get()` with no retry; transient network error → `os.Exit(1)`. `waitUntilReady()` also doesn't tolerate transient errors. Labeled `bug`. **Not protocol-critical.**
- **PR #189 — "fix: poll arkd until ready instead of failing on first connection error"** (Dunsin-cyber, 2026-05-24 20:58Z). The fix for #188 — replaces the fixed 3-second sleep with a poll loop (500ms interval, 60s timeout) and tolerates network errors in `waitUntilReady()`. Open, not merged. Release-coordinator notes it doesn't clear any of the 5 P0 bugs, but it's the kind of small infra fix you can land cheaply.
- **#172** — day 23 stalled, dirty merge conflicts, zero human reviews. Three weeks dead. Still your single highest-leverage item.
- **#186, #187** — Contract registry + idempotency, branch `origin/contract-registry` and `origin/feat/key-wrapper` respectively. Last commit on registry branch was 2 days ago (`30a5d5d` "handle insert race for single-key contract idempotency", Bob Smith). See **Parity desk** for the API surface details from arkana's parity report.

Working copy still has uncommitted local changes (`.gitignore` + `.worktrees/`) — daily-update skipped again today.

### arkade-wallet (PWA)
No commits in last 24h. Master still at `9848c02c` (5 days ago, `fix prettyAssetNumber` #626). Release-coordinator continues to list wallet as **HOLD** pending the ts-sdk v0.4.28 upgrade and issue #629.

## Parity desk

arkana's latest parity report is dated **2026-05-24** (well within the 14-day staleness gate). Reference versions: ts-sdk **v0.4.28** (released 2026-05-22) + arkd **v0.9.5** (2026-05-11). go-sdk has been **52 days without a release** (v0.9.1, 2026-04-03).

**Gaps where go-sdk lags** (unchanged since yesterday's edition, listed here as standing items):

- **`Wallet.restore()` (HD gap-scan recovery, user-facing)** — ts-sdk v0.4.28 has it as a static factory with `ContractManager.scanContracts` gap-limit discovery, `Discoverable` capability on contract handlers, and `SCAN_MAX_INDEX` guard. go-sdk: ❌. rust-sdk: `discover_keys()` (partial). dotnet-sdk: ⚠️ with PR #77 defects unresolved. _Parity report 2026-05-22, "HD wallet gap-limit recovery"._
- **`ElectrumOnchainProvider`** — ts-sdk has full provider; dotnet-sdk got `ArkNetworkConfig` URL defaults on 2026-05-20 (config-level parity); go-sdk has neither. _Parity report 2026-05-20._
- **Per-call `vtxoThreshold` on `renewVtxos`** — ts-sdk added `options.thresholdSeconds` on `IVtxoManager.renewVtxos()` (closes ts-sdk#388, post-v0.4.28 on master). go-sdk has no per-call threshold; rust-sdk and dotnet-sdk have no VtxoManager at all. _Parity report 2026-05-22._
- **Narrowed `settle()` semantics** — rust-sdk v0.9.0 narrowed `settle()` to expired/recoverable VTXOs and renamed full-renewal to `settle_all()`. ts-sdk still settles all by default. go-sdk's `Settle()` semantics: unchanged. **Divergence, not a gap** — but you may want to decide which model go-sdk follows when you next touch it. _Parity report 2026-05-22._

**Recent sibling-SDK landings (last 7 days, relevant to your wrap surface):**

- **rust-sdk #228 MERGED** (2026-05-24, Lucas Soriano) — `fix/exit-vhtlc`. Multiple fixes: avoid exponential unilateral-exit paths, validate condition witness decoding, finalize unilateral exit branches generically, VHTLC ancestor unilateral-exit test coverage. **If go-sdk has equivalent unilateral-exit logic, the rust-sdk diff is the cleanest specification of the bug + fix.** Cite commits: `da69cf2`, `97f856e`, `258006d`, `5304b64`.
- **rust-sdk #229 MERGED** (Bonomat) — `x-build-version` header on every request. Trivially copyable to go-sdk if there's interest in build telemetry.
- **rust-sdk #226 MERGED** (Bonomat) — pass invoice description through to Boltz reverse swaps. Affects the swap surface go-sdk wraps; check whether `pkg/client/` exposes a description path.
- **go-sdk `contract-registry` branch detail** (parity report 2026-05-24, not yet on master): `Registry` interface with `GetHandler(ContractType)` and `SupportedTypes() []ContractType`, `NewRegistry()` constructor merging built-ins with caller-supplied custom handlers (validates non-empty type, non-nil handler, no collision with built-ins). New `WithContractHandler(t, h)` wallet option propagates through `walletOptions.customHandlers` map into `ContractManager`. `mgr.Registry().SupportedTypes()` returns alphabetically deterministic listing. **Note:** arkana flagged the 9 commits on this work as direct-to-master pattern (GOV#21 candidate). They are still on branches, not master.

## Protocol surface — arkd → go-sdk

No new commits on arkd master in last 24h. Master last touched 3 days ago (`299b7ad6` "client-lib: Add WithTxOutsTaprootTree SendOption #1068", louisinger). The two PRs in flight that touch your wrap surface remain open and unchanged:

- **arkd#1080 — `fix: tolerate wallet connection loss`** (Dunsin-cyber, +26/-9, 3 files). Same contributor who filed go-sdk#188/#189. Branch CI is ❌ on `ci_integration` per yesterday's daily-briefing. **Impact on go-sdk**: server-side resilience — should be transparent to clients but could shift the error semantics on long-running streams in `pkg/client/`. Worth a 10-minute read before it merges.
- **arkd#1078 — "Split client-lib and client-wallet"** (131-file refactor, 2 CRITICAL + 8 HIGH code-review findings, flagged "seems bad" in #dev by @U07MSN1PNTU on 2026-05-23). Still open, still flagged. If it lands, this **will** ripple into go-sdk's `pkg/client/` since go-sdk wraps it. Status quo: do not merge until findings resolved.

## Slack briefing

### Decisions and blockers (from arkana)

Last ~7 days, only items still live or new since yesterday's newspaper:

- **go-sdk #172 → day 23** (release-coordinator 2026-05-25T04:00Z). Unchanged, zero human reviews. Listed alongside 5 P0 bugs as hard release block.
- **go-sdk #188 + #189 opened** (issue-triage 2026-05-25T00:00Z, release-coordinator 2026-05-25T00:00Z). New contributor, small infra fix, not protocol-critical.
- **enclave #115 MERGED** (release-coordinator 2026-05-24T16:00Z, GOV#64) — Route53 zone ID + A record management. APPROVED but flagged as GOV violation for landing on a HARD BLOCKED repo with ungated pipeline.
- **enclave #116 MERGED** (release-coordinator 2026-05-25T04:00Z, GOV#65) — AWS profile support. Opened 03:22Z, merged 04:02Z (~40 min). 8th consecutive aruokhai self-merge.
- **enclave #117 opened** (issue-triage 2026-05-25T08:00Z) — `Improve Environment Variables Handling`. Stub, enhancement label. Not protocol-critical.
- **VTXO persistent storage atomicity** — security-triage still cannot independently verify the 2026-05-23 merge resolved the 2026-05-17 repositories atomicity findings. Unchanged from yesterday's edition. Worth your verification if you touch persisted VTXO state.
- **4 concurrent open VHTLC PRs** (unchanged) — none signed off. Affects fulmine's VHTLC RPC surface.

### Research findings (from arkana)

Research-monitor runs Mon/Wed/Fri at 14:00 UTC — today's run hasn't happened yet. Last run was **2026-05-23**, already covered in yesterday's newspaper. Standing items relevant to your work:

- **Ibis Wallet adds Ark protocol support** (Optech #406, 2026-05-22) — second third-party wallet integration after Chimera. Android, power-user focus, by aeonBTC. _Adoption signal, no immediate action._
- **Bitcoin Core #34893** — `combinepsbt` now preserves BIP174 proprietary fields (was silently dropping them). Directly relevant to Ark's PSBT-based collaborative tx building. _Worth knowing when v32 lands._
- **LDK #4575 `splice_in_inputs`** — manual UTXO selection during splicing. Relevant for Ark↔Lightning bridge flows; if fulmine ever splices Ark-settled funds into LDK channels, this is the API.
- **PQ discourse** — 4 PQ threads in 6 days on Delving Bitcoin (no consensus). Long-term issue for Ark's Schnorr/MuSig2; no action this week.
- **Credible Exit paper** (BitcoinErrorLog, 2026-05-13) — Ark supports ~2M credible exits per 14-day window vs Lightning's ~1.1M (1.8×). Standing positioning material.
- **CTV signaling 0%, 13+ weeks** — dead for this window.

### Most important — expanded

1. **go-sdk #188 + #189 are the only fresh items in your direct scope today, and the contributor is new.** Dunsin-cyber opened both within 24 minutes of each other late on 2026-05-24. The bug is real (`time.Sleep(3s)` + immediate `get()` with no retry in `test/docker/setup.go:39`), the fix is small and idiomatic (poll loop with timeout, tolerate transient errors in `waitUntilReady`), and issue-triage already posted the analysis comment. Two things worth deciding today: (a) is the fix shape right, or do you want a different polling primitive; (b) Dunsin-cyber also opened arkd#1080 with similar shape — this is a same-pattern contribution streak that probably deserves a review/welcome cycle rather than getting batched behind the #172 backlog.

2. **The arkd#1078 client-lib split remains the highest-stakes thing on your wrap surface, and it has not moved.** It's been a week since @U07MSN1PNTU flagged it as "seems bad" in #dev. 131 files, 2 CRITICAL + 8 HIGH findings, all still unresolved. If it lands without remediation, every go-sdk caller of `pkg/client/` could break in subtle ways. Today it costs you nothing because nobody is touching it; if it suddenly self-merges (the pattern is in fashion this month), the next 48 hours of your work get reorganized. Worth checking PR status when you sit down.

3. **The contract-registry work is real progress that hasn't landed on master yet.** Per arkana's 2026-05-24 parity report, the `Registry` interface + `WithContractHandler` wallet option + alphabetical `SupportedTypes` are functionally complete on `origin/contract-registry` (last commit 2 days ago) — but it's still nine direct commits on a non-master branch with race-condition fixes in flight. arkana already filed it as a GOV#21 candidate if it merges direct-push. The work is good; the path it ships through matters. If you're the one pressing the merge button, a proper PR with review checkpoint is the cheap way to land it cleanly.

## TL;DR

- **Triage go-sdk #188 + #189.** New contributor, fixable small infra PR, opened together with arkd#1080 — give them a same-day review and either land or guide.
- **Land go-sdk #172.** Day 23. Three weeks dead. CI is green. Your single highest-leverage standing item.
- **Check arkd#1078 status when you sit down.** If it has moved, you want to know before it self-merges into your `pkg/client/` wrap surface.
- **Commit or shelve the local `.gitignore` and `.worktrees/` changes in fulmine + go-sdk** — daily-update has skipped both for at least three days.
- **No urgent research or Slack escalations today** — research-monitor runs again this afternoon; if anything fires, it'll be in tomorrow's edition.
