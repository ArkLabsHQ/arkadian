# Ark Daily — 2026-05-02

> _Quiet day on master for fulmine/go-sdk, but a HTLC arkade script v2 landed on introspector that reshapes how downstream callers build claims — coordinate before mirroring._

## Front page

- **introspector HTLC script v2 merged** (`697f94f`, louisinger): drops the hardcoded `amount` push, derives the input index via `OP_PUSHCURRENTINPUTINDEX`, switches to `OP_GREATERTHANOREQUAL`, and pins output[i]↔input[i] so multi-HTLC claims can be batched. **Witness shape changed: arkade witness is now empty (was `[output_index]`).** Anyone replicating this in fulmine/go-sdk needs to drop the witness element and stop pushing `amount`. (see Contract surface)
- **go-sdk first release still zero — C2+C3 CRITICAL findings on master unfixed**: C1 fix (key sort) is APPROVED but unmerged in #162; C2 (unsalted SHA-256 negates PBKDF2) and C3 (zero-value credential persistence) untouched. Five APPROVED PRs (#162/#160/#159/#158/#153) are held pending companion test PR #165 review. **No tag should be cut without human audit sign-off.** (see Slack briefing)
- **ts-sdk v0.4.23 imminent — versioning decision needed**: 3 merges yesterday (#461 Electrum e2e, #439 HD primitives, #440 Phase C1 HDDescriptorProvider). #472 (`fix!: bigint`, APPROVED) is a **breaking change** — if included, the tag must be **v0.5.0**, not v0.4.23. #460 (mainnet default) and #464 (ContractWatcher) are also candidates. (see Slack briefing → Most important)
- **ts-sdk Phase C1 HD wallet shipped; Phase C2 (#441) still ships a P1 race**: `offchainTapscript` is mutated while concurrent settle/send hold a reference — change VTXOs end up tracked under the wrong address. Code-review flagged it CHANGES_REQUESTED; not confirmed resolved. Alt PR #473 has its own state-desync bug (rotation before contract registration). (see Slack briefing)
- **PQ landscape shift in Optech #403**: Buchner's tapscript PQ keys are deployable **today** without a soft fork via BIP342 unknown-key handling; BIP54 Consensus Cleanup demoed on signet and is the most likely next soft fork — ahead of CTV (still 0% signaling for ~6 weeks). (see Slack briefing → Research findings)

## Wallet beat

### fulmine

No commits in the last 24h on master. Local clone has `.gitignore` modifications only — daily-update SKIPPED the repo for that reason. Nothing on github.com/ArkLabsHQ/fulmine has shifted since the previous edition.

Open thread to track: **fulmine ci_integration** has been failing 1–3× per day for over a week (slack-monitor 2026-04-27 → 2026-05-01). No root-cause yet.

### go-sdk

No commits on `master` in 24h. Local clone is on branch `claude-electrum-test-break` with **9 unpushed commits** authored by sub-agents (`Ark Developer <dev@arkade.sh>` and `Bob Smith`) — all electrum-explorer concurrency fixes + tests + docs that look like the workbench for go-sdk PR #160 (ElectrumX) and the companion test PR #165:

- `675c71d` docs: comprehensive electrum package documentation (lifecycle/concurrency contracts in `explorer/electrum/doc.go` — new file)
- `d85501c` test: robustness tests for in-flight request hang + serial resubscribe (A1 + A3 break repros)
- `d5621ea` test: electrum break tests + regtest integration tests (5 bug repros incl. Subscribe/Unsubscribe panic)
- `bc44bb4` fix four concurrency bugs in electrum explorer
- `154dd4c` fix electrum explorer: concurrency bugs, goroutine leak, and reliability issues

Code-review (slack-monitor 2026-05-01) flagged **2 CRITICAL concurrency bugs in #160** (goroutine leak + concurrent TCP writes). The local commits look like they address those — verify before pushing/merging.

### arkade-wallet (PWA)

`wallet` repo (`master`): no new pulls today (changelog: "Up to date"). Recent landings within 3 days were UI/test polish and a ts-sdk 0.4.22 + boltz-swap 0.3.23 bump:
- `04741938` Add bulk recovery button in Apps → Boltz → Settings (#581, pietro909)
- `ee44a83d` Upgrade ts-sdk 0.4.22 - boltz-swap 0.3.23 (#574, pietro909)
- `d15ed649` fix swap detail crash on restored chain swaps (#575, pietro909)

Wallet Playwright Tests have been failing 4× per day in the slack-monitor logs — keep an eye if you bump `wallet` next.

## Contract surface — introspector

- **HTLC arkade script v2** — `test/htlc_test.go` only (test changes — `+124/-41`); no opcode/grammar additions, but the script *shape* and *witness layout* both change. New `claim_multiple` test exercises the batched path. Commit `697f94f` (PR #76, louisinger).
  - **Cross-cutting impact** (per daily changelog): "fulmine/go-sdk callers building HTLC claims: drop the `output_index` witness element and stop pushing `amount`. Coordinate with whoever owns the `enforcePayTo` helper / arkade script builder."

## Slack briefing

### Decisions and blockers (from arkana, last ~7 days)

In dusan's scope:
- **ts-sdk#439 MERGED** (2026-05-01) — HD wallet primitives on SeedIdentity. Unblocks Phase C1 (#440 also merged) and Phase C2 (#441, race unfixed).
- **ts-sdk 0.4.22 released and merged downstream into wallet** (2026-04-29) — ContractWatcher vtxo event fix.
- **regtest images bumped to arkd v0.9.4 + fulmine v0.3.21** (2026-04-29) — regtest now tracks production.
- **arkd v0.9.4 deployed to production** (2026-04-28) — protocol-critical exit-delay change live.
- **Block-based unilateral exit delay PR opened** (2026-04-28), the day after seconds-based #456 merged — may signal #456's unit-of-time was wrong; protocol-critical, watch.
- **"Networking & Connection Investigation: Arkd Stack" issue opened** (2026-04-30) — formal post-mortem for the 4/24 SSE/HTTP-1.1 congestion incident; Traefik h2c migration on the table.
- **Multi-day blockers**: go-sdk CI 13+ consecutive bad days; rust-sdk E2E Matrix multi-week streak; bancod release pipeline broken; fulmine ci_integration escalating.

### Research findings (from arkana, last ~7 days)

- **Optech #403 (May 1)** — four parallel PQ wallet proposals, including Buchner's deployable-today tapscript PQ keys and Osuntokun's working zk-STARK quantum-recovery prover. Binary fuse filters offer 6×–80× speedup vs BIP158. **No CTV / OP_CAT / Ark mentions.**
- **BIP54 Consensus Cleanup** — Antoine Poinsot demoed slow-to-validate blocks on signet. Most likely next soft fork to activate, ahead of CTV.
- **CTV signaling still 0%** (period 470+, ~6 weeks of zero) — effectively dead this attempt despite continued developer enthusiasm on Delving Bitcoin (CTV+CSFS BitVM bridges, RobinLinus Apr 28).
- **Bitcoin 2026 (Apr 27–29)** — Adam Back (optional Taproot PQ extensions) vs Lopp (forced 5-year migration via BIP-361). Governance dynamics affect every soft-fork prospect including CTV.
- **No movement** on Spark, Mercury Layer, OP_CAT.

### Most important — expanded

1. **introspector HTLC v2 reshapes the witness contract**. _What:_ the arkade witness is now empty (was `[output_index]`), the script no longer pushes a hardcoded amount, and output[i]↔input[i] pinning enables batched multi-HTLC claims in a single ark tx. _Who:_ louisinger (PR #76, merged yesterday). _Why this matters for dusan:_ go-sdk's VHTLC code (PR #113, idle 14+ days as of last parity report) and any fulmine HTLC-claim path that builds witnesses against the v1 layout will silently produce malformed claims if pointed at a v2 script. Whoever owns `enforcePayTo` / the arkade script builder needs to coordinate this before downstream consumes it.

2. **ts-sdk's next tag is a versioning fork in the road**. _What:_ release-coordinator queued ts-sdk v0.4.23 with three merged PRs from yesterday — but #472 (bigint asset amounts) carries the `!` breaking-change marker. If it ships in the same tag, the SemVer answer is **v0.5.0**, not v0.4.23. _Who:_ Pietro Grandi cut the prior 0.4.21 / 0.4.22 releases; the bigint PR is the alt-to-#469 branch. _Why this matters for dusan:_ go-sdk and the wallet downstream both consume ts-sdk. A silent v0.4.23 with a breaking bigint change would break wallet TS code and any go-sdk parity tracking that mirrors the asset-amount type. Worth pinging release-coordinator / Pietro before the tag is cut to confirm v0.5.0.

3. **go-sdk is sitting on three CRITICAL findings while five APPROVED PRs queue up**. _What:_ HD Wallet (#143, dusan's own commit `6ce96d1`) introduced C1/C2/C3 — only C1 has a fix (in #162, APPROVED but unmerged); C2 (unsalted SHA-256 over the password) and C3 (zero-value credentials persisted) are untouched. Meanwhile #160/#159/#158/#153/#162 are all APPROVED but held behind companion test PR #165. _Who:_ release-coordinator + security-triage have flagged this as the recurring P0 blocker on the go-sdk first release. _Why this matters for dusan:_ this is **your repo, your release**. The go-sdk first tag has been P0 for weeks; today's queue makes the path forward concrete: review/merge #165, then merge #162 (C1 fix), then write fixes for C2 and C3 before any v0.x tag is cut.

## TL;DR

- **Decide ts-sdk versioning** before the v0.4.23 tag is cut: if #472 (bigint) is in, tag must be v0.5.0. Ping release-coordinator / Pietro.
- **Audit the go-sdk electrum-explorer fixes on `claude-electrum-test-break`** locally (9 unpushed commits) — they look like the fix for the 2 CRITICAL concurrency findings on PR #160. Push or roll into #160 once verified.
- **Schedule the C2/C3 fix work on go-sdk #143** — these are the gating CRITICALs for the first release. C1 is already in #162 (APPROVED).
- **Sync with louisinger (or whoever owns `enforcePayTo`)** before any fulmine/go-sdk code copies the new HTLC v2 shape — witness layout and amount push are gone.
- **Watch BIP54** as a governance signal — if it activates, it sets the template for any future CTV activation; CTV miner signaling remains at 0%.
