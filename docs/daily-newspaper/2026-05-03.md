# Ark Daily — 2026-05-03

> _Quiet Sunday: zero merges on fulmine, go-sdk, wallet, arkd, or introspector. One boltz-backend fix landed (`4a9c282b`, racy renegotiation) that touches `ArkNursery.ts` — the only externally-moving piece worth knowing about. Standing items from yesterday all unchanged._

## Front page

- **Boltz fixed a racy chain-swap renegotiation** (`4a9c282b`, michael1011, PR [#1383](https://github.com/BoltzExchange/boltz-backend/pull/1383)) — touches `ArkNursery.ts` (+36/−24) and `ChainSwapRepository.ts` (+98/−27). It's the one piece of new code today on a path fulmine consumes. (see Wallet beat → boltz adjacent)
- **Standing — go-sdk first release still blocked**: C2 (unsalted SHA-256 → PBKDF2 negation) and C3 (zero-value credential persistence) on `#143` HD wallet remain unfixed; C1 fix in `#162` is APPROVED but unmerged behind companion test PR `#165`. **No tag should be cut without human audit sign-off.** (see Slack briefing → Most important)
- **Standing — 9 unpushed commits on `claude-electrum-test-break`** in your local go-sdk clone (Apr 29 → May 1, by Ark Developer + Bob Smith) — these look like the fix for the 2 CRITICAL concurrency bugs flagged on `#160` (goroutine leak + concurrent TCP writes). Audit + push or roll into `#160`. (see Wallet beat → go-sdk)
- **Standing — introspector HTLC v2 witness shape change** (`697f94f`, louisinger, merged 2026-05-01) — arkade witness is now empty (was `[output_index]`) and the script no longer pushes `amount`. Anyone replicating in fulmine/go-sdk needs to coordinate with whoever owns `enforcePayTo` before mirroring. (see Slack briefing → Most important)
- **SDK parity report is stale (30 days old, last run 2026-04-03)**. Triggering a fresh parity run is the cheapest way to know whether go-sdk's Boltz/VHTLC PR `#113` and the un-tracked CovVHTLC / ArkadeScript / SettlementConfig gaps have moved at all. (see TL;DR)

## Wallet beat

### fulmine

No commits on `master` in 24h (last commit `3ea61e2`, Apr 24). Local clone is on `master` with only `.gitignore` modified — daily-update SKIPPED for that reason. No PR/issue activity from arkana logs today.

Standing: **fulmine ci_integration** has been failing 1–3× per day for over a week (slack-monitor 2026-04-27 → 2026-05-01). Still no public root-cause.

### go-sdk

No commits on `master` in 24h (last merge `2d035b6`, Apr 24 — `Bump client-lib`). Local clone is still on branch `claude-electrum-test-break`, 9 commits ahead of `master`, unchanged from yesterday's edition:

- `675c71d` docs: comprehensive electrum package documentation
- `d85501c` test: robustness tests for in-flight request hang + serial resubscribe
- `d5621ea` test: electrum break tests + regtest integration tests
- `bc44bb4` fix four concurrency bugs in electrum explorer
- `ae3b4d0` fix race
- `154dd4c` fix electrum explorer: concurrency bugs, goroutine leak, reliability
- `ff7b2fb` fix parity bugs + expand test coverage
- `ce31472` add ElectrumX explorer backend
- (plus `6ce96d1` HD Wallet, already on `master`)

These should be reviewed and either pushed against `#160` or rolled into a fresh PR — they're the concrete fix for the 2 CRITICAL findings code-review flagged on `#160`.

### arkade-wallet (PWA)

Repo `wallet` (`master`): no new pulls today. Last activity 2026-04-30 (3 days ago) was UI/test polish — already covered in yesterday's edition. Nothing new.

### Boltz adjacent

`boltz-backend` pulled one commit: `4a9c282b fix: racy renegotiation` (#1383, michael1011, +617/−76). Files touched:
- `lib/swap/ArkNursery.ts` — Ark-side chain-swap renegotiator
- `lib/db/repositories/ChainSwapRepository.ts`
- `lib/service/Renegotiator.ts`
- Plus parallel UTXO/Ethereum nursery fixes and tests

If fulmine or arkade-wallet pin a Boltz dep / docker image for chain swaps, this is the fix that wants to flow downstream.

## Slack briefing

### Decisions and blockers (from arkana, last ~7 days, only items in dusan's scope)

- **ts-sdk `#439` MERGED** (2026-05-01) — HD wallet primitives on SeedIdentity. Unblocks Phase C1 (`#440`, also merged) and Phase C2 (`#441`, P1 race unfixed).
- **`fix!: represent asset amounts as bigint` (alt to `#469`)** opened on ts-sdk (2026-05-01) — `!` BREAKING CHANGE marker. If included in the next ts-sdk tag, the SemVer answer is **v0.5.0**, not v0.4.23.
- **arkd v0.9.4 in production** since 2026-04-28 (regtest images bumped same day to `arkd:v0.9.4` + `fulmine:v0.3.21`). Block-based unilateral exit-delay PR opened the day after the seconds-based `#456` merged — protocol-critical, watch.
- **"Networking & Connection Investigation: Arkd Stack" issue opened** (2026-04-30) — formal post-mortem for the 4/24 SSE/HTTP-1.1 congestion incident; Traefik h2c migration on the table.
- **arkd `#908` "Scale the DAG" deep re-review** — 2 blockers found by code-review: unbounded memory DoS + marker over-reach in partial sweeps (fund-loss risk).
- **Multi-day blockers**: go-sdk CI 13+ consecutive bad days; rust-sdk E2E Matrix multi-week streak; bancod release pipeline broken; fulmine ci_integration escalating to 3× / day.

### Research findings (from arkana, last ~7 days)

- **Optech #403 (May 1)** — four parallel PQ wallet proposals: Conduition (BIP32+SPHINCS), Poinsot (separating spend-disabling from migration), **Buchner (deployable-today tapscript PQ keys via BIP342 unknown-key handling)**, **Osuntokun (working zk-STARK quantum-recovery prover, <3s proofs)**. Binary fuse filters: 6×–80× speedup vs BIP158. **No CTV / OP_CAT / Ark mentions.**
- **BIP54 Consensus Cleanup** — Antoine Poinsot demoed slow-to-validate blocks on signet. Most likely next soft fork to activate, ahead of CTV.
- **CTV signaling still 0%** (period 470+, ~6 weeks of zero) — effectively dead this attempt despite continued developer enthusiasm on Delving Bitcoin (CTV+CSFS BitVM bridges, RobinLinus Apr 28).
- **Bitcoin 2026 (Apr 27–29)** — quantum debate dominates: Adam Back (optional Taproot PQ extensions) vs Lopp (forced 5-year migration via BIP-361). Governance dynamics affect every soft-fork prospect.
- **No movement** on Spark, Mercury Layer, OP_CAT.

### Most important — expanded

1. **go-sdk first release: the C2/C3 fixes are now the entire gating item.** _What:_ HD Wallet `#143` (your own commit `6ce96d1`) introduced three CRITICAL findings. C1 is fixed in `#162` (APPROVED, unmerged); C2 (unsalted SHA-256 over the password — negates PBKDF2) and C3 (zero-value credentials persisted on init failure) are untouched. Five APPROVED PRs (`#162/#160/#159/#158/#153`) are queued behind companion test PR `#165`. _Who:_ release-coordinator + security-triage have been carrying this as the standing P0 for ~2 weeks. _Why this matters today:_ this is **your repo, your release**. With nothing else moving on the 5/3 timeline, today is a clean window to write the C2/C3 patches without contention; once written, the merge order is `#165` → `#162` (C1) → C2/C3 fix → tag.

2. **The 9 electrum commits in your local clone are the fix for `#160`'s CRITICAL findings.** _What:_ `claude-electrum-test-break` carries goroutine-leak fixes, race fixes, in-flight request hang repros, serial resubscribe tests, and a regtest integration suite — exactly the surface area code-review flagged as 2 CRITICALs on `#160`. _Who:_ authored by sub-agents (Ark Developer + Bob Smith), based on tests reproducing the bugs. _Why this matters today:_ pushing these (or rolling them into `#160`) is the unblocker for the ElectrumX merge. Without it, `#160` stays APPROVED-but-unmergeable in the queue. Worth half an hour of audit before the C2/C3 work above.

3. **introspector HTLC v2 has changed the witness contract — fulmine/go-sdk callers need to coordinate before mirroring.** _What:_ `697f94f` (louisinger, PR `#76`, merged 2026-05-01) drops the hardcoded `amount` push, derives input index via `OP_PUSHCURRENTINPUTINDEX`, switches the value check to `OP_GREATERTHANOREQUAL`, and pins `output[i] ↔ input[i]`. Arkade witness is now **empty** (was `[output_index]`). _Who:_ louisinger; cross-cutting coordination flagged in yesterday's daily-changelog. _Why this matters for dusan:_ go-sdk's VHTLC code (PR `#113`, idle 14+ days as of last parity report) and any fulmine HTLC-claim path that builds witnesses against v1 will silently produce malformed claims if pointed at a v2 script. Whoever owns `enforcePayTo` / the arkade-script builder should confirm the v2 layout before any downstream consumer mirrors it.

## TL;DR

- **Spend the quiet day on go-sdk C2/C3** — HD Wallet `#143` salted KDF + zero-value credential checks are the gating fixes for the first release tag.
- **Audit + push the 9 electrum commits on `claude-electrum-test-break`** — they're the fix for the 2 CRITICALs on PR `#160`.
- **Trigger an SDK parity refresh** — the live report is from 2026-04-03 (30 days old). Until it's regenerated, "go-sdk lags X" claims are guesses.
- **Ping louisinger on `enforcePayTo`** before any fulmine/go-sdk code mirrors the introspector HTLC v2 shape (witness empty, no `amount` push).
- **Note for swap path**: boltz-backend `#1383` (`ArkNursery` race fix) is the only externally-moving piece today. If you pin a Boltz image for chain swaps, plan to bump.
