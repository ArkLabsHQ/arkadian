# Ark Daily — 2026-06-24

> _Quiet across dusan's repos — fulmine and go-sdk had no commits — but the parity desk is loud: dotnet-sdk landed three features (EARS coin selector, per-input fee weights, unilateral refunds) and arkd shipped v0.9.10. Note: Slack/parity/research feeds are stale (newest 2026-06-19), so the June 22 signer rotation has since passed unobserved._

## Front page

- **June 22 signer rotation has now passed**, but the last parity snapshot (2026-06-19) still showed go-sdk rotation support as ❌ — PR #199 (`Migrate deprecated-signer VTXOs onto current signer`) was open with one approval and one change-request from altafan. Confirm it merged and shipped. (see Parity desk)
- **dotnet-sdk landed 3 features today** go-sdk doesn't have: EARS Coin Selector (#124), per-input weight fee estimation (#145), unilateral swap refunds + full refund matrix (#141). All parity candidates for go-sdk. (see Parity desk)
- **arkd cut v0.9.10** (ark-infra release #96). go-sdk's client-lib targets a build-version header — confirm the target string still matches a supported arkd. (see Protocol surface)
- **fulmine & go-sdk both quiet** — no commits in 24h; only a local `.gitignore` edit in each working tree. (see Wallet beat)
- **Data caveat**: Slack and parity feeds last refreshed 2026-06-19; this edition can't see the last 5 days of chatter, including rotation-day outcomes. (see Slack briefing)

## Wallet beat

### fulmine
No activity. Last commit is `3ea61e2 Bump go-sdk@2d035b64` (#401) from 2026-04-24. Working tree has an uncommitted `.gitignore` change only (daily-update skipped the repo for that reason).

### go-sdk
No activity on origin. Last commit `7f6b8d0 Contract registry: allow registering custom contract handlers` (#186, 2026-05-25). Working tree has an uncommitted `.gitignore` change only. Open PRs from the 2026-06-19 parity snapshot still pending — see Parity desk.

### arkade-wallet (PWA)
Two fixes landed (both minor/cosmetic UX, no logic of interest to SDK work):
- `09fa6ef6 fix(receive): allow clearing a set amount on the mobile keyboard` (#693, Andrew Camilleri)
- `87a5771f fix hand written lnurl` (#696, João Bordalo)

## Parity desk

_Source: parity report 2026-06-19 (5 days old, within the 14-day window). Not recomputed._

**Gaps where go-sdk lags:**
- **Arkd signer-rotation support** — go-sdk ❌ (PR #199 open, 1 approval / altafan changes-requested). ts-sdk ✅ #554, dotnet-sdk ✅ #132. Highest priority — rotation day was June 22. (parity gap table, row "Arkd signer-rotation support")
- **X-SDK-VERSION header** — go-sdk ❌; ts-sdk ✅ #568, rust-sdk ✅ #242, dotnet-sdk ✅ #139. go-sdk is the only SDK without it on origin/master.
- **HD wallet restore (deprecated signers)** — go-sdk ❌; ts-sdk ✅, dotnet-sdk ✅ #104.
- **Selective settle (expired/recoverable)** — go-sdk ❌; ts-sdk ✅, rust-sdk ✅ #227.
- **Arkade extension system** — go-sdk ❌ (PRs #190/#191 open); ts-sdk ✅.
- **Public buildVersion/sdkVersion export** — go-sdk ❌; ts-sdk ✅ #569.
- **Default explorer URL alignment** — go-sdk ⚠️ PR #198 (approved, unmerged — ready to land).

**Recent sibling-SDK landings (last 7 days):**
- **dotnet-sdk EARS Coin Selector** (#124) — new coin-selection strategy; go-sdk has no equivalent named selector. Worth evaluating for go-sdk coin selection.
- **dotnet-sdk per-input weight fee estimation** (#145) — replaces fixed tx caps with per-input weight estimate. Relevant to go-sdk's fee math and the TX_TOO_LARGE caps go-sdk still lacks.
- **dotnet-sdk unilateral refunds + full refund test matrix** (#141) — non-cooperative swap refund path. Parity item for go-sdk swap completeness.
- **ts-sdk Arkade Script + emulator co-signing** (PR #575, still DRAFT as of 06-19) — watch item; once merged, all other SDKs (incl. go-sdk) gain a new gap.

## Protocol surface — arkd → go-sdk

No arkd commits to client-facing paths in the last 24h. Recent relevant landings (last ~7 days, for context):
- `51384c05 Return accepted or finalized tx in indexer.GetVirtualTx and service.GetPendingTx` (#1123, louisinger, 2026-06-22) — **Impact on go-sdk**: if go-sdk wraps `GetVirtualTx`/`GetPendingTx`, callers may now receive accepted-but-not-finalized txs; verify status handling doesn't assume finalized.
- `0bae0249 fix: convert guard errors to gRPC status before they reach the client` (#1108, Dunsin Abisuwa, 2026-06-18) — **Impact on go-sdk**: DIGEST_MISMATCH / version-guard rejections now arrive as proper gRPC status codes instead of bare `Unknown`; go-sdk error handling can match on status codes.
- **arkd v0.9.10 released** (ark-infra #96, today) — **Impact on go-sdk**: confirm go-sdk's client-lib build-version target is compatible; build-version enforcement (`BUILD_VERSION_TOO_OLD`) was being prepared around the rotation.

## Slack briefing

_Source: slack-log.md, newest entry 2026-06-19 — this briefing cannot see 06-20 → 06-24._

### Decisions and blockers (from arkana)
- **BREAKING: `ARKD_SIGNER_ADDR` now required** (2026-06-19) — standalone arkd-signer extraction means deployments must point `ARKD_SIGNER_ADDR` at the signer service before June 22. Ops/protocol-critical; affects anyone running arkd locally for fulmine/go-sdk dev.
- **go-sdk CI heavily unstable** (2026-06-19) — 6+ ci_integration + 3 ci_unit failures flagged 3 days before rotation; "most critical CI risk." Check whether this was stabilized.
- **`client-lib: support deprecated signer verification` MERGED** (2026-06-17) — client-side verification of deprecated-signer signatures; directly relevant to go-sdk's rotation story (arkd #1117).
- **`feat(ark-client): narrow settle() to expired/recoverable VTXOs` MERGED** (2026-06-17) — settle() no longer settles valid VTXOs; the "selective settle" feature go-sdk still lacks per the parity table.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question open** — should settlement trigger when one VTXO is near-expiry vs all-VTXOs? Affects settlement aggressiveness; unresolved.

### Research findings (from arkana)
_Source: research-monitor.md, newest run 2026-06-18._
- **LND Zero-Timestamp Gossip DoS** (Delving Bitcoin, 06-18) — zero-timestamp gossip crashes LND <v0.20.1. ASPs/anyone running an LND node for swap testing should verify version ≥ v0.20.1.
- **Botanix Spiderchain shutdown** (06-10) — a Bitcoin L2 wound down after 1 yr: tech worked, adoption didn't (users preferred wrapped BTC). Cautionary data point for Ark positioning.
- **CTV** — still 0% signaling, ~18 weeks in. No movement on covenants.

### Most important — expanded
1. **go-sdk signer rotation is the open question this week.** As of the 2026-06-19 parity snapshot, go-sdk's rotation support (PR #199, `sekulicd:deprecated-signer-migrate-only`) had `arkanaai (Approved)` but `altafan (Changes requested)`, and go-sdk CI was carrying 6+ integration failures. Rotation day (June 22) has now passed and the feeds are stale, so the most valuable thing dusan can do is confirm PR #199 landed, CI is green, and deprecated-signer VTXOs can actually be migrated — ts-sdk (#554) and dotnet-sdk (#132) already shipped this, leaving go-sdk the laggard on a now-live operational requirement.
2. **dotnet-sdk is pulling ahead on coin selection and fee math.** Today it landed an EARS Coin Selector (#124) and replaced fixed tx-size caps with per-input weight fee estimation (#145). go-sdk still lacks both the TX_TOO_LARGE caps and a named selector, so these are concrete parity items — and the fee-estimation change is the kind of thing that surfaces as subtle settlement/size bugs if go-sdk's math diverges from the server's expectations under v0.9.10.
3. **`ARKD_SIGNER_ADDR` is a new required env var.** The standalone arkd-signer extraction makes `ARKD_SIGNER_ADDR` mandatory for arkd deployments. For dusan this matters for local fulmine/go-sdk dev loops: any arkd brought up without the signer service configured will fail to start, so dev-environment scripts and docs may need updating.

## TL;DR

- **Confirm go-sdk PR #199 (signer-rotation migration) merged** and CI is green — rotation day has passed and go-sdk was the only laggard with one outstanding change-request.
- **Evaluate dotnet-sdk's EARS Coin Selector (#124) and per-input fee estimation (#145)** as go-sdk parity work; also unilateral refunds (#141).
- **Land the cheap wins**: go-sdk PR #198 (explorer URL alignment, approved/unmerged) and the X-SDK-VERSION header — go-sdk is the only SDK still missing it.
- **Verify go-sdk's client-lib build-version target against arkd v0.9.10** (ark-infra #96) before build-version enforcement bites.
- **Refresh the Slack/parity feeds** — they're stale at 2026-06-19; rotation-day outcomes are not yet visible to this desk.
