# Ark Daily — 2026-06-25

> _Quiet day: only arkd moved (a crypto/net bump that quietly refactors gRPC transport to stdlib HTTP/2); fulmine and go-sdk had zero commits. The Slack/research feeds are frozen at June 19, so nothing here covers the June 22 signer rotation that has since come and gone — biggest open question of the day._

## Front page

- **Slack + research feeds are stale (last entry June 19).** No briefing data covers the June 22 signer rotation or July 4 cutoff — confirm rotation outcome out-of-band. (see Slack briefing)
- **arkd `3e11a6fc` swaps `x/net/http2`+`h2c` for Go stdlib HTTP/2** in `internal/interface/grpc/service.go`. No proto/endpoint change, but plaintext h2c moved to `SetUnencryptedHTTP2` — smoke-test fulmine/go-sdk cleartext gRPC against this build. (see Protocol surface)
- **Last known go-sdk state (June 19): CI heavily unstable** — 6 integration + 3 unit failures flagged as top rotation risk. Verify current CI is green. (see Wallet beat / Slack briefing)
- **LND zero-timestamp gossip DoS** (patched v0.20.1+) still relevant to any fulmine Lightning path touching mutinynet. (see Research findings)

## Wallet beat

### fulmine
No commits in last 24h. Only an uncommitted `.gitignore` tweak in the local checkout (changelog skipped the repo for that reason). No PRs/issues observable from local state.

### go-sdk
No commits in last 24h. Same uncommitted `.gitignore` skip. Last substantive Slack signal (June 19) was CI instability ahead of rotation — see Slack briefing.

### arkade-wallet (PWA)
No commits in `wallet` (master) in last 24h — up to date per changelog. Sibling `arkade-explorer` shipped `cbdeba2` (fix skewed address balance + browser crash on high-activity addresses, #29, Andrew Camilleri) — not the wallet itself, cosmetic-adjacent to dusan's stack.

## Protocol surface — arkd → go-sdk

- **`x/crypto` 0.49→0.52, `x/net` 0.52→0.55 dep bump + gRPC transport refactor** — `3e11a6fc` (louisinger, #1126). Touches only `internal/interface/grpc/service.go`: drops `x/net/http2`+`h2c` in favor of stdlib `http.Protocols`/`http.HTTP2Config`. Insecure mode → `SetUnencryptedHTTP2`; TLS mode → `SetHTTP2`; `MaxConcurrentStreams` preserved.
  - **Impact on go-sdk**: Client wrap surface unchanged (no proto/endpoint/signature changes). One watch item: cleartext gRPC now negotiates via stdlib `UnencryptedHTTP2` — if fulmine/go-sdk dial arkd over plaintext HTTP/2 locally, smoke-test the connection still negotiates against this build.

## Slack briefing

> ⚠️ Source caveat: `slack-log.md` and the research-monitor log have not been updated since **2026-06-19**. Everything below is ≥6 days old; the June 22 rotation and any fallout are NOT yet captured.

### Decisions and blockers (from arkana)
- **go-sdk CI heavily unstable (June 19)** — 6+ `ci_integration` + 3 `ci_unit` failures; flagged as "most critical CI risk" ahead of June 22 rotation. Status now unknown — verify.
- **BREAKING: `ARKD_SIGNER_ADDR` env var now required** — standalone arkd-signer extraction; deployments must point it at the signer service. Relevant if fulmine/go-sdk integration envs talk to a self-hosted arkd.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** (open, June 19) — PROTOCOL-CRITICAL for rotation correctness; was flagged for urgent review before June 22.
- **`feat(ark-client): narrow settle() to expired/recoverable VTXOs` MERGED (June 17)** — go-sdk `Settle()` no longer settles valid VTXOs, only expired/recoverable. Behavior change worth confirming in fulmine's settle flows.
- **`SETTLEMENT_MIN_EXPIRY_GAP` design question (June 19)** — should settlement trigger when ≥1 VTXO is near-expiry vs the current all-VTXOs gate? Affects settlement aggressiveness/latency.
- **`Add Arkade Script support with emulator co-signing` PR opened (June 19)** — arkade-script co-signing via emulator (the repo formerly known as introspector); contract-surface relevant.

### Research findings (from arkana)
- **LND zero-timestamp gossip DoS** (Delving Bitcoin, June 18) — crafted zero-timestamp gossip crashes LND <v0.20.1; ASPs should verify LN node versions. Lightning-interop relevant for fulmine swap paths.
- **Botanix Spiderchain shutdown** (June 10) — BTC L2 wound down after a year (25M txs, zero incidents) because users preferred wrapped BTC. Adoption cautionary tale, not technical.
- **CTV** — still 0% signaling, ~18 weeks in; nearly halfway through the timeout.

### Most important — expanded
- **The June 22 signer rotation is now in the past, and this briefing can't see it.** As of the last Slack snapshot (June 19), the critical path was go-sdk CI stability (6 integration + 3 unit failures) and the open `select signer key per leaf across all multisig closures` fix, with three key people off on rotation day. Today is June 25 — rotation (June 22) has happened and the July 4 cutoff is ~9 days out. Because the feed is frozen, dusan should confirm out-of-band: did rotation land cleanly, is go-sdk CI green again, and did the per-leaf signer fix merge? Anything go-sdk wraps around settlement/signing inherits the answer.
- **arkd's gRPC transport now rides Go stdlib HTTP/2.** `3e11a6fc` is dressed as a dependency bump but materially moves cleartext h2c onto `SetUnencryptedHTTP2`. It's a server-only change with no client-API impact, so go-sdk needs no code change — but local/integration setups that hit arkd over plaintext HTTP/2 should be smoke-tested against this build before assuming the connection still negotiates.

## TL;DR

- **Confirm the June 22 rotation outcome out-of-band** — the briefing feed is stale and silent on it; July 4 cutoff is ~9 days away.
- **Check go-sdk CI is actually green** — last known state (June 19) was 6 integration + 3 unit failures.
- **Smoke-test cleartext gRPC** from fulmine/go-sdk against arkd `3e11a6fc` (stdlib `UnencryptedHTTP2` path).
- **Verify fulmine settle flows** still behave under the merged `settle()` narrowing to expired/recoverable VTXOs.
- If any fulmine path touches mutinynet LND, **confirm it's ≥v0.20.1** (zero-timestamp gossip DoS).
