# Ark Daily — 2026-07-03

> _Quiet in dusan's own repos (fulmine/go-sdk/wallet all silent), but arkd landed two fixes that both hit go-sdk's wrap surface, and introspector's `feat/extended-checksig` branch is teaching the CHECKSIG family to speak ECDSA/P-256._

## Front page

- **arkd fixed CSVMultisigClosure small-int decoding** in `pkg/ark-lib/script` — closure/locktime decode logic go-sdk vendors directly. Vendor-bump needed. (see Protocol surface)
- **arkd client-lib** fixed a "send on closed channel" race in batch-session teardown **and** switched `explorer.GetFeeRate` to mempool `/v1/fees/recommended` — both live in `pkg/client-lib`, both wrapped by go-sdk. (see Protocol surface)
- **introspector `feat/extended-checksig`**: msinkec extended `OP_CHECKSIGFROMSTACK` and the whole `OP_CHECKSIG` family to ECDSA over secp256k1 **and** secp256r1 (P-256). New signing schemes for Arkade Script contracts. (see Contract surface)
- **No activity in fulmine, go-sdk, or arkade-wallet** in the last 24h. Both go-sdk and fulmine local checkouts are sitting on uncommitted changes (daily-update skipped their pull). (see Wallet beat)
- Slack/research logs are still ~2 weeks stale (last entry Jun 19) — no fresh decisions to surface. (see Slack briefing)

## Wallet beat

### fulmine
No activity. Local checkout has uncommitted changes and was skipped by daily-update — no new upstream commits either.

### go-sdk
No activity. Same as fulmine — uncommitted local changes, no upstream commits in 24h. Note: two arkd changes below land in packages go-sdk vendors, so a bump is pending (see Protocol surface).

### arkade-wallet (PWA)
No activity in the last 24h (`$WALLET_REPO` up to date, no commits).

## Protocol surface — arkd → go-sdk

- **Fix CSVMultisigClosure small-int decoding** — `af56a868` (louisinger, #1135). Touches `pkg/ark-lib/script/closure.go` + `pkg/ark-lib/locktime.go`.
  **Impact on go-sdk:** corrects locktime/closure decode for CSV/CLTV multisig closures — affects any VTXO/forfeit script parsing go-sdk does downstream. Vendor-bump `pkg/ark-lib`.
- **client-lib: fix send on closed replay channel + update explorer.GetFeeRate** — `61976b61` (Oli, #1134). Touches `pkg/client-lib/batch_session_handler.go` + `pkg/client-lib/explorer/mempool/explorer.go`.
  **Impact on go-sdk:** (1) closes a batch-session teardown race (panic when caller closes the replay channel before the goroutine sends) — relevant to go-sdk's settle/batch flow; (2) `GetFeeRate` now calls mempool `/v1/fees/recommended` instead of the deprecated `/fee-estimates` (unsupported on mutinynet/arkade), backward-compat retained — changes fee-estimation behavior for anything wrapping the explorer.

_No proto/gRPC surface touched — vendor bump only, no regen._

## Contract surface — introspector

Active work on branch `feat/extended-checksig` (not yet on master), all by msinkec / Mihael Šinkec in the last ~24h. Touches `pkg/arkade/{opcode,sigscheme,sigvalidate}.go`:

- **Extend `OP_CHECKSIGFROMSTACK` to ECDSA/secp256k1 and ECDSA/secp256r1** — `3925d8a`. Adds ECDSA and P-256 verification alongside Schnorr.
- **Wire scheme layer into the `OP_CHECKSIG` family** — `d12ea92`. The whole CHECKSIG family now dispatches through a signature-scheme abstraction.
- **`schemeKey.verify` with ECDSA support + `parseSchemePubKey`/scheme types** — `9c030dd`, `49d5f5f`. New pubkey parsing and scheme typing for extended checksig.
- **Benchmark + compute-limits doc for P-256 ECDSA verify** — `0c42c55`. Documents the cost of the new curve in `compute_limits`.

**Semantics note:** this adds new valid signature schemes to CHECKSIG opcodes — contracts written against a Schnorr-only assumption keep working, but P-256/ECDSA-based Arkade Script contracts become expressible. Watch for merge to master before relying on it.

## Slack briefing

> _Note: arkana's `slack-log.md` is last updated **2026-06-19** and research-monitor **2026-06-18** — ~2 weeks stale. The June 22 signer rotation has since passed; treat any dated action items as likely resolved and verify._

### Decisions and blockers (from arkana, through Jun 19)
- **`Add Arkade Script support with emulator co-signing`** [Jun 19] — PR opened; arkade-script co-signing via the emulator. On the introspector/emulator contract track — pairs with today's extended-checksig work.
- **`fix(arkd-wallet): select signer key per leaf across all multisig closures`** [Jun 19] — PROTOCOL-CRITICAL for rotation correctness; relevant to go-sdk's rotation handling.
- **go-sdk CI instability** [Jun 19] — 6+ ci_integration + 3 ci_unit failures flagged pre-rotation. Almost certainly stale now — worth a quick confirm that CI is green.

### Research findings (from arkana, through Jun 18)
- **LND zero-timestamp gossip DoS** [Delving Bitcoin, Jun 18]: crafted gossip crashes LND < v0.20.1; patched in v0.20.1+. Relevant to fulmine's Lightning interop — verify ASP/LN node versions.
- **CTV** still ~0% signaling, ~18 weeks in.

### Most important — expanded
1. **Introspector is adding ECDSA/P-256 to the CHECKSIG family (today).** msinkec's `feat/extended-checksig` branch extends `OP_CHECKSIGFROMSTACK` and the `OP_CHECKSIG` family to verify ECDSA over both secp256k1 and secp256r1, wiring a scheme-dispatch layer into `pkg/arkade`. This matters because it broadens what Arkade Script contracts can express (P-256 signers, e.g. WebAuthn/passkey-style keys) — once merged, contract emulation and any go-sdk/fulmine paths that exercise the emulator will see new valid signatures. It pairs with the Jun 19 "Arkade Script support with emulator co-signing" PR: the contract-signing surface is actively moving, so pin introspector versions in contract tests.
2. **Two arkd fixes land squarely in go-sdk's vendored packages.** `af56a868` corrects CSV/CLTV closure small-int decoding in `pkg/ark-lib/script`, and `61976b61` fixes a batch-session teardown race plus repoints `explorer.GetFeeRate` at mempool's `/v1/fees/recommended` in `pkg/client-lib`. Both are packages go-sdk wraps directly, so a vendor bump is the concrete next action — the closure fix affects VTXO/forfeit parsing correctness, and the fee-rate switch changes explorer behavior on mutinynet/arkade instances where the old endpoint was unsupported.

## TL;DR

- **Bump go-sdk's `pkg/ark-lib` and `pkg/client-lib`** to pick up `af56a868` (closure decode fix) and `61976b61` (batch-session race + GetFeeRate endpoint). No proto regen needed.
- **Commit or stash** the uncommitted changes sitting in your fulmine and go-sdk checkouts — daily-update skipped both pulls, so they're drifting from upstream.
- **Watch `introspector:feat/extended-checksig`** for merge to master — ECDSA/P-256 in the CHECKSIG family changes contract semantics; pin the introspector version in any contract emulation tests.
- **Sanity-check go-sdk CI is green** — the June-19 instability flags predate the (now passed) rotation and are almost certainly stale, but confirm.
