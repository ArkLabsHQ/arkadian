# Ark Daily — 2026-05-22

> _Your stack is quiet on commits but the protocol surface moved: arkd landed a bidi-streaming **`GetSubscription`** redesign that fulmine + go-sdk are now expected to migrate to (INC-1 mitigation), and the one commit on arkd that wasn't Bob's was yours. Slack surfaced two protocol-critical PRs that touch the client surface (`narrow settle()` and `unilateral exit with VHTLC ancestor`). introspector has three opcode PRs in flight plus a "compute-budget" architecture issue that would let heavy crypto opcodes ship._

## Front page

- **arkd #951 merged the non-breaking single-connection `GetSubscription` flow** — replaces the per-script subscribe/unsubscribe loop with one connection + `UpdateSubscription` filters. Directly addresses INC-1's 429 amplification path. **fulmine and go-sdk both need to migrate.** Client-side adoption is still pending (PR explicitly reverted `pkg/client-lib` changes to branch `bob/client-lib-stream-changes`); only server + proto landed on master. (see Protocol surface)
- **Your own arkd fix landed: `2336e19e` client-lib explorer polling fallback panic fix** (#1076). Closes the panic when poll interval is unset. (see Protocol surface)
- **"feat(ark-client): narrow settle() to expired/recoverable VTXOs" PR opened** — direct fix for the 2026-05-15 PROTOCOL-CRITICAL auto-settle race that produced `VTXO_ALREADY_SPENT` for users. Touches the settle path go-sdk wraps; should land before next release. (see Slack briefing → Most important)
- **"Support unilateral exit with VHTLC ancestor" PR opened** — PROTOCOL-CRITICAL. Unilateral exit interacting with VHTLC ancestors; sits adjacent to the still-open non-interactive VHTLC claim PRs on fulmine (#411) and go-sdk (#182). Governance-gated. (see Slack briefing → Most important)
- **introspector contract-surface activity is heating up**: three opcode PRs in flight (OP_MODEXP #80, OP_ECADD/ECMUL/ECPAIRING #79, OP_REVERSEBYTES #84) plus an issue to **"Unify compute-budget system for heavy opcodes"** that's the prerequisite for any of the crypto opcodes to ship. (see Contract surface)

## Wallet beat

### fulmine
- No commits landed on `master` in the last 24h. Local mirror still skipped by daily-update (`.gitignore` dirty).
- Open PR #411 **"feat: `NonInteractiveClaim` option to `CreateVHTLC`"** (opened 2026-05-18, last updated 2026-05-21) — still iterating; PROTOCOL-CRITICAL, governance-gated.
- Slack: fulmine `ci_integration` down to **1 failure** in 24h (was 3× the prior day). Recovering.

### go-sdk
- No commits landed in the last 24h. Local mirror still skipped (`.gitignore` dirty, untracked `.worktrees/`). Last merge on `master` remains `6b16cbe` Cursor-based pagination (#178, 2026-05-20).
- Open PRs worth eyes:
  - **#183 "Synchronize spend operations to prevent VTXO_ALREADY_SPENT races"** (opened 2026-05-20, updated 2026-05-21) — pair this with the new `narrow settle()` ark-client PR (see Slack briefing); they're complementary fixes for the same auto-settle race.
  - **#182 "fix for non interactive VHTLC claim"** (updated 2026-05-21) — PROTOCOL-CRITICAL; partners with fulmine #411.
  - **#179 "Allow registering custom contract handlers"** (last touched 2026-05-18) — quiet but open.
- Slack: go-sdk CI **not listed** in today's blocker section — first day off the blocker list in a while (yesterday it was 2× ci_integration + 1× ci_unit on day 26+). Verify the next CI run, but this may be real recovery.

### arkade-wallet (PWA)
- No commits landed on `master` in the last 24h.
- Four OPEN PRs from the last 48h, all redesign work by pietro909 (touched 2026-05-20/21):
  - **#627** redesign home & settings entry points
  - **#628** align recent activity with prototype
  - **#629** align send flow with redesign prototype
  - **#630** add bitcoin detail page
- Mostly UI/cosmetic — wallet stack changes are concentrated upstream (SDK) this week, not in the PWA.

## Protocol surface — arkd → go-sdk

Three arkd commits in the last 24h. Two are relevant to the go-sdk wrap surface; one is infra.

- **`700026fe` Non-breaking single-connection `GetSubscription` rpc (#951)** (Bob Smith) — touches `api-spec/protobuf/ark/v1/indexer.proto` (+74 lines), generated `indexer.pb.go` (+694 lines), `indexer.pb.rgw.go` (+114), `indexer_grpc.pb.go` (+44), `internal/interface/grpc/handlers/indexer.go` (+172), permissions, and a 968-line indexer handler test file.
  - **Behaviour**: `GetSubscriptionRequest` now accepts an optional `SubscriptionFilter`; empty `subscription_id` ⇒ server creates one and emits `SubscriptionStartedEvent`. New `UpdateSubscription` RPC with pluggable filters (`ScriptsFilter` = add/remove or overwrite) replaces multi-connection subscribe/unsubscribe. HTTP: adds `GET /v1/indexer/subscription` and `POST /v1/indexer/subscription/update`.
  - **Impact on go-sdk**: regenerate the client stubs from the new proto, then migrate fulmine's per-script reconnect loop to the single-connection + `UpdateSubscription` filter pattern. Server + proto are on master; **client-lib changes were reverted to branch `bob/client-lib-stream-changes`** and are not in master yet — coordinate before doing your own wrap.
- **`2336e19e` client-lib: Fix explorer polling fallback panic when poll interval is unset (#1076)** (Dusan Sekulic) — your own PR. `pkg/client-lib/explorer/mempool` panic fix; mempool URL bumped.
- **`a6f1b71f` Upgrade `nbxplorer` to 2.6.7 (#1077)** (Se7enZ) — bump in `docker-compose.regtest.yml`. No client-side impact.

## Contract surface — introspector

No commits in the last 24h (last merge was `697f94f` "test: htlc script v2" on 2026-05-01). All the action is in open PRs/issues:

- **PR #80 "Implement OP_MODEXP"** (opened 2026-05-19, by louisinger track) — modular exponentiation opcode. Joins OP_ECADD/ECMUL/ECPAIRING. Heavy crypto.
- **PR #79 "Implement OP_ECADD, OP_ECMUL, OP_ECPAIRING"** (opened 2026-05-14) — elliptic-curve opcodes. Significant cryptographic primitives. Protocol-adjacent.
- **PR #84 "Implement OP_REVERSEBYTES"** (opened 2026-05-20) — utility opcode for byte-reverse.
- **Issue: "Unify compute-budget system for heavy opcodes"** (opened recently per slack 2026-05-19) — compute-budget architecture is the precondition for ECPAIRING/MODEXP/ECADD shipping with metering. This is the blocker that unlocks the three opcode PRs above.
- **PR #85 "Add CI-enforced README opcode sync from interpreter source"** (opened 2026-05-21) — meta: keep README opcode table in sync with code. Quality-of-life for contract authors.
- **PR #82 "test: key migration"** (opened 2026-05-19) — test coverage; not opcode-level.

Net: contract surface is moving from "test additions" (May) into a real opcode-extension wave (May 14–20). If you ship Arkade Script contracts soon, the EC opcodes + compute-budget design will determine which contract shapes are feasible.

## Slack briefing

### Decisions and blockers (from arkana)

_Source: `slack-log.md` 2026-05-21 unless noted._

- **"Guard quoteSwap against adversarial Boltz quotes" MERGED** (boltz-swap #151, pietro909). Treats Boltz quotes as adversarial — relevant for any fulmine/go-sdk code path that consumes Boltz quote responses.
- **"feat(electrum): fall back to EsploraBlockchain when Electrum plugin is the backend" MERGED** — resilience improvement for SDK consumers using Electrum.
- **"Convert ts-sdk to pnpm monorepo with boltz-swap" MERGED** — parity question for go-sdk: how does the boltz-swap coupling story translate?
- **"Batch sessions" MERGED** — session-batching perf improvement; check whether it affects go-sdk client-side session APIs.
- **fulmine `ci_integration`: 1 failure** — down from 3× yesterday. Your repo, recovering.
- **rust-sdk CI: 4 failures + E2E Test Matrix Runner: 1 failure** — very elevated.
- **dotnet-sdk CI: 4 failures** — escalating back up; today's `perf(sync)` and `perf(wallet) BIP-39 caching` PRs (GOV#49-flagged) probably contributed.
- **enclave ACME Integration Test: 3 failures** — NEW failure mode after "add deploy-time Let's Encrypt/ACME support" landed. Out of your scope but indicates enclave release pipeline is shaky.
- **#dev: Fulcrum required for Electrum TRUC support** (@U0854KRKV3Q) — MUST use Fulcrum; regular Electrumx doesn't support TRUC package submissions. Constraint to document if go-sdk grows Electrum integration.
- **#dev: Mutinynet VTXO indexer stale-status race** (@U0AEWD7LHTJ) — `isPreconfirmed:true / isSpent:false` self-resolved minutes later. Possible indexer lag / status-propagation race; worth a watch when validating fulmine swap flows against mutinynet.

### Research findings (from arkana)

_Source: `research-monitor.md` 2026-05-21 unless noted._

- **PQ discourse on Delving Bitcoin accelerating** — three PQ threads in four days (sipa BoP-2 May 18; Lopp "Quantum Attack Game Theory" May 21; nkaretnikov "Lattice-based signatures" May 21). Educational, not proposals. Worth tracking because Ark's MuSig2/Schnorr signing would need adaptation in any PQ transition — affects VTXO tx weights.
- **Chimera TGE in 5 days (May 27)** — first detailed third-party (Blockonomi) review confirms 3-layer architecture (mainchain + Lightning/Boltz + Arkade VTXOs), PWA-based. SDK-level integration details still not exposed; remains a gap in our knowledge of which Arkade SDK surface they use.
- **CTV** — still 0% signaling, ~13 weeks. Dead window.
- **Bitcoin Core 31.0** — still rc4 on testnet. Cluster mempool / V3 policy / sibling-eviction RBF benefits arkd's forfeit/connector broadcast.
- **BOLTs #995 (simple taproot channels) + #1228 (zero-fee commitments)** standardized (Optech #404). Direct benefit for Ark↔LN interop work.

### Most important — expanded

1. **arkd's `GetSubscription` redesign is the INC-1 mitigation, and it's now on you to migrate.** WHAT: Bob Smith's PR #951 (commit `700026fe`) replaces the per-script subscribe/unsubscribe loop with a single bidi-stream that takes filters via `UpdateSubscription`. Server, proto, REST bindings, permissions, and a 968-line handler test are on master; the client-lib changes were explicitly reverted to branch `bob/client-lib-stream-changes`. WHO: Bob Smith, merged 2026-05-21. WHY: this is the protocol-level fix for the 429 amplification path flagged as INC-1. Regenerating go-sdk stubs is the easy part; the open question is whether you migrate fulmine's indexer subscription path now (pulling the client-lib branch yourself) or wait for Bob to land the client-side. Worth a quick sync this week.

2. **`narrow settle()` on the ark-client lands the fix for the auto-settle race that's been a known production problem since 2026-05-15.** WHAT: PR "feat(ark-client): narrow settle() to expired/recoverable VTXOs" was opened today; it narrows `settle()` to only expired/recoverable VTXOs, preventing the race against healthy VTXOs that produced `VTXO_ALREADY_SPENT`. WHO: opened in past 24h (slack-monitor 2026-05-21). WHY: this is the upstream half of the fix; **go-sdk #183 "Synchronize spend operations to prevent VTXO_ALREADY_SPENT races"** is the downstream half. Track both into the same release window. The auto-settle race is real-money impact and your repo holds the consumer-side fix.

3. **"Support unilateral exit with VHTLC ancestor" is a third VHTLC PR in flight, layered on top of the still-open non-interactive claim PRs.** WHAT: A new PR opened today touches unilateral exit paths interacting with VHTLC ancestors. WHO: opened 2026-05-21 (slack-monitor). WHY: VHTLC exit paths are protocol-critical and governance-gated. Combined with fulmine #411 (`NonInteractiveClaim` on `CreateVHTLC`) and go-sdk #182 (non-interactive VHTLC claim fix), there are now three open VHTLC-mode-changing PRs in your stack at once. Before any of these merge they need explicit human sign-off, and they should probably be reviewed together rather than serially.

## TL;DR

- **Plan the `GetSubscription` migration for fulmine + go-sdk** — the proto is on master; the client-lib branch is `bob/client-lib-stream-changes`. Decide whether to pull or wait.
- **Track `narrow settle()` (ark-client) and go-sdk #183 together** — same race, two halves of the fix. They should land in the same release window.
- **Coordinate the three VHTLC PRs** — fulmine #411, go-sdk #182, and the new unilateral-exit PR all touch the same governance-gated surface. Review them as a set.
- **Watch the introspector compute-budget issue** — the prerequisite for shipping OP_MODEXP / OP_ECADD / OP_ECMUL / OP_ECPAIRING. If it's contentious, the heavy-crypto opcode track stalls.
- **fulmine and go-sdk CI both improved today** — fulmine `ci_integration` 3→1, go-sdk dropped off the blocker list. Confirm the trend on the next run before declaring victory.
